---
milestone: 2.0
name: Safety & Trust
audited: 2026-06-27
status: tech_debt
resolved:
  - id: "TRUST-04 / SC-4 (BLOCKER-1)"
    status: "RESOLVED 2026-06-28 (commit 9e0fcb5)"
    evidence: >
      Armed register_market_open_snapshot + register_anomaly_evaluator in the FastAPI
      lifespan (dashboard/app.py steps 2c/2d, mirroring register_daily_pnl_cron). The
      start-of-day snapshot denominator is now written near NYSE open, so evaluate_drawdown
      can trip on both the post-fill hook and the interval tick. Guarded by a source-level
      invariant test (tests/unit/test_scheduler.py::test_lifespan_arms_anomaly_snapshot_and_evaluator)
      in the project's AST/grep-gate idiom. TRUST-04 is now functional in production.
scores:
  requirements: 10/11 satisfied (1 deferred-by-design to Phase 7)
  phases: 5/5 shipped (3 with stale/deferred verification status)
  integration: 10/11 cross-phase links wired (1 Phase-7-deferred)
  flows: 4/5 E2E flows fully wired (autonomous cadence is Phase-7 scope)
gaps:
  requirements: []
  integration:
    - id: "BLOCKER-2"
      status: "deferred-by-design (Phase 7) — flagged for owner decision"
      evidence: >
        schedule_strategy_daily is never called outside jobs.py — no recurring strategy run is
        armed. The scheduler job target (trigger_strategy_run) has no post_run_result wrapper /
        APScheduler listener, so a scheduled propose-only run would persist a proposal + audit
        events but never post the HITL Slack card. ROADMAP scopes market-hours scheduling +
        supervision to Phase 7 (not started), so the cadence itself is deferred-by-design — but
        v2.0 has no unattended trading loop today, which materially affects the trust-ladder
        value proposition (auto-execute only matters on autonomous cadence).
      fix: >
        Defer cadence to Phase 7 (document). When Phase 7 arms cadence, also wrap the scheduled
        run target with post_run_result + an APScheduler listener so scheduled propose-only runs
        surface HITL cards.
  flows:
    - "Autonomous scheduled runs — not wired (Phase 7 scope, BLOCKER-2 — accepted as deferred-by-design)"
tech_debt:
  - phase: 04-agent-architecture-cost-bounds
    items:
      - "WARNING-1: reschedule_strategy_degraded / restore_strategy_normal_cadence exported but never called from runtime.py — COST-04 'longer cadence' degradation is unrealized (Haiku/context-trim half works; reschedule half does not). Depends on the same unarmed run-job as BLOCKER-2."
      - "VERIFICATION status human_needed (human UAT closed per ROADMAP: 3 live pass, 2 deferred-with-coverage; status flag not flipped)."
  - phase: 05-trust-ladder
    items:
      - "WARNING-2: post_run_result routes only on result['outcome'] (stays 'propose_trade') and ignores auto_outcome, so manually triggering an auto strategy posts a redundant Approve/Reject card for an already-executed trade. Not dangerous (re-approval hits OrderGuard/resolved state); confusing surface. Fix: short-circuit post_run_result when auto_outcome in {auto_executed, awaiting_2nd_channel}."
  - phase: 03-production-hitl-ux
    items:
      - "VERIFICATION status human_needed + 03-HUMAN-UAT.md partial (0 pending scenarios) — human UAT closed per ROADMAP (98/98 threats); status flags not flipped to passed/complete."
  - phase: 02-orderguard
    items:
      - "VERIFICATION status passed_with_deferred (deferred items carried with coverage)."
  - phase: 01-foundation
    items:
      - "No VERIFICATION.md — predates the convention; verified by manual demo 2026-06-12 (22-event audit chain across 3 paper runs) per ROADMAP."
  - cross-cutting:
    items:
      - "Full pytest suite is order-dependent / non-deterministically red (~25-29 pre-existing isolation/env failures, identical before Phase 5). Reliable signal is per-file isolation. Worth a test-isolation cleanup pass."
---

# Milestone v2.0 (Safety & Trust) — Audit

**Status: tech_debt** (no in-scope blockers remaining). BLOCKER-1 (TRUST-04/SC-4 anomaly reflex inert) was **fixed inline before close** (commit `9e0fcb5` — armed the snapshot + evaluator jobs in the lifespan + a source-gate test). The remaining cross-phase gap (autonomous cadence, BLOCKER-2) is Phase-7-deferred-by-design; two warnings are routed to backlog/tech-debt.

## What is solid (verified in committed source, not SUMMARY claims)

- **Single deterministic order firewall:** every order (HITL approve AND auto-execute) reaches the broker only through `OrderGuard.place_order`; pipeline kill → paper_live → universe → hard_caps → **portfolio_caps → capital_ceiling** → qty_price → pdt → t1 → market_hours → broker; `place_order` is zero-decorator; no second order path exists.
- **Trust ladder stacks on the first-live gate:** both the auto-branch and the Slack approve handler route LIVE + `first_live_trade_confirmed_at IS NULL` → `AWAITING_2ND_CHANNEL` (never direct execute). SI-4 consistent across HITL and auto.
- **Notification routing consistent:** bypass set identical in both DM wrappers; `cost_alert` + `anomaly_demotion` bypass quiet hours; `auto_execution` FYI respects them; daily digest aggregates auto-exec + anomaly lines.
- **Audit chain linear:** migrations 0001→0007 single head; all 24 written event types present in the 0007 CHECK vocabulary; zero orphans.

## Requirements integration map

| Requirement | Status | Note |
|---|---|---|
| EXEC-03/04/05 (OrderGuard single firewall) | ✅ WIRED | — |
| TRUST-01 (view/promote/demote) | ✅ WIRED | — |
| TRUST-02 (portfolio caps + auto recorded) | ✅ WIRED | — |
| TRUST-03 (capital scaling rung, $1K default) | ✅ WIRED | — |
| TRUST-04 (anomaly auto-demote) | ✅ WIRED | snapshot+evaluator armed in lifespan (BLOCKER-1 fixed, commit 9e0fcb5) |
| TRUST-05 (blocked-with-explanation) | ✅ WIRED | — |
| TRUST-06 (safety gate + live+auto stacking) | ✅ WIRED | — |
| COST-01/04 (cost halt/degrade) | ⚠ PARTIAL | halt + Haiku/triage wired; "longer cadence" reschedule unwired (WARNING-1) |
| COST-05 (cost ledger) | ✅ WIRED | — |
| Phase-1 cadence / scheduled HITL surfacing | ❌ UNWIRED | Phase-7 scope (BLOCKER-2) |

## Recommendation

**BLOCKER-1 resolved inline (commit `9e0fcb5`)** — TRUST-04 anomaly reflex is now armed in the lifespan and guarded by a source-level test. No in-scope blockers remain. **BLOCKER-2** (autonomous cadence) is accepted as Phase-7-deferred — when Phase 7 arms cadence it must also wrap the scheduled run target with `post_run_result` + an APScheduler listener so scheduled propose-only runs surface HITL cards (carry-forward note). The two warnings (COST-04 longer-cadence reschedule; redundant HITL card after manual auto-exec) are routed to backlog. **v2.0 is OK to close as an override/tech-debt closeout** with BLOCKER-2 + warnings documented as deferred.
