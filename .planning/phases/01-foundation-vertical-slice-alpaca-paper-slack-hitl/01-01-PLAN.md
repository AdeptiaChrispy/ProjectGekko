---
phase: 01-foundation-vertical-slice-alpaca-paper-slack-hitl
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .ruff.toml
  - .gitignore
  - src/gekko/__init__.py
  - src/gekko/__main__.py
  - src/gekko/cli.py
  - src/gekko/py.typed
  - src/gekko/core/__init__.py
  - src/gekko/schemas/__init__.py
  - src/gekko/db/__init__.py
  - src/gekko/brokers/__init__.py
  - src/gekko/audit/__init__.py
  - src/gekko/agent/__init__.py
  - src/gekko/agent/tools/__init__.py
  - src/gekko/execution/__init__.py
  - src/gekko/approval/__init__.py
  - src/gekko/reporter/__init__.py
  - src/gekko/scheduler/__init__.py
  - src/gekko/slack/__init__.py
  - src/gekko/dashboard/__init__.py
  - src/gekko/vault/__init__.py
  - tests/__init__.py
  - tests/unit/__init__.py
  - tests/integration/__init__.py
  - tests/fixtures/cassettes/.gitkeep
  - tests/fixtures/strategies/.gitkeep
autonomous: false
requirements: []
user_setup:
  - service: anthropic
    why: "Claude Agent SDK requires ANTHROPIC_API_KEY for Researcher and Decision subagents"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/settings/keys"
  - service: alpaca-paper
    why: "Phase 1 broker round-trip requires Alpaca paper credentials (paper-only enforced; live rejected at construction)"
    env_vars:
      - name: ALPACA_PAPER_API_KEY
        source: "https://app.alpaca.markets/paper/dashboard/overview -> API Keys"
      - name: ALPACA_PAPER_SECRET_KEY
        source: "https://app.alpaca.markets/paper/dashboard/overview -> API Keys"
    dashboard_config:
      - task: "Create Alpaca paper account (free); ensure account is in paper mode"
        location: "https://app.alpaca.markets/signup"
  - service: slack
    why: "HITL approval requires real Slack workspace, bot, signing secret"
    env_vars:
      - name: SLACK_BOT_TOKEN
        source: "Slack App -> OAuth & Permissions -> Bot User OAuth Token (xoxb-...)"
      - name: SLACK_SIGNING_SECRET
        source: "Slack App -> Basic Information -> App Credentials -> Signing Secret"
      - name: SLACK_USER_ID
        source: "Slack user profile -> Copy member ID (U...) — the user who will receive DMs"
    dashboard_config:
      - task: "Create Slack app with bot scopes: chat:write, im:write, commands; add /gekko slash command; enable Interactivity with Request URL pointing to local cloudflared tunnel /slack/events"
        location: "https://api.slack.com/apps"
  - service: finnhub
    why: "RES-02 news evidence (optional; degrade gracefully if absent)"
    env_vars:
      - name: FINNHUB_API_KEY
        source: "https://finnhub.io/dashboard (free tier OK)"

must_haves:
  truths:
    - "uv sync installs all P1 dependencies on Python 3.12 (Mac + Windows) without errors"
    - "gekko --help exits 0 and prints CLI command stubs"
    - "ruff lint passes on the empty scaffold (zero violations)"
    - "mypy --strict passes on the empty scaffold (no type errors)"
    - "pytest --collect-only runs cleanly (zero collection errors)"
    - "All package __init__.py files import without ImportError"
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project metadata, P1 dependency pins, ruff/mypy/pytest config"
      contains: "claude-agent-sdk>=0.2.93,<0.3"
    - path: "src/gekko/cli.py"
      provides: "Typer CLI app stub with init/serve/run/doctor/strategy/audit subcommand placeholders"
      exports: ["app"]
    - path: ".ruff.toml"
      provides: "ruff lint rules including float-ban in gekko/brokers/, gekko/execution/, gekko/core/money.py per D-20/EXEC-01"
      contains: "per-file-ignores"
  key_links:
    - from: "pyproject.toml"
      to: "src/gekko/cli.py"
      via: "[project.scripts] gekko = \"gekko.cli:app\""
      pattern: "gekko\\s*=\\s*[\"']gekko\\.cli:app[\"']"
    - from: ".ruff.toml"
      to: "src/gekko/brokers/, src/gekko/execution/, src/gekko/core/money.py"
      via: "ruff per-file-ignores ban float import"
      pattern: "brokers|execution|money"
---

<objective>
Bootstrap the Project Gekko Python project: `pyproject.toml` with all P1 dependency pins, `uv` lockfile, lint/type/test tool configs, src-layout directory tree with empty `__init__.py` files for every module the SKELETON.md file set requires, and a Typer-based `gekko` CLI entry point with command stubs so `gekko --help` works end-to-end. After this plan completes, every subsequent Wave 1+ plan can import from `gekko.*` and run `uv run pytest` / `uv run mypy` / `uv run ruff check` without further setup.

Purpose: Foundation — every later plan's `<read_first>` and `<files>` references assume this scaffold exists. SKELETON.md explicitly lists Wave 0 as the smoke-runnable gate before Wave 1.

Output: A `uv sync`-able repo where `gekko --help` exits 0, `pytest --collect-only` runs cleanly, and the directory layout matches RESEARCH.md §"Recommended Project Structure".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-CONTEXT.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-SKELETON.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (PyPI audit)</name>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md  (§Package Legitimacy Audit — all packages tagged [ASSUMED])
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-CONTEXT.md  (Locked stack decisions D-18..D-25)
  </read_first>
  <what-built>
    No code yet. This is a BLOCKING package-legitimacy gate per RESEARCH §Package Legitimacy Audit. Every P1 third-party package is [ASSUMED] because `slopcheck` was not run. The executor MUST verify each package's PyPI homepage before any `uv add` / `pip install`. Especially scrutinize `sqlcipher3-wheels` (community fork — coleifer/sqlcipher3 is the more-established alternative).
  </what-built>
  <how-to-verify>
    Open each PyPI page below in a browser. Confirm: (a) "Homepage" / "Repository" link points to the expected GitHub org (NOT a typo-squat), (b) version on PyPI matches the pin below, (c) recent release date (within last ~12 months), (d) no suspicious post-install scripts called out in the project's README.

    1. https://pypi.org/project/claude-agent-sdk/  -> github.com/anthropics/claude-agent-sdk-python  (pin >=0.2.93,<0.3)
    2. https://pypi.org/project/alpaca-py/  -> github.com/alpacahq/alpaca-py  (pin >=0.42,<0.50)
    3. https://pypi.org/project/slack-bolt/  -> github.com/slackapi/bolt-python  (pin >=1.18,<2)
    4. https://pypi.org/project/fastapi/  -> github.com/fastapi/fastapi  (pin >=0.115,<0.120)
    5. https://pypi.org/project/sqlcipher3-wheels/  -> github.com/laggykiller/sqlcipher3  (pin >=0.5.7) — IF this looks under-maintained, decision-pivot to `sqlcipher3-binary` from coleifer/sqlcipher3 instead; document the choice in COMMIT message.
    6. https://pypi.org/project/apscheduler/  -> github.com/agronholm/apscheduler  (pin >=3.10,<4 — explicitly 3.x per RESEARCH §APScheduler decision)
    7. https://pypi.org/project/pydantic/  (pin >=2.7,<3)
    8. https://pypi.org/project/sqlalchemy/  (pin >=2.0,<3)
    9. https://pypi.org/project/structlog/  (pin >=24.5)
    10. https://pypi.org/project/httpx/  (pin >=0.27)
    11. https://pypi.org/project/pandas-market-calendars/  (pin >=4.4)
    12. https://pypi.org/project/finnhub-python/
    13. https://pypi.org/project/yahooquery/
    14. https://pypi.org/project/typer/  (pin >=0.12)
    15. https://pypi.org/project/tzdata/  (required on Windows per Pitfall 5)

    Run `python -m pip index versions <package>` to confirm the latest available version inside the declared range.
  </how-to-verify>
  <resume-signal>Type "verified" to confirm all 15 packages legitimate. If you decision-pivot any package (e.g., sqlcipher3-binary instead of sqlcipher3-wheels), state the substitution clearly before resuming.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create pyproject.toml + tool configs (uv, ruff with float-ban, mypy strict, pytest)</name>
  <files>pyproject.toml, .ruff.toml, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md  (§Standard Stack — exact pins; §Dev Tools — ruff/mypy/pytest config)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-VALIDATION.md  (§Wave 0 Requirements — pytest config keys)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-SKELETON.md  (§Stack Touched in Phase 1)
  </read_first>
  <action>
    Create `pyproject.toml` with `[project]` block declaring `name = "gekko"`, `version = "0.1.0"`, `requires-python = ">=3.12,<3.13"`, and `[project.dependencies]` array containing the EXACT pins from RESEARCH.md §Standard Stack installation block (claude-agent-sdk>=0.2.93,<0.3; alpaca-py>=0.42,<0.50; slack-bolt>=1.18,<2; fastapi>=0.115,<0.120; uvicorn[standard]; jinja2; python-multipart; sqlalchemy>=2.0,<3; alembic; sqlcipher3-wheels>=0.5.7 OR the substitute per Task 1; apscheduler>=3.10,<4 (explicitly 3.x per Open Question 1); pydantic>=2.7,<3; structlog>=24.5; httpx>=0.27; pandas_market_calendars>=4.4; finnhub-python; yahooquery; python-dateutil; tzdata; typer>=0.12).

    Add `[project.optional-dependencies]` dev = ["pytest>=8", "pytest-asyncio", "pytest-mock", "respx", "freezegun", "pytest-alembic", "mypy", "ruff", "pre-commit"].

    Add `[project.scripts]` block: `gekko = "gekko.cli:app"`. This MUST be the exact string per RESEARCH §Open Question 6 — Typer's `app` becomes the console-script entry.

    Add `[build-system]` requires = ["hatchling"], build-backend = "hatchling.build".

    Add `[tool.hatch.build.targets.wheel]` packages = ["src/gekko"] (src-layout).

    Add `[tool.pytest.ini_options]` (per VALIDATION.md §Wave 0):
      - `testpaths = ["tests"]`
      - `asyncio_mode = "auto"`
      - `markers = ["integration: hits external services unless cassettes are used"]`
      - `addopts = "-q --no-header -x"` (matches VALIDATION.md quick-run command)

    Add `[tool.mypy]` block: `python_version = "3.12"`, `strict = true`, with `[[tool.mypy.overrides]]` modules = ["alpaca.*", "finnhub.*", "yahooquery.*", "apscheduler.*", "sqlcipher3.*", "claude_agent_sdk.*"] ignore_missing_imports = true.

    Create `.ruff.toml` (separate file for visibility) with `line-length = 100`, `target-version = "py312"`, `[lint] select = ["E","F","I","B","UP","SIM","ANN","D"]` and the EXEC-01 / D-20 float ban via `[lint.per-file-ignores]` is NOT how this is done — use `[lint.flake8-tidy-imports.banned-module-level-imports]` is also wrong. The correct mechanism:
      Use `[lint.flake8-builtins]` plus a custom rule via `[lint.per-file-ignores]` is insufficient. The actual mechanism is: write a custom `pre-commit` hook OR use ruff's `flake8-no-pep420`. Simpler and proven approach per RESEARCH §Don't Hand-Roll: create a `pytest` gate at module import time. For ruff, the achievable enforcement is to set `[lint] extend-select = ["FLY","PERF","TRY"]` and add the explicit float ban as a `tests/unit/test_money_math.py` static-grep test (created in 01-05). Document this in a comment block in `.ruff.toml` so future executors don't expect a ruff-native rule.

    Note in `.ruff.toml` comment: "Per EXEC-01 / D-20, float usage in `src/gekko/brokers/**`, `src/gekko/execution/**`, `src/gekko/core/money.py` is enforced by `tests/unit/test_money_math.py` (grep-gate). See Plan 01-05 for the test implementation."

    Create `.gitignore` with standard Python ignores: `__pycache__/`, `*.pyc`, `.venv/`, `.uv/`, `dist/`, `build/`, `*.egg-info/`, `.pytest_cache/`, `.ruff_cache/`, `.mypy_cache/`, `*.db`, `*.db-wal`, `*.db-shm`, `~/.gekko/` (the runtime DB path per RESEARCH A13), `.env`, `.env.local`.

    After writing, run `uv sync` to confirm all deps resolve.
  </action>
  <verify>
    <automated>uv sync --frozen 2>&1 | tee /tmp/uv-sync.log; uv run ruff check . --output-format=concise; uv run mypy --version; uv run pytest --collect-only -q</automated>
  </verify>
  <done>
    `uv sync` exits 0 with a lockfile generated; `uv run ruff check .` exits 0 (zero violations on empty scaffold); `uv run mypy --version` prints a version (confirms install); `uv run pytest --collect-only` exits 0 (zero tests, zero collection errors — directories exist but are empty).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create src-layout directory tree with empty __init__.py files and CLI stub (per D-06 trigger surface)</name>
  <files>
    src/gekko/__init__.py,
    src/gekko/__main__.py,
    src/gekko/cli.py,
    src/gekko/py.typed,
    src/gekko/core/__init__.py,
    src/gekko/schemas/__init__.py,
    src/gekko/db/__init__.py,
    src/gekko/brokers/__init__.py,
    src/gekko/audit/__init__.py,
    src/gekko/agent/__init__.py,
    src/gekko/agent/tools/__init__.py,
    src/gekko/execution/__init__.py,
    src/gekko/approval/__init__.py,
    src/gekko/reporter/__init__.py,
    src/gekko/scheduler/__init__.py,
    src/gekko/slack/__init__.py,
    src/gekko/dashboard/__init__.py,
    src/gekko/vault/__init__.py,
    tests/__init__.py,
    tests/unit/__init__.py,
    tests/integration/__init__.py,
    tests/fixtures/cassettes/.gitkeep,
    tests/fixtures/strategies/.gitkeep
  </files>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md  (§Recommended Project Structure — exact directory layout)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-SKELETON.md  (§Absolute Minimum File Set)
  </read_first>
  <action>
    Create the empty package marker files exactly as RESEARCH.md §Recommended Project Structure dictates. Every `__init__.py` is empty except `src/gekko/__init__.py` which contains a single line: `__version__ = "0.1.0"`.

    Create `src/gekko/py.typed` as an empty file (PEP 561 marker so downstream consumers can pick up type info).

    Create `src/gekko/__main__.py` containing exactly:
        from gekko.cli import app
        if __name__ == "__main__":
            app()
    This is what makes `python -m gekko` route through Typer per RESEARCH Open Question 6.

    Create `src/gekko/cli.py` as the Typer app with command STUBS only (no real logic yet). The cli MUST declare these subcommands (per D-06, D-07, D-09, REG-02, and SKELETON.md §Demo Script) using `typer.Typer()`:
      - `init` — placeholder that prints "TODO: first-run wizard (01-09)"
      - `serve` — placeholder that prints "TODO: start FastAPI + Slack + scheduler (01-09)"
      - `run` taking a `strategy_name` positional argument — placeholder that prints "TODO: trigger strategy run (01-09)"
      - `doctor` — env-audit subcommand (implemented in Task 4 below)
      - `strategy create` — subcommand group with `create` action placeholder ("TODO: create strategy (01-09)")
      - `audit verify` and `audit dump` — placeholders ("TODO: audit chain operations (01-04 / 01-09)")

    Each stub returns exit code 0 (so `gekko --help` and `gekko run foo` both succeed during scaffolding). Use `typer.echo()` for output. The Typer app variable MUST be named `app` (matches `[project.scripts]` entry in pyproject.toml).

    Create `tests/fixtures/cassettes/.gitkeep` and `tests/fixtures/strategies/.gitkeep` as empty files so the directories exist in git.

    Do NOT add any business logic to cli.py beyond stubs. Real implementations are owned by later plans.
  </action>
  <verify>
    <automated>uv run gekko --help 2>&1 | tee /tmp/gekko-help.log && uv run gekko run dummy-strategy && uv run python -c "import gekko; import gekko.core; import gekko.schemas; import gekko.db; import gekko.brokers; import gekko.audit; import gekko.agent; import gekko.agent.tools; import gekko.execution; import gekko.approval; import gekko.reporter; import gekko.scheduler; import gekko.slack; import gekko.dashboard; import gekko.vault; print('ALL_MODULES_IMPORT_OK')"</automated>
  </verify>
  <done>
    `uv run gekko --help` exits 0 and lists subcommands `init`, `serve`, `run`, `doctor`, `strategy`, `audit`. `uv run gekko run dummy-strategy` exits 0 and prints the TODO placeholder. `python -c "import gekko; ..."` prints `ALL_MODULES_IMPORT_OK` confirming every package in the SKELETON.md file set imports cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement `gekko doctor` env-audit + smoke test (per VALIDATION.md Wave 0 row 1+2)</name>
  <files>
    src/gekko/cli.py,
    tests/unit/test_cli.py,
    tests/unit/test_smoke.py
  </files>
  <read_first>
    - src/gekko/cli.py  (the stub from Task 3)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-VALIDATION.md  (§Per-Task Verification Map rows 1+2; §Wave 0 Requirements last bullet — `tests/unit/test_smoke.py::test_imports`)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md  (§Environment Availability — what `gekko doctor` MUST check)
  </read_first>
  <action>
    Extend `src/gekko/cli.py` to implement the real `doctor` subcommand. The doctor function MUST:
      1. Print Python version; FAIL if not 3.12.x
      2. Check presence of env vars: `ANTHROPIC_API_KEY`, `ALPACA_PAPER_API_KEY`, `ALPACA_PAPER_SECRET_KEY`, `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_USER_ID`; print PRESENT / MISSING for each (do NOT echo their values — security: AUTH-04)
      3. Optionally check `FINNHUB_API_KEY` (degrade gracefully if missing — note per RESEARCH §Environment Availability "missing dependencies with fallback")
      4. Check that `tzdata` is importable (Windows zoneinfo gotcha per Pitfall 5) — `import zoneinfo; zoneinfo.ZoneInfo("America/New_York")` must not raise
      5. Check that `sqlcipher3` is importable (verifies wheel installed correctly per A9)
      6. Return exit code 0 if all REQUIRED checks pass; exit code 1 if any required missing.

    Required = Python 3.12, ANTHROPIC_API_KEY, ALPACA_PAPER_API_KEY/SECRET, SLACK_BOT_TOKEN/SIGNING_SECRET, tzdata-zoneinfo, sqlcipher3 import.
    Optional = FINNHUB_API_KEY, SLACK_USER_ID (warned but not failed).

    Create `tests/unit/test_cli.py`:
      - `test_help_smoke()`: invokes `gekko --help` via `typer.testing.CliRunner` and asserts exit code 0 and the strings "init", "serve", "run", "doctor", "strategy", "audit" appear in stdout. This implements VALIDATION.md row 2 ("gekko --help shows commands").
      - `test_doctor_missing_envvar_exits_nonzero(monkeypatch)`: monkeypatch deletes ANTHROPIC_API_KEY from env, invokes `gekko doctor`, asserts exit code != 0 and "ANTHROPIC_API_KEY" appears with "MISSING" in stdout.
      - `test_doctor_redacts_values(monkeypatch)`: monkeypatch sets ANTHROPIC_API_KEY="secret-value-xyz", invokes `gekko doctor`, asserts "secret-value-xyz" NOT in stdout (AUTH-04 redaction).

    Create `tests/unit/test_smoke.py` with `test_imports()`: imports every module listed in VALIDATION.md §Wave 0 Requirements last bullet (every `src/gekko/` package). This is the canary test catching broken `__init__.py` early per VALIDATION.md.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_cli.py tests/unit/test_smoke.py -q --no-header -x</automated>
  </verify>
  <done>
    `pytest tests/unit/test_cli.py tests/unit/test_smoke.py` exits 0 with 4 tests passing. `gekko doctor` exits 0 on a properly-configured env and exits 1 with a clear "MISSING: ANTHROPIC_API_KEY" message when the var is unset. No credential values are echoed in any code path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine -> PyPI | npm/pip/cargo install path — `[ASSUMED]` packages must be human-verified before install (per RESEARCH §Package Legitimacy Audit) |
| Operator env -> CLI stdout | Credentials (API keys, signing secrets) must never appear in `gekko doctor` output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip/uv installs of P1 deps | mitigate | Task 1 blocking-human checkpoint verifies each [ASSUMED] package's PyPI homepage and GitHub org before install; `sqlcipher3-wheels` flagged for extra scrutiny (community fork) |
| T-01-01 | Information Disclosure | `gekko doctor` stdout | mitigate | Task 4 — doctor prints PRESENT/MISSING flags only, never echoes env var values; covered by `test_doctor_redacts_values` |
| T-01-02 | Spoofing | `[project.scripts] gekko` entry | accept | Console-script entry resolves via `gekko.cli:app` — typo here would break the entire CLI; verified by `gekko --help` smoke test |
</threat_model>

<verification>
- `uv sync` resolves and locks all deps on Python 3.12
- `uv run gekko --help` exits 0 with all 6 subcommands listed
- `uv run gekko doctor` correctly reports env-var presence WITHOUT echoing values
- `uv run pytest --collect-only` exits 0
- `uv run pytest tests/unit/ -q` runs 4 tests passing
- `uv run ruff check .` exits 0 (clean scaffold)
- All 16 `gekko.*` subpackages import without error
</verification>

<success_criteria>
1. Wave 0 gate from SKELETON.md §"Wave 0" item 7 (`pytest --collect-only` runs cleanly) is met
2. The `gekko` console script entry works (`gekko --help` exits 0) — every later plan can extend cli.py confidently
3. Package legitimacy gate from RESEARCH §Package Legitimacy Audit is closed (Task 1 checkpoint approved)
4. Float-ban policy is documented (in `.ruff.toml` comment) and tagged for enforcement in Plan 01-05 test module
5. VALIDATION.md rows 1 ("env audit via `gekko doctor`") and 2 ("gekko --help shows commands") have automated commands implemented and passing
</success_criteria>

<output>
Create `.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-01-SUMMARY.md` when done. Include:
- Confirmed final dependency pins (especially the sqlcipher3 choice — wheels vs binary)
- Whether `gekko doctor` flagged any missing env vars (so user knows what to set up before Wave 2 e2e test)
- Any deviations from RESEARCH.md §Standard Stack pins (with rationale)
</output>
