---
phase: 01-foundation-vertical-slice-alpaca-paper-slack-hitl
plan: 05
type: execute
wave: 1
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/gekko/core/types.py
  - src/gekko/core/money.py
  - src/gekko/core/ids.py
  - src/gekko/brokers/base.py
  - src/gekko/brokers/alpaca.py
  - src/gekko/brokers/stream.py
  - tests/unit/test_money_math.py
  - tests/unit/test_client_order_id.py
  - tests/unit/test_brokerage_abc.py
  - tests/unit/test_alpaca_constructor_guard.py
  - tests/integration/test_alpaca_paper_round_trip.py
  - tests/fixtures/cassettes/alpaca_paper_round_trip.json
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-07, BROK-A-01, BROK-A-03, BROK-A-04, BROK-A-05, BROK-A-06]

must_haves:
  truths:
    - "Every money value in gekko/brokers/, gekko/execution/, gekko/core/money.py is a Decimal (float-ban enforced by tests/unit/test_money_math.py grep gate)"
    - "client_order_id = sha256(f'{strategy_id}|{decision_id}|{side}|{qty}|{ticker}').hexdigest()[:32] — same inputs always produce the same id"
    - "Brokerage ABC defines health_check, get_account, get_positions, get_quote, place_order, get_order_by_client_order_id, cancel_order (the load-bearing interface P2/P8/P9 extend)"
    - "AlpacaBroker(paper=False) raises BrokerConfigError unconditionally (P1 Knight Capital insurance per Pitfall 7)"
    - "AlpacaBroker.place_order with a duplicate client_order_id (HTTP 422) calls get_order_by_client_order_id and returns the existing order — never re-POSTs (Pitfall 4)"
    - "AlpacaBroker supports LIMIT, MARKET, and STOP order types (EXEC-07)"
    - "TradingStream listener subscribes to trade_updates and routes 'fill' events through a callback"
    - "Full Alpaca paper round-trip integration test (cassette-replay default; live with GEKKO_TEST_LIVE_ALPACA=1)"
  artifacts:
    - path: "src/gekko/brokers/base.py"
      provides: "Brokerage ABC + OrderRequest/OrderResult dataclasses + OrderSide/OrderType/TimeInForce enums — the load-bearing interface that P2 OrderGuard, P8 IBKR/Schwab, P9 browser-fallback brokers extend"
      exports: ["Brokerage", "OrderRequest", "OrderResult", "OrderSide", "OrderType", "TimeInForce"]
    - path: "src/gekko/brokers/alpaca.py"
      provides: "AlpacaBroker — paper-only Phase 1 implementation; constructor rejects non-paper"
      exports: ["AlpacaBroker"]
    - path: "src/gekko/brokers/stream.py"
      provides: "AlpacaFillStream — TradingStream wrapper that routes fill events to async callbacks"
      exports: ["AlpacaFillStream"]
    - path: "src/gekko/core/ids.py"
      provides: "compute_client_order_id(strategy_id, decision_id, side, qty, ticker) per D-20"
      exports: ["compute_client_order_id"]
    - path: "src/gekko/core/money.py"
      provides: "Decimal helpers — to_decimal(str_or_decimal), assert_positive(d), round_money(d, places=4)"
      exports: ["to_decimal", "assert_positive", "round_money"]
  key_links:
    - from: "src/gekko/brokers/alpaca.py"
      to: "alpaca.trading.client.TradingClient"
      via: "TradingClient(api_key, secret_key, paper=True) constructed only after paper-mode probe"
      pattern: "TradingClient.*paper=True"
    - from: "src/gekko/core/ids.py"
      to: "hashlib.sha256"
      via: "sha256(pipe-delimited input).hexdigest()[:32] per D-20"
      pattern: "sha256|hexdigest"
---

<objective>
Land the deterministic safety primitives every Phase 1 trade execution depends on: (1) `gekko.core` — Decimal helpers (float-ban tested via grep gate), the deterministic `client_order_id` function locked by D-20, the enum + dataclass core types; (2) `gekko.brokers.base` — the `Brokerage` ABC that P2 OrderGuard wraps, P8 IBKR/Schwab and P9 Robinhood/Fidelity implement; (3) `gekko.brokers.alpaca` — the paper-only AlpacaBroker with constructor-time live-key rejection (Pitfall 7 insurance) and HTTP-422 duplicate-id handling (Pitfall 4); (4) `gekko.brokers.stream` — the TradingStream fill listener; (5) a full Alpaca paper round-trip integration test with cassette default + live-paper opt-in.

Purpose: This plan is the centerpiece of Phase 1's "real money safety floor" requirement. EXEC-01, EXEC-02, and the BROK-A-* family are all here. The `Brokerage` ABC is one of two one-shot architectural primitives this phase locks (the other is `ResearchBrief`, owned by 01-06): every later phase (P2 OrderGuard, P8 IBKR/Schwab, P9 browser-fallback) extends it without rewriting. Per RESEARCH §Architecture Patterns "this is the load-bearing interface."

Output: An async `AlpacaBroker(api_key=..., secret_key=..., paper=True)` instance whose `.place_order(OrderRequest(...))` deposits a real paper order with the deterministic client_order_id, returns an `OrderResult`, and whose `AlpacaFillStream` background task routes the fill event to the audit log via `append_event("fill", ...)`. Constructing the broker with `paper=False` raises `BrokerConfigError` immediately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-CONTEXT.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-SKELETON.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-VALIDATION.md
@src/gekko/db/models.py
@src/gekko/core/errors.py
@src/gekko/config.py
@src/gekko/audit/log.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core types — Decimal helpers, client_order_id, enums, OrderRequest/Result dataclasses (D-20, EXEC-01, EXEC-02)</name>
  <files>
    src/gekko/core/types.py,
    src/gekko/core/money.py,
    src/gekko/core/ids.py,
    tests/unit/test_money_math.py,
    tests/unit/test_client_order_id.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md  (§Code Examples — Brokerage ABC enum block; §Pattern 4 — Deterministic client_order_id; §Pitfall 6 — Decimal serialization; §Pitfall 10 — Decimal JSON round-trip)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-CONTEXT.md  (D-20 — exact hashing scheme)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-VALIDATION.md  (EXEC-01 + EXEC-02 rows)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-01-PLAN.md  (Task 2 note — float-ban enforced via test_money_math grep gate)
  </read_first>
  <behavior>
    ## money.py
    - Test: `to_decimal("100.50")` returns `Decimal("100.50")`
    - Test: `to_decimal(Decimal("100.50"))` returns the same Decimal (idempotent)
    - Test: `to_decimal(100.5)` raises `TypeError` — accepting a float here is the bug we're guarding against (EXEC-01); only str and Decimal are accepted
    - Test: `assert_positive(Decimal("0"))` raises `ValueError`; `assert_positive(Decimal("-1"))` raises; `assert_positive(Decimal("0.01"))` returns without raising
    - Test: `round_money(Decimal("1.234567"), places=4)` returns `Decimal("1.2346")` (ROUND_HALF_EVEN)
    - Test (the grep gate): `test_float_banned_in_money_paths()` walks `src/gekko/brokers/`, `src/gekko/execution/`, and `src/gekko/core/money.py` and asserts NO source line (ignoring comments) contains the bare token `float(` or `: float` or `-> float`. Use a regex like `r'(?<![A-Za-z_])float\b'`. Read each .py file, strip comment lines (lines starting with `#` after lstrip), then assert no match. Comment-only mentions of `float` are allowed (for documentation). This is the EXEC-01 enforcement gate per Plan 01-01 Task 2 note. (Plan 01-05 owns this test now; Plan 01-07 / 01-08 must not re-introduce float in those modules.)

    ## ids.py
    - Test: `compute_client_order_id(strategy_id="s1", decision_id="d1", side="buy", qty=Decimal("100"), ticker="NVDA")` returns a 32-char lowercase hex string
    - Test: Same inputs produce the same output (deterministic — D-20)
    - Test: Different `decision_id` produces different output (legitimate re-runs get fresh ids — RESEARCH §Pitfall 4)
    - Test: `qty` is normalized before hashing: `compute_client_order_id(..., qty=Decimal("100"))` and `compute_client_order_id(..., qty=Decimal("100.0"))` produce the SAME hash. Per RESEARCH §Pattern 4 — "standardize on `str(qty.normalize())` if fractional shares are ever in play." Plan 01-05 normalizes always.
    - Test: `side` is normalized to lowercase: `side="BUY"` and `side="buy"` produce the same hash
    - Test: `ticker` is normalized to uppercase: `ticker="nvda"` and `ticker="NVDA"` produce the same hash
    - Test: Output length is exactly 32 characters

    ## types.py (no separate test — covered indirectly by brokers tests)
    - `OrderSide` is `Enum` with `.BUY = "buy"`, `.SELL = "sell"` (str enum so JSON-serializable)
    - `OrderType` is str enum with `.MARKET`, `.LIMIT`, `.STOP`
    - `TimeInForce` is str enum with `.DAY`, `.GTC`
  </behavior>
  <action>
    Implement per behaviors above following the exact shape from RESEARCH §Code Examples — `Brokerage` ABC enum block + §Pattern 4 — client_order_id function.

    `src/gekko/core/types.py`:
      - `class OrderSide(str, Enum): BUY = "buy"; SELL = "sell"`
      - `class OrderType(str, Enum): MARKET = "market"; LIMIT = "limit"; STOP = "stop"`
      - `class TimeInForce(str, Enum): DAY = "day"; GTC = "gtc"`
      Use `str, Enum` mixin so values are JSON-serializable (Pydantic + canonical_json handle these automatically).

    `src/gekko/core/money.py`:
      - `def to_decimal(value: str | Decimal) -> Decimal`: returns Decimal; raises TypeError on float
      - `def assert_positive(value: Decimal) -> None`: raises ValueError if `value <= 0`
      - `def round_money(value: Decimal, places: int = 4) -> Decimal`: uses `Decimal.quantize(Decimal(f"1e-{places}"), rounding=ROUND_HALF_EVEN)`
      - NO `float` imports anywhere in this module

    `src/gekko/core/ids.py`:
      - `def compute_client_order_id(*, strategy_id: str, decision_id: str, side: str, qty: Decimal, ticker: str) -> str`:
            normalized_side = side.lower()
            normalized_ticker = ticker.upper().strip()
            normalized_qty = str(qty.normalize())  # Decimal("100.0") -> "1E+2" — wrong; use str(qty.normalize()) but verify "100" not "1E+2"
            # Decimal("100").normalize() returns Decimal("1E+2") which str()-s to "1E+2"
            # We want stable "100" output. Use: f"{qty.normalize():f}" formatter to get plain decimal
            # OR: do a manual canonicalization: strip trailing zeros after decimal point, strip trailing decimal point
        IMPORTANT: the executor MUST verify the canonical qty string form. Decimal("100.0").normalize() = Decimal("1E+2"); str() = "1E+2". The test "Decimal('100') and Decimal('100.0') produce same hash" requires a stable canonical form. Use `format(qty.normalize(), 'f')` which yields "100" for both. Test EXACTLY this form.
            raw = f"{strategy_id}|{decision_id}|{normalized_side}|{normalized_qty}|{normalized_ticker}"
            return hashlib.sha256(raw.encode("utf-8")).hexdigest()[:32]

      Add a module docstring referencing D-20 + RESEARCH §Pitfall 4.

    Create `tests/unit/test_money_math.py`:
      - 6 behavior tests for money.py
      - The grep-gate test `test_float_banned_in_money_paths()`. Implementation hint:
          import re
          BANNED = re.compile(r'(?<![A-Za-z_])float\b')
          dirs = [Path("src/gekko/brokers"), Path("src/gekko/execution"), Path("src/gekko/core/money.py")]
          for path in dirs:
              for py in (path.rglob("*.py") if path.is_dir() else [path]):
                  text = py.read_text(encoding="utf-8")
                  # Strip comment lines so `# float` in docs doesn't trip the gate (per planner antipatterns: grep-c counts comments)
                  code_only = "\n".join(line for line in text.splitlines() if not line.lstrip().startswith("#"))
                  matches = BANNED.findall(code_only)
                  assert not matches, f"float usage banned in {py}: {matches}"
        Note in the test docstring: "Per EXEC-01 / D-20, float in money-handling modules is a Knight-Capital risk. This grep gate is the planner-locked enforcement (per Plan 01-01 .ruff.toml comment)."

    Create `tests/unit/test_client_order_id.py` with all 7 behaviors.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_money_math.py tests/unit/test_client_order_id.py -q --no-header -x</automated>
  </verify>
  <done>
    13 tests pass. `test_float_banned_in_money_paths` is the grep gate that fails CI if any later plan introduces `float` into `brokers/`, `execution/`, or `core/money.py`. Smoke: `python -c "from gekko.core.ids import compute_client_order_id; from decimal import Decimal; print(compute_client_order_id(strategy_id='s1', decision_id='d1', side='buy', qty=Decimal('100'), ticker='NVDA'))"` prints a 32-char hex.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Brokerage ABC + AlpacaBroker paper-only constructor guard (load-bearing interface; Pitfall 7)</name>
  <files>
    src/gekko/brokers/base.py,
    src/gekko/brokers/alpaca.py,
    tests/unit/test_brokerage_abc.py,
    tests/unit/test_alpaca_constructor_guard.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md  (§Code Examples — Brokerage ABC + AlpacaBroker full code; §Pitfall 4 — duplicate client_order_id 422 handling; §Pitfall 7 — paper-vs-live mix-up)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-CONTEXT.md  (D-24 — alpaca-py paper-only in P1; live keys rejected by orchestrator until P2; D-21 — per-user broker)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-SKELETON.md  (§What's Real vs Minimal — Broker: paper only; live constructor-rejected; Cancel/rate-limit hardening deferred to P2)
    - src/gekko/core/types.py, src/gekko/core/ids.py, src/gekko/core/errors.py
  </read_first>
  <behavior>
    ## base.py
    - Test: `Brokerage` is an `ABC` and cannot be instantiated directly
    - Test: A subclass missing any of {health_check, get_account, get_positions, get_quote, place_order, get_order_by_client_order_id, cancel_order} cannot be instantiated
    - Test: `OrderRequest(symbol="NVDA", side=OrderSide.BUY, qty=Decimal("5"), order_type=OrderType.LIMIT, limit_price=Decimal("1234.56"))` constructs; the dataclass is frozen
    - Test: `OrderResult(broker_order_id="x", client_order_id="y", status="filled", filled_qty=Decimal("5"), avg_fill_price=Decimal("1234.56"), raw={...})` constructs; frozen
    - Test: Comments in `base.py` reference future extensions: P2 OrderGuard wraps `place_order`; P8 IBKR + Schwab adapters; P9 Robinhood + Fidelity browser-fallback adapters (per SKELETON.md). Use `# Phase 2 hook:`, `# Phase 8 extension:`, `# Phase 9 extension:` markers in the source. Test asserts these markers exist via grep on the file content.

    ## alpaca.py
    - Test: `AlpacaBroker(api_key="x", secret_key="y", paper=False)` raises `BrokerConfigError` immediately (BEFORE constructing the TradingClient) with a message mentioning "Phase 1" and "paper" and "Phase 2 OrderGuard"
    - Test: `AlpacaBroker(api_key="x", secret_key="y", paper=True)` succeeds in constructing the object (we'll mock the TradingClient at the unit-test layer)
    - Test: `AlpacaBroker(api_key="x", secret_key="y")` defaults to `paper=True` (defensive default — D-24 paper-only)
    - Test: `broker.is_paper is True` after construction
    - Test: `broker.name == "alpaca"`
    - Test: `broker.supports_fractional is True` (alpaca paper supports fractional shares)
    - Test: A unit-test variant where the constructor's internal `get_account()` probe returns a non-paper-shaped account raises `BrokerConfigError("Paper-mode assertion failed; refusing to construct broker")` per RESEARCH Pitfall 7. Mock `TradingClient.get_account` to return an object whose `.id` does NOT start with "paper-" and whose `_base_url` is the live endpoint; assert the second-layer guard fires.
  </behavior>
  <action>
    Implement `src/gekko/brokers/base.py` exactly per RESEARCH §Code Examples — Brokerage ABC. Key requirements:
      - `@dataclass(frozen=True) class OrderRequest`: fields `symbol: str`, `side: OrderSide`, `qty: Decimal`, `order_type: OrderType`, `limit_price: Optional[Decimal] = None`, `stop_price: Optional[Decimal] = None`, `time_in_force: TimeInForce = TimeInForce.DAY`, `client_order_id: str = ""`
      - `@dataclass(frozen=True) class OrderResult`: as in RESEARCH
      - `class Brokerage(ABC)` with class attributes `name: str` (subclass sets), `supports_fractional: bool`, `is_paper: bool`
      - Abstract methods (per RESEARCH list): `async def health_check`, `get_account`, `get_positions`, `get_quote(symbol)`, `place_order(req)`, `get_order_by_client_order_id(client_order_id)`, `cancel_order(broker_order_id)`
      - Add the inline comments:
          # Phase 2 hook: P2 OrderGuard wraps `place_order` with universe-whitelist + hard-cap + paper/live env-pairing checks before delegating here. See ROADMAP.md Phase 2 success criteria.
          # Phase 8 extension: IBKRBroker (via ib_async + TWS/IB Gateway side-process) and SchwabBroker (schwab-py + 7-day refresh-token coordinator) implement this same ABC.
          # Phase 9 extension: RobinhoodBrowserBroker and FidelityBrowserBroker use browser-use; same ABC contract, additional `capture_screenshot()` audit hook per BROK-R-05.
      - Imports must NOT include `float` anywhere; the grep gate from Task 1 enforces this.

    Implement `src/gekko/brokers/alpaca.py` per RESEARCH §Code Examples — AlpacaBroker. CRITICAL details:
      - `__init__(self, *, api_key: str, secret_key: str, paper: bool = True)`:
          1. If `not paper`: `raise BrokerConfigError("Phase 1 supports paper trading only (live blocked until Phase 2 OrderGuard). If you intended paper, pass paper=True.")`
          2. `self.is_paper = True`
          3. Construct `self._client = TradingClient(api_key, secret_key, paper=True)`
          4. Belt-and-braces probe per RESEARCH Pitfall 7:
                acct = self._client.get_account()  # sync call; OK inside __init__
                base_url = str(getattr(self._client, "_base_url", ""))
                if "paper" not in base_url.lower() and not getattr(acct, "id", "").startswith("paper-"):
                    raise BrokerConfigError("Paper-mode assertion failed; refusing to construct broker")
              Note: this matches RESEARCH §Pitfall 7 exactly. The executor MUST verify the actual `paper` Alpaca account id prefix from alpaca-py 0.42+ — research is plausible but not 100% verified. If `paper-` prefix is not how alpaca-py exposes paper accounts, fall back to URL-only check (`"paper" in base_url.lower()`) and TODO-comment the account-id check. The integration test in Task 4 will surface the correct value.
      - `name = "alpaca"`, `supports_fractional = True` as class attributes
      - Methods stubs only at this task; full async implementations land in Task 3.

    Create `tests/unit/test_brokerage_abc.py` for the 5 ABC behaviors. Define a minimal `class _DummyBroker(Brokerage)` that implements every method so instantiability tests work; a `class _IncompleteBroker(Brokerage)` missing one method to verify ABC rejection.

    Create `tests/unit/test_alpaca_constructor_guard.py` for the 7 alpaca constructor behaviors. Use `mocker.patch("alpaca.trading.client.TradingClient")` to intercept the TradingClient constructor and inject the get_account return shape needed for each test.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_brokerage_abc.py tests/unit/test_alpaca_constructor_guard.py -q --no-header -x</automated>
  </verify>
  <done>
    12 tests pass. Smoke: `python -c "from gekko.brokers.alpaca import AlpacaBroker; from gekko.core.errors import BrokerConfigError; \ntry: AlpacaBroker(api_key='x', secret_key='y', paper=False)\nexcept BrokerConfigError as e: print('REJECTED:', e)"` prints REJECTED. Live keys cannot reach the TradingClient constructor.
  </done>
</task>

<task type="auto">
  <name>Task 3: AlpacaBroker async method implementations (place_order with 422 handling, get_*, cancel, stream)</name>
  <files>
    src/gekko/brokers/alpaca.py,
    src/gekko/brokers/stream.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-RESEARCH.md  (§Code Examples — AlpacaBroker complete method bodies; §Pitfall 4 — 422 duplicate handling; §TradingStream fill listener code)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-CONTEXT.md  (D-24 — paper-only)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-SKELETON.md  (§What's Real vs Minimal — Cancel/rate-limit hardening deferred to P2; P1 implements basic submit + cancel)
    - src/gekko/brokers/alpaca.py  (Task 2 constructor)
    - src/gekko/core/types.py, src/gekko/core/errors.py
    - src/gekko/audit/log.py  (so the stream listener can call append_event in Task 4 integration test)
  </read_first>
  <action>
    Implement the async methods on `AlpacaBroker` per RESEARCH §Code Examples — AlpacaBroker. Concrete requirements:

      `async def health_check(self) -> bool`: try `await self.get_account()`; return True on success, False on any Exception (caught broadly because health check should never raise).

      `async def get_account(self) -> dict`: `acct = await asyncio.to_thread(self._client.get_account)`; return `acct.model_dump(mode="json")`. (alpaca-py is sync; wrap in `to_thread` per RESEARCH — async story is "wrap sync calls".)

      `async def get_positions(self) -> list[dict]`: same pattern via `get_all_positions`. Return list of `p.model_dump(mode="json")`.

      `async def get_quote(self, symbol: str) -> dict`: use `alpaca.data.historical.StockHistoricalDataClient` constructed inside `__init__` (add a `self._data_client = StockHistoricalDataClient(api_key, secret_key)`); call `await asyncio.to_thread(self._data_client.get_stock_latest_quote, StockLatestQuoteRequest(symbol_or_symbols=symbol))` and return `result[symbol].model_dump(mode="json")`. Document the returned shape in a docstring (ask/bid/timestamp).

      `async def place_order(self, req: OrderRequest) -> OrderResult`: the heart of EXEC-07 + BROK-A-04. Match RESEARCH §Code Examples body:
        - Map `OrderSide` -> `alpaca.trading.enums.OrderSide`, `TimeInForce` -> `alpaca.trading.enums.TimeInForce`
        - Build the correct request type based on `req.order_type`:
            LIMIT -> `LimitOrderRequest(symbol=req.symbol, qty=str(req.qty), side=side, limit_price=str(req.limit_price), time_in_force=tif, client_order_id=req.client_order_id)`
            MARKET -> `MarketOrderRequest(symbol=req.symbol, qty=str(req.qty), side=side, time_in_force=tif, client_order_id=req.client_order_id)`
            STOP -> `StopOrderRequest(symbol=req.symbol, qty=str(req.qty), side=side, stop_price=str(req.stop_price), time_in_force=tif, client_order_id=req.client_order_id)`
          IMPORTANT: pass `qty` and prices as `str(Decimal)` per RESEARCH A5 — alpaca-py 0.42+ accepts strings reliably. The float-ban grep gate enforces this.
        - `try`: `order = await asyncio.to_thread(self._client.submit_order, order_data=order_req)`
        - `except Exception as e`: if `"duplicate" in str(e).lower() or "already exists" in str(e).lower() or "422" in str(e)`, fall through to `get_order_by_client_order_id(req.client_order_id)` and return that result. Otherwise `raise BrokerOrderError(f"submit_order failed: {e}") from e`. This implements RESEARCH §Pitfall 4 — "duplicate rejection IS the safety; never re-POST."
        - Build OrderResult with `filled_qty=Decimal(str(order.filled_qty or 0))`, `avg_fill_price=Decimal(str(order.filled_avg_price)) if order.filled_avg_price else None`, `raw=order.model_dump(mode="json")`.

      `async def get_order_by_client_order_id(self, client_order_id: str) -> OrderResult | None`: per RESEARCH; wrap `self._client.get_order_by_client_id` (verify exact alpaca-py method name during integration test); return None on any exception (catch broadly because this is a probe).

      `async def cancel_order(self, broker_order_id: str) -> bool`: `await asyncio.to_thread(self._client.cancel_order_by_id, broker_order_id)`; return True. P1 keeps this minimal per SKELETON — rate-limit hardening + retry policy is P2.

    Create `src/gekko/brokers/stream.py` implementing `AlpacaFillStream` per RESEARCH §TradingStream fill listener. Concrete requirements:

      class AlpacaFillStream:
          def __init__(self, *, api_key: str, secret_key: str, user_id: str, on_fill: Callable[[dict], Awaitable[None]]):
              self._stream = TradingStream(api_key, secret_key, paper=True)
              self._user_id = user_id
              self._on_fill = on_fill   # async callback receiving the fill payload dict
              self._task: asyncio.Task | None = None

          async def _handle_trade_update(self, data):
              order = data.order
              if data.event == "fill":
                  payload = {
                      "client_order_id": order.client_order_id,
                      "broker_order_id": str(order.id),
                      "filled_qty": str(order.filled_qty),
                      "filled_avg_price": str(order.filled_avg_price),
                      "ticker": order.symbol,
                      "user_id": self._user_id,
                  }
                  await self._on_fill(payload)

          def start(self) -> asyncio.Task:
              self._stream.subscribe_trade_updates(self._handle_trade_update)
              # alpaca-py's TradingStream.run() is sync-blocking. We need to run it in an executor.
              # Per RESEARCH A6: alpaca-py's stream uses websockets internally; wrap in to_thread or use its async API.
              # Verify in integration test which one works. Default approach:
              self._task = asyncio.create_task(asyncio.to_thread(self._stream.run))
              return self._task

          async def stop(self):
              if self._task:
                  self._stream.stop()
                  self._task.cancel()
                  await asyncio.gather(self._task, return_exceptions=True)

      Decoupling: the `on_fill` callback is supplied by the caller — Plan 01-08 (executor) will pass a closure that calls `append_event("fill", payload)` and updates the matching proposal row's status to FILLED.

    Validate per the grep-gate float ban: `place_order` MUST NOT use `float(...)` anywhere. Pass `str(Decimal)` to alpaca-py request constructors.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_alpaca_constructor_guard.py tests/unit/test_money_math.py::test_float_banned_in_money_paths -q --no-header -x</automated>
  </verify>
  <done>
    Constructor tests still pass. Float-ban grep gate passes (no `float(` in `src/gekko/brokers/`). Smoke: `python -c "import asyncio; from gekko.brokers.alpaca import AlpacaBroker; from gekko.core.types import OrderRequest, OrderSide, OrderType; from decimal import Decimal; async def t(): b = AlpacaBroker(api_key='${ALPACA_PAPER_API_KEY}', secret_key='${ALPACA_PAPER_SECRET_KEY}', paper=True); h = await b.health_check(); print('HEALTH:', h); asyncio.run(t())"` returns True (requires real paper creds; failing on CI without creds is acceptable — Task 4's integration test is gated on the env vars).
  </done>
</task>

<task type="auto">
  <name>Task 4: Alpaca paper round-trip integration test (BROK-A-01..06, EXEC-07) with cassette default + live opt-in</name>
  <files>
    tests/integration/test_alpaca_paper_round_trip.py,
    tests/fixtures/cassettes/alpaca_paper_round_trip.json
  </files>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-VALIDATION.md  (BROK-A-01..06 row — cassette by default, `GEKKO_TEST_LIVE_ALPACA=1` for live)
    - .planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-SKELETON.md  (§First-to-Nth Task Ordering — Wave 1 item 9: "Integration test: place limit order against Alpaca paper, observe fill via websocket, verify event chain in DB — wave gate")
    - src/gekko/brokers/alpaca.py, src/gekko/brokers/stream.py  (Tasks 2+3)
    - src/gekko/audit/log.py  (so the test can confirm fill event written)
  </read_first>
  <action>
    Create `tests/integration/test_alpaca_paper_round_trip.py` with these tests (all `@pytest.mark.integration`):

      `test_alpaca_paper_health_check`: constructs AlpacaBroker with env creds, asserts health_check returns True. SKIP if `ALPACA_PAPER_API_KEY` env not set.

      `test_alpaca_paper_get_account`: asserts get_account returns dict with keys `id`, `account_number`, `buying_power`, `status`. Asserts account is in paper mode (e.g., `id` startswith "paper-" or url contains "paper").

      `test_alpaca_paper_get_positions`: asserts get_positions returns a list (may be empty). Just structural check.

      `test_alpaca_paper_place_limit_order_and_cancel`: places a LIMIT BUY order for 1 share of a well-known liquid ticker (e.g., AAPL) at a limit price 10% below the current ask (so it will NOT fill immediately). client_order_id = compute_client_order_id(strategy_id="test", decision_id=str(uuid4()), side="buy", qty=Decimal("1"), ticker="AAPL"). Asserts OrderResult returned. Then calls `get_order_by_client_order_id(client_order_id)` and asserts the same order is retrievable. Finally calls `cancel_order(result.broker_order_id)` and asserts True. Catches BrokerOrderError if Alpaca rejects (test environment edge case) — log and skip.

      `test_alpaca_paper_duplicate_client_order_id_returns_existing`: places an order with a fresh client_order_id, captures result. Immediately calls place_order with the SAME OrderRequest (same client_order_id). Asserts the second call returns an OrderResult with the same broker_order_id as the first (Pitfall 4 — never re-POST, return existing). This is the critical Knight Capital insurance test.

      `test_alpaca_paper_trading_stream_receives_fill_event`: places a MARKET BUY for 1 share of AAPL (will fill at market open or immediately during RTH). Starts the AlpacaFillStream with a callback that records the fill payload in a list. Waits up to 30s for the fill event. Asserts the recorded fill has matching client_order_id. SKIP if outside market hours (`pandas_market_calendars` schedule check) — VALIDATION.md §Manual-Only Verifications row 2 acknowledges websocket replay is brittle, so this test is gated on (a) live env var AND (b) market hours open. Cassette fallback: load `tests/fixtures/cassettes/alpaca_paper_round_trip.json` and replay the recorded fill payload through the stream's on_fill callback directly.

    Cassette mode (default — runs in CI without creds):
      - Create `tests/fixtures/cassettes/alpaca_paper_round_trip.json` as a JSON blob containing recorded responses for: get_account, get_positions, place_order (limit), get_order_by_client_order_id, cancel_order, trade_updates fill event. Use `respx` (already in pyproject.toml dev deps) to intercept httpx calls. For the alpaca-py SDK which uses requests/httpx internally, respx will match on URL patterns.
      - The cassette can be regenerated by running with `GEKKO_TEST_LIVE_ALPACA=1` and the test recording responses. Document this in the file header.
      - If respx cannot easily intercept alpaca-py's HTTP calls (it uses `requests` not httpx in some versions), use `unittest.mock.patch` on the broker's underlying client methods directly to inject canned responses. Document the chosen mocking approach.

    All tests require fixtures from conftest: `clean_settings_env` is enough for credential setup since they read from `Settings`. Skip via `pytest.skip(...)` if env vars absent.

    Note: when running with `GEKKO_TEST_LIVE_ALPACA=1`, the test MUST run during market hours (use `pandas_market_calendars` NYSE schedule check inside the test — and yes, this is a circular import since 01-08 owns market_hours.py. Workaround: inline the schedule check in this test using pandas_market_calendars directly; do NOT import gekko.execution.market_hours which doesn't exist yet).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_alpaca_paper_round_trip.py -q --no-header -m integration; if [ -n "${GEKKO_TEST_LIVE_ALPACA:-}" ]; then echo "LIVE_PAPER_MODE_RAN"; fi</automated>
  </verify>
  <done>
    All 6 integration tests pass in cassette-replay mode (default, no creds required). When `GEKKO_TEST_LIVE_ALPACA=1` AND market is open AND paper creds are set, the live variants all pass against real Alpaca paper. The cassette file exists and is checked into git (paper-only, no live data, no credentials embedded).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process -> Alpaca API | Live keys MUST NEVER reach the network in P1 |
| Process -> Alpaca API | Duplicate POSTs (HTTP 422) MUST be detected and handled by querying existing order, never re-POSTed (Knight Capital) |
| Application LLM -> Order placement | LLM never directly calls `place_order` — the Decision subagent's tool list is exactly `[propose_trade, propose_no_action]` (enforced in Plan 01-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | AlpacaBroker constructor accepting live keys | mitigate | Task 2 — `paper=False` raises `BrokerConfigError` immediately; double-check probe asserts the account id / base URL also indicates paper |
| T-01-05-02 | Tampering | Duplicate order POSTs causing Knight-Capital double-fills | mitigate | Task 3 — HTTP 422 / duplicate / already-exists exception is caught and routed to `get_order_by_client_order_id`; original order returned, never re-POSTed (Pitfall 4) |
| T-01-05-03 | Tampering | Float arithmetic in money paths | mitigate | Task 1 grep-gate test (`test_float_banned_in_money_paths`) fails CI if any later plan re-introduces float in brokers/, execution/, core/money.py |
| T-01-05-04 | Spoofing | Non-deterministic client_order_id allowing duplicate fills under retries | mitigate | Task 1 — `compute_client_order_id` is SHA-256 over canonical pipe-delimited inputs; idempotent (D-20) |
| T-01-05-05 | Information Disclosure | API keys appearing in raw broker response dicts logged via structlog | mitigate | structlog `_redact` processor (Plan 01-02) scrubs PK/sk-/Bearer patterns; broker raw responses don't include the key but defense-in-depth applies |
| T-01-05-06 | Repudiation | Fill event missed if TradingStream task dies silently | accept (P1) | P1 ships the listener; supervised auto-restart is P7. Plan 01-09 e2e test verifies the fill event arrives in the audit log; a missed fill surfaces as a stuck PENDING_FILL proposal in the dashboard. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_money_math.py tests/unit/test_client_order_id.py tests/unit/test_brokerage_abc.py tests/unit/test_alpaca_constructor_guard.py -q` runs 25+ tests passing
- `uv run pytest tests/integration/test_alpaca_paper_round_trip.py -q -m integration` runs 6 tests passing (cassette mode)
- With `GEKKO_TEST_LIVE_ALPACA=1` set and market open, the same suite passes against real Alpaca paper
- Grep gate: `grep -RnE '(?<![A-Za-z_])float\b' src/gekko/brokers/ src/gekko/execution/ src/gekko/core/money.py` returns zero non-comment matches
- Constructor test: `AlpacaBroker(paper=False)` raises BEFORE any TradingClient is constructed (mock patches confirm)
- Duplicate-id test: second submit_order call returns the SAME broker_order_id as the first
</verification>

<success_criteria>
1. EXEC-01 (Decimal everywhere) enforced by `test_float_banned_in_money_paths` grep gate
2. EXEC-02 (deterministic client_order_id) implemented and tested for normalization stability (qty/side/ticker case)
3. EXEC-07 (limit + market + stop) supported in `place_order` with proper alpaca-py request type mapping
4. BROK-A-01 (paper connect), BROK-A-03 (account+positions), BROK-A-04 (place with client_order_id), BROK-A-05 (cancel), BROK-A-06 (websocket fills) all exercised by integration test
5. Pitfall 7 (paper-vs-live mix-up) mitigated by two-layer constructor guard
6. Pitfall 4 (Knight Capital duplicate-order prevention) mitigated by 422-handler and tested
7. `Brokerage` ABC is the load-bearing interface — comments in `base.py` explicitly mark P2/P8/P9 extension hooks so future plans know where to plug in without rewriting
</success_criteria>

<output>
Create `.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-05-SUMMARY.md` when done. Include:
- alpaca-py version actually used + the exact paper-account-id prefix the live probe sees (so future plans can update the Pitfall 7 guard if needed)
- Whether the TradingStream wrapper used `asyncio.to_thread(self._stream.run)` or alpaca-py's native async API (RESEARCH A6 was uncertain — document what works)
- Cassette mocking approach: respx-based or unittest.mock.patch-based
- Confirmation that the float-ban grep gate is in place and passes
</output>
