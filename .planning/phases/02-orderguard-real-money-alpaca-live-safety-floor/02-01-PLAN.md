---
phase: 02-orderguard-real-money-alpaca-live-safety-floor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/gekko/db/models.py
  - src/gekko/db/migrations/versions/0002_orderguard.py
  - src/gekko/schemas/proposal.py
  - src/gekko/schemas/strategy.py
  - src/gekko/agent/tools/propose_trade.py
  - src/gekko/core/errors.py
  - src/gekko/approval/proposals.py
  - tests/unit/test_orderguard.py
  - tests/unit/test_orderguard_paper_live.py
  - tests/unit/test_kill_switch.py
  - tests/integration/test_kill_switch.py
  - tests/integration/test_kill_persistence.py
  - tests/unit/test_alpaca_retry.py
  - tests/unit/test_wash_sale.py
  - tests/integration/test_alpaca_live_credentials.py
  - tests/unit/test_decision_prompt_isolation.py
  - tests/unit/test_research_tools_wrapping.py
  - tests/unit/test_web_allowlist.py
  - tests/integration/test_first_live_gate.py
  - tests/unit/test_live_confirm_idempotent.py
  - tests/integration/test_orderguard_chain_paper.py
  - tests/integration/test_promote_paper_to_live_end_to_end.py
  - tests/unit/test_alembic_0002.py
  - tests/unit/test_pdt_t1_detection.py
  - tests/unit/test_wash_sale_flag.py
  - tests/integration/test_orderguard_cap_rejection.py
  - tests/unit/test_decision_tool_target_notional.py
  - tests/unit/test_prompt_injection_minimums.py
  - tests/unit/test_rate_limit_backoff.py
  - tests/unit/test_alpaca_live_construction_locked.py
  - tests/unit/test_trade_proposal_account_mode.py
  - tests/unit/test_state_transitions_phase2.py
  - tests/conftest.py
autonomous: false
requirements:
  - EXEC-04
  - EXEC-05
  - EXEC-06
  - EXEC-08
  - EXEC-09
  - EXEC-11
  - BROK-A-02
  - HITL-06
  - RES-06
  - RES-07
  - EXEC-03

user_setup: []

must_haves:
  truths:
    - "Alembic 0002 migration creates strategy_metadata table, BrokerCredential.kind column, users.kill_active/kill_active_since/kill_active_reason, proposals.AWAITING_2ND_CHANNEL+APPROVED_LIVE states, proposals.account_mode NOT NULL column with PAPER backfill, and proposals.status CHECK extension — all reversible (downgrade reverts cleanly) per D-31, D-32, D-34, D-35, D-36"
    - "TradeProposal carries a required target_notional_usd: Decimal field (D-27) AND a required account_mode: Literal['PAPER', 'LIVE'] field (TOCTOU closure per BLOCKER #5); propose_trade tool schema exposes target_notional_usd; account_mode is runtime-stamped (NOT LLM-authored) per D-27 trust-boundary"
    - "STATE_TRANSITIONS frozenset at src/gekko/approval/proposals.py is extended IN THIS PLAN with the 5 new edges: (PENDING,AWAITING_2ND_CHANNEL), (AWAITING_2ND_CHANNEL,APPROVED_LIVE), (AWAITING_2ND_CHANNEL,REJECTED), (AWAITING_2ND_CHANNEL,EXPIRED), (APPROVED_LIVE,EXECUTING) per BLOCKER #1 (Wave-1 foundational, not deferred to plan 02-06)"
    - "Wave-0 test stubs are present and skipped (pytest.skip) until their owning plan lands real assertions — no plan starts coding before its test file exists"
    - "tenacity 9.1.x is in pyproject.toml after operator-confirmed package-legitimacy checkpoint passes (RESEARCH §Package Legitimacy Audit)"
    - "Existing Phase-1 22-event audit chain still validates after migration runs forward and backward (walk_chain returns [])"
  artifacts:
    - path: src/gekko/db/migrations/versions/0002_orderguard.py
      provides: "Alembic 0002 — additive schema migration for Phase 2"
      contains: "def upgrade()"
    - path: src/gekko/schemas/proposal.py
      provides: "TradeProposal with target_notional_usd (D-27) + account_mode (BLOCKER #5 TOCTOU closure) fields"
      contains: "target_notional_usd"
    - path: src/gekko/schemas/strategy.py
      provides: "Strategy is unchanged at the snapshot row; metadata fields live in strategy_metadata table per RESEARCH §7"
    - path: src/gekko/db/models.py
      provides: "StrategyMetadata ORM class + extended User columns + extended BrokerCredential.kind + extended _PROPOSAL_STATUSES + Proposal.account_mode column"
      contains: "class StrategyMetadata"
    - path: src/gekko/approval/proposals.py
      provides: "STATE_TRANSITIONS extended with 5 Phase-2 edges (per BLOCKER #1)"
      contains: "AWAITING_2ND_CHANNEL"
    - path: src/gekko/core/errors.py
      provides: "OrderGuardRejected exception class with reject_code + reject_reason + extra"
      contains: "class OrderGuardRejected"
    - path: tests/unit/test_orderguard.py
      provides: "Wave-0 stub (skipped) for OrderGuard unit tests"
      min_lines: 10
    - path: tests/unit/test_alembic_0002.py
      provides: "Alembic 0002 forward + backward migration test"
      min_lines: 20
    - path: tests/unit/test_alpaca_live_construction_locked.py
      provides: "Module-level grep gate locking _allow_live=True / paper=False to _build_broker only (per BLOCKER #4)"
      min_lines: 15
    - path: tests/unit/test_trade_proposal_account_mode.py
      provides: "Wave-0 stub for TradeProposal.account_mode required-field tests (per BLOCKER #5 schema half)"
      min_lines: 10
    - path: tests/unit/test_proposal_writer_account_mode_stamp.py
      provides: "Wave-0 stub for ProposalWriter stamping account_mode at proposal-build time (per BLOCKER #5 runtime half — closes TOCTOU window between proposal-gen and approve-click)"
      min_lines: 15
    - path: tests/unit/test_state_transitions_phase2.py
      provides: "Wave-0 + Wave-1 test confirming the 5 new STATE_TRANSITIONS edges exist (per BLOCKER #1)"
      min_lines: 15
  key_links:
    - from: "src/gekko/db/migrations/versions/0002_orderguard.py"
      to: "src/gekko/db/models.py"
      via: "shared _PROPOSAL_STATUSES + _EVENT_TYPES vocab"
      pattern: "AWAITING_2ND_CHANNEL.*APPROVED_LIVE"
    - from: "src/gekko/agent/tools/propose_trade.py"
      to: "src/gekko/schemas/proposal.py"
      via: "TradeProposal.model_json_schema() drives propose_trade input schema"
      pattern: "_PROPOSE_TRADE_SCHEMA"
    - from: "src/gekko/approval/proposals.py::STATE_TRANSITIONS"
      to: "src/gekko/db/migrations/versions/0002_orderguard.py CHECK constraint"
      via: "shared status vocabulary"
      pattern: "AWAITING_2ND_CHANNEL"
---

<objective>
Lay Wave-0 foundations: ship Alembic 0002 schema migration (D-31/D-32/D-34/D-35/D-36 columns + state vocab + Proposal.account_mode), add TradeProposal.target_notional_usd field (D-27) AND TradeProposal.account_mode field (BLOCKER #5 TOCTOU closure), extend STATE_TRANSITIONS frozenset with 5 Phase-2 edges (BLOCKER #1 — moved from plan 02-06 to Wave-1 foundational), add OrderGuardRejected exception class, create every Wave-0 test stub from VALIDATION.md so subsequent plans cannot start coding without a failing test, and gate tenacity install behind operator package-legitimacy checkpoint.

Purpose: every later plan in Phase 2 reads from this foundation (state machine vocab, schema fields, test file shape, error type). Without it they cannot start. BLOCKER #1 specifically: plan 02-06 Task 2 claimed "plan 02-01 added the frozenset entries" — this plan now actually does so.

Output: 1 Alembic migration + 1 new ORM class (StrategyMetadata) + 1 new column on User + 1 new column on BrokerCredential + 1 new column on Proposal (account_mode) + 2 new schema fields (target_notional_usd, account_mode) + 5 new STATE_TRANSITIONS edges + 1 new error class + Wave-0 test stub files + 1 dep added behind a human-verified checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-CONTEXT.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-VALIDATION.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-09-SUMMARY.md
@src/gekko/db/models.py
@src/gekko/db/migrations/versions/0001_initial.py
@src/gekko/schemas/proposal.py
@src/gekko/schemas/strategy.py
@src/gekko/agent/tools/propose_trade.py
@src/gekko/approval/proposals.py
@src/gekko/core/errors.py
@pyproject.toml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Checkpoint — tenacity 9.1.x</name>
  <what-built>
    The researcher tagged tenacity 9.1.4 as [ASSUMED] per RESEARCH §Package Legitimacy Audit because slopcheck was unavailable this session. Per project policy, the planner inserts a blocking human-verify checkpoint BEFORE the uv add task. This task does not run code — it asks the operator to confirm the package.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/tenacity/ — confirm: maintainer "jd" / Julien Danjou, license Apache-2.0, repo github.com/jd/tenacity, current version 9.1.x (≥9.1, &lt;10).
    2. Confirm the repo has at least several years of commit history and is the well-known retry library used by OpenAI/Anthropic cookbooks.
    3. Confirm the package name is exactly `tenacity` (no homoglyph variant `tenacrty`, `tenarcity`, etc.).
    4. Approve to allow `uv add 'tenacity>=9.1,<10'` to run in Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" once the PyPI page + repo identity match the description above, or describe the discrepancy.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add tenacity dependency + create Wave-0 test stub files</name>
  <read_first>
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-VALIDATION.md (Wave 0 Requirements + Wave 0 Gaps list — the canonical list of stub files)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§9 Validation Architecture, §10 Pitfalls)
    - pyproject.toml (current dependency block — find where to insert tenacity alongside `alpaca-py`)
    - tests/conftest.py (existing fixture patterns; the new fixtures `account_mode`, `kill_state`, `live_credential_pair` must follow this style)
  </read_first>
  <action>
    Run `uv add 'tenacity>=9.1,<10'` to add tenacity (per D-29 BACKOFF / EXEC-08, RESEARCH §6 — gate already passed in Task 1).

    Create EVERY Wave-0 test stub file enumerated in VALIDATION.md §"Wave 0 Requirements" and RESEARCH §9 "Wave 0 Gaps". Each stub is a Python file with module docstring naming the owning plan + REQ-ID, plus a single placeholder test that calls `pytest.skip("Wave-0 stub — plan 02-NN owns")`. Files to create (24 total):
    - `tests/unit/test_orderguard.py` (universe + hard caps + qty×price + paper/live + kill-active block + PDT + T+1 — owned by plans 02-02 and 02-03)
    - `tests/unit/test_orderguard_paper_live.py` (4-6 EXEC-05 mismatch cases — owned by plan 02-02)
    - `tests/unit/test_kill_switch.py` (DB-column persistence + transition unit-level — owned by plan 02-05)
    - `tests/integration/test_kill_switch.py` (5s SLA + parallel cancel — owned by plan 02-05)
    - `tests/integration/test_kill_persistence.py` (cross-restart — owned by plan 02-05)
    - `tests/unit/test_alpaca_retry.py` (tenacity-on-GETs + no-retry-on-place_order grep gate — owned by plan 02-03)
    - `tests/unit/test_rate_limit_backoff.py` (EXEC-08 wrapper introspection — owned by plan 02-03)
    - `tests/unit/test_wash_sale.py` (FLAG path — owned by plan 02-03)
    - `tests/unit/test_wash_sale_flag.py` (FLAG dict shape — owned by plan 02-03)
    - `tests/unit/test_pdt_t1_detection.py` (PDT 4th-round-trip + T+1 unsettled — owned by plan 02-03)
    - `tests/integration/test_alpaca_live_credentials.py` (vault load → AlpacaBroker(paper=False) — owned by plan 02-06)
    - `tests/unit/test_decision_prompt_isolation.py` (RES-06 — owned by plan 02-04)
    - `tests/unit/test_research_tools_wrapping.py` (RES-07 site-1 wrap — owned by plan 02-04)
    - `tests/unit/test_web_allowlist.py` (RES-07 host parsing — owned by plan 02-04)
    - `tests/unit/test_prompt_injection_minimums.py` (D-40 Decision system_prompt — owned by plan 02-04)
    - `tests/integration/test_first_live_gate.py` (HITL-06 dual-channel — owned by plan 02-06)
    - `tests/unit/test_live_confirm_idempotent.py` (HITL-06 double-click — owned by plan 02-06)
    - `tests/integration/test_orderguard_chain_paper.py` (4-event cap_rejection chain — owned by plan 02-02)
    - `tests/integration/test_orderguard_cap_rejection.py` (cap_rejection end-to-end — owned by plan 02-02)
    - `tests/integration/test_promote_paper_to_live_end_to_end.py` (Phase-2 walking skeleton — owned by plan 02-07)
    - `tests/unit/test_alembic_0002.py` (forward + backward migration — owned by plan 02-01 Task 4)
    - `tests/unit/test_decision_tool_target_notional.py` (D-27 schema field — owned by plan 02-01 Task 3)
    - `tests/unit/test_alpaca_live_construction_locked.py` (BLOCKER #4 — module-level grep gate locking `_allow_live=True` / `_allow_live = True` / `paper=False` to `src/gekko/execution/executor.py::_build_broker` only; any other occurrence in `src/gekko/` fails the test — owned by plan 02-06)
    - `tests/unit/test_trade_proposal_account_mode.py` (BLOCKER #5 schema half — TradeProposal.account_mode required Literal['PAPER','LIVE']; missing field raises pydantic.ValidationError — owned by plan 02-01 Task 3)
    - `tests/unit/test_proposal_writer_account_mode_stamp.py` (BLOCKER #5 runtime half — ProposalWriter stamps account_mode from strategy state at proposal-build time; closes TOCTOU window — owned by plan 02-06)
    - `tests/unit/test_state_transitions_phase2.py` (BLOCKER #1 — the 5 new STATE_TRANSITIONS edges exist; owned by plan 02-01 Task 5)

    Extend `tests/conftest.py` per VALIDATION.md: add `account_mode` parametrized fixture yielding `"PAPER"` and `"LIVE"`; add `kill_state` fixture that opens a SQLCipher session, ensures `users.kill_active=False` before yield and resets to False after; add `live_credential_pair` fixture that seeds two BrokerCredential rows (kind="alpaca_paper" + kind="alpaca_live") in a per-test SQLCipher DB. Mirror the Phase-1 fixture patterns already in conftest.py (PATTERNS §5a `_get_session_factory` test seam).

    All stubs MUST contain the marker comment `# WAVE-0 STUB: owned by plan 02-NN — DO NOT delete the skip until that plan's tasks land`.

    Per D-29 / D-30 — names of OrderGuardRejected.reject_code values used in stubs (lock these strings now so later plans use them verbatim): `"universe"`, `"hard_cap_position_pct"`, `"hard_cap_daily_loss"`, `"hard_cap_trades_per_day"`, `"hard_cap_sector_exposure"`, `"qty_price_drift"`, `"ref_price_missing"`, `"paper_live_mismatch_broker"`, `"paper_live_mismatch_account"`, `"paper_live_mismatch_credential"` (from plan 02-06 / BLOCKER #7 — emitted from check_paper_live_pairing now, NOT from cap_rejection handler), `"kill_active"`, `"market_closed"`, `"pdt_rule"`, `"pdt_rule_local"`, `"t1_settlement"`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit tests/integration --collect-only -q 2>&amp;1 | grep -E "test_(orderguard|kill|alpaca_retry|rate_limit|wash_sale|pdt_t1|alpaca_live|decision_prompt|research_tools|web_allowlist|prompt_injection|first_live|live_confirm|orderguard_chain|promote_paper|alembic_0002|decision_tool_target|orderguard_cap|alpaca_live_construction|trade_proposal_account_mode|state_transitions_phase2)" | wc -l</automated>
  </verify>
  <done>
    - `tenacity>=9.1,&lt;10` appears in pyproject.toml dependencies block
    - `uv lock` shows tenacity 9.1.x resolved
    - The 25 Wave-0 stub files exist and each contains `pytest.skip("Wave-0 stub` token
    - `uv run pytest -q --collect-only` collects 25+ new test files without ImportError
    - `tests/conftest.py` exposes the three new fixtures (`account_mode`, `kill_state`, `live_credential_pair`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TradeProposal.target_notional_usd field (D-27) + TradeProposal.account_mode field (BLOCKER #5 TOCTOU closure) + propose_trade schema extension</name>
  <read_first>
    - src/gekko/schemas/proposal.py (full file — TradeProposal at lines 55-111, `model_config = ConfigDict(frozen=False, extra="ignore")`, the `qty: Decimal = Field(..., gt=Decimal("0"))` shape at line 91, the `_validate_price_for_order_type` model_validator at lines 103-111)
    - src/gekko/agent/tools/propose_trade.py (full file — schema-strip helper at lines 51-72; `_runtime_only` tuple at line 65; @tool decorator at lines 77-87)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§3f Pydantic schema + §3l @tool decorator pattern — exact insertion sites)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§1 OrderGuard `check_qty_price_sanity` — confirms `target_notional_usd` is the LLM-authored field; ref_price selection logic for LIMIT/STOP/MARKET; 2% drift bound is OrderGuard runtime policy NOT schema policy)
  </read_first>
  <behavior>
    target_notional_usd (D-27):
    - Test: instantiating `TradeProposal(...)` without `target_notional_usd` raises `pydantic.ValidationError` (field is required)
    - Test: `target_notional_usd=Decimal("0")` raises `pydantic.ValidationError` (gt=0)
    - Test: `target_notional_usd=Decimal("100.00")` validates successfully
    - Test: `target_notional_usd=-100` raises `pydantic.ValidationError`
    - Test: `propose_trade._PROPOSE_TRADE_SCHEMA["properties"]` contains key `"target_notional_usd"` (LLM sees it)
    - Test: `"target_notional_usd"` is in the schema's `required` list (LLM must supply)
    - Test: `"target_notional_usd"` is NOT in `_runtime_only` tuple (runtime does NOT fill it)

    account_mode (BLOCKER #5 — TOCTOU closure):
    - Test: instantiating `TradeProposal(...)` without `account_mode` raises `pydantic.ValidationError` (field is required)
    - Test: `account_mode="PAPER"` validates successfully
    - Test: `account_mode="LIVE"` validates successfully
    - Test: `account_mode="paper"` (lowercase) raises `pydantic.ValidationError` (Literal is case-sensitive)
    - Test: `account_mode="MARGIN"` raises `pydantic.ValidationError` (not in allowed values)
    - Test: `"account_mode"` IS in `_runtime_only` tuple — ProposalWriter stamps it; the LLM does NOT supply it (per D-27 trust-boundary: account_mode is determined by strategy.live_mode_eligible AND strategy.mode AT PROPOSAL-BUILD TIME, locked in immediately to close the TOCTOU window between proposal-gen and approve-click)
    - Test: `"account_mode"` is NOT in `_PROPOSE_TRADE_SCHEMA["properties"]` (stripped by `_runtime_only`)

    Phase-1 audit-chain roundtrip:
    - Test: Phase-1 audit-chain JSON roundtrip — a TradeProposal with the new fields model_dumps + model_validates back identical via `mode="python"` (canonical_json invariant from D-15 / Plan 01-04)
  </behavior>
  <action>
    Replace the WAVE-0 stub in `tests/unit/test_decision_tool_target_notional.py` with the 8 target_notional_usd behaviors using pytest + pydantic.ValidationError + the Phase-1 fixture pattern from `tests/unit/test_proposal_schema.py`.

    Replace the WAVE-0 stub in `tests/unit/test_trade_proposal_account_mode.py` with the 7 account_mode behaviors (per BLOCKER #5).

    Extend `src/gekko/schemas/proposal.py` TradeProposal class:
    - D-27: add field `target_notional_usd: Decimal = Field(..., gt=Decimal("0"))` between the `qty` field (line ~91) and `order_type` field (line ~92) per PATTERNS §3f. Do NOT add a model_validator for the 2% drift bound — that is OrderGuard runtime policy per RESEARCH §1.
    - BLOCKER #5: add `account_mode: Literal["PAPER", "LIVE"] = Field(...)` REQUIRED field (no default — every TradeProposal must declare account_mode at construction time, stamped by ProposalWriter per plan 02-06 Task 2; missing field raises ValidationError). Place AFTER `client_order_id`. Add `Literal` to existing imports.
    - Add `wash_sale_flag: dict[str, Any] | None = None` after `account_mode` per RESEARCH §5 (forward-compat slot consumed by plan 02-03). Add `Any` to imports if not present.
    - Keep `model_config = ConfigDict(frozen=False, extra="ignore")` unchanged at line 84.

    `src/gekko/agent/tools/propose_trade.py`: the schema-strip helper `_build_propose_trade_schema` at lines 51-72 strips fields in `_runtime_only` — add `"account_mode"` and `"wash_sale_flag"` to the `_runtime_only` tuple (so the LLM does NOT see account_mode in its tool input schema; ProposalWriter stamps it at proposal-build time per BLOCKER #5 / D-27 trust-boundary). DO NOT add `target_notional_usd` to `_runtime_only` (it IS LLM-authored).

    Update the @tool decorator description string at lines 79-86 (append): "Provide `target_notional_usd` as the LLM's dollar intent — OrderGuard rejects if `qty × ref_price` drifts > 2% from this value." per PATTERNS §3l.

    `src/gekko/agent/proposal_writer.py` — check whether ProposalWriter constructs TradeProposal from the LLM tool input dict; if it does (it should per Plan 01-07 SUMMARY D-20/D-27 trust-boundary), the new target_notional_usd field falls through automatically. ProposalWriter also needs to stamp `account_mode` at construction time — plan 02-06 Task 2 wires this; for plan 02-01 we only define the schema field (LLM-not-authored). For Phase-1 paper-only operation, ProposalWriter defaults `account_mode="PAPER"` when constructing TradeProposal. The Alembic 0002 migration handles the DB column.

    Per Phase 1 D-16: adding new payload fields like `target_notional_usd` + `account_mode` + `wash_sale_flag` is forward-additive per RESEARCH §10 Open Question #5 — existing audit chain hashes unchanged because the canonical-subset shape `{event_type, payload, ts, user_id}` did not grow.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_decision_tool_target_notional.py tests/unit/test_trade_proposal_account_mode.py tests/unit/test_proposal_schema.py -x -q</automated>
  </verify>
  <done>
    - All 8 tests in `tests/unit/test_decision_tool_target_notional.py` pass
    - All 7 tests in `tests/unit/test_trade_proposal_account_mode.py` pass (per BLOCKER #5)
    - Existing `tests/unit/test_proposal_schema.py` tests still pass (no regression on the 25 Phase-1 TradeProposal field tests)
    - `python -c "from gekko.agent.tools.propose_trade import _PROPOSE_TRADE_SCHEMA; assert 'target_notional_usd' in _PROPOSE_TRADE_SCHEMA['properties']; assert 'target_notional_usd' in _PROPOSE_TRADE_SCHEMA['required']; assert 'account_mode' not in _PROPOSE_TRADE_SCHEMA['properties']"` exits 0
    - Phase-1 audit chain integration test `tests/integration/test_trigger_run_end_to_end.py` still passes (no schema-level regression)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Alembic 0002 migration + StrategyMetadata ORM + User.kill_active + BrokerCredential.kind + Proposal.account_mode + state vocab extension</name>
  <read_first>
    - src/gekko/db/migrations/versions/0001_initial.py (Plan 01-03 — the EXACT shape Phase 2 mirrors per PATTERNS §1d "Direct shape mirror")
    - src/gekko/db/models.py (FULL FILE — `_PROPOSAL_STATUSES` tuple at lines 47-54, `_EVENT_TYPES` tuple at lines 57-67, `User` ORM at lines 93-113 with the `__repr__` exclusion pattern at line 112-113, `Strategy` ORM at lines 121-157, `BrokerCredential` ORM at lines 325-353 — including its `__repr__` at lines 348-353)
    - src/gekko/db/engine.py (get_async_engine / get_sync_engine — Alembic 0002 must NOT regress AUTH-03 / T-01-03-05 — passphrase stays in connect-event closure, never in URL string)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§2 BrokerCredential `kind` column shape + backfill SQL; §3 `users.kill_active` + `kill_active_since` + `kill_active_reason` columns; §7 `strategy_metadata` table preferred over snapshot-row extension per Open Question #2; D-32 state machine `AWAITING_2ND_CHANNEL` + `APPROVED_LIVE`)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§2 row 1 "Extension Point Map" for `src/gekko/db/models.py` — exact line ranges for every column to add)
  </read_first>
  <behavior>
    - Test (forward): apply 0002 against a fresh DB, assert columns exist: `strategies` table UNCHANGED (snapshot rows preserved); new `strategy_metadata` table with columns (user_id VARCHAR PK, strategy_name VARCHAR PK, live_mode_eligible BOOLEAN NOT NULL DEFAULT 0, live_promoted_at VARCHAR NULL, first_live_trade_confirmed_at VARCHAR NULL, FK user_id REFERENCES users.user_id); `users` table gains `kill_active BOOLEAN NOT NULL DEFAULT 0`, `kill_active_since VARCHAR NULL`, `kill_active_reason VARCHAR NULL`; `broker_credentials` table gains `kind VARCHAR` column with CHECK constraint `kind IN ('alpaca_paper','alpaca_live')` and composite PK becomes `(user_id, broker, kind)`; `proposals.status` CHECK constraint now accepts `AWAITING_2ND_CHANNEL` and `APPROVED_LIVE`; `proposals` table gains `account_mode VARCHAR NOT NULL` column with CHECK constraint `account_mode IN ('PAPER','LIVE')` (BLOCKER #5 TOCTOU closure).
    - Test (Proposal.account_mode backfill, BLOCKER #5): an existing pre-migration Proposal row with NULL account_mode gets backfilled to `'PAPER'` via `op.execute("UPDATE proposals SET account_mode='PAPER' WHERE account_mode IS NULL")` BEFORE the NOT NULL alter; verify by seeding a Phase-1 DB with PENDING/APPROVED proposals, upgrading, and asserting they all have `account_mode='PAPER'`.
    - Test (Proposal.account_mode CHECK constraint): inserting a proposal with `account_mode='MARGIN'` after migration raises IntegrityError.
    - Test (backfill BrokerCredential.kind): an existing pre-migration BrokerCredential row with `paper=1` survives migration as `kind='alpaca_paper'`; with `paper=0` survives as `kind='alpaca_live'`.
    - Test (rollback): apply 0002 then `alembic downgrade -1` and assert (a) `strategy_metadata` table is dropped, (b) `users` loses kill_active/kill_active_since/kill_active_reason, (c) `broker_credentials` loses `kind` (or returns to original PK shape), (d) `proposals.status` CHECK reverts to Phase-1 vocab, (e) `proposals.account_mode` column is dropped.
    - Test (audit chain still intact): walk_chain over the Phase-1 22-event chain returns [] after 0002 upgrade.
    - Test (ORM model): `from gekko.db.models import StrategyMetadata` imports successfully; `User.kill_active` column attribute exists; `BrokerCredential.kind` column attribute exists; `Proposal.account_mode` column attribute exists.
    - Test (state vocab): `_PROPOSAL_STATUSES` tuple now includes `"AWAITING_2ND_CHANNEL"` and `"APPROVED_LIVE"`.
  </behavior>
  <action>
    Replace the WAVE-0 stub in `tests/unit/test_alembic_0002.py` with the 8 behaviors above. Mirror the test structure from `tests/integration/test_alembic_0001.py` if it exists; otherwise use `pytest-alembic` per RESEARCH §9 framework declaration.

    Extend `src/gekko/db/models.py` per PATTERNS §2 row 1:
    1. After `_PROPOSAL_STATUSES` at lines 47-54: extend the tuple to include `"AWAITING_2ND_CHANNEL"` and `"APPROVED_LIVE"` (in this order — keep the existing alphabetical/state-order convention used in Phase 1).
    2. After the existing `User` columns at lines 93-113: add `kill_active: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("0"))`, `kill_active_since: Mapped[str | None] = mapped_column(String, nullable=True)`, `kill_active_reason: Mapped[str | None] = mapped_column(String, nullable=True)`. Update `__repr__` at lines 112-113 if it lists specific columns — keep the kill_active fields VISIBLE in `__repr__` (they are non-credential, operator-debugging useful) per the existing pattern.
    3. Add new class `StrategyMetadata(Base)` after the existing `Strategy` class at line ~157: composite PK `(user_id, strategy_name)`, `live_mode_eligible: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("0"))`, `live_promoted_at: Mapped[str | None]`, `first_live_trade_confirmed_at: Mapped[str | None]`, FK `user_id` references `users.user_id`. `__tablename__ = "strategy_metadata"`. Match Strategy's column type idioms.
    4. After `BrokerCredential` at lines 325-353: add `kind: Mapped[str] = mapped_column(String, nullable=False, default="alpaca_paper")` column. Extend the composite PK declaration to `(user_id, broker, kind)`. Add `CheckConstraint("kind IN ('alpaca_paper','alpaca_live')", name="ck_broker_credentials_kind")` to `__table_args__`. The `__repr__` at lines 348-353 stays: it must continue to exclude `key_blob` + `secret_blob` (PATTERNS §4 anti-pattern "Plaintext credentials in `__repr__`").
    5. Locate the `Proposal` ORM class (Phase-1 `proposals` table). Add `account_mode: Mapped[str] = mapped_column(String, nullable=False, server_default=text("'PAPER'"))` column (BLOCKER #5). Add a CheckConstraint `CheckConstraint("account_mode IN ('PAPER','LIVE')", name="ck_proposals_account_mode")` to `__table_args__`.
    6. The CheckConstraint for `proposals.status` (around line 305-308 or wherever `_in_check("status", _PROPOSAL_STATUSES)` lives) will automatically pick up the new states because it references the tuple — confirm by re-running the test.

    Create `src/gekko/db/migrations/versions/0002_orderguard.py` mirroring the shape of `0001_initial.py` exactly (`revision`, `down_revision = "0001_initial"`, `branch_labels = None`, `depends_on = None`, `def upgrade()`, `def downgrade()`). Operations in `upgrade()`:
    1. `op.add_column("users", sa.Column("kill_active", sa.Boolean, nullable=False, server_default=sa.text("0")))` — same for `kill_active_since` (String, nullable=True) and `kill_active_reason` (String, nullable=True).
    2. `op.create_table("strategy_metadata", ...)` — columns per behavior above; PK on (user_id, strategy_name); FK to users.user_id.
    3. `op.add_column("broker_credentials", sa.Column("kind", sa.String, nullable=True))` — start nullable for backfill.
    4. Backfill SQL: `op.execute("UPDATE broker_credentials SET kind = 'alpaca_paper' WHERE broker = 'alpaca' AND paper = 1")` and `op.execute("UPDATE broker_credentials SET kind = 'alpaca_live' WHERE broker = 'alpaca' AND paper = 0")`.
    5. Alter `broker_credentials.kind` to NOT NULL using `op.alter_column("broker_credentials", "kind", nullable=False)`.
    6. Add CHECK constraint on `kind`. Drop the old PK; add new PK on (user_id, broker, kind). NOTE: SQLite cannot ALTER PK directly — use `op.batch_alter_table("broker_credentials") as bop:` for PK changes per the SQLAlchemy SQLite migration pattern.
    7. Drop the old `proposals.status` CHECK constraint; add a new one that includes `AWAITING_2ND_CHANNEL` and `APPROVED_LIVE` (CHECK constraint regeneration via `batch_alter_table`).
    8. (BLOCKER #5) `op.add_column("proposals", sa.Column("account_mode", sa.String, nullable=True))` — start nullable.
    9. (BLOCKER #5) Backfill: `op.execute("UPDATE proposals SET account_mode='PAPER' WHERE account_mode IS NULL")` — every existing row gets stamped PAPER (Phase-1 was paper-only per D-24, so this is correct).
    10. (BLOCKER #5) `op.alter_column("proposals", "account_mode", nullable=False)` via `batch_alter_table`.
    11. (BLOCKER #5) Add `CheckConstraint("account_mode IN ('PAPER','LIVE')", name="ck_proposals_account_mode")` via `batch_alter_table`.

    `downgrade()`: reverse every step in reverse order; preserve audit-chain rows (do NOT delete events). Drop `proposals.account_mode` column. Drop CHECK constraint. Restore Phase-1 `proposals.status` CHECK. Drop strategy_metadata table. Drop `broker_credentials.kind` and restore the (user_id, broker) PK. Drop `users.kill_active*` columns.

    Per Plan 01-03 D-?? convention from STATE.md: the migration's CheckConstraint vocabularies are duplicated locally in the migration body (Alembic frozen-history convention — migration cannot import live `_PROPOSAL_STATUSES`). Include `_FROZEN_PROPOSAL_STATUSES = ("PENDING", "APPROVED", "REJECTED", "EXECUTING", "FILLED", "FAILED", "AWAITING_2ND_CHANNEL", "APPROVED_LIVE")` at top of the migration file.

    Migration MUST NOT pass the SQLCipher passphrase via URL — read it from env per `alembic/env.py`'s existing pattern (Plan 01-03 D-?? "sqlalchemy.url is LEFT BLANK in alembic.ini").

    NEVER log or print the passphrase. The migration body never references it directly; env.py owns the engine construction.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_alembic_0002.py -x -q &amp;&amp; uv run python -c "from gekko.db.models import StrategyMetadata, User, BrokerCredential, Proposal, _PROPOSAL_STATUSES; assert 'AWAITING_2ND_CHANNEL' in _PROPOSAL_STATUSES; assert 'APPROVED_LIVE' in _PROPOSAL_STATUSES; assert hasattr(User, 'kill_active'); assert hasattr(BrokerCredential, 'kind'); assert hasattr(Proposal, 'account_mode')"</automated>
  </verify>
  <done>
    - 8 behaviors in `tests/unit/test_alembic_0002.py` pass (including BLOCKER #5 account_mode backfill)
    - `alembic upgrade head` on a fresh DB applies both 0001 and 0002 with no errors
    - `alembic upgrade head` on a DB seeded with Phase-1 PENDING/APPROVED proposals backfills `account_mode='PAPER'` on ALL existing rows
    - `alembic downgrade -1` cleanly reverts 0002 (including dropping account_mode column)
    - Existing Phase-1 22-event audit chain in a test DB walk_chain-validates after 0002 upgrade
    - Existing `tests/unit/test_db_sync_engine_no_passphrase_in_repr.py` still passes (no AUTH-03 / T-01-03-05 regression)
    - `python -c "from gekko.db.models import StrategyMetadata, Proposal; assert hasattr(Proposal, 'account_mode')"` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Extend STATE_TRANSITIONS frozenset with 5 Phase-2 edges (BLOCKER #1 — Wave-1 foundational, moved from plan 02-06)</name>
  <read_first>
    - src/gekko/approval/proposals.py (FULL FILE — STATE_TRANSITIONS frozenset at lines 51-60; transition_status function body at lines 68-119; docstring at lines 1-29 describing Phase-1 lifecycle table)
    - src/gekko/db/models.py (the `_PROPOSAL_STATUSES` tuple — confirm `AWAITING_2ND_CHANNEL` + `APPROVED_LIVE` are added by Task 4 BEFORE this task lands)
    - src/gekko/db/migrations/versions/0002_orderguard.py (the migration's `_FROZEN_PROPOSAL_STATUSES` and the CHECK constraint regeneration — confirms both new states are in the DB-level vocabulary, satisfying BLOCKER #1's "Also: the Alembic 0002 migration's CHECK constraint must include AWAITING_2ND_CHANNEL + APPROVED_LIVE")
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§7 — state machine extension code shape; the 5 new edges)
  </read_first>
  <behavior>
    BLOCKER #1 — the 5 new STATE_TRANSITIONS edges:
    - Test: `from gekko.approval.proposals import STATE_TRANSITIONS; ('PENDING', 'AWAITING_2ND_CHANNEL') in STATE_TRANSITIONS` is True
    - Test: `('AWAITING_2ND_CHANNEL', 'APPROVED_LIVE') in STATE_TRANSITIONS` is True
    - Test: `('AWAITING_2ND_CHANNEL', 'REJECTED') in STATE_TRANSITIONS` is True
    - Test: `('AWAITING_2ND_CHANNEL', 'EXPIRED') in STATE_TRANSITIONS` is True (per BLOCKER #1 fix list — even though `EXPIRED` is not yet in `_PROPOSAL_STATUSES` for Phase 2, the transition edge is reserved for a future timeout path; the test asserts the edge tuple exists in the frozenset; if the migration's CHECK constraint does NOT yet accept EXPIRED, a runtime attempt to transition will fail at DB constraint time — this is the safe Wave-1 prep)
    - Test: `('APPROVED_LIVE', 'EXECUTING') in STATE_TRANSITIONS` is True
    - Test: STATE_TRANSITIONS retains ALL 6 Phase-1 edges (no regression)
    - Test: STATE_TRANSITIONS total entries == 11 (6 Phase-1 + 5 Phase-2)

    Body-unchanged invariant (PATTERNS §3e):
    - Test: `transition_status` body at lines 68-119 of `src/gekko/approval/proposals.py` is UNCHANGED — the function is data-driven on the frozenset; this plan extends the data, not the function. Verified by `inspect.getsource(transition_status)` containing no new branches.
    - Test: `transition_status` idempotency-on-same-state still works (Phase-1 invariant at lines 98-100).

    Migration alignment (BLOCKER #1 second checklist item):
    - Test: the Alembic 0002 migration's `_FROZEN_PROPOSAL_STATUSES` constant (from Task 4) contains `"AWAITING_2ND_CHANNEL"` AND `"APPROVED_LIVE"` (confirms the DB-level CHECK constraint admits the new states).

    Reachability sanity:
    - Test: with a seeded Proposal row in PENDING, calling `transition_status(session, pid, from_status="PENDING", to_status="AWAITING_2ND_CHANNEL")` succeeds AND the DB CHECK constraint allows the UPDATE (no IntegrityError).
    - Test: with a seeded Proposal row in AWAITING_2ND_CHANNEL, calling `transition_status(session, pid, from_status="AWAITING_2ND_CHANNEL", to_status="APPROVED_LIVE")` succeeds.
    - Test: invalid transition `(APPROVED, APPROVED_LIVE)` raises ValueError per Phase-1 invariant at lines 102-107.
  </behavior>
  <action>
    Replace the WAVE-0 stub in `tests/unit/test_state_transitions_phase2.py` with the behaviors above.

    Extend `src/gekko/approval/proposals.py::STATE_TRANSITIONS` (BLOCKER #1 fix). The current frozenset at lines 51-60 contains 6 Phase-1 tuples. Add 5 new Phase-2 tuples to the frozenset literal:

    ```
    STATE_TRANSITIONS: frozenset[tuple[str, str]] = frozenset(
        {
            # Phase 1 — Plan 01-08
            ("PENDING", "APPROVED"),
            ("PENDING", "REJECTED"),
            ("APPROVED", "EXECUTING"),
            ("APPROVED", "FAILED"),
            ("EXECUTING", "FILLED"),
            ("EXECUTING", "FAILED"),
            # Phase 2 — Plan 02-01 Task 5 (BLOCKER #1): dual-channel gate edges
            # WIRING for these transitions lives in plan 02-06 Task 2 (Slack approve
            # diverts to AWAITING_2ND_CHANNEL; dashboard /live-confirm fires APPROVED_LIVE).
            # The frozenset entries land here in Wave 1 so the state-machine DB layer
            # accepts the new states the moment plan 02-06 wires them, with no Wave-2+
            # state-machine surgery required.
            ("PENDING", "AWAITING_2ND_CHANNEL"),
            ("AWAITING_2ND_CHANNEL", "APPROVED_LIVE"),
            ("AWAITING_2ND_CHANNEL", "REJECTED"),
            ("AWAITING_2ND_CHANNEL", "EXPIRED"),
            ("APPROVED_LIVE", "EXECUTING"),
        }
    )
    ```

    Also extend the module docstring at lines 1-29 to document the new edges (mirror the Phase-1 lifecycle table). Add a new section "Phase 2 dual-channel gate (D-32 / HITL-06):" listing the 5 edges with their semantic meaning.

    DO NOT modify the `transition_status` function body at lines 68-119 — it is data-driven on the frozenset (PATTERNS §3e + §4 anti-pattern row 10). The body must remain byte-identical to Phase 1.

    Verify the Alembic 0002 migration from Task 4 already extends the proposals.status CHECK constraint to admit AWAITING_2ND_CHANNEL + APPROVED_LIVE (per Task 4 step 7). If the migration did NOT include those vocabulary entries, fix Task 4 first — both must be coherent in Wave 1.

    NOTE on `EXPIRED`: this edge tuple is included in the frozenset because BLOCKER #1's fix list names 5 edges including `EXPIRED`. The `EXPIRED` status itself is NOT yet added to `_PROPOSAL_STATUSES` in Task 4 (it is reserved for a future timeout path — out of scope for Phase 2 per CONTEXT.md Deferred Ideas "Timeout=REJECT" item). The frozenset entry is harmless until a caller attempts to use it; the DB CHECK constraint will reject `EXPIRED` until a future migration adds it. This is the safe Wave-1 prep position: the frozenset is forward-compatible without committing to the runtime path yet.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_state_transitions_phase2.py tests/unit/test_approval_proposals.py -x -q &amp;&amp; uv run python -c "from gekko.approval.proposals import STATE_TRANSITIONS; assert ('PENDING','AWAITING_2ND_CHANNEL') in STATE_TRANSITIONS; assert ('AWAITING_2ND_CHANNEL','APPROVED_LIVE') in STATE_TRANSITIONS; assert ('AWAITING_2ND_CHANNEL','REJECTED') in STATE_TRANSITIONS; assert ('AWAITING_2ND_CHANNEL','EXPIRED') in STATE_TRANSITIONS; assert ('APPROVED_LIVE','EXECUTING') in STATE_TRANSITIONS; assert len(STATE_TRANSITIONS) == 11"</automated>
  </verify>
  <done>
    - All 5 new STATE_TRANSITIONS edges are in the frozenset (BLOCKER #1 fix landed)
    - `len(STATE_TRANSITIONS) == 11` (6 Phase-1 + 5 Phase-2)
    - `transition_status` body unchanged (PATTERNS §3e invariant preserved)
    - Phase-1 `tests/unit/test_approval_proposals.py` still passes (no regression on the 6 original edges)
    - Alembic 0002 CHECK constraint admits AWAITING_2ND_CHANNEL + APPROVED_LIVE (DB-layer alignment confirmed)
    - Reachability tests pass: PENDING → AWAITING_2ND_CHANNEL → APPROVED_LIVE chain executes end-to-end against a real test DB
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml → installed packages | Slopsquat risk on every new dep |
| alembic env.py → SQLCipher engine | Passphrase must stay in connect-event closure; never in URL/log |
| LLM tool input → TradeProposal schema | Untrusted LLM bytes cross into the schema validator |
| Audit chain canonical-subset | Migration must not invalidate existing 22-event hash chain |
| Proposal-build time → approve-click time | account_mode locked in at construction (BLOCKER #5 TOCTOU closure) |
| STATE_TRANSITIONS frozenset literal | Authoritative source of state-machine edges; DB CHECK constraint must align |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | `uv add tenacity` install | mitigate | Task 1 blocking-human checkpoint — operator confirms PyPI maintainer + repo + version match RESEARCH §Package Legitimacy Audit before install runs |
| T-02-01-A-01 | Authentication | Alembic 0002 migration loads SQLCipher passphrase | mitigate | env.py reads `GEKKO_DB_PASSPHRASE` from env at runtime per Plan 01-03 D-?? — migration body never references passphrase; alembic.ini sqlalchemy.url stays blank; CI step that runs migrations sources the same env-var that production uses |
| T-02-01-M-01 | Tampering | TradeProposal.target_notional_usd field is REQUIRED | mitigate | Pydantic `Field(..., gt=Decimal("0"))` — schema rejects missing or non-positive values at validation time before the proposal reaches the audit log. Test `test_target_notional_usd_required_field_missing_raises` proves rejection. |
| T-02-01-M-05 (BLOCKER #5 closure) | Tampering — TOCTOU | TradeProposal.account_mode field locks paper/live decision at proposal-build time | mitigate | `account_mode: Literal["PAPER","LIVE"] = Field(...)` REQUIRED field. ProposalWriter (plan 02-06 Task 2 — `src/gekko/agent/proposal_writer.py` in 02-06 files_modified) stamps it from `strategy.mode == "live" AND strategy_metadata.live_mode_eligible` at proposal-build time. Slack approve handler (`_approve_workflow` in `src/gekko/approval/slack_handler.py` — plan 02-06 Task 2) reads `tp.account_mode == "LIVE"` from the proposal row. Executor's `_build_broker` (plan 02-06 Task 1) derives `is_live = account_mode == "LIVE"` from the parameter the executor passes in (sourced from the proposal row). NONE of these sites re-read `strategy.mode` or `strategy_metadata.live_mode_eligible` at execute-time — closes the TOCTOU window where strategy state could change between proposal-gen (T0) and approve-click / execute (T1). DB-layer CHECK constraint + migration backfill (PAPER for legacy rows) ensure every row has a coherent value. Verification: `tests/unit/test_proposal_writer_account_mode_stamp.py` (BLOCKER #5 runtime half — promote-after-stamp test asserts the proposal row's account_mode is immune to post-stamp strategy changes). |
| T-02-01-D-01 | Information Disclosure | BrokerCredential.kind column addition could leak credential identity in `__repr__` | mitigate | `BrokerCredential.__repr__` at lines 348-353 already excludes `key_blob` + `secret_blob` per Phase 1 D-?? — confirm Task 4 preserves this exclusion. The new `kind` column is non-sensitive (it's a string discriminator, not a key) and IS allowed in repr. Test `test_broker_credential_repr_still_excludes_secrets` extends the Phase 1 test. |
| T-02-01-O-01 | Operational | Alembic downgrade could corrupt audit chain | mitigate | Task 4 behavior test "audit chain still intact" — walk_chain runs after both upgrade and downgrade against the Phase-1 22-event seed chain; SHA-256 chain must remain unbroken. The migration is additive (no event row mutation) so the chain is preserved by construction. |
| T-02-01-O-02 | Operational | Wave-0 stubs could ship as passing tests (false-green) | mitigate | Every stub contains `pytest.skip("Wave-0 stub` — pytest reports as skipped not passed. CI counts skipped vs passed separately; an audit script run in plan 02-07 confirms zero `pytest.skip("Wave-0 stub` markers remain at phase close. |
| T-02-01-M-02 | Tampering | Schema migration creates new state machine vocab; risk that prior code paths still construct proposals with old states only | accept | The new states `AWAITING_2ND_CHANNEL` + `APPROVED_LIVE` are unreachable from Phase-1 code paths because no Phase-1 code constructs them. They become reachable only when plan 02-06 wires the dual-channel approve handler. The CheckConstraint extension is forward-compatible (accepts old + new). |
| T-02-01-M-03 (BLOCKER #1 closure) | Tampering | plan 02-06 Task 2 claimed plan 02-01 added frozenset entries, but 02-01 did not modify proposals.py | mitigate | Task 5 of this plan now extends STATE_TRANSITIONS with all 5 Phase-2 edges in Wave 1. Plan 02-06 Task 2 (WIRING) no longer depends on a non-existent Wave-1 deliverable. Alembic 0002 CHECK constraint admits the new states (Task 4) — DB layer is aligned. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_alembic_0002.py tests/unit/test_decision_tool_target_notional.py tests/unit/test_trade_proposal_account_mode.py tests/unit/test_state_transitions_phase2.py -x -q` — all pass
- `uv run pytest -q --collect-only` collects 25 new test stub files without ImportError
- Phase-1 walking-skeleton test `tests/integration/test_trigger_run_end_to_end.py` still green
- `alembic upgrade head` + `alembic downgrade -1` round-trips cleanly
- `grep -rn 'pytest.skip("Wave-0 stub' tests/` returns ≥23 matches (stub files all in place)
- `python -c "from gekko.approval.proposals import STATE_TRANSITIONS; assert len(STATE_TRANSITIONS) == 11"` exits 0
</verification>

<success_criteria>
- Phase 2 Wave-0 foundations laid: 25 stub files + 1 migration + 1 ORM class + 2 schema fields (target_notional_usd + account_mode) + 5 STATE_TRANSITIONS edges + 1 dep + 3 conftest fixtures
- BLOCKER #1 closed: STATE_TRANSITIONS extended in Wave 1; plan 02-06 Task 2 WIRING depends on an actually-existing foundation
- BLOCKER #5 closed: TradeProposal.account_mode is a required Literal field; Alembic 0002 backfills existing rows to PAPER; downstream callers can read account_mode directly from the proposal row (no TOCTOU)
- No Phase-1 regression — existing 54/54 unit tests + 11/11 integration tests still pass
- Audit chain integrity preserved through migration upgrade + downgrade
- Subsequent plans (02-02..02-07) can each open their owning stub file and replace `pytest.skip` with real assertions
- Operator-confirmed package legitimacy gate cleared for tenacity
</success_criteria>

<output>
Create `.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-01-SUMMARY.md` when done. Include: migration revision id, list of stub files created with their owning plan, the tenacity version installed, any conftest fixture deltas, the verbatim STATE_TRANSITIONS frozenset literal landed (Phase-1 + Phase-2 edges), confirmation that TradeProposal.account_mode is required + in `_runtime_only`, confirmation that BLOCKER #1 and BLOCKER #5 fixes both landed, confirmation that the Phase-1 walking-skeleton test still passes.
</output>
