---
phase: 02-orderguard-real-money-alpaca-live-safety-floor
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/gekko/execution/orderguard.py
  - src/gekko/execution/checks/__init__.py
  - src/gekko/execution/checks/_universe.py
  - src/gekko/execution/checks/_hard_caps.py
  - src/gekko/execution/checks/_qty_price.py
  - src/gekko/execution/checks/_paper_live.py
  - src/gekko/execution/checks/_kill_switch.py
  - src/gekko/execution/checks/_market_hours.py
  - src/gekko/execution/executor.py
  - src/gekko/brokers/alpaca.py
  - src/gekko/brokers/base.py
  - tests/unit/test_orderguard.py
  - tests/unit/test_orderguard_paper_live.py
  - tests/integration/test_orderguard_chain_paper.py
  - tests/integration/test_orderguard_cap_rejection.py
autonomous: true
requirements:
  - EXEC-04
  - EXEC-05

user_setup: []

must_haves:
  truths:
    - "OrderGuard is a `Brokerage` subclass that wraps a concrete broker; `Brokerage.place_order` signature is preserved (D-26)"
    - "Universe whitelist BLOCK fires when ticker is NOT in `strategy.watchlist` and raises `OrderGuardRejected(reject_code='universe', ...)`"
    - "All 4 hard caps BLOCK individually (D-29): `max_position_pct`, `max_daily_loss_usd`, `max_trades_per_day`, `max_sector_exposure_pct`"
    - "qty × ref_price 2% drift BLOCK fires for LIMIT path (uses `limit_price`), STOP path (uses `stop_price`), MARKET path (uses `broker.get_quote().ask_price`) — reject_code `qty_price_drift`"
    - "Three-way paper/live invariant: `strategy.mode == 'live' ⇔ account_mode == 'LIVE' ⇔ broker.is_paper is False`; mismatch hard-rejects with `paper_live_mismatch_broker` or `paper_live_mismatch_account`"
    - "Cap rejection writes a `cap_rejection` audit event with `{reject_code, reject_reason, ticker, proposal_id, check_name}` payload and transitions APPROVED → FAILED (D-30 reuses existing FAILED state)"
    - "OrderGuard is NEVER imported by `claude_agent_sdk` (extends Plan 01-08 anti-pattern 1 grep gate to new files)"
    - "OrderGuard module-level functions follow Phase-1 test-seam pattern: `_get_session_factory`, `_get_passphrase` indirection so tests monkeypatch cleanly"
    - "Float (binary-fp) is banned from `src/gekko/execution/orderguard.py` and `src/gekko/execution/checks/*.py` (extends Phase-1 `test_float_banned_in_money_paths`)"
  artifacts:
    - path: src/gekko/execution/orderguard.py
      provides: "OrderGuard(Brokerage) decorator class — wraps a concrete broker; place_order runs all BLOCK checks then delegates"
      contains: "class OrderGuard(Brokerage)"
    - path: src/gekko/execution/checks/_universe.py
      provides: "check_universe(req, strategy) → None | raises OrderGuardRejected"
      contains: "def check_universe"
    - path: src/gekko/execution/checks/_hard_caps.py
      provides: "check_hard_caps(req, strategy, broker, user_id) — 4 caps"
      contains: "def check_hard_caps"
    - path: src/gekko/execution/checks/_qty_price.py
      provides: "check_qty_price_sanity(req, target_notional_usd, broker) — 2% drift"
      contains: "def check_qty_price_sanity"
    - path: src/gekko/execution/checks/_paper_live.py
      provides: "check_paper_live_pairing(broker, strategy_mode, account_mode, user_id)"
      contains: "def check_paper_live_pairing"
    - path: src/gekko/execution/checks/_kill_switch.py
      provides: "check_kill_switch(user_id) — DB read; raises if kill_active=True"
      contains: "def check_kill_switch"
    - path: src/gekko/execution/checks/_market_hours.py
      provides: "Thin wrapper around Phase-1's is_market_open per PATTERNS §1a row 11"
      contains: "from gekko.execution.market_hours import is_market_open"
  key_links:
    - from: "src/gekko/execution/executor.py::_build_broker"
      to: "src/gekko/execution/orderguard.py::OrderGuard"
      via: "constructor wrap"
      pattern: "OrderGuard\\(.*AlpacaBroker"
    - from: "src/gekko/execution/orderguard.py::OrderGuard.place_order"
      to: "src/gekko/execution/checks/*"
      via: "sequential check calls"
      pattern: "check_universe.*check_hard_caps.*check_qty_price_sanity"
    - from: "src/gekko/execution/executor.py::execute_proposal"
      to: "audit_log.append_event(event_type='cap_rejection')"
      via: "OrderGuardRejected exception catch + state transition"
      pattern: "cap_rejection"
---

<objective>
Ship the OrderGuard decorator skeleton + the 6 immediate-block checks (universe, hard_caps, qty_price, paper_live, kill_switch read-side, market_hours) and wire it into the executor's `_build_broker` so every proposal in the paper path now passes through OrderGuard. Also wire the `cap_rejection` audit-event branch around `broker.place_order` in `execute_proposal` (mirror of `executor.market_closed`).

Per PATTERNS §6: PDT/T+1 (`_pdt.py`, `_t1.py`) and rate-limit backoff are scoped to plan 02-03; kill_switch WRITE side is plan 02-05; live-credentials path is plan 02-06. This plan ships the read-only kill check + the 6 PAPER-path checks so the existing Phase-1 paper trades stay green AND the cap-rejection path is exercised end-to-end.

Purpose: Knight Capital + off-by-magnitude defense (D-26..D-30); EXEC-04 universe + caps; EXEC-05 paper/live invariant.

Output: 1 new OrderGuard class + 6 check modules + executor wiring + 4 test files filled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-CONTEXT.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-VALIDATION.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-01-SUMMARY.md
@src/gekko/brokers/base.py
@src/gekko/brokers/alpaca.py
@src/gekko/execution/executor.py
@src/gekko/execution/market_hours.py
@src/gekko/schemas/strategy.py
@src/gekko/schemas/proposal.py
@src/gekko/audit/log.py
@src/gekko/core/errors.py
@src/gekko/db/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrderGuard class skeleton + universe + paper_live + kill_switch (read) + market_hours checks</name>
  <read_first>
    - src/gekko/brokers/base.py (Brokerage ABC — lines 1-44 docstring pre-declares the OrderGuard decorator pattern; ABC body at lines 117-184)
    - src/gekko/brokers/alpaca.py (concrete Brokerage subclass — `name`, `supports_fractional`, `is_paper` class attrs at lines 80-82; two-layer constructor guard at lines 85-119; GET wrappers at 137-160; place_order at 162-198 — DO NOT modify)
    - src/gekko/execution/executor.py (the `_get_session_factory` test seam at lines 87-99; `_build_broker` at lines 102-114; the `executor.market_closed` audit-event branch at lines 188-220 — THIS IS THE TEMPLATE for the cap_rejection branch)
    - src/gekko/execution/market_hours.py (is_market_open — Phase-1 PATTERNS §1a row 11 says Phase 2 imports it directly, no new code needed)
    - src/gekko/schemas/strategy.py (Strategy.watchlist at lines ~120; HardCaps at lines 32-55 — 4 Pydantic-validated caps)
    - src/gekko/schemas/proposal.py (TradeProposal — including new target_notional_usd field from plan 02-01)
    - src/gekko/core/errors.py (Phase-1 `GekkoError` + `BrokerConfigError` + `BrokerOrderError` shape; OrderGuardRejected class added by plan 02-01)
    - src/gekko/db/models.py (User.kill_active column added by plan 02-01)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§1a/§1b/§3a/§3c/§3d — the decorator pattern, Decimal coercion idiom, test-seam pattern, audit-event template)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§1 OrderGuard implementation including code shape; §2 paper/live pairing check; §3 kill switch read-side check)
    - tests/unit/test_orderguard.py (Wave-0 stub from plan 02-01)
    - tests/unit/test_orderguard_paper_live.py (Wave-0 stub from plan 02-01)
    - tests/unit/test_executor.py (Phase-1 — `test_executor_module_does_not_import_claude_agent_sdk` is the grep-gate template; `test_market_closed_path` is the cap_rejection template)
  </read_first>
  <behavior>
    Universe (EXEC-04):
    - Test: place_order req with `ticker="TSLA"` and `strategy.watchlist=["AAPL", "NVDA"]` raises `OrderGuardRejected(reject_code="universe", ...)`; reject_reason includes the ticker and the watchlist
    - Test: ticker in watchlist passes through to next check

    Paper/live pairing (EXEC-05, RESEARCH §2):
    - Test: `strategy.mode="paper"` + `broker.is_paper=False` → `OrderGuardRejected(reject_code="paper_live_mismatch_broker")`
    - Test: `strategy.mode="live"` + `broker.is_paper=True` → `OrderGuardRejected(reject_code="paper_live_mismatch_broker")`
    - Test: `strategy.mode="live"` + `account_mode="PAPER"` (parameter drift) → `OrderGuardRejected(reject_code="paper_live_mismatch_account")`
    - Test: `strategy.mode="paper"` + `account_mode="LIVE"` (parameter drift) → `OrderGuardRejected(reject_code="paper_live_mismatch_account")`
    - Test: aligned three-way invariant (`paper ⇔ PAPER ⇔ is_paper=True`) passes

    Kill switch (READ side only — write side is plan 02-05):
    - Test: `users.kill_active=True` in seeded DB → `OrderGuardRejected(reject_code="kill_active", reject_reason includes 'Kill switch is ON')`
    - Test: `users.kill_active=False` passes through
    - Test: check_kill_switch uses the `_get_session_factory(user_id)` shim and disposes the engine in `finally`

    Market hours:
    - Test: `is_market_open=False` → `OrderGuardRejected(reject_code="market_closed", ...)` (mirroring Phase-1 executor.market_closed but at the OrderGuard layer for defense in depth — the executor's existing market_closed branch still fires first; OrderGuard's check is a no-op in that case)

    Architectural (cross-cutting):
    - Test: `OrderGuard` IS-A `Brokerage` subclass (isinstance check)
    - Test: `OrderGuard.place_order` is NOT decorated with tenacity / has no `__wrapped__` attribute (EXEC-03 grep gate extension)
    - Test: `OrderGuard.name`, `OrderGuard.supports_fractional`, `OrderGuard.is_paper` mirror the wrapped broker
    - Test: OrderGuard delegates GET methods (`get_account`, `get_positions`, `get_quote`, `get_order_by_client_order_id`, `cancel_order`) to `self._wrapped` unchanged
    - Test: `src/gekko/execution/orderguard.py` source bytes contain NO `claude_agent_sdk` substring (anti-pattern 1 — extends Plan 01-08 grep gate)
    - Test: same grep gate covers each `src/gekko/execution/checks/_*.py`
  </behavior>
  <action>
    Replace WAVE-0 stubs in `tests/unit/test_orderguard.py` and `tests/unit/test_orderguard_paper_live.py` with the behaviors above using pytest + the Phase-1 `_get_session_factory` monkeypatch test seam pattern from `tests/unit/test_executor.py`.

    Create `src/gekko/execution/checks/__init__.py` re-exporting symbols (PATTERNS §1a row 2): `from ._universe import check_universe`, `from ._paper_live import check_paper_live_pairing`, `from ._kill_switch import check_kill_switch`, `from ._market_hours import check_market_hours`. Define `__all__ = ("check_universe", "check_paper_live_pairing", "check_kill_switch", "check_market_hours", "check_hard_caps", "check_qty_price_sanity")` — the hard_caps + qty_price symbols come in Task 2.

    Create `src/gekko/execution/checks/_universe.py` per RESEARCH §1 / PATTERNS §1a row 3:
    - One async function `check_universe(req: OrderRequest, *, strategy: Strategy) -> None`
    - Raises `OrderGuardRejected("universe", f"ticker {req.symbol!r} not in strategy.watchlist {strategy.watchlist!r}", ticker=req.symbol)` when not in watchlist; the watchlist comparison is case-sensitive against the already-normalized list (Strategy.watchlist normalizer from Phase 1 D-? handles uppercase already)
    - Pure-Python in-memory check; no DB

    Create `src/gekko/execution/checks/_paper_live.py` per RESEARCH §2 + PATTERNS §1a row 7:
    - One function `check_paper_live_pairing(*, broker: Brokerage, strategy_mode: str, account_mode: str, user_id: str) -> None` (sync — no IO)
    - Three-way invariant per RESEARCH §2 exact body — `expected_paper = strategy_mode == "paper"`; reject with `paper_live_mismatch_broker` if `broker.is_paper is not expected_paper`; reject with `paper_live_mismatch_account` if `account_mode != ("PAPER" if expected_paper else "LIVE")`

    Create `src/gekko/execution/checks/_kill_switch.py` per RESEARCH §3 + PATTERNS §1a row 8:
    - One async function `check_kill_switch(user_id: str) -> None`
    - Uses module-level `_get_session_factory(user_id)` shim per PATTERNS §3c (verbatim copy of `gekko.execution.executor._get_session_factory` but local to this module so tests monkeypatch the orderguard-check seam independently)
    - SELECT `User.kill_active` for the user_id; raise `OrderGuardRejected("kill_active", "Kill switch is ON; no orders will fire until /gekko unkill", user_id=user_id)` when True
    - `finally: if engine is not None: await engine.dispose()` per the load-bearing pattern

    Create `src/gekko/execution/checks/_market_hours.py` per PATTERNS §1a row 11:
    - `from gekko.execution.market_hours import is_market_open`
    - Async function `check_market_hours(req: OrderRequest) -> None` — Phase-1 executor still does the primary check at lines 188-220, but OrderGuard re-checks as defense in depth (state may have changed); raise `OrderGuardRejected("market_closed", "NYSE not in regular trading hours")` when False

    Create `src/gekko/execution/orderguard.py` per RESEARCH §1 + PATTERNS §1a row 1 + §3a:
    - `class OrderGuard(Brokerage):` — mirror the `class AlpacaBroker(Brokerage)` shape at alpaca.py:72-119
    - Class attrs `name`, `supports_fractional`, `is_paper` set from `wrapped` in `__init__` per RESEARCH §1 code shape
    - `__init__(self, wrapped: Brokerage, *, strategy: Strategy, account_mode: Literal["PAPER","LIVE"], user_id: str)` — kwargs-only after wrapped
    - GET passthrough methods (`health_check`, `get_account`, `get_positions`, `get_quote`, `get_order_by_client_order_id`, `cancel_order`) — each delegates to `self._wrapped` per RESEARCH §1 code shape
    - `async def place_order(self, req: OrderRequest) -> OrderResult` runs (in order, per RESEARCH §1): `check_kill_switch(user_id)`, `check_paper_live_pairing(...)`, `check_universe(req, strategy=self._strategy)`, then (Task 2 adds hard_caps + qty_price), then `check_market_hours(req)` defense-in-depth, then `await self._wrapped.place_order(req)`
    - DO NOT add a `@retry` decorator to `place_order` (EXEC-03 — PATTERNS §4 anti-pattern row 3). The function has no `__wrapped__` attribute after definition.

    `OrderGuardRejected` exception is from `gekko.core.errors` (added by plan 02-01).

    NEVER import `claude_agent_sdk` in any new module — extends Plan 01-08 anti-pattern 1 grep gate (PATTERNS §4 anti-pattern row 2).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_orderguard.py tests/unit/test_orderguard_paper_live.py -x -q</automated>
  </verify>
  <done>
    - All test behaviors pass (universe + paper/live 4 cases + kill_switch 2 cases + market_hours + 5 architectural tests)
    - `python -c "from gekko.execution.orderguard import OrderGuard; from gekko.brokers.base import Brokerage; assert issubclass(OrderGuard, Brokerage)"` exits 0
    - `python -c "from gekko.execution.orderguard import OrderGuard; assert not hasattr(OrderGuard.place_order, '__wrapped__')"` exits 0
    - `grep -v '^#' src/gekko/execution/orderguard.py src/gekko/execution/checks/_*.py | grep -c claude_agent_sdk` returns 0
    - Phase-1 `test_executor_module_does_not_import_claude_agent_sdk` still passes; new test of the same shape extended to cover the orderguard module
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hard caps (4 fields) + qty×price 2% sanity checks</name>
  <read_first>
    - src/gekko/schemas/strategy.py (HardCaps Pydantic class at lines 32-55 — `max_position_pct le=0.20`, `max_daily_loss_usd gt=0`, `max_trades_per_day ge=1`, `max_sector_exposure_pct le=1`)
    - src/gekko/brokers/alpaca.py (get_account at lines 137-145, get_positions at lines 142, get_quote at lines 147-160 — these are the broker APIs the checks consume; `_order_to_result` at lines 294-316 is the Decimal coercion idiom)
    - src/gekko/audit/log.py (append_event signature; the events table walk pattern for daily-loss + trades-per-day counting per RESEARCH §1 "Hard-Cap Enforcement")
    - src/gekko/db/models.py (Event ORM — `select(Event).where(Event.event_type == 'fill', Event.user_id == ..., Event.ts >= today_iso)` query shape; Event.payload_json carries the parseable fill data)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§1 "Hard-Cap Enforcement" + "Qty × Price 2% Sanity" — exact field selection for ref_price by order_type; §6 says NO retry on get_quote in this plan — that lands in plan 02-03)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§1a row 4 hard_caps + row 5 qty_price; §3b Decimal coercion at the broker boundary)
  </read_first>
  <behavior>
    Hard caps (EXEC-04 / D-29 — 4 caps, individually parametrized):
    - Test: max_position_pct — proposed `qty × ref_price / account.equity > strategy.hard_caps.max_position_pct` → `OrderGuardRejected(reject_code="hard_cap_position_pct", reject_reason names the cap value and the actual %)`
    - Test: max_position_pct — at exactly the cap passes
    - Test: max_daily_loss_usd — walk `events` table for today's `fill` event_type rows; sum realized loss in `payload_json.realized_pnl_usd` (or equivalent — RESEARCH §1 leaves the choice); reject when cumulative loss ≥ cap → `hard_cap_daily_loss`
    - Test: max_trades_per_day — count today's `order_submitted` event_type rows for this user_id + strategy_id; reject when count ≥ cap → `hard_cap_trades_per_day`
    - Test: max_sector_exposure_pct — fetch sector via `broker._wrapped` (alpaca-py Asset.attributes per RESEARCH §1) for the proposed ticker; sum existing positions in same sector; reject when sum+proposed > cap → `hard_cap_sector_exposure`. If sector resolver returns None (alpaca-py shape variation), the check warns but does not block (defensive — sector lookup is best-effort per RESEARCH §1)
    - Test: each cap fires in isolation; the check function calls them in a consistent order (position → daily_loss → trades_per_day → sector) and stops on the first rejection

    qty × price 2% sanity (EXEC-04 / D-27):
    - Test: LIMIT order — `req.qty=100`, `req.limit_price=Decimal("100")`, `req.target_notional_usd=Decimal("10000")` → drift=0% → passes
    - Test: LIMIT order — `req.qty=100`, `req.limit_price=Decimal("90")`, `req.target_notional_usd=Decimal("10000")` → drift=10% → `OrderGuardRejected("qty_price_drift", ...)`
    - Test: LIMIT order — drift=2.01% → reject; drift=1.99% → pass
    - Test: MARKET order — broker.get_quote is mocked to return `{"ask_price": "100"}`; req.qty=100 + target_notional_usd=10000 → drift=0% → pass
    - Test: MARKET order — broker.get_quote returns shape `{"ap": "100"}` (alternative key per RESEARCH §1) — same pass
    - Test: STOP order — uses `req.stop_price` (RESEARCH §1 explicit branch); same drift logic
    - Test: ref_price missing (LIMIT with limit_price=None or quote returns None) → `OrderGuardRejected("ref_price_missing", ...)`
    - Test: every Decimal computation uses `Decimal(str(value))` coercion (no float intermediates) — verified by parsing source bytes (PATTERNS §3b grep gate)
  </behavior>
  <action>
    Replace WAVE-0 stub sections in `tests/unit/test_orderguard.py` for hard caps and qty_price with the behaviors above. Use parametrized tests where possible (4 caps × pass/reject = 8 parametrized cases for hard caps; LIMIT/MARKET/STOP × pass/reject for qty_price).

    Create `src/gekko/execution/checks/_hard_caps.py` per RESEARCH §1 "Hard-Cap Enforcement" + PATTERNS §1a row 4:
    - `async def check_hard_caps(*, req: OrderRequest, strategy: Strategy, broker: Brokerage, user_id: str) -> None`
    - Internal helpers: `async def _check_position_pct(...)`, `async def _check_daily_loss(...)`, `async def _check_trades_per_day(...)`, `async def _check_sector_exposure(...)` — each raises `OrderGuardRejected` with the corresponding reject_code from the locked list in plan 02-01
    - For daily_loss + trades_per_day: open the per-user SQLCipher engine via `_get_session_factory(user_id)` (PATTERNS §3c); query `Event` rows for today's UTC date window; parse `payload_json` for realized P&L per RESEARCH §1 (key name: TBD by executor based on Phase-1 Plan 01-08 fill event payload shape — read the existing fill-event handler in executor.py:334-432 to confirm)
    - For position_pct + sector_exposure: call `broker.get_account()` for equity; call `broker.get_positions()` for existing exposure; sector via `await asyncio.to_thread(broker._wrapped._client.get_asset, symbol)` then read `.attributes` if accessible — per RESEARCH §1 Open Question (sector lookup is best-effort)
    - All money math uses `Decimal(str(value))` (PATTERNS §3b — float ban)
    - Add to `src/gekko/execution/checks/__init__.py` `__all__` + re-export

    Create `src/gekko/execution/checks/_qty_price.py` per RESEARCH §1 "Qty × Price 2% Sanity" + PATTERNS §1a row 5 + §3b:
    - `async def check_qty_price_sanity(*, req: OrderRequest, target_notional_usd: Decimal, broker: Brokerage) -> None`
    - Body follows the RESEARCH §1 code shape verbatim: branch on `req.order_type.value` ("limit" → `req.limit_price`; "stop" → `req.stop_price`; "market" → `await broker.get_quote(req.symbol)` and extract `ask_price` or `ap` key with `Decimal(str(...))`)
    - Drift computed as `abs(actual_notional - target_notional_usd) / target_notional_usd`; reject when `> Decimal("0.02")`
    - The 2% literal is `Decimal("0.02")` (NOT `0.02` — float ban)
    - The reject_reason string includes the computed actual notional, drift percentage, and target value (RESEARCH §1 exact format)
    - On `ref_price <= 0` or missing → `OrderGuardRejected("ref_price_missing", ...)`

    Update `src/gekko/execution/orderguard.py` `place_order` body to call `check_hard_caps(...)` and `check_qty_price_sanity(...)` in the order specified in Task 1's design (between paper_live and market_hours per RESEARCH §1 code shape).

    Per PATTERNS §4 anti-pattern row 1 (Float ban): extend `tests/unit/test_money_math.py::test_float_banned_in_money_paths` directory walk to include `src/gekko/execution/orderguard.py` and `src/gekko/execution/checks/`. The existing grep gate already walks `src/gekko/execution/` per Plan 01-05 — confirm by reading the test and extending if needed.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_orderguard.py tests/unit/test_money_math.py -x -q</automated>
  </verify>
  <done>
    - All hard_caps test cases pass (4 caps × pass/reject + ordering)
    - All qty_price test cases pass (LIMIT, MARKET, STOP, drift boundary, ref_price_missing)
    - `test_float_banned_in_money_paths` covers the new orderguard module + checks directory (no float literals in money paths)
    - `python -c "from gekko.execution.checks import check_hard_caps, check_qty_price_sanity"` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire OrderGuard into executor._build_broker + add cap_rejection state-transition branch + integration chain test</name>
  <read_first>
    - src/gekko/execution/executor.py (`_build_broker` at lines 102-114 — current signature `(user_id: str) -> Brokerage`; the `executor.market_closed` audit-event branch at lines 188-220 — THIS IS THE EXACT TEMPLATE for cap_rejection; the `try: ... except BrokerOrderError` around place_order at lines 236-274 — the new cap_rejection handler is a sibling `except OrderGuardRejected` branch)
    - src/gekko/approval/proposals.py (transition_status — already supports APPROVED → FAILED transition; lines 51-60 frozenset)
    - src/gekko/audit/log.py (append_event signature)
    - src/gekko/core/money.py (normalize_decimals — required wrapping for any audit payload per Plan 01-04 D-?)
    - tests/integration/test_orderguard_chain_paper.py (Wave-0 stub from plan 02-01)
    - tests/integration/test_orderguard_cap_rejection.py (Wave-0 stub from plan 02-01)
    - tests/integration/test_slack_approval_to_executor.py (Phase-1 walking-skeleton chain test — the model for the 4-event cap_rejection chain)
    - tests/unit/test_executor.py (Phase-1 `test_market_closed_path` — the sibling test pattern for cap_rejection)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§1 "Where `OrderGuard.__init__` gets `account_mode`"; §1 `_build_broker` modification body; §1 cap_rejection state transition reuses FAILED)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§2 row 4 — executor.py extension locations; §3d audit-event template with normalize_decimals; §5d background-task wrapper pattern)
  </read_first>
  <behavior>
    Wiring (executor.py):
    - Test: `_build_broker(user_id="chris", strategy=<paper strategy>, account_mode="PAPER")` returns an `OrderGuard` instance wrapping `AlpacaBroker(paper=True)`; the wrapped broker uses the env-loaded paper credentials (live-credentials vault path is plan 02-06)
    - Test: `_build_broker` for a paper strategy still works identically to Phase 1 (no regression on existing paper trades)
    - Test: monkeypatch-friendly — tests can replace `_build_broker` per Phase-1 seam pattern

    Cap rejection chain (EXEC-04 + D-30):
    - Test (integration, end-to-end): seed a PENDING proposal with `ticker="TSLA"` and `strategy.watchlist=["AAPL"]`; call `execute_proposal(proposal_id, user_id)`; assert audit chain emits `[decision, proposal, approval, cap_rejection]` in order (4-event chain); walk_chain returns []; proposal status transitions PENDING → APPROVED → FAILED; cap_rejection payload contains `{reject_code: "universe", reject_reason: <str>, ticker: "TSLA", proposal_id: <id>, check_name: "universe"}` after normalize_decimals
    - Test (integration): same as above but for hard_cap_position_pct (mock broker.get_account to return small equity)
    - Test (integration): same as above but for qty_price_drift
    - Test (integration): same as above but for paper_live_mismatch (seed a "live" mode strategy with paper broker)
    - Test (integration): same as above but for kill_active (set users.kill_active=True directly in DB)

    Idempotency / consistency:
    - Test: the cap_rejection branch does NOT call `broker.place_order` — OrderGuardRejected is raised inside place_order before the wrapped broker's HTTP POST runs
    - Test: cap_rejection write happens in a fresh transaction (mirrors `executor.market_closed` pattern at lines 188-220), not the read-side proposal-load transaction
    - Test: structlog redaction holds — `log.warning("executor.cap_rejection", ...)` keyword args never include credential strings
  </behavior>
  <action>
    Replace WAVE-0 stubs in `tests/integration/test_orderguard_chain_paper.py` and `tests/integration/test_orderguard_cap_rejection.py` with the behaviors above. Mirror the cassette + monkeypatch-asyncio.create_task drain pattern from `tests/integration/test_slack_approval_to_executor.py` (Phase-1 walking-skeleton — PATTERNS §1c row "tests/integration/test_first_live_gate.py" identifies this as the model). The `_get_session_factory` test seam is monkey-patched to `(pre_built_factory, None)` per PATTERNS §3c.

    Extend `src/gekko/execution/executor.py` per PATTERNS §2 row 4 (executor.py extension points):
    1. Change `_build_broker(user_id: str)` signature at line 102 to `_build_broker(user_id: str, strategy: Strategy, account_mode: str) -> Brokerage`. For Phase 2 paper path: load paper credentials from env via settings (no change from Phase 1), construct `AlpacaBroker(paper=True)`, then wrap: `return OrderGuard(broker, strategy=strategy, account_mode=account_mode, user_id=user_id)`. The live-credentials path is added by plan 02-06 (a conditional branch on `strategy.live_mode_eligible` AND `strategy.mode == "live"`).
    2. Update every caller of `_build_broker(user_id)` inside `execute_proposal` (line 224 or wherever — read current source) to pass the strategy + account_mode. The strategy is already loaded earlier in execute_proposal as `strategy = ...`; the account_mode is `"LIVE" if (strategy.mode == "live" and strategy_metadata.live_mode_eligible) else "PAPER"` — but since plan 02-02 only ships the PAPER path, this resolves to `"PAPER"` unconditionally for now (plan 02-06 adds the live branch).
    3. Add cap_rejection handler around the `await broker.place_order(req)` call site at lines 236-274. Pattern: `except OrderGuardRejected as exc:` sibling to the existing `except BrokerOrderError`. The handler body is a near-verbatim copy of the `executor.market_closed` block at lines 188-220 per PATTERNS §3d:
       - `log.warning("executor.cap_rejection", proposal_id=proposal_id, ticker=tp.ticker, reject_code=exc.reject_code, reject_reason=exc.reject_reason)`
       - Open a fresh transaction (`async with sf() as session, session.begin():`)
       - `await append_event(session, user_id=user_id, strategy_id=strategy_id, event_type="cap_rejection", payload=normalize_decimals({"reject_code": exc.reject_code, "reject_reason": exc.reject_reason, "ticker": tp.ticker, "proposal_id": proposal_id, "check_name": exc.reject_code, **exc.extra}))`
       - `await transition_status(session, proposal_id, from_status="APPROVED", to_status="FAILED")`
       - `return` (do NOT continue to FILL path)

    `OrderGuardRejected.extra` is a dict of additional context the check sets (e.g., `ref_price`, `actual_notional`); merge it into the cap_rejection payload so audit log filtering is rich.

    Per PATTERNS §5c — `normalize_decimals(payload)` MUST wrap the entire payload before `append_event`. Per Plan 01-04 D-? — canonical-subset shape is locked at `{event_type, payload, ts, user_id}`; new payload field keys are forward-additive.

    `cap_rejection` event_type is already in `_EVENT_TYPES` tuple (Phase 1 D-14 per CONTEXT.md) — no models.py change needed.

    Per PATTERNS §4 anti-pattern row 11 ("Holding the same `_append_locks` lock across an async DM"): the cap_rejection branch does NOT send a Slack DM (that's a plan 02-05 / 02-06 surface — operator sees the cap rejection on the dashboard rejections panel and on the audit log; Slack DM surfacing is part of plan 02-05's Slack rejection card per UI-SPEC §4a). For plan 02-02, the audit event is the only side effect.

    Confirm Phase-1 `tests/integration/test_trigger_run_end_to_end.py` (the walking-skeleton 5-event chain) still passes — the OrderGuard wrap in `_build_broker` is INVISIBLE to a happy-path paper trade because all 6 checks pass; the underlying `AlpacaBroker.place_order` still runs.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_orderguard_chain_paper.py tests/integration/test_orderguard_cap_rejection.py tests/integration/test_trigger_run_end_to_end.py tests/unit/test_executor.py -x -q</automated>
  </verify>
  <done>
    - All 5 cap_rejection integration tests pass (universe, hard_cap, qty_price, paper_live_mismatch, kill_active)
    - 4-event audit chain `[decision, proposal, approval, cap_rejection]` validates with walk_chain returning [] for each rejection case
    - Phase-1 walking-skeleton 5-event chain test still passes (no regression on the happy path)
    - Existing Phase-1 unit tests on executor (`tests/unit/test_executor.py`) still pass
    - `python -c "from gekko.execution.executor import _build_broker; import inspect; sig = inspect.signature(_build_broker); assert 'strategy' in sig.parameters and 'account_mode' in sig.parameters"` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approved-proposal → broker.place_order | OrderGuard is the deterministic Python firewall — runs BEFORE any HTTP POST |
| Strategy.watchlist authoritative universe | Universe check rejects hallucinated tickers (Pitfall 2) |
| HardCaps Pydantic-validated bounds | Runtime check against actual broker state (equity, positions, today's events) |
| LLM-authored target_notional_usd vs computed qty × ref_price | Schema field vs runtime math — disagreement at >2% catches off-by-magnitude (Pitfall 3) |
| broker.is_paper introspection | Three-way invariant against strategy.mode + account_mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-M-01 | Tampering | OrderGuard could be bypassed by direct `_wrapped.place_order` call from another module | mitigate | `_build_broker` is the SINGLE construction site for the broker in the executor pipeline. The grep gate in `tests/unit/test_executor.py` plus the new `tests/unit/test_orderguard.py` confirm no other module constructs `AlpacaBroker` directly except via `_build_broker`. Future plans must respect this invariant. |
| T-02-02-M-02 | Tampering | Knight Capital duplicate-order loop via OrderGuard.place_order being retry-decorated | mitigate | Task 1 behavior test asserts `not hasattr(OrderGuard.place_order, "__wrapped__")`; PATTERNS §4 anti-pattern row 3 names this as a tripwire; plan 02-03 will add the GET-only tenacity decorator and the grep-gate test for place_order. |
| T-02-02-M-03 | Tampering | Off-by-magnitude position sizing through `target_notional_usd` mismatch | mitigate | `check_qty_price_sanity` enforces 2% drift bound (Decimal-exact math, no float). Tests cover the boundary (1.99% pass, 2.01% reject) and all three order_type branches (LIMIT, STOP, MARKET). Verification command runs the parametrized test suite. |
| T-02-02-M-04 | Tampering | Hallucinated ticker reaches broker | mitigate | `check_universe` is the first BLOCK after kill/paper-live; Phase-1's `ProposalWriter` watchlist guard catches at proposal-build time (Pitfall 2 — already shipped); OrderGuard re-checks at place_order time per D-29 defense in depth. |
| T-02-02-M-05 | Tampering | Paper credential places a live order via env-var typo | mitigate | `check_paper_live_pairing` enforces the three-way invariant (strategy.mode ⇔ account_mode ⇔ broker.is_paper) at every place_order. Task 1 covers all four mismatch combinations. The live-credentials path (plan 02-06) extends the check via `BrokerCredential.kind` validation. |
| T-02-02-O-01 | Operational | OrderGuard's `_get_session_factory` could leak SQLCipher engines on exception | mitigate | The `finally: if engine is not None: await engine.dispose()` pattern from PATTERNS §3c is mandatory in every check that opens an engine. Test `test_check_kill_switch_disposes_engine_on_exception` verifies. |
| T-02-02-O-02 | Operational | OrderGuard could import claude_agent_sdk (LLM bytes leak into firewall) | mitigate | Grep gate at `tests/unit/test_orderguard.py::test_orderguard_module_does_not_import_claude_agent_sdk` extends Plan 01-08's anti-pattern 1 lock to the new file + every check module. Confirmed in Task 1 done criteria. |
| T-02-02-I-01 | Information Disclosure | Cap-rejection payload could leak credentials via `extra` kwargs | mitigate | OrderGuardRejected.extra is set explicitly per check — credential strings are NEVER passed as extra kwargs. Tests assert payloads contain only safe scalars (ticker, ref_price, percentage). The structlog `_redact` processor (Plan 01-02 D-25) catches any accidental key inclusion. |
| T-02-02-O-03 | Operational | Phase-1 walking-skeleton breaks because OrderGuard wraps every paper trade now | mitigate | Task 3 done criteria explicitly requires `tests/integration/test_trigger_run_end_to_end.py` to remain green — the OrderGuard wrap on the happy path is invisible because all 6 checks pass. CI enforces. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_orderguard.py tests/unit/test_orderguard_paper_live.py tests/integration/test_orderguard_chain_paper.py tests/integration/test_orderguard_cap_rejection.py -x -q` — all pass
- `uv run pytest tests/integration/test_trigger_run_end_to_end.py -x -q` — Phase-1 walking-skeleton still green
- `grep -v '^#' src/gekko/execution/orderguard.py src/gekko/execution/checks/_*.py | grep -c claude_agent_sdk` returns 0
- `python -c "from gekko.execution.checks import check_universe, check_hard_caps, check_qty_price_sanity, check_paper_live_pairing, check_kill_switch, check_market_hours"` exits 0
- Phase-1 22-event audit chain in a fresh DB still validates after a cap_rejection run extends it to 26 events with walk_chain returning []
</verification>

<success_criteria>
- OrderGuard is the deterministic Python firewall sitting between every approved paper proposal and `AlpacaBroker.place_order` (D-26 architecture realized)
- 6 BLOCK checks (universe, paper_live, kill, market_hours, hard_caps × 4 sub-checks, qty_price) all individually verified
- cap_rejection state transition (APPROVED → FAILED with audit event) works end-to-end
- 4-event integration chain `[decision, proposal, approval, cap_rejection]` validates against walk_chain
- Phase-1 5-event happy-path chain still passes (no regression on paper trades)
- Float ban + claude_agent_sdk grep gate extended to new files
</success_criteria>

<output>
Create `.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-02-SUMMARY.md` when done. Include: list of new modules under `src/gekko/execution/checks/`, the `_build_broker` signature change, the cap_rejection event payload schema lock, the 6 reject_code strings used (universe / paper_live_mismatch_broker / paper_live_mismatch_account / kill_active / market_closed / hard_cap_* / qty_price_drift / ref_price_missing), confirmation that Phase-1 walking-skeleton + 22-event chain still validate.
</output>
