---
phase: 02-orderguard-real-money-alpaca-live-safety-floor
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/gekko/brokers/_retry.py
  - src/gekko/brokers/alpaca.py
  - src/gekko/execution/checks/__init__.py
  - src/gekko/execution/checks/_pdt.py
  - src/gekko/execution/checks/_t1.py
  - src/gekko/execution/checks/_wash_sale.py
  - src/gekko/execution/orderguard.py
  - src/gekko/agent/proposal_writer.py
  - src/gekko/schemas/proposal.py
  - tests/unit/test_alpaca_retry.py
  - tests/unit/test_rate_limit_backoff.py
  - tests/unit/test_pdt_t1_detection.py
  - tests/unit/test_wash_sale.py
  - tests/unit/test_wash_sale_flag.py
autonomous: true
requirements:
  - EXEC-03
  - EXEC-08
  - EXEC-09
  - EXEC-11

user_setup: []

must_haves:
  truths:
    - "tenacity retry decorator wraps broker GET methods only (get_account, get_positions, get_quote, get_orders_open, get_order_by_client_order_id) — EXEC-08"
    - "AlpacaBroker.place_order MUST NOT be retry-decorated; OrderGuard.place_order MUST NOT be retry-decorated; AlpacaBroker.cancel_order MUST NOT be retry-decorated — EXEC-03 / Pitfall 4 / Knight Capital"
    - "EXEC-03 grep gate is AST-walk based (NOT text-grep): asserts `AlpacaBroker.place_order` and `OrderGuard.place_order` AST FunctionDef nodes have `len(decorator_list) == 0` (per WARNING #2 fix — text grep is brittle to comment placement)"
    - "PDT block: when broker reports pattern_day_trader=True + daytrade_count≥3 + equity<$25K AND a 4th day-trade would result → OrderGuardRejected('pdt_rule') — EXEC-11"
    - "PDT block defense-in-depth: local audit-log walk over 5 business days; when local count ≥3 + equity<$25K AND this order would complete a 4th round-trip → OrderGuardRejected('pdt_rule_local') — EXEC-11"
    - "T+1 block on cash account: BUY when order_cost > non_marginable_buying_power → OrderGuardRejected('t1_settlement') — EXEC-11"
    - "Wash-sale FLAG (NEVER BLOCK): a same-ticker fill within the past 30 days surfaces `wash_sale_flag` dict on TradeProposal; EXEC-09 explicit invariant — does NOT raise; OrderGuard does NOT re-check at place_order"
    - "ProposalWriter attaches the wash_sale flag to TradeProposal.wash_sale_flag at proposal-build time per D-28"
  artifacts:
    - path: src/gekko/brokers/_retry.py
      provides: "tenacity retry decorator factory for rate-limit 429s on GETs only"
      contains: "retry_on_rate_limit"
    - path: src/gekko/execution/checks/_pdt.py
      provides: "check_pdt — two-source PDT BLOCK"
      contains: "def check_pdt"
    - path: src/gekko/execution/checks/_t1.py
      provides: "check_t1_settlement — T+1 BLOCK on cash account"
      contains: "def check_t1_settlement"
    - path: src/gekko/execution/checks/_wash_sale.py
      provides: "flag_wash_sale — FLAG-only 30-day lookback"
      contains: "def flag_wash_sale"
  key_links:
    - from: "src/gekko/brokers/alpaca.py::get_account"
      to: "src/gekko/brokers/_retry.py::retry_on_rate_limit"
      via: "decorator"
      pattern: "@retry_on_rate_limit"
    - from: "src/gekko/agent/proposal_writer.py"
      to: "src/gekko/execution/checks/_wash_sale.py::flag_wash_sale"
      via: "function call at proposal-build time"
      pattern: "flag_wash_sale\\("
    - from: "src/gekko/execution/orderguard.py::place_order"
      to: "src/gekko/execution/checks/_pdt.py + _t1.py"
      via: "sequential BLOCK calls after qty_price check"
      pattern: "check_pdt.*check_t1_settlement"
---

<objective>
Ship the regulatory + operational BLOCK checks (PDT + T+1 per EXEC-11), the wash-sale FLAG path (per EXEC-09), and the rate-limit retry decorator on broker GETs (EXEC-08) + the no-retry-on-POST AST-walk gate (EXEC-03 / Knight Capital — WARNING #2 fix replaces brittle text-grep with AST inspection).

This plan completes the OrderGuard BLOCK/FLAG matrix. Plan 02-02 shipped 6 immediate-block checks; this plan adds the 2 regulatory blocks (PDT, T+1) and the 1 flag (wash-sale), plus retro-fits the existing alpaca-py GET methods with `@retry_on_rate_limit` and locks down the place_order EXEC-03 invariant via AST gate.

Per BLOCKER #2: this plan is moved from Wave 2 → Wave 3 (depends on 02-02) because both plans touch `orderguard.py`, `checks/__init__.py`, `alpaca.py`, and `test_orderguard.py` — running them in parallel causes merge conflicts and test ordering inversions. Sequencing after 02-02 eliminates the file-overlap problem.

Purpose: regulatory compliance (FINRA PDT, SEC T+1, IRC §1091 wash-sale) + Pitfall 4 / Knight Capital reinforcement.

Output: 3 new check modules + 1 retry decorator module + alpaca.py GET-only @retry adornments + ProposalWriter wash-sale FLAG wiring + 4 test files filled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-CONTEXT.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-01-SUMMARY.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-02-SUMMARY.md
@src/gekko/brokers/alpaca.py
@src/gekko/execution/orderguard.py
@src/gekko/execution/checks/__init__.py
@src/gekko/agent/proposal_writer.py
@src/gekko/audit/log.py
@src/gekko/db/models.py
@src/gekko/schemas/proposal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tenacity retry decorator on broker GETs only + place_order AST-walk gate (EXEC-03 + EXEC-08, WARNING #2 fix)</name>
  <read_first>
    - src/gekko/brokers/alpaca.py (current `AlpacaBroker` — `get_account` at lines 137, `get_positions` at lines 142, `get_quote` at lines 147-160, `get_order_by_client_order_id` at lines 200, `place_order` at lines 162-198 — UNCHANGED, `cancel_order` — note the explicit decision NOT to retry cancel per RESEARCH §6)
    - src/gekko/execution/orderguard.py (shipped by plan 02-02 — must also remain undecorated)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§6 in full — exact tenacity decorator shape `retry_on_rate_limit = retry(wait=wait_random_exponential(min=1,max=60), stop=stop_after_attempt(6), retry=retry_if_exception(_is_rate_limit), reraise=True)`; the `_is_rate_limit(exc)` helper recognizing 429 + text-match fallback)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§4 anti-pattern row 3 "Blind retry on POSTs" — Knight Capital prevention)
    - tests/unit/test_alpaca_retry.py (Wave-0 stub from plan 02-01)
    - tests/unit/test_rate_limit_backoff.py (Wave-0 stub from plan 02-01)
    - tenacity docs: imports needed — `from tenacity import retry, retry_if_exception, stop_after_attempt, wait_random_exponential, before_sleep_log` (RESEARCH §6 verbatim)
    - Python `ast` module docs — `ast.parse`, `ast.walk`, `ast.AsyncFunctionDef`, `ast.FunctionDef`, `node.decorator_list` (for the AST-walk gate per WARNING #2)
  </read_first>
  <behavior>
    Retry decorator presence (EXEC-08):
    - Test: `AlpacaBroker.get_account` has `__wrapped__` attribute (tenacity decoration confirmed)
    - Test: `AlpacaBroker.get_positions` has `__wrapped__`
    - Test: `AlpacaBroker.get_quote` has `__wrapped__`
    - Test: `AlpacaBroker.get_order_by_client_order_id` has `__wrapped__`
    - Test: `AlpacaBroker.get_orders_open` (added in plan 02-05 for kill switch) WILL have `__wrapped__` — for plan 02-03 this test xfails or skips referencing plan 02-05

    No-retry-on-POST AST gate (EXEC-03 / Pitfall 4 / Knight Capital — WARNING #2 fix):
    - Test: `AlpacaBroker.place_order` does NOT have `__wrapped__` attribute
    - Test: `AlpacaBroker.cancel_order` does NOT have `__wrapped__` attribute (RESEARCH §6 Open Question #1 — explicit decision)
    - Test: `OrderGuard.place_order` does NOT have `__wrapped__` attribute (defense in depth — plan 02-02's class)
    - Test (AST gate, BLOCKER WARNING #2): parse `src/gekko/brokers/alpaca.py` and `src/gekko/execution/orderguard.py` via `ast.parse(Path(...).read_text())`; walk all nodes; for any `ast.AsyncFunctionDef` (or `ast.FunctionDef`) named `place_order` or `cancel_order` inside an `ast.ClassDef` named `AlpacaBroker` or `OrderGuard`, assert `len(node.decorator_list) == 0`. This catches any decorator addition (including `@retry`, `@retry_on_rate_limit`, `@functools.wraps(...)`, or any custom wrapper) regardless of comment placement, whitespace, or `# noqa` markers — the text-grep was brittle to "comment-out the decorator" tricks; AST walk inspects the parsed structure.
    - Test (AST gate negative): the gate also confirms `AlpacaBroker.get_account` HAS `len(node.decorator_list) >= 1` AND the decorator's `id` (or `attr` on Attribute nodes) is `retry_on_rate_limit` — positive control that the AST inspection is reading the real tree, not a stub.

    Retry behavior (functional):
    - Test: respx/mock returns 429 with `Retry-After: 1` header on first call to `get_account`, then 200 on second call; total elapsed time ≤ 5s; final result is the 200 payload
    - Test: respx/mock returns 429 repeatedly (6 attempts exhausted) → final exception is `APIError(429)` and total attempts == 6 (stop_after_attempt(6))
    - Test: a non-429 APIError (e.g., 500) is NOT retried (retry_if_exception only fires on 429)
  </behavior>
  <action>
    Replace WAVE-0 stub in `tests/unit/test_alpaca_retry.py` and `tests/unit/test_rate_limit_backoff.py` with the behaviors above. Use respx to mock the alpaca HTTP layer for the retry-functional tests (RESEARCH §9 Test Framework declares respx is in the stack).

    Implement the AST-walk gate (WARNING #2 fix) in `tests/unit/test_alpaca_retry.py::test_place_order_ast_gate_no_decorators`:
    - `import ast; from pathlib import Path`
    - `tree = ast.parse(Path("src/gekko/brokers/alpaca.py").read_text())`
    - Walk top-level ClassDef nodes; find `AlpacaBroker`; iterate its body; for each `ast.AsyncFunctionDef` named `place_order` or `cancel_order`, assert `node.decorator_list == []`
    - Repeat for `src/gekko/execution/orderguard.py` → `OrderGuard.place_order`
    - Positive control: find `AlpacaBroker.get_account` and assert `len(node.decorator_list) == 1` AND the decorator's source representation contains `retry_on_rate_limit`

    Create `src/gekko/brokers/_retry.py` per RESEARCH §6 verbatim (PATTERNS §6 "No Analog Found" — follow RESEARCH text):
    - `import structlog` + `from tenacity import retry, retry_if_exception, stop_after_attempt, wait_random_exponential, before_sleep_log`
    - `from alpaca.common.exceptions import APIError`
    - `def _is_rate_limit(exc: BaseException) -> bool:` per RESEARCH §6 body — checks `APIError.status_code == 429`; defense-in-depth text-match for "rate limit" / "too many requests" / " 429" substrings on `str(exc).lower()`
    - `retry_on_rate_limit = retry(wait=wait_random_exponential(min=1, max=60), stop=stop_after_attempt(6), retry=retry_if_exception(_is_rate_limit), before_sleep=before_sleep_log(log, logging.WARNING), reraise=True)`
    - Do NOT ship the `WaitRetryAfter` waiter (RESEARCH §6 marks it "OPTIONAL polish for P2; the simpler wait_random_exponential is sufficient")

    Extend `src/gekko/brokers/alpaca.py` per PATTERNS §2 row 3:
    - Import `from gekko.brokers._retry import retry_on_rate_limit` at top of file
    - Add `@retry_on_rate_limit` decorator immediately above each of:
      - `async def get_account` (line 137)
      - `async def get_positions` (line 142)
      - `async def get_quote` (line 147)
      - `async def get_order_by_client_order_id` (line 200)
    - DO NOT add to `async def place_order` (line 162) — EXEC-03 invariant
    - DO NOT add to `async def cancel_order` — RESEARCH §6 Open Question #1 explicit decision: a 429 retry storm during kill timing is the worst possible failure mode; the kill switch's `asyncio.gather` + 4s timeout in plan 02-05 is the failure-tolerant scaffolding

    The AST-walk gate is the canonical EXEC-03 enforcement after WARNING #2. The earlier text-grep approach is removed (comment-out tricks bypassed it). The AST gate inspects the parsed tree directly — every `place_order` / `cancel_order` AST node must have an empty `decorator_list`.

    Per PATTERNS §1c row "test_rate_limit_backoff.py": this test file is a NEW pattern with no Phase-1 analog. RESEARCH §6 documents the assertion shape verbatim.

    Add `get_orders_open` introspection test as xfail/skip(reason="added in plan 02-05") so plan 02-05 can flip the marker without re-writing.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_alpaca_retry.py tests/unit/test_rate_limit_backoff.py tests/unit/test_alpaca_place_order.py -x -q</automated>
  </verify>
  <done>
    - All retry-presence tests pass for GET methods
    - All no-retry tests pass for place_order + cancel_order (AlpacaBroker + OrderGuard) — both `__wrapped__` introspection AND AST-walk gate
    - AST-walk gate (WARNING #2 fix) passes: `AlpacaBroker.place_order`, `AlpacaBroker.cancel_order`, `OrderGuard.place_order` all have `decorator_list == []`
    - AST positive control passes: `AlpacaBroker.get_account` has 1 decorator referencing `retry_on_rate_limit`
    - Functional retry-on-429 test passes with respx mocks
    - Phase-1 `tests/unit/test_alpaca_place_order.py` (which covers EXEC-02 / 422-handler) still passes — no regression on the dedup path
    - `python -c "from gekko.brokers.alpaca import AlpacaBroker; assert hasattr(AlpacaBroker.get_account, '__wrapped__'); assert not hasattr(AlpacaBroker.place_order, '__wrapped__'); assert not hasattr(AlpacaBroker.cancel_order, '__wrapped__')"` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDT + T+1 BLOCK checks (EXEC-11) wired into OrderGuard.place_order</name>
  <read_first>
    - src/gekko/brokers/alpaca.py (`get_account` returns dict from `_model_dump(acct)` at lines 137-145 — these dict keys are the source of truth for PDT + T+1 fields)
    - src/gekko/db/models.py (Event ORM — for the local 5-day round-trip walk; `event_type` includes `fill`)
    - src/gekko/execution/orderguard.py (place 02-02 ships — Task 2 inserts the PDT + T+1 calls AFTER qty_price_sanity)
    - src/gekko/execution/market_hours.py (pandas_market_calendars for business-day arithmetic — RESEARCH §4 says reuse this for the rolling 5-business-day window)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§4 in full — PDT field names verified via Alpaca docs `pattern_day_trader`, `daytrade_count`, `non_marginable_buying_power`, `equity`; the local 5-day rolling round-trip count walks `events` table for today's `fill` event_type rows; T+1 logic uses `non_marginable_buying_power` on cash accounts; `shorting_enabled` discriminates cash vs margin)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§1a row 8 _pdt.py + row 9 _t1.py; §3b Decimal coercion)
    - tests/unit/test_pdt_t1_detection.py (Wave-0 stub from plan 02-01)
  </read_first>
  <behavior>
    PDT block (EXEC-11):
    - Test (broker source): `account = {"pattern_day_trader": True, "daytrade_count": "3", "equity": "10000"}` + req would be 4th day-trade → `OrderGuardRejected("pdt_rule", reject_reason describes daytrade_count + equity)`
    - Test (broker source): same shape but equity=$30K → passes (above $25K threshold)
    - Test (broker source): `daytrade_count=2` + equity=$10K → passes (below the 3-count gate; the 4th day-trade is what triggers)
    - Test (broker source): `pattern_day_trader=False` regardless of daytrade_count → passes (not yet flagged)
    - Test (local source): seed audit log with 3 round-trips (BUY+SELL same ticker same day, repeated 3 times across 3 business days within the 5-business-day window); req would complete a 4th round-trip + equity<$25K → `OrderGuardRejected("pdt_rule_local", reject_reason names local count)`
    - Test (local source): same but equity=$30K → passes
    - Test (local source): 2 round-trips in window + req would complete a 3rd → passes
    - Test (business-day arithmetic): the rolling window uses `pandas_market_calendars.get_calendar('NYSE')`-based 5-business-day lookback (NOT calendar days); a 7-calendar-day-old round-trip on a holiday-adjacent date IS within the 5-business-day window

    T+1 block (EXEC-11):
    - Test (cash account): `account = {"shorting_enabled": False, "non_marginable_buying_power": "1000"}`; BUY req with qty × ref_price = $2000 → `OrderGuardRejected("t1_settlement", reject_reason names order_cost + non_marginable)`
    - Test (cash account): order_cost ≤ non_marginable → passes
    - Test (margin account): `shorting_enabled=True` → ALL BUYs pass regardless of non_marginable (margin advances credit per RESEARCH §4)
    - Test (SELL on cash): SELL side is ALWAYS exempt from T+1 (RESEARCH §4: "SELL doesn't have a T+1 constraint" — `if req.side.value != "buy": return`)
    - Test (ref_price selection): mirrors check_qty_price_sanity — LIMIT uses limit_price; STOP uses stop_price; MARKET uses last_quote from `account.get("last_quote_ask")` OR a fresh `broker.get_quote()` call (RESEARCH §4 leaves the choice; pick "reuse cached quote from check_qty_price_sanity OR refetch" — for Task 2 pick refetch via broker.get_quote(req.symbol) per RESEARCH §4 simpler illustrative path; the open question of pre-fetch-once is captured as a Phase-2 polish in RESEARCH §10 Open Question #3)

    Wiring (orderguard.py):
    - Test: `OrderGuard.place_order` calls check_pdt then check_t1_settlement AFTER check_qty_price_sanity AND BEFORE delegating to `_wrapped.place_order` (defense-in-depth check ordering)
    - Test: both checks receive the SAME `account` dict (one `broker.get_account()` call shared by both — RESEARCH §1 code shape `account = await self._wrapped.get_account()` followed by both checks)
  </behavior>
  <action>
    Replace WAVE-0 stub in `tests/unit/test_pdt_t1_detection.py` with the behaviors above. Mirror `tests/unit/test_market_hours.py` (PATTERNS §1c row "test_pdt_t1_detection.py" — same "binary block decision read from structured external source" shape).

    Create `src/gekko/execution/checks/_pdt.py` per RESEARCH §4 body + PATTERNS §1a row 8:
    - `async def check_pdt(*, req: OrderRequest, account: dict[str, Any], user_id: str) -> None`
    - Body parses `account` per RESEARCH §4 with `Decimal(str(...))` coercion (PATTERNS §3b — float ban)
    - Internal helper `async def _count_recent_round_trips(user_id: str, lookback_days: int) -> int` walks the `events` table per RESEARCH §4 stub; uses `pandas_market_calendars` for business-day arithmetic
    - Internal helper `_would_be_round_trip(req, user_id) -> bool` — checks if today's events table has a same-day opposite-side fill for the same ticker (BUY then SELL or SELL then BUY)
    - Uses module-level `_get_session_factory(user_id)` test seam (PATTERNS §3c)

    Create `src/gekko/execution/checks/_t1.py` per RESEARCH §4 body + PATTERNS §1a row 9:
    - `async def check_t1_settlement(*, req: OrderRequest, account: dict[str, Any], broker: Brokerage | None = None) -> None`
    - Body follows RESEARCH §4 exactly; ref_price selection branches on `req.order_type.value`; MARKET path calls `broker.get_quote(req.symbol)` if `account.get("last_quote_ask")` is absent
    - All money math via `Decimal(str(...))`

    Update `src/gekko/execution/checks/__init__.py` to export `check_pdt` and `check_t1_settlement`.

    Extend `src/gekko/execution/orderguard.py::place_order` per RESEARCH §1 code shape: add after the existing qty_price_sanity call:
    - `account = await self._wrapped.get_account()`
    - `await check_pdt(req=req, account=account, user_id=self._user_id)`
    - `await check_t1_settlement(req=req, account=account, broker=self._wrapped)`

    The single `account` fetch is shared by both checks (cost-saving per RESEARCH §10 Open Question #3 implicit recommendation). The `@retry_on_rate_limit` decorator added in Task 1 covers the get_account call.

    Float ban (PATTERNS §4 anti-pattern row 1): the float ban grep gate extended in plan 02-02 already walks `src/gekko/execution/checks/` — the new `_pdt.py` and `_t1.py` files inherit the gate. Test confirmation in the existing `test_float_banned_in_money_paths` run.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_pdt_t1_detection.py tests/unit/test_orderguard.py tests/unit/test_money_math.py -x -q</automated>
  </verify>
  <done>
    - All 8 PDT behaviors + 4 T+1 behaviors + 2 wiring tests pass
    - `python -c "from gekko.execution.checks import check_pdt, check_t1_settlement"` exits 0
    - `test_float_banned_in_money_paths` still green (no float regression)
    - Phase-1 walking-skeleton test still passes (the new PDT + T+1 checks pass for the existing test scenario which uses a happy-path account)
    - `grep -v '^#' src/gekko/execution/checks/_pdt.py src/gekko/execution/checks/_t1.py | grep -c claude_agent_sdk` returns 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wash-sale FLAG (EXEC-09) + ProposalWriter wiring (FLAG attaches to TradeProposal.wash_sale_flag)</name>
  <read_first>
    - src/gekko/agent/proposal_writer.py (Plan 01-07 — `ProposalWriter.write_proposal` is where the flag attaches per RESEARCH §5; the existing watchlist-violation handler at lines describing rollback + re-emission is the pattern; the dict-merge into TradeProposal pre-persistence is the seam)
    - src/gekko/schemas/proposal.py (TradeProposal.wash_sale_flag field added by plan 02-01 Task 3 — `dict[str, Any] | None = None`)
    - src/gekko/db/models.py (Event ORM — `select(Event).where(Event.user_id == ..., Event.event_type == 'fill', Event.ts >= window_start).order_by(Event.id.desc()).limit(100)` per RESEARCH §5)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§5 in full — wash-sale FLAG-only invariant, IRC §1091 30-day window, P2 simplification scope = same-ticker exact match; the flag dict shape `{"would_be_wash_sale": True, "lookback_event_id": ..., "lookback_date": ..., "ticker": ..., "lookback_qty": ..., "lookback_side": ..., "note": ...}`)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§1a row 10 _wash_sale.py — "Returns a dict-or-None flag (no exception)"; §4 anti-pattern row 12 "Wash-sale BLOCK" — tripwire test required)
    - tests/unit/test_wash_sale.py + tests/unit/test_wash_sale_flag.py (Wave-0 stubs from plan 02-01)
    - tests/unit/test_proposal_writer.py (Phase-1 — the 11 ProposalWriter behaviors; the new flag-attach test extends this pattern)
  </read_first>
  <behavior>
    Wash-sale FLAG (EXEC-09):
    - Test: seed `Event` table with `event_type='fill'`, `ticker='AAPL'`, `ts` = 15 days ago; call `flag_wash_sale(req=BUY AAPL, user_id="chris")` → returns dict with `would_be_wash_sale=True`, `lookback_event_id=<id>`, `lookback_date=<iso>`, `ticker="AAPL"`, `lookback_side="buy"` (or "sell" — whichever was seeded)
    - Test: seed `Event` 31 days ago → `flag_wash_sale` returns None (outside the 30-day IRC §1091 window)
    - Test: seed `Event` 0 days ago (today) → returns dict (within window)
    - Test: different ticker `Event` rows within window → returns None (P2 simplification: same-ticker exact match per RESEARCH §5)
    - Test: 100+ fill events but only one matches → returns the matching dict (bounded scan of last 100 events per RESEARCH §5)
    - Test: NEVER raises an exception — `flag_wash_sale` returns None or dict; the FLAG-only invariant per PATTERNS §4 anti-pattern row 12

    ProposalWriter wiring (D-28):
    - Test: ProposalWriter.write_proposal calls `flag_wash_sale(req=order_request_built_from_llm_args, user_id=user_id)` after validating the TradeProposal but before the audit-event append
    - Test: when `flag_wash_sale` returns a dict, the dict is attached to `TradeProposal.wash_sale_flag` (the Pydantic field added in plan 02-01); the proposal row's `payload_json` contains the flag
    - Test: when `flag_wash_sale` returns None, `TradeProposal.wash_sale_flag` stays None
    - Test: the flag attach happens INSIDE the same transaction as the proposal_id insert (audit chain integrity — single canonical TradeProposal model_dump)

    OrderGuard invariant (EXEC-09 / D-29):
    - Test: `OrderGuard.place_order` does NOT call `flag_wash_sale` (wash-sale is NOT re-checked at execute time per D-29 BLOCK list and the deferred FLAG list)
    - Test: a proposal with `wash_sale_flag` set still passes through OrderGuard.place_order successfully (the flag is informational, not blocking — confirmed by reading the existing place_order body and asserting `flag_wash_sale` is NOT in the call site)
  </behavior>
  <action>
    Replace WAVE-0 stubs in `tests/unit/test_wash_sale.py` and `tests/unit/test_wash_sale_flag.py` with the behaviors above. Mirror `tests/unit/test_proposal_writer.py` for the ProposalWriter integration test pattern.

    Create `src/gekko/execution/checks/_wash_sale.py` per RESEARCH §5 body + PATTERNS §1a row 10:
    - `async def flag_wash_sale(*, req: OrderRequest, user_id: str) -> dict[str, Any] | None`
    - Body per RESEARCH §5: open per-user SQLCipher engine via `_get_session_factory(user_id)`; `SELECT * FROM events WHERE user_id=:u AND event_type='fill' AND ts >= :window_start ORDER BY id DESC LIMIT 100`; walk results parsing `json.loads(row.payload_json)`; return dict on first same-ticker match; return None otherwise
    - The returned dict shape is exactly `{"would_be_wash_sale": True, "lookback_event_id": row.id, "lookback_date": row.ts, "ticker": req.symbol, "lookback_qty": payload.get("filled_qty"), "lookback_side": payload.get("side", "unknown"), "note": <human-readable explainer string from RESEARCH §5>}`
    - NEVER raises an exception (the function signature `dict | None` is the contract per PATTERNS §4 anti-pattern row 12)

    Extend `src/gekko/agent/proposal_writer.py` per D-28 / RESEARCH §5 "Integration with HITL Card":
    - In `ProposalWriter.write_proposal`, after the TradeProposal is validated but BEFORE the proposal row insert + audit event append, call `flag = await flag_wash_sale(req=req, user_id=user_id)` where `req` is constructed from the LLM's tool args (or an `OrderRequest` shape that wash_sale needs)
    - Set `tp.wash_sale_flag = flag` (the field exists from plan 02-01 with `extra="ignore"` ConfigDict — the field assignment works post-validation because `frozen=False`)
    - The flag is part of the persisted `payload_json` (via the standard TradeProposal.model_dump path) — audit chain canonical-subset shape is unchanged (forward-additive per RESEARCH §10 Open Question #5)

    Update `src/gekko/execution/checks/__init__.py` to export `flag_wash_sale`.

    Add tripwire test in `tests/unit/test_wash_sale_flag.py::test_wash_sale_never_raises` per PATTERNS §4 anti-pattern row 12.

    DO NOT add `flag_wash_sale` to `OrderGuard.place_order` (PATTERNS §4 anti-pattern row 12 + EXEC-09 explicit "agent does NOT block").
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_wash_sale.py tests/unit/test_wash_sale_flag.py tests/unit/test_proposal_writer.py tests/unit/test_orderguard.py -x -q</automated>
  </verify>
  <done>
    - All 5 wash-sale FLAG behaviors + 4 ProposalWriter wiring behaviors + 2 OrderGuard invariant tests pass
    - `python -c "from gekko.execution.checks import flag_wash_sale; import inspect; sig = inspect.signature(flag_wash_sale); ret = sig.return_annotation; assert 'dict' in str(ret) and 'None' in str(ret)"` exits 0 (signature is `dict | None`)
    - Phase-1 walking-skeleton + ProposalWriter unit tests still green (no regression)
    - `grep -nE '@retry|raise OrderGuardRejected' src/gekko/execution/checks/_wash_sale.py` returns 0 (no retry decorator, no exception raise)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker GET ↔ rate limit (429) | Tenacity retries here; never on POSTs |
| `place_order` (POST) ↔ retry policy | NEVER retried — Knight Capital |
| Alpaca account fields (PDT flag, daytrade_count, non_marginable_buying_power) | Trust the broker's view but defense-in-depth with local audit-log walk |
| 30-day wash-sale window vs trade decision | FLAG only; user owns the tax decision |
| AST gate (parsed source tree) ↔ EXEC-03 invariant | Source-tree inspection is more robust than text-grep (WARNING #2 fix) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-M-01 | Tampering (CRITICAL — Knight Capital) | `AlpacaBroker.place_order` retry-decorator slip | mitigate | Task 1 AST-walk gate (WARNING #2 fix) inspects parsed source tree AND introspects `__wrapped__` attribute; two independent assertions. PATTERNS §4 anti-pattern row 3 names this as the highest-severity tripwire. CI fails if either assertion regresses. The AST gate catches `# decorator-commented-out` tricks the previous text-grep missed. |
| T-02-03-M-02 | Tampering | `OrderGuard.place_order` retry-decorator slip via future plan | mitigate | Task 1 AST gate extends to `OrderGuard.place_order` — same parsed-tree inspection. The gate also confirms no `@retry`, no `@retry_on_rate_limit`, no `tenacity` import substrings in the orderguard module via secondary text-grep (defense in depth). |
| T-02-03-M-03 | Tampering | `AlpacaBroker.cancel_order` retry-decorator added in error | mitigate | RESEARCH §6 Open Question #1 explicit decision is no-retry; Task 1 AST gate asserts `cancel_order.decorator_list == []`. Plan 02-05's kill switch depends on this — cancel calls fail fast in 4s timeout window. |
| T-02-03-M-04 | Tampering (regulatory) | PDT 5-day round-trip count could be undercounted (broker's daytrade_count lags) | mitigate | Two-source defense per RESEARCH §4: broker's `pattern_day_trader` + `daytrade_count` is primary; local audit-log walk over 5 business days is defense in depth. Either trigger results in BLOCK. Tests cover both sources independently. |
| T-02-03-M-05 | Tampering (regulatory) | T+1 settlement violation on cash account | mitigate | `check_t1_settlement` uses `account.non_marginable_buying_power` (Alpaca-verified field per RESEARCH §4); BUY cost > non_marginable on a cash account hard-rejects. Tests cover cash vs margin (`shorting_enabled` discriminator). |
| T-02-03-R-01 | Repudiation (regulatory) | User claims agent caused wash-sale violation | mitigate | EXEC-09 FLAG only: agent surfaces the warning via `wash_sale_flag` on TradeProposal; user clicks Approve with full disclosure (HITL card shows the warning per D-28). Audit log persists the flag dict in the proposal payload — full traceability. |
| T-02-03-O-01 | Operational | Rate-limit retries thrash on cascade 429s | accept | `wait_random_exponential(min=1, max=60)` + `stop_after_attempt(6)` = max 5 retries spread over ~3-5 minutes. If Alpaca is sustained-429, OrderGuard fails closed (the get_account / get_quote call raises after 6 attempts, OrderGuardRejected fires on the qty_price or PDT check that can't compute), which is the safe state. |
| T-02-03-I-01 | Information Disclosure | wash_sale_flag dict could include sensitive fields | mitigate | RESEARCH §5 dict shape is locked: `lookback_event_id` (audit-row PK — internal), `lookback_date` (ISO timestamp), `ticker`, `lookback_qty`, `lookback_side`, `note`. NO credentials, NO API keys. The structlog `_redact` processor catches anything else by regex (Plan 01-02 D-25). |
| T-02-03-M-06 | Tampering | wash-sale check raised as exception instead of flag (BLOCK creep) | mitigate | Task 3 tripwire test `test_wash_sale_never_raises` directly imports the function and asserts via `try: result = await flag_wash_sale(...); except Exception: pytest.fail()` for every test case. PATTERNS §4 anti-pattern row 12 documents the explicit invariant. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_alpaca_retry.py tests/unit/test_rate_limit_backoff.py tests/unit/test_pdt_t1_detection.py tests/unit/test_wash_sale.py tests/unit/test_wash_sale_flag.py -x -q` — all pass
- `uv run pytest tests/integration/test_trigger_run_end_to_end.py -x -q` — Phase-1 walking-skeleton still green
- `python -c "from gekko.execution.checks import check_pdt, check_t1_settlement, flag_wash_sale"` exits 0
- `python -c "from gekko.brokers.alpaca import AlpacaBroker; assert hasattr(AlpacaBroker.get_account, '__wrapped__'); assert not hasattr(AlpacaBroker.place_order, '__wrapped__'); assert not hasattr(AlpacaBroker.cancel_order, '__wrapped__')"` exits 0
- AST gate test (WARNING #2): `uv run pytest tests/unit/test_alpaca_retry.py::test_place_order_ast_gate_no_decorators -x -q` passes
- `grep -v '^#' src/gekko/execution/orderguard.py | grep -cE 'tenacity|retry_on_rate_limit'` returns 0 (defense in depth — OrderGuard module never imports tenacity)
</verification>

<success_criteria>
- Tenacity retry decorator is applied to broker GETs (4 methods) — EXEC-08 satisfied
- `place_order` + `cancel_order` are NOT retry-decorated; AST-walk gate enforces (WARNING #2 fix) — EXEC-03 / Knight Capital prevention reinforced
- PDT check fires on both broker-flag and local-audit-walk paths — EXEC-11 BLOCK satisfied for PDT
- T+1 check fires on cash-account BUYs exceeding settled cash — EXEC-11 BLOCK satisfied for T+1
- Wash-sale FLAG attaches to TradeProposal.wash_sale_flag at proposal-build time; never blocks — EXEC-09 satisfied
- No regression on Phase-1 walking skeleton; no regression on Phase-1 unit tests
- Plan sequenced after 02-02 (BLOCKER #2) — no file-overlap merge conflicts with the OrderGuard-skeleton plan
</success_criteria>

<output>
Create `.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-03-SUMMARY.md` when done. Include: the tenacity decorator shape used, exact list of GET methods decorated + list of methods NOT decorated, AST-walk gate implementation (file paths + node assertions per WARNING #2), PDT detection two-source logic confirmation, T+1 cash-vs-margin discriminator field name, wash-sale flag dict shape lock, confirmation that EXEC-03 AST gate covers both AlpacaBroker.place_order AND OrderGuard.place_order, walking-skeleton test still green.
</output>
