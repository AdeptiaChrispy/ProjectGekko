---
phase: 02-orderguard-real-money-alpaca-live-safety-floor
plan: 06
type: execute
wave: 5
depends_on: ["02-02", "02-05"]
files_modified:
  - src/gekko/vault/credentials.py
  - src/gekko/strategy/__init__.py
  - src/gekko/strategy/promotion.py
  - src/gekko/cli.py
  - src/gekko/brokers/alpaca.py
  - src/gekko/execution/executor.py
  - src/gekko/agent/proposal_writer.py
  - src/gekko/approval/proposals.py
  - src/gekko/approval/slack_handler.py
  - src/gekko/dashboard/routes.py
  - src/gekko/dashboard/templates/base.html.j2
  - src/gekko/dashboard/templates/live_banner.html.j2
  - src/gekko/dashboard/templates/first_live_confirm.html.j2
  - src/gekko/dashboard/templates/live_confirm_success.html.j2
  - src/gekko/dashboard/templates/strategies_list.html.j2
  - src/gekko/dashboard/templates/strategy_edit.html.j2
  - src/gekko/reporter/slack.py
  - src/gekko/reporter/templates.py
  - tests/unit/test_proposal_writer_account_mode_stamp.py
  - tests/integration/test_alpaca_live_credentials.py
  - tests/integration/test_first_live_gate.py
  - tests/unit/test_live_confirm_idempotent.py
  - tests/integration/test_promote_paper_to_live.py
autonomous: true
requirements:
  - BROK-A-02
  - HITL-06

user_setup:
  - service: alpaca
    why: "Live trading requires a separate Alpaca live API key + secret (different from paper). Phase 2 stores these encrypted in the SQLCipher vault."
    env_vars: []
    dashboard_config:
      - task: "Sign up for / activate an Alpaca brokerage account (live trading enabled)"
        location: "https://alpaca.markets/"
      - task: "Generate live API key + secret"
        location: "Alpaca Dashboard → Live Trading → API Keys"
      - task: "Run `gekko credentials add alpaca-live` (Task 1 in this plan) and paste the live key + secret when prompted"
        location: "Local CLI on operator's machine"

must_haves:
  truths:
    - "Live API key + secret stored encrypted at rest in the SQLCipher vault (BrokerCredential.kind='alpaca_live'); NEVER in .env per D-34"
    - "`gekko credentials add alpaca-live` CLI writes the encrypted credential row + emits an audit event (D-34)"
    - "Strategy promotion to live (`gekko strategy promote-live <name>` + dashboard `POST /strategies/{name}/promote-to-live`) requires typed-strategy-name confirmation (D-31)"
    - "First-live trade per strategy requires AWAITING_2ND_CHANNEL → APPROVED_LIVE transition; Slack DM has the URL-button only (no inline Approve), dashboard form has typed acknowledgement checkboxes + 5s read timer (D-32 + UI-SPEC §3)"
    - "Once `strategy_metadata.first_live_trade_confirmed_at` is set on first successful FILL, subsequent trades on that strategy skip the dual-channel gate (D-32 per-strategy semantics)"
    - "OrderGuard `check_paper_live_pairing` enforces three-way invariant `strategy.mode ⇔ account_mode ⇔ broker.is_paper AND BrokerCredential.kind` (EXEC-05 invariant extension)"
    - "Live banner + [LIVE] chips render based on `request.state.banner_mode` (D-33) — operator sees red on every dashboard page when any live strategy is eligible"
    - "AlpacaBroker constructor lifts `paper=False` guard ONLY when called from `_build_broker` with a valid live-credentials path (`strategy.live_mode_eligible=True`)"
  artifacts:
    - path: src/gekko/vault/credentials.py
      provides: "store_live_credentials / load_live_credentials — encrypted SQLCipher vault access"
      contains: "def store_live_credentials"
    - path: src/gekko/strategy/promotion.py
      provides: "promote_strategy_to_live / first_live_trade_stamp helpers"
      contains: "def promote_strategy_to_live"
    - path: src/gekko/dashboard/templates/first_live_confirm.html.j2
      provides: "Full-page dashboard confirmation with checkboxes + 5s timer"
      min_lines: 30
    - path: src/gekko/dashboard/templates/live_banner.html.j2
      provides: "Persistent red banner partial"
      min_lines: 5
  key_links:
    - from: "src/gekko/execution/executor.py::_build_broker"
      to: "src/gekko/vault/credentials.py::load_live_credentials"
      via: "conditional live path"
      pattern: "if strategy.mode == \"live\" and .*live_mode_eligible"
    - from: "src/gekko/approval/slack_handler.py::_approve_workflow"
      to: "AWAITING_2ND_CHANNEL transition"
      via: "first-live-trade branch"
      pattern: "AWAITING_2ND_CHANNEL"
    - from: "src/gekko/dashboard/routes.py::live_confirm"
      to: "APPROVED_LIVE transition + executor dispatch"
      via: "POST /live-confirm/{proposal_id}"
      pattern: "transition_status.*APPROVED_LIVE"
    - from: "src/gekko/execution/executor.py::on_fill_event"
      to: "strategy_metadata.first_live_trade_confirmed_at"
      via: "stamp on first live fill"
      pattern: "first_live_trade_confirmed_at"
---

<objective>
Ship the live-mode unlock end-to-end: vault-encrypted live credentials (BROK-A-02 / D-34), strategy promotion CLI + dashboard surfaces (D-31), HITL-06 dual-channel state machine (D-32), live-banner + chip visuals (D-33), three-way paper/live invariant tightening (EXEC-05 with kind column), and the on-fill stamp that closes the gate per-strategy.

Per D-32 "first live trade per strategy" semantics: the dual-channel gate fires once. After `first_live_trade_confirmed_at` is stamped on a successful FILL, subsequent live trades on the same strategy use the normal Slack-approve → APPROVED → EXECUTING path.

Per D-34 invariant: live API keys NEVER touch `.env`. The vault is the single source of truth, gated by the SQLCipher passphrase that's already cached at process startup (Phase-1 Plan 01-09 D-19).

Out of scope (P3): production HITL UX hardening (idempotent button DBs, quiet hours, timeout=REJECT, edit-size, dashboard fallback). Plan 02-06 ships the dual-channel gate; P3 productionizes the surrounding HITL flow.

Output: 1 new vault module + 1 new strategy module + 1 new CLI command (`credentials add alpaca-live`) + 2 new dashboard routes (`/strategies/{name}/promote-to-live`, `/live-confirm/{proposal_id}`) + 1 new Slack approve-branch (AWAITING_2ND_CHANNEL) + 3 new templates + Live banner wiring + LiveBanner chip on strategies list + AlpacaBroker constructor lift + on_fill_event stamp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-CONTEXT.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-UI-SPEC.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-02-SUMMARY.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-05-SUMMARY.md
@src/gekko/brokers/alpaca.py
@src/gekko/execution/executor.py
@src/gekko/approval/proposals.py
@src/gekko/approval/slack_handler.py
@src/gekko/dashboard/routes.py
@src/gekko/dashboard/templates/base.html.j2
@src/gekko/dashboard/templates/strategies_list.html.j2
@src/gekko/dashboard/templates/strategy_edit.html.j2
@src/gekko/dashboard/templates/user_agreement.html.j2
@src/gekko/db/models.py
@src/gekko/vault/passphrase.py
@src/gekko/reporter/slack.py
@src/gekko/reporter/templates.py
@src/gekko/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vault-encrypted live credentials + `gekko credentials add alpaca-live` CLI + AlpacaBroker constructor lift + paper/live pairing check extension</name>
  <read_first>
    - src/gekko/vault/passphrase.py (the existing process-wide passphrase singleton — D-19; the credentials vault uses the SAME passphrase to unlock the same SQLCipher DB)
    - src/gekko/db/models.py (BrokerCredential ORM with new `kind` column from plan 02-01; composite PK is now `(user_id, broker, kind)`; `__repr__` excludes `key_blob` + `secret_blob` per Phase-1 D-?)
    - src/gekko/brokers/alpaca.py (constructor at lines 85-119 — two-layer paper-only guard from Phase 1 D-24; PATTERNS §2 row "src/gekko/brokers/alpaca.py" — lift `paper=False` ONLY when called from `_build_broker` with a valid live path)
    - src/gekko/execution/executor.py (`_build_broker` from plan 02-02 — extends to read vault credentials on the live path; PATTERNS §2 row "src/gekko/execution/executor.py" item 1)
    - src/gekko/execution/checks/_paper_live.py (the three-way invariant from plan 02-02 — Task 1 extends with `BrokerCredential.kind` validation per EXEC-05 D-34 invariant)
    - src/gekko/cli.py (Plan 01-09 Typer app — pattern for the new `credentials` subcommand)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§2 in full — BrokerCredential.kind column + composite PK; "_build_broker" modification at lines 449-462 verbatim; EXEC-05 three-way invariant)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§4 anti-pattern row 4 — paper=False slipping through; row 9 — plaintext credentials in __repr__; §2 rows for executor.py + alpaca.py + reporter/slack.py)
    - tests/integration/test_alpaca_live_credentials.py (Wave-0 stub from plan 02-01)
  </read_first>
  <behavior>
    Vault store + load (D-34 / BROK-A-02):
    - Test: `store_live_credentials(user_id, api_key, secret_key)` writes a BrokerCredential row with `broker="alpaca"`, `kind="alpaca_live"`, `paper=False`, `key_blob=api_key` (PLAINTEXT string verbatim), `secret_blob=secret_key` (PLAINTEXT string verbatim). **Encryption model (per Phase-1 D-19 / AUTH-03 — verified against `src/gekko/db/models.py:325-353` BrokerCredential schema):** the blob columns store the PLAINTEXT API key + secret bytes-for-bytes. Data-at-rest protection is provided entirely by SQLCipher whole-DB encryption (the `.db` file on disk is encrypted; SQLAlchemy reads/writes plaintext through the SQLCipher driver when given the correct passphrase). Threat model: anyone with the SQLCipher passphrase reads these blobs by design. Phase 2 does NOT add an application-layer Fernet wrap on top of SQLCipher — that defense-in-depth layer is deferred to a future security-hardening phase if the threat model warrants it (logged in deferred-items).
    - Test: `load_live_credentials(user_id) -> (api_key, secret_key)` returns the plaintext strings byte-for-byte identical to what was passed to `store_live_credentials` (no application-layer decryption step — SQLCipher handles transparently at the file boundary)
    - Test: BrokerCredential `__repr__` does NOT include `key_blob` or `secret_blob` (Phase-1 invariant preserved with the new `kind` column)
    - Test: writing duplicate `(user_id, "alpaca", "alpaca_live")` rows fails with IntegrityError (composite PK enforced)
    - Test: storing a paper credential coexists with a live credential for the same user (the new composite PK allows both)
    - Test (audit): `store_live_credentials` emits a `credentials_added` event (or uses an existing event_type that fits — read `_EVENT_TYPES` tuple; if no fitting type exists, write a `error` event with structured payload describing the addition, or extend `_EVENT_TYPES` minimally if Phase-1 already includes `credentials_added`)

    CLI `gekko credentials add alpaca-live`:
    - Test (interactive): running `gekko credentials add alpaca-live` prompts for "Alpaca live API key:" + "Alpaca live secret key:" via `typer.prompt(..., hide_input=True)` (NEVER prints to terminal echo)
    - Test (passphrase): the CLI uses the existing `gekko.vault.passphrase.get_passphrase()` to unlock the DB — if not set, prompts the operator for it first per RESEARCH §10 Open Question #6 (default: assume cache; if not cached, prompt via getpass)
    - Test (success): writes the credential row + prints "Live credentials stored for user {user_id}. Use `gekko strategy promote-live <name>` to enable a strategy for live trading."
    - Test (error): if the CLI is run on a fresh DB (no User row), prints a clear error pointing at `gekko init`

    AlpacaBroker constructor lift (D-34 / EXEC-05):
    - Test: `AlpacaBroker(api_key="key", secret_key="sec", paper=False)` STILL raises `BrokerConfigError` by default (Phase-1 guard) UNLESS called via the vetted `_build_broker` path
    - Test (vetted path): `_build_broker(user_id, strategy=<live-eligible strategy>, account_mode="LIVE")` constructs `AlpacaBroker(paper=False, api_key=<vault-loaded>, secret_key=<vault-loaded>)` successfully (the guard is conditional now — accept paper=False when explicitly opted into via a documented kwarg or via the executor's load path)
    - Implementation choice: add a `_allow_live: bool = False` kwarg to AlpacaBroker that defaults False; `_build_broker` sets it True on the live path. The kwarg is NOT documented in user-facing API; it's an internal opt-in to prevent accidental bypass (PATTERNS §2 row "src/gekko/brokers/alpaca.py" "Lift the constructor `paper=False` guard at lines 85-95 — accept a non-paper variant when called with explicit `paper=False` from `_build_broker` AND the live-credentials path is taken").

    Three-way invariant extension (EXEC-05 / D-34):
    - Test: `check_paper_live_pairing` now ALSO validates that the credential `kind` matches: `strategy.mode="live"` requires a vault row with `kind="alpaca_live"` to have been loaded by `_build_broker`. Mismatch → `OrderGuardRejected("paper_live_mismatch_credential", ...)`. (This is a NEW reject_code on top of plan 02-02's `paper_live_mismatch_broker` + `paper_live_mismatch_account`.)
    - Test: the check_paper_live_pairing function accepts a new kwarg `credential_kind: str | None = None`; when None, the check is the same as plan 02-02; when set, the function additionally enforces credential_kind ⇔ strategy_mode
    - Test: a live strategy with paper credential row (e.g., operator forgot to run `gekko credentials add alpaca-live`) hard-rejects at OrderGuard with `paper_live_mismatch_credential`

    Logs hygiene (PATTERNS §4 anti-pattern row 9):
    - Test: `_redact` processor scrubs any `api_key` / `secret_key` / `live_api_key` / `alpaca_live` strings from log records (extending Plan 01-02 D-25 patterns)
    - Test: structured log lines from `store_live_credentials` use keyword args (NEVER f-strings) — `log.info("credentials.added", user_id=user_id, kind="alpaca_live")` (NEVER `log.info(f"added key={api_key}")`)
  </behavior>
  <action>
    Replace WAVE-0 stub in `tests/integration/test_alpaca_live_credentials.py` with the behaviors above.

    Create `src/gekko/vault/credentials.py`:
    - `async def store_live_credentials(*, user_id: str, api_key: str, secret_key: str) -> None` — opens per-user SQLCipher engine via `_get_session_factory(user_id)`; INSERT a BrokerCredential row with `(broker="alpaca", kind="alpaca_live", paper=False, key_blob=api_key, secret_blob=secret_key, created_at=<iso>)`. The SQLCipher whole-DB encryption transparently encrypts the row at rest per Phase-1 AUTH-03. Emit an audit event documenting the credential addition (event_type to be confirmed against `_EVENT_TYPES`).
    - `async def load_live_credentials(user_id: str) -> tuple[str, str] | None` — SELECT BrokerCredential row WHERE (user_id, "alpaca", "alpaca_live"); return `(key_blob, secret_blob)` if present (decrypted transparently by SQLCipher); else None.

    Per PATTERNS §3c — both functions use the module-local `_get_session_factory(user_id)` shim with `finally: if engine is not None: await engine.dispose()`.

    Add Typer subcommand to `src/gekko/cli.py`:
    - `credentials = typer.Typer(help="Manage broker credentials in the SQLCipher vault.")` sub-app
    - `app.add_typer(credentials, name="credentials")`
    - `@credentials.command("add-alpaca-live")` — Typer command body prompts for `api_key = typer.prompt("Alpaca live API key", hide_input=True)`; `secret = typer.prompt("Alpaca live secret key", hide_input=True)`. Reads settings.gekko_user_id. Reads passphrase via `gekko.vault.passphrase.get_passphrase()` (or prompts via `gekko.vault.passphrase.prompt_passphrase()` if not cached). Calls `anyio.run(store_live_credentials, user_id=..., api_key=..., secret_key=...)`. On success prints the confirmation message.
    - Naming: per UI-SPEC §"Copywriting Contract" — "Live credentials stored for user {user_id}. ..." (banking-grade neutral tone)

    Extend `src/gekko/brokers/alpaca.py` constructor per PATTERNS §2 row "src/gekko/brokers/alpaca.py":
    - Add `_allow_live: bool = False` kwarg at the end of `__init__` signature
    - Replace the `if not paper: raise BrokerConfigError(...)` at lines 85-95 with `if not paper and not _allow_live: raise BrokerConfigError("Live mode requires explicit live-credentials path via _build_broker; do not construct AlpacaBroker(paper=False) directly.")`. The two-layer post-construct probe at lines 103-119 (which verifies `_base_url` actually points at the paper endpoint) stays UNCHANGED — and adds a symmetric check that when `_allow_live=True`, the base_url MUST be the live endpoint.

    Extend `src/gekko/execution/executor.py::_build_broker` per RESEARCH §1 modification body lines 449-462 — **note: `is_live` derives from the LOCKED proposal row's `account_mode`, NOT from current strategy state. This closes the TOCTOU window per BLOCKER #5 (plan-checker iteration 2). The executor MUST pass `proposal.account_mode` into `_build_broker`; do not re-read `strategy.mode` or `strategy_metadata.live_mode_eligible` at execute-time.**
    ```
    async def _build_broker(user_id: str, strategy: Strategy, account_mode: str) -> Brokerage:
        settings = get_settings()
        # is_live is derived from the LOCKED proposal row's account_mode, passed in by the caller.
        # Re-reading strategy.mode or strategy_metadata.live_mode_eligible at execute-time would
        # reopen the TOCTOU window between proposal-build (T0) and execute (T1).
        is_live = account_mode == "LIVE"
        if is_live:
            creds = await load_live_credentials(user_id)
            if creds is None:
                raise OrderGuardRejected("paper_live_mismatch_credential", f"Strategy {strategy.name} is live but no alpaca_live credentials in vault. Run `gekko credentials add alpaca-live`.")
            api_key, secret_key = creds
            wrapped = AlpacaBroker(api_key=api_key, secret_key=secret_key, paper=False, _allow_live=True)
            credential_kind = "alpaca_live"
        else:
            wrapped = AlpacaBroker(api_key=settings.alpaca_paper_api_key.get_secret_value(), secret_key=settings.alpaca_paper_secret_key.get_secret_value(), paper=True)
            credential_kind = "alpaca_paper"
        return OrderGuard(wrapped, strategy=strategy, account_mode=account_mode, user_id=user_id, credential_kind=credential_kind)
    ```

    Extend `src/gekko/agent/proposal_writer.py::ProposalWriter` to stamp `account_mode` at proposal-build time (closes BLOCKER #5 TOCTOU window per plan-checker iteration 2):
    - Read the Strategy + StrategyMetadata rows in the same transaction that creates the proposal row
    - Stamp `tp.account_mode = "LIVE" if (strategy.mode == "live" and strategy_metadata is not None and strategy_metadata.live_mode_eligible) else "PAPER"`
    - The stamp is final — never re-read or overwritten downstream; `_runtime_only` tuple already includes `account_mode` (per 02-01 Task 3 line 260) so the LLM cannot author it
    - Test (`tests/unit/test_proposal_writer_account_mode_stamp.py` — Wave-0 stub from 02-01 Task 2):
      - Test: paper strategy (`strategy.mode="paper"`) → `tp.account_mode == "PAPER"` regardless of metadata
      - Test: live-eligible strategy (`strategy.mode="live"` AND `strategy_metadata.live_mode_eligible=True`) → `tp.account_mode == "LIVE"`
      - Test: live-but-not-eligible (`strategy.mode="live"` AND `strategy_metadata` missing OR `live_mode_eligible=False`) → `tp.account_mode == "PAPER"` (defensive default)
      - Test: TOCTOU defense — promote strategy to live AFTER ProposalWriter has stamped a paper proposal; the proposal row's account_mode remains "PAPER"; demoting AFTER stamping a live proposal keeps the proposal's "LIVE" stamp

    Extend `src/gekko/execution/orderguard.py::OrderGuard.__init__` to accept `credential_kind: str` and pass it through to `check_paper_live_pairing`.

    Extend `src/gekko/execution/checks/_paper_live.py::check_paper_live_pairing` to accept `credential_kind: str | None = None` kwarg; when provided, enforce the additional invariant `credential_kind == "alpaca_live" ⇔ strategy_mode == "live"`. Mismatch raises `OrderGuardRejected("paper_live_mismatch_credential", ...)`.

    Per PATTERNS §4 anti-pattern row 9: structlog `_redact` processor (Plan 01-02 D-25) must continue to catch live API keys. Verify by extending the existing redaction test (`tests/unit/test_logging_redaction.py` or similar) with an explicit alpaca_live key shape.

    NEVER write the plaintext API key into any log record. NEVER include it in `__repr__`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_alpaca_live_credentials.py tests/unit/test_orderguard_paper_live.py tests/unit/test_orderguard.py -x -q</automated>
  </verify>
  <done>
    - All vault store/load + CLI + constructor lift + three-way invariant extension + log redaction tests pass
    - `gekko credentials add-alpaca-live --help` shows the command (CLI registration confirmed)
    - `python -c "from gekko.vault.credentials import store_live_credentials, load_live_credentials"` exits 0
    - `python -c "from gekko.brokers.alpaca import AlpacaBroker; b = AlpacaBroker(api_key='a', secret_key='b', paper=True); assert b.is_paper"` exits 0 (paper path unchanged)
    - `python -c "from gekko.brokers.alpaca import AlpacaBroker, BrokerConfigError; b = AlpacaBroker(api_key='a', secret_key='b', paper=False)" 2>&amp;1 | grep -c BrokerConfigError` returns 1 (raw paper=False still blocked)
    - Phase-1 walking-skeleton + 22-event chain still validates
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strategy promotion CLI + dashboard route + state-machine extensions (D-31 + D-32 — AWAITING_2ND_CHANNEL / APPROVED_LIVE wiring)</name>
  <read_first>
    - src/gekko/db/models.py (StrategyMetadata ORM added by plan 02-01; `_PROPOSAL_STATUSES` tuple now includes `AWAITING_2ND_CHANNEL` + `APPROVED_LIVE`)
    - src/gekko/approval/proposals.py (STATE_TRANSITIONS frozenset at lines 51-60; `transition_status` body at lines 68-119 is data-driven — Phase 2 EXTENDS the frozenset literal, body unchanged per PATTERNS §3e)
    - src/gekko/approval/slack_handler.py (`_approve_workflow` at lines 116-180 — the dual-channel branch lands inside the cross-user check at line 135 and the row-load at line 151; PATTERNS §2 row "src/gekko/approval/slack_handler.py")
    - src/gekko/dashboard/routes.py (form-based POST + HTMX partial return pattern from `strategy_save`)
    - src/gekko/cli.py (existing `strategy create` subcommand — model for the new `strategy promote-live`)
    - src/gekko/execution/executor.py (`on_fill_event` at lines 334-432 — Phase 2 adds the `first_live_trade_confirmed_at` stamp per RESEARCH §7 lines 1390-1406)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§7 in full — state machine extension at lines 1241-1259; Slack approve handler dual-channel branch at lines 1267-1327; dashboard /live-confirm/{proposal_id} route at lines 1333-1385; on_fill_event stamp at lines 1390-1406; idempotency layers at lines 1410-1415)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-UI-SPEC.md (Surface 3 — dual-channel confirmation; first_live_confirm.html.j2 layout at §3b)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§3e state-machine pattern; §2 rows for slack_handler.py + routes.py)
    - tests/integration/test_first_live_gate.py + tests/unit/test_live_confirm_idempotent.py + tests/integration/test_promote_paper_to_live.py (Wave-0 stubs)
  </read_first>
  <behavior>
    Strategy promotion (D-31):
    - Test (CLI): `gekko strategy promote-live <name>` prompts "Type the strategy name to confirm:" — only proceeds when input EXACTLY matches the strategy name. UI-SPEC §"Destructive Action Confirmations" — typed strategy-name match.
    - Test (CLI): on confirm, sets `strategy_metadata.live_mode_eligible=True` + `strategy_metadata.live_promoted_at=<iso>` (UPSERT shape via SQLAlchemy `on_conflict_do_update`); writes a `strategy_promoted` audit event (or `error` with structured payload if event_type not in tuple); DMs the operator (UI-SPEC §"Success States" copy)
    - Test (CLI): wrong typed name → exit 1 + "Aborted — typed confirmation did not match"
    - Test (dashboard): `POST /strategies/{name}/promote-to-live` with `Form(strategy_name_confirm=...)` — only proceeds when value matches `{name}`; symmetric error on mismatch
    - Test (Slack does NOT have a promotion command — D-31 deliberate friction): `/gekko promote-live` is NOT registered as a subcommand (the bot replies with the help text listing only `run` / `kill` / `unkill`)

    State machine (D-32 / PATTERNS §3e):
    - Test: `STATE_TRANSITIONS` frozenset NOW contains 5 new tuples added by plan 02-01 + this plan's wiring: `("APPROVED", "AWAITING_2ND_CHANNEL")`, `("AWAITING_2ND_CHANNEL", "APPROVED_LIVE")`, `("AWAITING_2ND_CHANNEL", "REJECTED")`, `("APPROVED_LIVE", "EXECUTING")`, `("APPROVED_LIVE", "FAILED")` — Phase 2 plan 02-01 added the frozenset entries to the source; this plan adds the WIRING that triggers them
    - Test: `transition_status` body at lines 68-119 is UNCHANGED (PATTERNS §3e + §4 anti-pattern row 10 — body is data-driven; do not refactor)
    - Test: `transition_status` is idempotent on same-state — calling it twice on `APPROVED_LIVE → APPROVED_LIVE` returns the row unchanged (Phase-1 invariant at lines 99-100 preserved)

    Slack approve dual-channel branch (D-32 / HITL-06):
    - Test: `_approve_workflow` for a paper proposal still transitions PENDING → APPROVED → executor (no regression — Phase-1 5-event chain)
    - Test: `_approve_workflow` for a LIVE proposal AND `strategy_metadata.first_live_trade_confirmed_at is None` AND `strategy_metadata.live_mode_eligible=True` AND `strategy.mode="live"` → transitions PENDING → AWAITING_2ND_CHANNEL (NOT APPROVED); DOES NOT dispatch executor; DMs operator the dashboard URL via `_send_slack_dm` (identity-split safe)
    - Test: same scenario but `first_live_trade_confirmed_at` is already set → transitions PENDING → APPROVED → executor (subsequent live trades use standard path per D-32)
    - Test: cross-user defense (Phase-1 lines 135-146) preserved across the new branch

    Dashboard `/live-confirm/{proposal_id}` (HITL-06 second channel):
    - Test: `POST /live-confirm/{id}` with form fields `ack_real_money=on`, `ack_read_rationale=on`, `page_load_ts=<timestamp >= 5s ago>` → transitions AWAITING_2ND_CHANNEL → APPROVED_LIVE; dispatches `asyncio.create_task(execute_proposal(...))`; returns `live_confirm_success.html.j2`
    - Test: missing either checkbox → HTTPException(400) "Both acknowledgements are required..." (UI-SPEC §"Error States")
    - Test: page_load_ts indicates < 5s elapsed → HTTPException(400) "Please read the trade details for the full 5 seconds..."
    - Test: proposal in status APPROVED_LIVE (idempotent re-visit) → returns success template with "already confirmed at {ts}" copy (UI-SPEC §"Empty / Idle States")
    - Test: proposal in any other status (e.g., FAILED, FILLED) → HTTPException(400) "This proposal is in status {status} and cannot be confirmed..."
    - Test (idempotent): two simultaneous POSTs to /live-confirm/{id} result in exactly ONE APPROVED_LIVE transition + ONE executor dispatch (state-machine idempotency layer per RESEARCH §7 three-layer defense)

    On-fill stamp (D-32 closure):
    - Test: `on_fill_event` for the FIRST live fill of a strategy sets `strategy_metadata.first_live_trade_confirmed_at` to the fill timestamp
    - Test: subsequent live fills for the same strategy do NOT overwrite `first_live_trade_confirmed_at` (the field is set-once)
    - Test: paper fills do NOT set `first_live_trade_confirmed_at` (only live mode triggers the stamp)
  </behavior>
  <action>
    Replace WAVE-0 stubs in `tests/integration/test_first_live_gate.py`, `tests/unit/test_live_confirm_idempotent.py`, and `tests/integration/test_promote_paper_to_live.py` with the behaviors above. Mirror the chain-integration test pattern from `tests/integration/test_slack_approval_to_executor.py`.

    Create `src/gekko/strategy/__init__.py` (empty package init).

    Create `src/gekko/strategy/promotion.py`:
    - `async def promote_strategy_to_live(*, user_id: str, strategy_name: str) -> None` — UPSERTs the StrategyMetadata row setting `live_mode_eligible=True` + `live_promoted_at=<iso>`; writes audit event
    - `async def demote_strategy_from_live(*, user_id: str, strategy_name: str) -> None` — symmetric (sets live_mode_eligible=False; does NOT clear first_live_trade_confirmed_at — once stamped, stays stamped)
    - `async def stamp_first_live_trade(*, user_id: str, strategy_name: str, fill_ts: str) -> None` — set-once UPDATE on `first_live_trade_confirmed_at` (only sets when current value is NULL)
    - Per PATTERNS §3c: `_get_session_factory(user_id)` shim + finally-dispose

    Add CLI subcommand `gekko strategy promote-live <name>` to `src/gekko/cli.py`:
    - Read strategy from DB (Phase-1 pattern); call `typer.prompt(f"Type the strategy name '{name}' to confirm")` — only proceed on EXACT match
    - On confirm: `anyio.run(promote_strategy_to_live, user_id=settings.gekko_user_id, strategy_name=name)` + print UI-SPEC §"Success States" copy

    Extend `src/gekko/approval/slack_handler.py::_approve_workflow` per RESEARCH §7 dual-channel branch at lines 1283-1325 + PATTERNS §2 row "src/gekko/approval/slack_handler.py" — **note: `is_live_first` reads `tp.account_mode` from the LOCKED proposal row, NOT from current strategy state. This closes the TOCTOU window per BLOCKER #5 (plan-checker iteration 2).**
    - After the cross-user check + ProposalRow load (line 151), load StrategyMetadata ONLY for the `first_live_trade_confirmed_at` once-per-strategy gate: `meta = await session.get(StrategyMetadata, (gekko_user_id, tp.strategy_name))`
    - Compute `is_live_first = (tp.account_mode == "LIVE" and (meta is None or meta.first_live_trade_confirmed_at is None))` — the live/paper decision reads `tp.account_mode` from the proposal row; the first-trade-per-strategy gate reads `meta.first_live_trade_confirmed_at`. Do NOT re-read `strategy.mode` or `meta.live_mode_eligible` here — those would reopen the TOCTOU window.
    - Branch on `is_live_first`:
      - True: `await transition_status(session, decision_id, from_status="PENDING", to_status="AWAITING_2ND_CHANNEL")`; emit `approval` audit event with payload `{"awaiting_2nd_channel": True, ...}`; AFTER transaction exits, DM the operator via `_send_slack_dm` with the dashboard URL "⚠️ This is your FIRST live trade for `{strategy_name}`. To execute, also click confirm in your dashboard at {settings.dashboard_url}/live-confirm/{decision_id}"; DO NOT dispatch executor; return
      - False: existing Phase-1 path — `approve_proposal(...)` → `asyncio.create_task(execute_proposal(...))`

    Add `POST /live-confirm/{proposal_id}` route to `src/gekko/dashboard/routes.py` per RESEARCH §7 lines 1333-1385 + UI-SPEC §3b:
    - Accept `ack_real_money: str = Form(...)`, `ack_read_rationale: str = Form(...)`, `page_load_ts: float = Form(...)`
    - Validate: both ack fields == "on"; `time.time() - page_load_ts >= 5.0`
    - On validation pass: open per-user SQLCipher engine; SELECT proposal row; branch on current status (AWAITING_2ND_CHANNEL → transition + dispatch; APPROVED_LIVE → idempotent re-render; else → 400)
    - On AWAITING_2ND_CHANNEL: `await transition_status(session, proposal_id, from_status="AWAITING_2ND_CHANNEL", to_status="APPROVED_LIVE")`; emit `approval` event with `{"second_channel": True, ...}` payload; OUTSIDE transaction `asyncio.create_task(execute_proposal(proposal_id, user_id))`; return `live_confirm_success.html.j2`
    - PATTERNS §3i background-task wrapper + §5d

    Add `POST /strategies/{name}/promote-to-live` route per UI-SPEC §"Destructive Action Confirmations":
    - Accept `strategy_name_confirm: str = Form(...)`; validate exact match; on success call `await promote_strategy_to_live(user_id=settings.gekko_user_id, strategy_name=name)`; return HTMX partial that swaps the "Promote to Live" button with "Strategy is live-eligible"

    Extend `src/gekko/execution/executor.py::on_fill_event` per RESEARCH §7 lines 1390-1406 + PATTERNS §2 row "src/gekko/execution/executor.py" item 3:
    - After the existing `transition_status(EXECUTING → FILLED)` at lines 387-392, branch on `account_mode == "LIVE"` (this requires the executor to know the account_mode — it does, via the proposal row carrying it OR via re-loading strategy metadata)
    - If LIVE AND `strategy_metadata.first_live_trade_confirmed_at is None` for this strategy_name: call `await stamp_first_live_trade(user_id=user_id, strategy_name=tp.strategy_name, fill_ts=<fill event ts>)`

    Update `src/gekko/approval/proposals.py::STATE_TRANSITIONS` was already extended by plan 02-01 (the frozenset literal). Verify the symbol contains all 5 new tuples; if plan 02-01 only updated `_PROPOSAL_STATUSES` and not STATE_TRANSITIONS, plan 02-06 Task 2 adds them HERE.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_first_live_gate.py tests/unit/test_live_confirm_idempotent.py tests/integration/test_promote_paper_to_live.py tests/unit/test_approval_proposals.py -x -q</automated>
  </verify>
  <done>
    - All CLI + dashboard promotion + Slack dual-channel + dashboard live-confirm + on-fill-stamp behaviors pass
    - State machine `transition_status` body unchanged (data-driven test still green)
    - Phase-1 walking-skeleton + 22-event chain still validate
    - `grep -nE 'AWAITING_2ND_CHANNEL|APPROVED_LIVE' src/gekko/approval/proposals.py` returns ≥5 matches (the frozenset entries)
    - `python -c "from gekko.strategy.promotion import promote_strategy_to_live, stamp_first_live_trade"` exits 0
    - `gekko strategy promote-live --help` shows the command
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Visuals — live banner, [LIVE] chips, first-live-confirm full-page template + Slack first-live card builder</name>
  <read_first>
    - src/gekko/dashboard/templates/base.html.j2 (modal-mount + kill-banner-mount slots added by plan 02-05; live banner is the parallel surface here)
    - src/gekko/dashboard/templates/user_agreement.html.j2 (Phase-1 — the EXACT analog for first_live_confirm.html.j2 per PATTERNS §1b row 1)
    - src/gekko/dashboard/templates/strategies_list.html.j2 (Phase-1 — Phase 2 adds [LIVE] chip per UI-SPEC §1)
    - src/gekko/dashboard/templates/strategy_edit.html.j2 (the mode `<select>` at lines 50-59 currently disables "live" — Phase 2 enables for live-eligible strategies)
    - src/gekko/dashboard/static/tailwind.css (the new `.banner-live-strong` + `.chip-live` + `.countdown-bar` + `.countdown-bar-fill` + `.countdown-numeral` classes from plan 02-05's append are present)
    - src/gekko/reporter/slack.py (`build_proposal_card(account_mode="PAPER")` already plumbed — D-33 extends the LIVE branch; PATTERNS §2 row "src/gekko/reporter/slack.py")
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-UI-SPEC.md (Surface 1 — live banner; Surface 3 — first-live dual-channel including the dashboard page layout at §3b; Surface 5 — caution section in HITL card)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-PATTERNS.md (§1b template rows; §2 row "src/gekko/dashboard/templates/base.html.j2"; §3g Slack Block Kit builder + _escape_mrkdwn universal route)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§7 first-live dashboard page; §2 red-banner attachment lines 554-561)
  </read_first>
  <behavior>
    Live banner (D-33 / UI-SPEC §1):
    - Test: when `request.state.banner_mode == "LIVE"` (because at least one strategy has `live_mode_eligible=True`), base.html.j2 renders the `banner-live-strong` partial with copy "[LIVE — REAL MONEY] Alpaca live trading is armed."
    - Test: when no live strategies, banner is `banner-paper` (Phase-1 behavior preserved)
    - Test: ARIA: live banner has `role="alert" aria-live="polite" aria-atomic="true"` (UI-SPEC §1 ARIA spec)
    - Test: kill banner (from plan 02-05) stacks BELOW the live banner — z-index 49 vs 50; sticky `top: 40px` puts it under the live banner — confirmed by reading the tailwind CSS classes

    [LIVE] chip on strategies list (UI-SPEC §"Visuals"):
    - Test: a strategy row with `live_mode_eligible=True` renders `<span class="chip-live">LIVE</span>` in the actions column
    - Test: paper-only strategies render no chip
    - Test: the strategies_list.html.j2 partial accepts a `strategy_meta` mapping that the route populates

    First-live confirm page (HITL-06 / UI-SPEC §3b):
    - Test: GET `/live-confirm/{proposal_id}` (which is the second-channel URL — render-only path) — returns the full-page `first_live_confirm.html.j2` extending base.html.j2 with the trade detail panel + two checkboxes + 5s countdown + Confirm button
    - Test: the live banner appears automatically at the top (inherited from base.html.j2 — UI-SPEC §3b "inherited from base")
    - Test: the form `<form hx-post="/live-confirm/{proposal_id}" hx-target="#confirm-result" hx-swap="outerHTML">` carries the hidden `page_load_ts` input (UI-SPEC §3b)
    - Test: NO inline `<script>` (PATTERNS §4 anti-pattern row 7)
    - Test: countdown is visual-only (UI-SPEC §3b "Phase 2 recommendation: pure server-side timestamp check, no client-side state")
    - Test: Cancel link `<a href="/strategies">Cancel — return to strategies</a>` triggers a `POST /live-confirm/{id}/cancel` route OR a direct status=REJECTED transition via a separate route (UI-SPEC §3b "server-side transitions AWAITING_2ND_CHANNEL → REJECTED")
    - Test (success swap): on confirm submission, the response is a partial that replaces the form with the UI-SPEC §"Success States" message "First live trade confirmed. The executor is placing your order..."

    Slack first-live card (UI-SPEC §3a):
    - Test: `build_first_live_card(proposal: TradeProposal, dashboard_url: str) -> list[dict]` returns a Block Kit card with:
      - Header "🔴 FIRST LIVE TRADE — DUAL CONFIRM REQUIRED"
      - Context block with strategy / ticker / action / notional (each interpolation through `_escape_mrkdwn`)
      - Rationale section (escaped)
      - Warning section "⚠️ This is your FIRST live trade on this strategy. Slack approval alone is NOT enough. Open your dashboard to complete the second-channel confirmation."
      - Actions block with ONE URL-button: `{"type": "button", "text": {"type": "plain_text", "text": "Open Dashboard to Confirm"}, "style": "primary", "url": f"{dashboard_url}/live-confirm/{proposal.decision_id}"}` — NO `action_id` (URL buttons don't round-trip)
    - Test: the Slack approve handler for first-live trades posts this card INSTEAD of the regular HITL card (UI-SPEC §3a "Approve/Reject inline buttons is suppressed for first-live proposals")
    - Test: for SUBSEQUENT live trades on the same strategy (after first_live_trade_confirmed_at is set), the regular HITL card is posted (with the LIVE banner styling from Surface 1, not the first-live-special card)

    Live banner attached to regular HITL card for LIVE mode (D-33 / UI-SPEC §1 Slack parallel):
    - Test: `build_proposal_card(proposal, account_mode="LIVE")` (existing function with the live branch from Phase 1 plumbed) NOW prepends `🔴 LIVE — REAL MONEY` header per UI-SPEC §1 Slack parallel
    - Test: the existing PAPER path is unchanged

    `request.state.banner_mode` dependency wiring:
    - Test: the FastAPI dependency (from plan 02-05) extends to query `strategy_metadata.live_mode_eligible` via `SELECT COUNT(*) FROM strategy_metadata WHERE user_id=:u AND live_mode_eligible=1` (UI-SPEC §1 Trigger logic); 60s TTL cache on `app.state`
    - Test: cache is invalidated when /promote-to-live or /demote-from-live runs
  </behavior>
  <action>
    Add tests for the visual + Slack-card behaviors above.

    Create `src/gekko/dashboard/templates/live_banner.html.j2` per PATTERNS §1b row "live_banner.html.j2" + UI-SPEC §1:
    ```
    <div class="banner-live-strong" role="alert" aria-live="polite" aria-atomic="true">[LIVE — REAL MONEY] Alpaca live trading is armed.</div>
    ```

    Create `src/gekko/dashboard/templates/first_live_confirm.html.j2` per PATTERNS §1b row "first_live_confirm.html.j2" + §3j Jinja template extension pattern + UI-SPEC §3b layout (full-page; extends base.html.j2):
    - `{% extends "base.html.j2" %}`
    - `{% block title %}Confirm First Live Trade — {{ strategy_name }}{% endblock %}`
    - Page hero `<h1>Confirm First Live Trade — {{ strategy_name }}</h1>`
    - Trade detail panel with `<section aria-labelledby="trade-detail-heading">` containing ticker / action / limit_price / target_notional / strategy / proposal_id / rationale (full text, not truncated)
    - Trust acknowledgement block with two checkboxes (caution-yellow surface — UI-SPEC §3b item 4)
    - Countdown numeral `<span class="countdown-numeral">5</span>` (decremented server-side based on page_load_ts — UI-SPEC §3b "Phase 2 recommendation: pure server-side timestamp check")
    - Form per UI-SPEC §3b verbatim with `hx-post="/live-confirm/{{ proposal_id }}"`
    - Cancel link

    Create `src/gekko/dashboard/templates/live_confirm_success.html.j2` per PATTERNS §1b row "live_confirm_success.html.j2":
    - Partial that returns the UI-SPEC §"Success States" copy "First live trade confirmed. The executor is placing your order with Alpaca. You'll receive a Slack DM when the fill confirms."

    Extend `src/gekko/dashboard/templates/base.html.j2` per PATTERNS §2 row "src/gekko/dashboard/templates/base.html.j2" + UI-SPEC §1:
    - Conditional Jinja block: `{% if request.state.banner_mode == "LIVE" %}{% include "live_banner.html.j2" %}{% else %}<div class="banner-paper">PAPER</div>{% endif %}` (replaces the unconditional banner-paper from plan 02-05 Task 3)

    Extend `src/gekko/dashboard/templates/strategies_list.html.j2` per UI-SPEC §"Visuals":
    - In the row action column, add `{% if s.live_mode_eligible %}<span class="chip-live">LIVE</span>{% endif %}` per PATTERNS §2 row item 1
    - Add "Promote to Live" button conditionally (paper strategies); button POSTs to `/strategies/{name}/promote-to-live`

    Extend `src/gekko/dashboard/templates/strategy_edit.html.j2` per PATTERNS §2 row "src/gekko/dashboard/templates/strategy_edit.html.j2":
    - Enable the mode `<select>` "live" option for live-eligible strategies; leave disabled for non-eligible

    Add `build_first_live_card(proposal, dashboard_url)` to `src/gekko/reporter/slack.py` per PATTERNS §2 row "src/gekko/reporter/slack.py" + UI-SPEC §3a verbatim block list. Every interpolation routes through `_escape_mrkdwn` per PATTERNS §3g + UI-SPEC §"Slack Block Kit Parallels Summary".

    Add `LIVE_BANNER_STRONG`, `FIRST_LIVE_HEADER`, `LIVE_DM_PREFIX` constants to `src/gekko/reporter/templates.py` (already partially added in plan 02-05; verify completeness).

    Extend `src/gekko/reporter/slack.py::build_proposal_card` per PATTERNS §2 row "src/gekko/reporter/slack.py" item 1:
    - The existing `_banner(account_mode)` call at line 217 already routes based on account_mode; Phase 2 EXTENDS the LIVE branch to use `LIVE_BANNER_STRONG` constant
    - Insert a `⚠️ THIS PLACES A REAL-MONEY ORDER ON YOUR ALPACA LIVE ACCOUNT` section block immediately BEFORE the actions block when account_mode=="LIVE" per UI-SPEC §1 Slack parallel

    Extend `_approve_workflow` (from Task 2) to use the new card: for is_live_first=True proposals, the bot DM posts `build_first_live_card(proposal, dashboard_url)` instead of the regular HITL card. For subsequent live trades (first_live_trade_confirmed_at set), use the regular `build_proposal_card(proposal, account_mode="LIVE")` (which has the new red header + warning line).

    Wire `request.state.banner_mode` in the FastAPI dependency from plan 02-05: the dependency now queries `strategy_metadata` (in addition to `users.kill_active`); cache TTL 60s; invalidation hooks in /promote-to-live + /demote-from-live routes.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_first_live_gate.py tests/integration/test_dashboard_strategy_edit.py tests/unit/test_dashboard_templates_sri.py -x -q</automated>
  </verify>
  <done>
    - All visual + Slack-card + dependency wiring behaviors pass
    - Phase-1 SRI lint test still green (no inline `<script>`)
    - `grep -nE 'banner-live-strong|chip-live|first-live-confirm|FIRST LIVE TRADE' src/gekko/dashboard/templates/*.j2 src/gekko/reporter/slack.py src/gekko/reporter/templates.py` returns ≥5 matches
    - `python -c "from gekko.reporter.slack import build_first_live_card"` exits 0
    - Phase-1 walking-skeleton + 22-event chain still validate
    - `grep -nE '<script|onclick=' src/gekko/dashboard/templates/first_live_confirm.html.j2 src/gekko/dashboard/templates/live_banner.html.j2 src/gekko/dashboard/templates/live_confirm_success.html.j2` returns 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's plaintext live API key ↔ SQLCipher vault | Encrypted at rest; passphrase unlock at process startup |
| AlpacaBroker constructor `paper=False` ↔ live credentials path | `_allow_live` kwarg is internal-only opt-in; raw construction still blocked |
| Slack approve click ↔ live trade execution | TWO channels required for first live trade per strategy (HITL-06) |
| Strategy promotion CLI ↔ dashboard symmetric | Both require typed-strategy-name confirmation; Slack does NOT have a promotion command (D-31 deliberate friction) |
| Dashboard `/live-confirm` form ↔ executor dispatch | Server-side checkboxes + 5s timer + state-machine idempotency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-D-01 (CRITICAL) | Information Disclosure | Live API key in logs / repr / audit payload | mitigate | structlog `_redact` regex set extended for alpaca-live key shape; `BrokerCredential.__repr__` continues to exclude `key_blob`+`secret_blob`; CLI uses `typer.prompt(..., hide_input=True)`; audit-log `credentials_added` event payload contains `kind="alpaca_live"` but NOT the key value. |
| T-02-06-D-02 (CRITICAL) | Information Disclosure | Live API key in `.env` file | mitigate | D-34 explicit decision: .env stays paper-only. CLI `gekko credentials add-alpaca-live` is the ONLY ingress path for live keys; it writes to the SQLCipher vault. Documentation in README explicitly forbids putting live keys in .env. |
| T-02-06-A-01 (CRITICAL) | Authentication | Live trade fires without dual-channel confirmation | mitigate | State machine REQUIRES `AWAITING_2ND_CHANNEL → APPROVED_LIVE` transition before executor dispatches. PATTERNS §3e idempotency on same-state prevents double-execution. Slack handler diverts is_live_first=True proposals to AWAITING_2ND_CHANNEL; executor will never run on a proposal in that state. |
| T-02-06-A-02 (CRITICAL) | Authentication | Operator accidentally clicks "Confirm First Live Trade" within first 5 seconds | mitigate | Server-side `time.time() - page_load_ts >= 5.0` gate at the POST route — bypasses any client-side state manipulation. UI-SPEC §3b "Phase 2 recommendation: pure server-side timestamp check, no client-side state". |
| T-02-06-M-01 (CRITICAL — Knight Capital sibling) | Tampering | `_build_broker` loads live credentials for a paper-mode strategy | mitigate | The conditional `is_live = strategy.mode == "live" and strategy_metadata.live_mode_eligible` is the only path that loads live credentials. The OrderGuard `check_paper_live_pairing` extended invariant (Task 1) catches a mismatched `credential_kind` AT THE FIRST place_order if `_build_broker` somehow loaded the wrong creds. |
| T-02-06-M-02 | Tampering | Operator promotes a strategy to live without typed confirmation | mitigate | Both CLI and dashboard require typed-strategy-name match. Slack does NOT have a promotion command (D-31). Three independent paths required for elevation = high friction. |
| T-02-06-M-03 | Tampering | Dashboard /live-confirm POST without acknowledgement checkboxes | mitigate | Server-side validation: `ack_real_money == "on"` AND `ack_read_rationale == "on"` else HTTPException(400). UI-SPEC §"Error States" copy makes the gate clear. |
| T-02-06-O-01 | Operational | first_live_trade_confirmed_at race — two simultaneous fills set it twice | mitigate | `stamp_first_live_trade` UPSERT only sets when current value IS NULL — second call is a no-op. Test in Task 2 covers. |
| T-02-06-O-02 | Operational | AWAITING_2ND_CHANNEL state machine drift — executor receives a proposal in unexpected state | mitigate | PATTERNS §3e + §4 anti-pattern row 10: `transition_status` body is data-driven on STATE_TRANSITIONS frozenset. The 5 new tuples (added by plan 02-01 + wired by this plan) are the ONLY paths between the new states. Test `test_state_transitions_table_completeness` extends Plan 01-08 pattern. |
| T-02-06-I-01 | Information Disclosure | Live banner reveals live strategies exist to non-operator surfaces | accept | REG-03 single-user-per-instance — the dashboard is bound to localhost / operator's tunnel; no anonymous access. Phase 6 adds magic-link auth. |
| T-02-06-P-01 | Spoofing | LLM-authored rationale contains injection text in first_live_confirm.html.j2 | mitigate | The dashboard template renders `proposal.rationale` via Jinja2 — Jinja2 autoescapes HTML by default. PATTERNS §3g universal `_escape_mrkdwn` route handles the Slack side. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_alpaca_live_credentials.py tests/integration/test_first_live_gate.py tests/unit/test_live_confirm_idempotent.py tests/integration/test_promote_paper_to_live.py tests/unit/test_orderguard_paper_live.py -x -q` — all pass
- `uv run pytest tests/integration/test_trigger_run_end_to_end.py -x -q` — Phase-1 walking-skeleton still green
- `python -c "from gekko.vault.credentials import store_live_credentials, load_live_credentials; from gekko.strategy.promotion import promote_strategy_to_live, stamp_first_live_trade; from gekko.reporter.slack import build_first_live_card"` exits 0
- `grep -nE 'AWAITING_2ND_CHANNEL|APPROVED_LIVE' src/gekko/approval/proposals.py` returns ≥5 matches
- `grep -nE 'banner-live-strong|chip-live' src/gekko/dashboard/templates/*.j2` returns ≥2 matches
- Phase-1 22-event chain still validates after a paper proposal → APPROVED → FILLED runs through the new code path
</verification>

<success_criteria>
- Live API key + secret stored encrypted in SQLCipher vault (BROK-A-02 / D-34 satisfied)
- `gekko credentials add-alpaca-live` CLI registered + functional
- Strategy promotion (CLI + dashboard) requires typed strategy-name confirmation (D-31)
- HITL-06 dual-channel gate fires for FIRST live trade per strategy: Slack URL-button + dashboard form
- Second + subsequent live trades on same strategy bypass the gate (per-strategy stamping)
- OrderGuard check_paper_live_pairing now validates credential_kind (EXEC-05 invariant extension)
- Live banner + [LIVE] chips visible on dashboard when any live strategy is eligible (D-33)
- Phase-1 walking-skeleton + 22-event chain not regressed
</success_criteria>

<output>
Create `.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-06-SUMMARY.md` when done. Include: the credentials CLI command name + the audit event_type used for credential addition; the STATE_TRANSITIONS frozenset entries added; the `_allow_live` kwarg signature; the dashboard route paths added (`/strategies/{name}/promote-to-live`, `/live-confirm/{proposal_id}`); confirmation that subsequent live trades skip the dual-channel gate; the Live banner Jinja conditional source; the `build_first_live_card` Block Kit shape; confirmation that the Phase-1 22-event chain still validates.
</output>
