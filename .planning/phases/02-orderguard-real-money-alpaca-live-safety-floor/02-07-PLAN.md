---
phase: 02-orderguard-real-money-alpaca-live-safety-floor
plan: 07
type: execute
wave: 6
depends_on: ["02-03", "02-04", "02-05", "02-06"]
files_modified:
  - tests/integration/test_promote_paper_to_live_end_to_end.py
  - tests/fixtures/cassettes/alpaca_live_promote_smoke.json
  - README.md
autonomous: false
requirements:
  - EXEC-03
  - EXEC-04
  - EXEC-05
  - EXEC-06
  - EXEC-08
  - EXEC-09
  - EXEC-11
  - BROK-A-02
  - HITL-06
  - RES-06
  - RES-07

user_setup:
  - service: alpaca
    why: "The manual portion of this plan places a real $1 live order on the operator's Alpaca live account."
    env_vars: []
    dashboard_config:
      - task: "Confirm a small balance is available on the operator's Alpaca live account (the demo places a single $1 limit order)"
        location: "https://app.alpaca.markets/dashboard/overview"

must_haves:
  truths:
    - "Phase-2 walking skeleton: operator promotes a paper-validated strategy to live, places a $1 real-money order via Slack + dashboard dual confirmation, and observes all 11 OrderGuard checks firing in the right order"
    - "6-event audit chain for first-live trade: [decision, proposal, approval (awaiting_2nd_channel=True), approval (second_channel=True), order_submitted, fill] — walk_chain returns []"
    - "Wave-0 stub audit: zero `pytest.skip(\"Wave-0 stub` markers remain in tests/ at phase close"
    - "Cassette-mode automated test (no live operator opt-in required) replays the same chain with mocked Alpaca live endpoint — green in CI"
    - "Manual demo (real $1 trade) records elapsed wall-clock from /gekko run to fill notification — operator records the time + chain ID for the closeout SUMMARY"
    - "Phase-1 walking skeleton (`tests/integration/test_trigger_run_end_to_end.py`) still passes; 22-event chain still validates"
  artifacts:
    - path: tests/integration/test_promote_paper_to_live_end_to_end.py
      provides: "Phase-2 walking-skeleton wave-gate test (cassette mode)"
      min_lines: 80
    - path: tests/fixtures/cassettes/alpaca_live_promote_smoke.json
      provides: "Recorded cassette of the live Alpaca paper endpoint for the walking-skeleton test"
      min_lines: 20
    - path: README.md
      provides: "Phase 2 walking-skeleton demo section"
      contains: "Phase 2 — walking-skeleton demo"
  key_links:
    - from: "tests/integration/test_promote_paper_to_live_end_to_end.py"
      to: "gekko.audit.verify.walk_chain"
      via: "chain-integrity assertion"
      pattern: "walk_chain.*\\[\\]"
    - from: "README.md"
      to: ".planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-07-SUMMARY.md"
      via: "demo recipe references operator manual demo"
      pattern: "Phase 2 — walking-skeleton demo"
---

<objective>
Ship the Phase-2 walking skeleton: the cassette-mode end-to-end test that exercises the entire promote-paper-to-live → first-live-HITL → OrderGuard → real-money-fill path, plus the operator's manual-demo recipe that runs the same flow with REAL Alpaca live credentials for a $1 trade.

Per VALIDATION.md + RESEARCH §9 + Phase-1 Plan 01-09 pattern: this is the load-bearing wave-gate test for Phase 2. It glues together every earlier plan's deliverable and asserts the 6-event audit chain `[decision, proposal, approval (awaiting_2nd_channel=True), approval (second_channel=True), order_submitted, fill]` lands intact across the dual-channel gate.

Per VALIDATION.md Manual-Only Verifications: the FIRST REAL-MONEY trade end-to-end requires real Alpaca live credentials + real Slack + real dashboard — cannot be replayed in CI. The manual demo task confirms the operator can execute the full flow and produces the artifacts (chain ID + wall-clock) that close Phase 2.

Output: 1 walking-skeleton integration test + 1 cassette fixture + README demo section + operator-completed manual demo + Wave-0 stub cleanup audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-CONTEXT.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-VALIDATION.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-01-SUMMARY.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-02-SUMMARY.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-03-SUMMARY.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-04-SUMMARY.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-05-SUMMARY.md
@.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-06-SUMMARY.md
@.planning/phases/01-foundation-vertical-slice-alpaca-paper-slack-hitl/01-09-SUMMARY.md
@tests/integration/test_trigger_run_end_to_end.py
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cassette-mode walking-skeleton end-to-end test (promote → first-live → fill)</name>
  <read_first>
    - tests/integration/test_trigger_run_end_to_end.py (Phase-1 walking skeleton — THE template per PATTERNS §1c row "tests/integration/test_first_live_gate.py")
    - tests/integration/test_orderguard_chain_paper.py (4-event cap_rejection chain from plan 02-02 — supplementary template for the cassette structure)
    - tests/integration/test_first_live_gate.py (HITL-06 dual-channel chain from plan 02-06 — base for the wave-7 test)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-VALIDATION.md (Wave 0 + Manual-Only Verifications + the walking-skeleton wave-gate row at the bottom)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (§9 Walking-Skeleton Hash-Chain Test at lines 1635-1643 — exact assertion shape: `tests/integration/test_promote_paper_to_live_end_to_end.py` "promotes a paper strategy to live via the CLI; places a $1 real-money limit order (using a recorded cassette of the live Alpaca paper endpoint OR a mock if no operator opt-in); asserts the chain [decision, proposal, approval, approval (2nd channel), order_submitted, fill] with walk_chain intact; asserts first_live_trade_confirmed_at is set on the strategy; second invocation skips the gate")
    - tests/integration/test_promote_paper_to_live_end_to_end.py (Wave-0 stub from plan 02-01)
  </read_first>
  <behavior>
    Cassette-mode walking-skeleton (RESEARCH §9):
    - Test (setup): create a fresh per-user SQLCipher DB; seed a User row + a paper strategy with watchlist=["AAPL"]; insert paper Alpaca credentials in env; verify Phase-1 walking-skeleton still runs (sanity)
    - Test (promote): call `await promote_strategy_to_live(user_id="chris", strategy_name="ai-infra")`; assert `strategy_metadata.live_mode_eligible=True` AND `live_promoted_at` is set
    - Test (credentials): call `await store_live_credentials(user_id="chris", api_key="TEST_LIVE_KEY", secret_key="TEST_LIVE_SECRET")`; assert `BrokerCredential.kind="alpaca_live"` row exists
    - Test (cassette mocks): use respx (RESEARCH §9 framework) to mock the Alpaca live REST endpoint to return: account with `equity=$100`, `pattern_day_trader=False`, `daytrade_count=0`, `non_marginable_buying_power=$100`, `shorting_enabled=False`; positions=[]; quote for AAPL with ask_price=$1.00; place_order returns a successful order with broker_order_id="cassette-live-001"
    - Test (proposal flow): trigger `await trigger_strategy_run(user_id="chris", strategy_name="ai-infra", source="cassette")` with a stubbed `fake_sdk_query` that returns a TradeProposal for AAPL qty=1 limit_price=$1.00 target_notional_usd=$1.00 (the LLM bytes are mocked entirely — Plan 01-07 SDK-mocked test pattern)
    - Test (Slack approve diverts to AWAITING_2ND_CHANNEL): simulate the Slack approve action via the `_approve_workflow` test seam; assert proposal status transitions PENDING → AWAITING_2ND_CHANNEL (NOT APPROVED); assert NO executor dispatch yet
    - Test (dashboard confirms second channel): POST to `/live-confirm/{proposal_id}` with `ack_real_money=on`, `ack_read_rationale=on`, `page_load_ts=<6 seconds ago>` (so the 5s gate passes); assert transition AWAITING_2ND_CHANNEL → APPROVED_LIVE; assert executor dispatched
    - Test (OrderGuard fires all 11 checks in order): the executor's `_build_broker` returns OrderGuard wrapping the live AlpacaBroker; OrderGuard.place_order runs kill_switch → paper_live_pairing (passes — strategy.mode=live, account_mode=LIVE, broker.is_paper=False, credential_kind="alpaca_live") → universe (AAPL in watchlist) → hard_caps (position_pct OK at $1 of $100 equity) → qty_price_sanity (drift=0% with target_notional_usd=$1.00) → pdt (no PDT — daytrade_count=0) → t1 (passes — $1 order_cost ≤ $100 non_marginable) → market_hours (mocked open) → delegates to AlpacaBroker.place_order
    - Test (fill event fires): the cassette returns the fill; on_fill_event stamps `strategy_metadata.first_live_trade_confirmed_at=<fill_ts>`
    - Test (audit chain): walk_chain returns [] across the 6 events `[decision, proposal, approval (awaiting_2nd_channel=True), approval (second_channel=True), order_submitted, fill]`
    - Test (subsequent live trade skips gate): trigger a SECOND run; the Slack approve handler now transitions PENDING → APPROVED directly (not AWAITING_2ND_CHANNEL) because first_live_trade_confirmed_at is set; the dispatch is immediate; chain ends at fill normally
    - Test (Phase-1 not regressed): the original 5-event paper chain test still runs and walk_chain still validates

    Cassette fixture:
    - The cassette file `tests/fixtures/cassettes/alpaca_live_promote_smoke.json` contains the recorded HTTP exchanges (account / positions / quote / place_order / fill via TradingStream). For automated CI, the cassette is used. For operator regeneration (manual demo Task 2), set `GEKKO_TEST_LIVE_ALPACA=1` to bypass the cassette and hit the real Alpaca live endpoint with the operator's actual creds.

    Wave-0 stub audit:
    - Test: `grep -rn 'pytest.skip("Wave-0 stub' tests/` returns 0 (every Wave-0 stub from plan 02-01 has been filled in by plans 02-02..02-06)
    - Test: if any stub remains, the test names the file + the owning plan from the stub's docstring
  </behavior>
  <action>
    Replace WAVE-0 stub in `tests/integration/test_promote_paper_to_live_end_to_end.py` with the behaviors above. Mirror the structure of Phase-1's `tests/integration/test_trigger_run_end_to_end.py` per RESEARCH §9 + PATTERNS §1c row "test_first_live_gate.py":
    - `monkeypatch` based test-seam replacement for `_get_session_factory`, `_build_broker` (cassette mode wraps the live broker in the same OrderGuard), `_send_slack_dm` (captures DMs in a list), the SDK `query` calls (uses `fake_sdk_query` returning the canned proposal)
    - `respx.mock` or `httpx.MockTransport` for the alpaca-py HTTP layer (PATTERNS §1c "respx framework" reference)
    - `asyncio.create_task` drain pattern from Plan 01-08 SUMMARY (monkeypatch + collect spawned tasks; drain until tree quiesces — the executor + on_fill chain spawns nested tasks)
    - Cassette file is JSON-shaped HTTP exchange recording compatible with respx loading

    Create `tests/fixtures/cassettes/alpaca_live_promote_smoke.json`:
    - 6 recorded exchanges: GET /v2/account → 200 + account body; GET /v2/positions → 200 + []; GET /v2/stocks/AAPL/quotes/latest → 200 + quote; POST /v2/orders → 200 + order body; GET /v2/orders/{id} → 200 + order body (for the order-by-coid lookup if needed); the TradingStream fill event payload as a separate JSON sub-document
    - The mock data: account.equity=100, AAPL ask=1.00, order_qty=1, limit_price=1.00, target_notional_usd=1.00, filled at $1.00, broker_order_id="cassette-live-001"

    Add an audit step inside the test: after all assertions pass, scan `tests/` for any remaining `pytest.skip("Wave-0 stub` markers and report. If any present, fail with a message naming the file + owning plan (so the executor knows which plan to revisit).

    DO NOT introduce a new TradeProposal field, audit event_type, or state — the test exercises EXISTING surfaces only.

    NEVER write real Alpaca live credentials into the cassette. The cassette uses placeholder values ("TEST_LIVE_KEY", "TEST_LIVE_SECRET") that the AlpacaBroker constructor accepts because `_allow_live=True` is set in `_build_broker` AND the structurally-correct base_url is mocked.

    Add to the verification step a structural test: the `_send_slack_dm` capture list contains (a) the first-live-card with the "Open Dashboard to Confirm" URL-button (UI-SPEC §3a), and (b) the fill confirmation DM with `🔴 LIVE:` prefix (UI-SPEC §"Banner & Section Headers" + plan 02-06 Task 3 build_fill_confirmation extension).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_promote_paper_to_live_end_to_end.py tests/integration/test_trigger_run_end_to_end.py tests/integration/test_orderguard_chain_paper.py -x -q &amp;&amp; grep -rn 'pytest.skip("Wave-0 stub' tests/ | wc -l</automated>
  </verify>
  <done>
    - The walking-skeleton test passes end-to-end (cassette mode)
    - All 11 OrderGuard checks fire in the correct order during the test (verified by structured-log inspection or explicit hook in the test)
    - 6-event audit chain validates via walk_chain
    - Subsequent live trade on same strategy bypasses the dual-channel gate
    - Phase-1 walking-skeleton test still green
    - Zero `pytest.skip("Wave-0 stub` markers remain in tests/
    - `cat tests/fixtures/cassettes/alpaca_live_promote_smoke.json | python -c "import json,sys; data = json.load(sys.stdin); assert 'GET /v2/account' in str(data) or any('/account' in str(k) for k in data.keys())"` exits 0 (cassette is valid JSON with the account exchange present)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: README — Phase 2 walking-skeleton demo recipe</name>
  <read_first>
    - README.md (FULL FILE — Phase-1 walking-skeleton demo section from Plan 01-09 — the structure to mirror)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-VALIDATION.md (Manual-Only Verifications #1 — the exact checklist the operator follows)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-UI-SPEC.md (§"Copywriting Contract" — exact button labels + banner copy for the operator to recognize)
    - .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-RESEARCH.md (Manual-Only Verifications + walking-skeleton wave gate description)
  </read_first>
  <action>
    Add a new section to README.md after the existing "Phase 1 — Walking-skeleton demo" section: `## Phase 2 — Walking-skeleton demo (OrderGuard + Real-Money Alpaca Live)`

    Content:
    1. Prerequisites — Phase 1 demo completed; operator has an Alpaca brokerage account with live trading enabled; live API key + secret in hand; small balance (≥$5) on the live account; cloudflared or equivalent dashboard tunnel running.
    2. Setup — `gekko credentials add-alpaca-live` (paste live key + secret when prompted); `gekko strategy promote-live ai-infra` (or whatever strategy was used in Phase-1 demo — type the strategy name to confirm).
    3. First live trade — kick off `gekko run ai-infra` OR send `/gekko run ai-infra` in Slack; the agent produces a proposal; Slack DM shows the dedicated first-live card with single "Open Dashboard to Confirm" URL-button (red banner, NO inline Approve); click the button; the dashboard opens `/live-confirm/{proposal_id}` showing the trade detail + two acknowledgement checkboxes + 5-second read timer; tick both checkboxes; wait for the 5-second timer to elapse; click "Confirm First Live Trade".
    4. Verify the fill — Slack DM "🔴 LIVE: ..." fill confirmation lands; the dashboard fills are visible at the Alpaca dashboard; `gekko audit dump` shows the 6-event chain; `gekko audit verify` confirms the chain is intact.
    5. Subsequent live trade — kick off `gekko run ai-infra` again; this time the Slack DM shows the regular HITL card (with the red LIVE banner + warning line — NOT the dedicated first-live card); single Approve click executes (no dashboard step) because `first_live_trade_confirmed_at` is set.
    6. Kill switch sanity check — while a live order is open, send `/gekko kill CONFIRM` in Slack; confirm cancel + tally + persistent red banner on dashboard.
    7. Demo close — record the chain ID + wall-clock elapsed from `/gekko run` to fill notification in the 02-07-SUMMARY.

    Per UI-SPEC §"Copywriting Contract": use the exact button labels and banner copy in the README so the operator can match what they see on screen.

    DO NOT promise specific timings (the demo is one-shot, not a benchmark). The 5-second SLA is a separate manual verification covered by plan 02-05 Task 4.

    Maintain the existing Phase-1 demo section unchanged.
  </action>
  <verify>
    <automated>grep -nE 'Phase 2 — walking-skeleton demo|gekko credentials add-alpaca-live|gekko strategy promote-live|Confirm First Live Trade' README.md | wc -l</automated>
  </verify>
  <done>
    - README contains the new Phase 2 demo section
    - Section references all four required commands (credentials add-alpaca-live, strategy promote-live, run, audit verify)
    - UI-SPEC copy matches verbatim (operator can match on-screen text)
    - grep returns ≥4 matches
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual demo — real $1 live trade end-to-end (VALIDATION Manual-Only Verification #1)</name>
  <what-built>
    Plans 02-01..02-06 ship the full Phase-2 surface: OrderGuard firewall, kill switch, live credentials vault, HITL-06 dual-channel gate, RES-06/07 prompt-injection minimums. Task 1 of this plan ships the cassette-mode walking-skeleton test. The final wave gate is the operator running the demo with REAL money — verifying every surface a real user touches works correctly with REAL Alpaca live credentials.

    Per VALIDATION.md "First real-money trade end-to-end via Slack DM + dashboard dual approval" — the SLA timing, the red-banner visual, the audit chain integrity, and the dual-channel behavior cannot all be replayed in CI. This is the operator's responsibility.
  </what-built>
  <how-to-verify>
    Follow the README "Phase 2 — walking-skeleton demo" section verbatim with the following expected observations:

    Pre-demo:
    1. Phase-1 demo previously passed (22-event chain validated)
    2. Operator's Alpaca live account has a small balance (≥$5)
    3. Cloudflared or equivalent dashboard tunnel is running (operator can reach the dashboard from the same browser as Slack)

    Setup phase:
    1. `gekko credentials add-alpaca-live` — paste real live API key + secret. Expected: the command prints "Live credentials stored for user chris..."; `gekko audit dump | head -50` shows the credentials_added event (or equivalent).
    2. `gekko strategy promote-live ai-infra` — type the strategy name to confirm. Expected: the command prints "Strategy `ai-infra` is now live-mode-eligible..."; the dashboard at `/strategies` shows a [LIVE] chip on the ai-infra row + a persistent red `[LIVE — REAL MONEY] Alpaca live trading is armed.` banner at the top of every page.

    First live trade:
    1. In Slack DM with the bot: send `/gekko run ai-infra` (or trigger from the dashboard "Trigger Run" button).
    2. Wait for the proposal Slack DM. Expected: the DM is a DEDICATED first-live card (NOT the regular HITL card with Approve/Reject buttons). Header reads "🔴 FIRST LIVE TRADE — DUAL CONFIRM REQUIRED". The only button is "Open Dashboard to Confirm" (URL-button). The card body contains ticker, qty, limit_price, target_notional_usd, strategy_name, rationale.
    3. Click the "Open Dashboard to Confirm" button. Expected: browser opens to `/live-confirm/{proposal_id}`; the page is a FULL-PAGE template (NOT a modal); the page hero reads "Confirm First Live Trade — ai-infra"; the trade detail panel shows the full proposal; two checkboxes are present ("I understand this is real money on my Alpaca live account." + "I have read the rationale and trade details above."); a 5-second countdown numeral is visible; the Confirm button is at the bottom.
    4. Tick both checkboxes.
    5. Wait for the 5-second countdown to elapse. Expected: the countdown numeral counts down 5..4..3..2..1; after it reaches zero the page text changes to "Read time complete."
    6. Click "Confirm First Live Trade". Expected: the page transitions to the success state "First live trade confirmed. The executor is placing your order with Alpaca. You'll receive a Slack DM when the fill confirms."
    7. Wait for the fill. Expected: Slack DM "🔴 LIVE: Filled ... $1.00 ..." lands.
    8. Open the Alpaca live dashboard: confirm the $1 order is filled + the position shows in the account.
    9. Run `gekko audit verify`. Expected: "Chain intact across 28 events for user chris" (or whatever count = 22 Phase-1 + 6 Phase-2 first-live).
    10. Run `gekko audit dump | tail -30` — confirm the 6 events for this trade are in order: decision → proposal → approval (awaiting_2nd_channel=True) → approval (second_channel=True) → order_submitted → fill.

    Subsequent live trade (sanity that the gate doesn't re-fire):
    1. Send `/gekko run ai-infra` again.
    2. Wait for the Slack proposal DM. Expected: this is the REGULAR HITL card (Approve/Reject buttons present) with a RED LIVE banner header "🔴 LIVE — REAL MONEY" + the warning line "⚠️ THIS PLACES A REAL-MONEY ORDER..." above the action buttons.
    3. Click Approve in Slack. Expected: order is placed directly (NO dashboard step); fill DM lands shortly.

    Kill switch sanity:
    1. While a live order is open (or place a new one with a far-from-market limit), send `/gekko kill` then `/gekko kill CONFIRM`. Expected: 5-second SLA; cancel tally DM; persistent kill banner on dashboard; running `gekko run` while killed produces a cap_rejection event with reject_code=kill_active.
    2. Send `/gekko unkill CONFIRM` to resume.

    Closeout:
    1. Record the full chain ID + the wall-clock elapsed from `/gekko run` to fill DM
    2. Record any unexpected behaviors / surfaces / copy / timing — file as Phase-3 / Phase-4 carry-forward backlog items.

    Approve when:
    - The 6-event first-live chain validates via `gekko audit verify`
    - The dual-channel gate fires on first trade and is skipped on second trade
    - Red banner + [LIVE] chip + first-live card + LIVE rendering on second-trade HITL card all appear as specified in UI-SPEC
    - Kill switch fires + persists + can be unkilled
    - No data loss / no audit chain breaks
  </how-to-verify>
  <resume-signal>Type "demo_passed" with the chain ID + wall-clock elapsed, OR describe any failure with copy / timing / screenshots / log excerpts. Edge cases / surface improvements / Phase-3 backlog items can be filed as part of the closeout.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cassette test fixtures ↔ real Alpaca live endpoint | Mocks via respx; `GEKKO_TEST_LIVE_ALPACA=1` opt-in for operator regeneration |
| Manual demo ↔ real money | The $1 trade IS real; operator's Alpaca live balance is on the line |
| 6-event chain integrity ↔ walk_chain validation | SHA-256 hash chain from Phase-1 D-16 — any tampering breaks the chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-O-01 | Operational | Walking-skeleton test passes in CI but breaks in production (cassette/real mismatch) | mitigate | The cassette structure mirrors the real Alpaca endpoint exactly (same HTTP shapes, same response field names verified in RESEARCH §1-§7). Operator's manual demo (Task 3) is the production validation that catches mismatches. |
| T-02-07-O-02 | Operational | Wave-0 stubs still skipped at phase close (false-green) | mitigate | Task 1 explicitly runs `grep -rn 'pytest.skip("Wave-0 stub' tests/` and fails if matches found. Phase cannot close until all stubs are filled. |
| T-02-07-M-01 (CRITICAL) | Tampering | A regression in plans 02-02..02-06 silently breaks the audit chain | mitigate | Walking-skeleton test asserts walk_chain returns [] across the full 6-event chain — any payload mutation, state-machine drift, or hash mismatch fails the test. The Phase-1 22-event chain is ALSO re-validated to confirm no historical regression. |
| T-02-07-D-01 (CRITICAL) | Information Disclosure | Cassette fixture or test logs leak real Alpaca live credentials | mitigate | Task 1 explicitly uses placeholder credentials ("TEST_LIVE_KEY"); the cassette JSON contains NO real keys; structlog `_redact` processor catches any test-time accidental log of the placeholder. Operator's real keys never enter the test scaffolding. |
| T-02-07-M-02 | Tampering | Operator's manual demo unintentionally bypasses a Phase-2 surface (e.g., skips the dashboard step) | accept | The walking-skeleton automated test is the canonical assertion of the dual-channel gate. If the operator manually drives `/live-confirm` from curl bypassing the Slack DM, the gate still fires correctly because it's state-machine enforced. The operator demo is a USABILITY check, not a security check — security is owned by the automated tests. |
| T-02-07-O-03 | Operational | Manual demo fails because Phase-2 surface has a Phase-3 backlog gap | accept | Phase-3 production HITL UX hardening (idempotent buttons, quiet hours, timeout=REJECT, edit-size, dashboard fallback) is explicitly OUT OF SCOPE per CONTEXT.md Deferred Ideas. Operator finds something like "I wish the timeout was clearer" — that's Phase-3 backlog, NOT a Phase-2 blocker. |
| T-02-07-A-01 | Authentication | Operator runs the manual demo as a different user than expected | mitigate | REG-03 single-user-per-instance — Phase 2 inherits Phase 1's invariant; `settings.gekko_user_id` is the single operator on the machine. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_promote_paper_to_live_end_to_end.py -x -q` — green (cassette mode)
- `uv run pytest tests/integration/test_trigger_run_end_to_end.py -x -q` — Phase-1 walking-skeleton still green
- `uv run pytest tests/ -x -q` — full suite green
- `grep -rn 'pytest.skip("Wave-0 stub' tests/` returns 0
- `grep -nE 'Phase 2 — walking-skeleton demo' README.md` returns 1
- Operator manual demo Task 3 returns "demo_passed" with chain ID + wall-clock
- `gekko audit verify` reports "Chain intact across N events for user chris" where N ≥ 28 (Phase-1 22 + Phase-2 first-live 6, plus any additional second-live + kill events from the demo)
</verification>

<success_criteria>
- Cassette-mode walking-skeleton wave-gate test green; all 11 OrderGuard checks fire in order; 6-event audit chain validates
- Zero Wave-0 stubs remaining in tests/ — every plan from 02-01 onwards filled in its owning stubs
- README contains the operator's Phase-2 demo recipe with verbatim UI-SPEC copy
- Operator manually verified a REAL $1 Alpaca live trade end-to-end with the chain ID recorded
- Phase-1 walking-skeleton + 22-event chain still validates (no regression)
- All 11 Phase-2 REQ-IDs (EXEC-03, EXEC-04, EXEC-05, EXEC-06, EXEC-08, EXEC-09, EXEC-11, BROK-A-02, RES-06, RES-07, HITL-06) demonstrably satisfied via the walking-skeleton + manual demo
</success_criteria>

<output>
Create `.planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/02-07-SUMMARY.md` when done. Include:
- Cassette file location + recorded HTTP exchanges
- Full chain length after the manual demo (e.g., 28+ events)
- Operator-recorded wall-clock from `/gekko run` to fill DM
- Any UI / copy / timing discrepancies that should become Phase-3 backlog items
- Confirmation that all 11 REQ-IDs are satisfied
- Confirmation that the Phase-1 22-event chain still validates intact
- The chain ID + transaction details of the actual $1 real-money trade for audit traceability
</output>
