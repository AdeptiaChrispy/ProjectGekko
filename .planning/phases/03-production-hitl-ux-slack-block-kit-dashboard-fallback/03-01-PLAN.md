---
phase: 03-production-hitl-ux-slack-block-kit-dashboard-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0004_p3_hitl_ux.py
  - src/gekko/db/models.py
  - src/gekko/schemas/strategy.py
  - src/gekko/agent/proposal_writer.py
  - src/gekko/approval/proposals.py
  - src/gekko/reporter/slack.py
  - tests/conftest.py
  - tests/unit/test_p3_alembic_round_trip.py
  - tests/unit/test_p3_schema_additions.py
  - tests/unit/test_proposal_writer_timeout.py
  - tests/unit/test_proposal_state_machine_expired.py
  - tests/unit/test_post_run_result_persists_ts.py
autonomous: true
requirements:
  - HITL-02
  - HITL-03
  - HITL-05
  - DASH-04
  - REPT-01

must_haves:
  truths:
    - "Alembic 0004 round-trips upgrade -> downgrade -> upgrade without losing the Phase 1 + 2 audit chain"
    - "Every new P3 vocabulary value (EXPIRED proposal status; expiration, dedup_click, edit_size, daily_pnl event types) is rejected at the DB layer if absent from the CheckConstraint"
    - "TradeProposal.expires_at is stamped at proposal-build time using strategy.proposal_timeout_minutes or 30 (D-51)"
    - "PENDING -> EXPIRED state-machine edge exists and the existing idempotent same-state return path still no-ops"
    - "Existing Phase 1 + 2 unit + integration suite still green after the migration"
    - "post_run_result() captures the chat.postMessage response and persists slack_message_ts + slack_message_channel on the Proposal row so Plan 03-04's chat.update of the expired card has the ts+channel to target (closes plan-checker BLOCKER #1 iteration 1 / P7 schema refinement)"
  artifacts:
    - path: "migrations/versions/0004_p3_hitl_ux.py"
      provides: "Schema migration: SlackActionDedup table, User quiet_hours_* + timezone columns, Proposal.expires_at + slack_message_ts + slack_message_channel columns, extended ck_proposal_status (+EXPIRED), extended ck_event_type (+expiration, +dedup_click, +edit_size, +daily_pnl)"
      contains: "def upgrade() -> None"
    - path: "src/gekko/db/models.py"
      provides: "SlackActionDedup ORM class + extended _PROPOSAL_STATUSES + extended _EVENT_TYPES + new User/Proposal columns"
      contains: "class SlackActionDedup"
    - path: "src/gekko/schemas/strategy.py"
      provides: "Strategy Pydantic v2 schema with quiet_hours_start, quiet_hours_end, proposal_timeout_minutes optional fields per D-47 + D-51"
      contains: "quiet_hours_start"
    - path: "src/gekko/agent/proposal_writer.py"
      provides: "ProposalWriter stamps expires_at at insertion time per D-51"
      contains: "expires_at"
    - path: "src/gekko/approval/proposals.py"
      provides: "STATE_TRANSITIONS extended with (PENDING, EXPIRED) edge"
      contains: "(\"PENDING\", \"EXPIRED\")"
  key_links:
    - from: "migrations/versions/0004_p3_hitl_ux.py"
      to: "src/gekko/db/models.py"
      via: "_PROPOSAL_STATUSES + _EVENT_TYPES vocabulary tuples are mirrored in the local _FROZEN_* tuples in the migration (Plan 02-01 / Phase 2 0002 + 0003 pattern)"
      pattern: "_FROZEN_PROPOSAL_STATUSES|_FROZEN_EVENT_TYPES"
    - from: "src/gekko/agent/proposal_writer.py"
      to: "src/gekko/db/models.py"
      via: "Proposal.expires_at column populated at insertion alongside account_mode (line 323 in P2)"
      pattern: "expires_at\\s*="
    - from: "src/gekko/schemas/strategy.py"
      to: "src/gekko/agent/proposal_writer.py"
      via: "ProposalWriter reads strategy.proposal_timeout_minutes or PROPOSAL_TIMEOUT_DEFAULT_MIN to compute expires_at"
      pattern: "proposal_timeout_minutes"
---

<objective>
Land the Phase 3 schema substrate end-to-end in a single Alembic 0004 migration so every downstream plan can operate against a stable schema with no further DDL.

Purpose: Wave 1 is the only DDL plan in P3. Every subsequent plan (dedup table inserts, expiry sweep WHERE clause, dashboard /approvals queries, daily P&L audit-event writes) reads/writes columns this plan creates. Splitting the schema across waves would force backwards-incompatible migrations mid-phase.

Output:
- Alembic revision `0004_p3_hitl_ux.py` (single migration; up+down) covering: new `slack_action_dedup` table per D-45; new columns on `users` (quiet_hours_start, quiet_hours_end, timezone per D-47/D-49); new columns on `proposals` (expires_at per D-51/D-61, slack_message_ts + slack_message_channel per D-53); CHECK-constraint extensions on `proposals.status` (+EXPIRED per A6) and `events.event_type` (+expiration, +dedup_click, +edit_size, +daily_pnl per D-45).
- ORM mirror in `src/gekko/db/models.py`: `SlackActionDedup` class; extended `_PROPOSAL_STATUSES`; extended `_EVENT_TYPES`; `User` quiet_hours_* + timezone columns; `Proposal` expires_at + slack_message_ts + slack_message_channel columns.
- Pydantic schema extension in `src/gekko/schemas/strategy.py`: `quiet_hours_start`, `quiet_hours_end`, `proposal_timeout_minutes` optional fields.
- ProposalWriter stamps `expires_at` at insertion time per D-51 (one of two behavior changes in this plan; all other modules read the new columns in Wave 2+).
- STATE_TRANSITIONS extension: `(PENDING, EXPIRED)` edge in `src/gekko/approval/proposals.py`.
- `post_run_result()` captures the `chat.postMessage` response and persists `slack_message_ts` + `slack_message_channel` on the Proposal row per RESEARCH §Pitfall 7 (Task 4 — closes plan-checker BLOCKER #1 iteration 1).
- Wave-0 test stubs for ~30 P3 tests (created empty so later plans fill bodies; this plan fills only the schema-round-trip, proposal-writer-timeout, state-machine-expired, and post-run-result-persists-ts tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-RESEARCH.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-PATTERNS.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-VALIDATION.md

@migrations/versions/0002_orderguard.py
@migrations/versions/0003_event_types_phase2.py
@src/gekko/db/models.py
@src/gekko/schemas/strategy.py
@src/gekko/agent/proposal_writer.py
@src/gekko/approval/proposals.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create all P3 test stubs + 3 conftest fixtures</name>
  <files>
    tests/conftest.py,
    tests/unit/test_slack_action_dedup.py,
    tests/unit/test_slack_retry_header.py,
    tests/unit/test_expire_stale_proposals.py,
    tests/unit/test_quiet_hours_predicate.py,
    tests/unit/test_dm_bypass_categories.py,
    tests/unit/test_dm_routine_suppressed.py,
    tests/unit/test_dashboard_login.py,
    tests/unit/test_dashboard_approvals.py,
    tests/unit/test_dashboard_edit_size.py,
    tests/unit/test_proposal_card_shared_partial.py,
    tests/unit/test_daily_pnl_aggregation.py,
    tests/unit/test_daily_pnl_bypasses_quiet.py,
    tests/unit/test_severity_tier_dm.py,
    tests/unit/test_executor_error_dms_coverage.py,
    tests/unit/test_chat_update_expired.py,
    tests/unit/test_proposal_writer_timeout.py,
    tests/unit/test_quiet_hours_dm_gate.py,
    tests/unit/test_transition_status_callers.py,
    tests/unit/test_edit_size_not_direct_broker.py,
    tests/unit/test_expiry_no_sdk_import.py,
    tests/unit/test_dashboard_middleware_order.py,
    tests/unit/test_proposal_state_machine_expired.py,
    tests/unit/test_p3_alembic_round_trip.py,
    tests/unit/test_p3_schema_additions.py,
    tests/integration/test_dedup_race.py,
    tests/integration/test_sweep_persistence.py,
    tests/integration/test_dashboard_approve_flow.py,
    tests/integration/test_dashboard_edit_size_happy.py,
    tests/integration/test_scheduler_quiet_hours.py,
    tests/integration/test_p3_walking_skeleton.py
  </files>
  <read_first>
    tests/conftest.py (existing P1+P2 fixtures: account_mode parametrized, kill_state, live_credential_pair, clean_settings_env — extend the same patterns),
    tests/unit/test_proposal_writer.py (Plan 01-07 — 11 ProposalWriter behaviors; the test-file shape P3 mirrors),
    tests/unit/test_market_hours.py (Plan 01-08 — DST + tz + IANA test shape for test_quiet_hours_predicate.py),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-VALIDATION.md (Per-Task Verification Map table — authoritative test name list)
  </read_first>
  <behavior>
    - Stubs MUST import `pytest` and `pytest.mark.asyncio` where appropriate, declare the test functions with `pass` bodies (or a `pytest.skip("Wave 0 stub — populated in Plan 03-XX")` marker so the collector finds the file but does not fail).
    - Three NEW conftest fixtures appended to `tests/conftest.py` per D-47 / D-50 / D-41: `quiet_hours_user` (User row with quiet_hours_start=time(22,0), quiet_hours_end=time(7,0), timezone='America/New_York'); `expired_proposal` (Proposal with status='PENDING', expires_at = utcnow - 1min, slack_message_ts populated for chat.update mocking); `dedup_row_factory` (callable yielding SlackActionDedup rows).
    - Existing P1+P2 fixtures (account_mode, kill_state, clean_settings_env, etc.) MUST be preserved verbatim.
    - `tests/unit/test_p3_alembic_round_trip.py` is the only stub whose body lands in THIS plan (Task 3 fills it).
    - `tests/unit/test_proposal_writer_timeout.py` body is filled by Task 5 of this plan.
    - `tests/unit/test_proposal_state_machine_expired.py` body is filled by Task 5.
    - All other stubs stay skipped until their owning plan (03-02..03-08) fills the body.
  </behavior>
  <action>
    Create ~30 stub test files listed in 03-VALIDATION.md §"Wave 0 Requirements" with `pytest.skip("Wave 0 stub — populated in Plan 03-XX")` bodies so `pytest --collect-only` discovers them but they do not red the suite. Append three new conftest fixtures (`quiet_hours_user`, `expired_proposal`, `dedup_row_factory`) to `tests/conftest.py` BELOW the existing P1+P2 fixtures (do not rewrite the file). Each fixture imports `time` from `datetime` and `timezone` from `datetime`; `quiet_hours_user` uses `timezone='America/New_York'` (D-49 default). `expired_proposal` populates `slack_message_ts='1234567890.000100'` and `slack_message_channel='D1234567890'` so the Slack chat.update mocks have realistic identifiers. `dedup_row_factory` accepts kwargs that match the SlackActionDedup columns Task 2 declares (proposal_id, action_id, actor_slack_user_id, actor_gekko_user_id, source, slack_trigger_id). All stubs are forward-deceleration only — no production code references.
  </action>
  <verify>
    <automated>uv run pytest --collect-only tests/unit/test_p3_alembic_round_trip.py tests/unit/test_slack_action_dedup.py tests/unit/test_quiet_hours_predicate.py tests/unit/test_dashboard_login.py tests/unit/test_daily_pnl_aggregation.py 2>&1 | grep -E "5 tests collected|5 items|collected (3[0-9]|[4-9][0-9]) (item|test)" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only tests/` lists every test file enumerated in 03-VALIDATION.md §"Wave 0 Requirements" (~30 stubs)
    - `uv run pytest tests/unit/test_p3_alembic_round_trip.py -x` skips with the Wave 0 marker (body fills in Task 3 of this plan)
    - `tests/conftest.py` contains three new fixture functions named `quiet_hours_user`, `expired_proposal`, `dedup_row_factory` discoverable by `grep -E "^def (quiet_hours_user|expired_proposal|dedup_row_factory)\(" tests/conftest.py`
    - Existing P1+P2 fixtures still exist (grep `clean_settings_env` and `account_mode` still find the original definitions)
    - No production-code file under `src/gekko/` is touched in this task
  </acceptance_criteria>
  <done>~30 Wave 0 test stubs land with skip markers; 3 new conftest fixtures append below existing fixtures; pytest collection passes with no errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alembic 0004 + ORM mirror (SlackActionDedup + quiet_hours_* + expires_at + extended CHECK)</name>
  <files>
    migrations/versions/0004_p3_hitl_ux.py,
    src/gekko/db/models.py,
    src/gekko/schemas/strategy.py
  </files>
  <read_first>
    migrations/versions/0002_orderguard.py (Phase 2 plan 02-01 — _FROZEN_* tuples + batch_alter_table + new-table create_table; the literal template),
    migrations/versions/0003_event_types_phase2.py (Phase 2 — drop_constraint + create_check_constraint pattern for extending ck_event_type; the literal template for both CHECK extensions in 0004),
    src/gekko/db/models.py:47-95 (CheckConstraint vocabularies _PROPOSAL_STATUSES + _EVENT_TYPES),
    src/gekko/db/models.py:121-168 (User columns — quiet_hours_* + timezone land here),
    src/gekko/db/models.py:220-261 (StrategyMetadata — closest analog for SlackActionDedup; composite-keyed audit-friendly row),
    src/gekko/db/models.py:314-373 (Proposal columns + ck_proposal_status declaration),
    src/gekko/schemas/strategy.py:63-150 (Strategy Pydantic schema; _validate_schedule_time field_validator is the IANA/format template),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md D-45/D-47/D-51/D-61 sections (column shapes are normative)
  </read_first>
  <behavior>
    - `slack_action_dedup` table per D-45 with columns: `id` (Integer autoincrement PK), `proposal_id` (String, FK proposals.proposal_id, NOT NULL), `action_id` (String NOT NULL — values in {'approve_proposal','reject_proposal','edit_size','escalate'}), `actor_slack_user_id` (String, nullable), `actor_gekko_user_id` (String NOT NULL, FK users.user_id), `source` (String NOT NULL — values in {'slack','dashboard','cli'}), `slack_trigger_id` (String, nullable), `inserted_at` (String ISO-UTC, NOT NULL), `result` (String NOT NULL — values in {'first_write','duplicate'}). Two UNIQUE indexes: `uq_dedup_slack` on `(proposal_id, action_id, actor_slack_user_id)` per D-42; `uq_dedup_dashboard` on `(proposal_id, action_id, actor_gekko_user_id, source)` per D-56. CHECK constraint `ck_dedup_source` restricts `source` to the three literal values; CHECK `ck_dedup_result` restricts `result` to the two literals.
    - `User` rows gain `quiet_hours_start` (String HH:MM:SS, nullable), `quiet_hours_end` (String HH:MM:SS, nullable), `timezone` (String IANA, nullable; default NULL means use 'America/New_York' at read time per D-49).
    - `Proposal` rows gain `expires_at` (String ISO-UTC, nullable per D-61 grandfathering), `slack_message_ts` (String, nullable), `slack_message_channel` (String, nullable).
    - `_PROPOSAL_STATUSES` tuple gains `"EXPIRED"` per A6; `ck_proposal_status` CHECK is dropped and recreated by 0004 via the 0003 batch_alter_table pattern.
    - `_EVENT_TYPES` tuple gains `"expiration"`, `"dedup_click"`, `"edit_size"`, `"daily_pnl"`; `ck_event_type` CHECK is dropped and recreated by 0004 via the 0003 pattern.
    - `Strategy` Pydantic schema gains `quiet_hours_start: str | None = None`, `quiet_hours_end: str | None = None`, `proposal_timeout_minutes: int | None = None` with `Field(None, gt=0)`. NO `Strategy.timezone` per D-47. A new `_validate_quiet_hours_format` field_validator mirroring `_validate_schedule_time` enforces HH:MM regex `^\d{2}:\d{2}$` on both quiet_hours_* fields.
    - Migration is reversible: `downgrade()` drops the table, the columns, and restores the pre-0004 CHECK vocabularies. The migration's `_FROZEN_PROPOSAL_STATUSES_PRE` + `_FROZEN_PROPOSAL_STATUSES_POST` + `_FROZEN_EVENT_TYPES_PRE` + `_FROZEN_EVENT_TYPES_POST` locals are the snapshot vocabularies (the migration must work against ANY future models.py state per Plan 01-03's locked decision).
  </behavior>
  <action>
    Create `migrations/versions/0004_p3_hitl_ux.py` with `revision = "0004_p3_hitl_ux"`, `down_revision = "0003_event_types_phase2"`. Use the exact Phase-2 0002+0003 idioms: top-of-file `_FROZEN_*` vocabulary tuples (pre + post for status + event_type), a local `_in_check` helper copied from 0002, `def upgrade()` that wraps every CHECK swap and add_column in `with op.batch_alter_table(table) as bop:` (per Plan 01-03 SQLite batch-mode lock-in), and `def downgrade()` that inverts every step in reverse order. Create the `slack_action_dedup` table via `op.create_table` with sa.Column declarations matching the D-45 schema; declare the two UNIQUE indexes via `op.create_index(..., unique=True, ...)` after the table is created. Mirror every schema change in `src/gekko/db/models.py`: add `class SlackActionDedup(Base)` near `StrategyMetadata` (line 220), extend `_PROPOSAL_STATUSES` + `_EVENT_TYPES` tuples in place, add the three new `User` columns at the existing column block (line 121-168), add the three new `Proposal` columns at the existing column block (line 314-373). `SlackActionDedup.__repr__` follows the StrategyMetadata pattern — include proposal_id + action_id + source + result; exclude slack_trigger_id (mildly sensitive). In `src/gekko/schemas/strategy.py`, append the three new optional fields to the Strategy class (lines 99-150) below `mode`, and add the `_validate_quiet_hours_format` field_validator after `_validate_schedule_time` (lines 63-96).
  </action>
  <verify>
    <automated>uv run alembic upgrade head 2>&1 | grep -E "0004_p3_hitl_ux" -q &amp;&amp; uv run pytest tests/unit/test_p3_schema_additions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class SlackActionDedup" src/gekko/db/models.py` returns exactly one line
    - `grep -n "EXPIRED" src/gekko/db/models.py` finds `_PROPOSAL_STATUSES` and `_FROZEN_PROPOSAL_STATUSES` references (or just `_PROPOSAL_STATUSES`)
    - `grep -nE "(expiration|dedup_click|edit_size|daily_pnl)" src/gekko/db/models.py` returns at least 4 lines (one per new event_type)
    - `grep -nE "quiet_hours_(start|end)|^\s*timezone:" src/gekko/db/models.py` returns at least 5 lines (3 User + 2 Strategy schema mirrors)
    - `grep -n "expires_at" src/gekko/db/models.py` returns at least 1 line on the Proposal column block
    - `grep -nE "(quiet_hours_start|quiet_hours_end|proposal_timeout_minutes)" src/gekko/schemas/strategy.py` returns at least 3 lines
    - `uv run alembic upgrade head` succeeds and `uv run alembic downgrade -1` then `uv run alembic upgrade head` succeeds (round-trip)
    - `tests/unit/test_p3_schema_additions.py` (filled by Task 5 in this plan) asserts the SlackActionDedup ORM class exists, the new User + Proposal columns are queryable, and the Strategy Pydantic schema accepts + rejects the new fields per D-47 / D-51
  </acceptance_criteria>
  <done>Alembic 0004 land alongside ORM + Pydantic mirrors; upgrade/downgrade/upgrade round-trips; schema-additions test green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire ProposalWriter to stamp expires_at + extend STATE_TRANSITIONS with PENDING -> EXPIRED + fill schema/round-trip tests</name>
  <files>
    src/gekko/agent/proposal_writer.py,
    src/gekko/approval/proposals.py,
    tests/unit/test_p3_alembic_round_trip.py,
    tests/unit/test_p3_schema_additions.py,
    tests/unit/test_proposal_writer_timeout.py,
    tests/unit/test_proposal_state_machine_expired.py
  </files>
  <read_first>
    src/gekko/agent/proposal_writer.py:190-329 (TradeProposal build + INSERT site; line ~207-211 is the account_mode ternary template; line 303 is the now_iso = datetime.now(UTC).isoformat() pattern; line 304-325 is the INSERT site where the new `expires_at=` kwarg lands),
    src/gekko/approval/proposals.py:80-101 (STATE_TRANSITIONS frozenset literal — append the new edge inside the braces; preserve the comment style — "Phase 3 — Plan 03-01 Task 3"),
    src/gekko/approval/proposals.py:139-141 (idempotent same-state return — this MUST be preserved verbatim; the sweep + click-race in plan 03-04 depends on it),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-PATTERNS.md §2a (state-machine extension idiom) + §2g (audit-event append with normalize_decimals),
    migrations/versions/0004_p3_hitl_ux.py (just authored — round-trip test reads its revision id)
  </read_first>
  <behavior>
    - `ProposalWriter._write_trade` (or its single insertion call site around line 304) computes `expires_at = (datetime.now(UTC) + timedelta(minutes=strategy.proposal_timeout_minutes or PROPOSAL_TIMEOUT_DEFAULT_MIN)).isoformat()` and persists it on the Proposal row.
    - A module-level constant `PROPOSAL_TIMEOUT_DEFAULT_MIN: int = 30` lives near the top of `src/gekko/agent/proposal_writer.py` (D-51 default).
    - `strategy.proposal_timeout_minutes` is read from the Pydantic Strategy model attached to the writer's input; if the strategy lacks the attribute (older row, forward-compat), the default 30 applies.
    - `STATE_TRANSITIONS` gains `("PENDING", "EXPIRED")` per A6; an optional `expire_proposal(session, proposal_id, *, reason, expired_at, configured_timeout_minutes)` convenience helper next to `approve_proposal` / `reject_proposal` is added that calls `transition_status(... PENDING -> EXPIRED)` then `append_event(event_type="expiration", payload=normalize_decimals({"proposal_id": ..., "reason": reason, "expired_at": expired_at, "configured_timeout_minutes": configured_timeout_minutes}))` in the same transaction (D-50 audit payload).
    - `test_p3_alembic_round_trip.py` exercises `alembic upgrade head -> downgrade -1 -> upgrade head` against a fresh SQLCipher DB and asserts every new column + table is present after the final upgrade.
    - `test_p3_schema_additions.py` exercises the ORM + Pydantic additions: insert a SlackActionDedup row; query it back; assert `Strategy(quiet_hours_start="22:00", quiet_hours_end="07:00", proposal_timeout_minutes=15).proposal_timeout_minutes == 15`; assert `Strategy(proposal_timeout_minutes=0)` raises `pydantic.ValidationError` (gt=0).
    - `test_proposal_writer_timeout.py` exercises three cases: (a) strategy with `proposal_timeout_minutes=None` → expires_at = now + 30min; (b) strategy with `proposal_timeout_minutes=15` → expires_at = now + 15min; (c) freezegun-pinned now so the expected ISO string matches.
    - `test_proposal_state_machine_expired.py` covers: (a) PENDING -> EXPIRED valid via `transition_status`; (b) re-entering EXPIRED is idempotent no-op (existing line 139-141 path); (c) APPROVED -> EXPIRED raises ValueError; (d) `expire_proposal` convenience writes the audit event with the D-50 payload shape.
  </behavior>
  <action>
    Add `PROPOSAL_TIMEOUT_DEFAULT_MIN: int = 30` at the top of `src/gekko/agent/proposal_writer.py` next to the existing constants. Locate the Proposal row INSERT site (around line 304-325 per PATTERNS §1b) and add `expires_at=(now_dt + timedelta(minutes=getattr(strategy, "proposal_timeout_minutes", None) or PROPOSAL_TIMEOUT_DEFAULT_MIN)).isoformat()` as a new kwarg alongside `account_mode=account_mode`. Compute `now_dt` once via `now_dt = datetime.now(UTC)` and reuse for both the existing `now_iso = now_dt.isoformat()` and the new `expires_at` calc. Append `("PENDING", "EXPIRED")` inside the STATE_TRANSITIONS frozenset literal at lines 80-101 of `src/gekko/approval/proposals.py` (with the comment "Phase 3 — Plan 03-01 Task 3: sweep-side expiry edge per D-50"). Add `expire_proposal(session, proposal_id, *, reason, expired_at, configured_timeout_minutes)` async function below `reject_proposal` (use `approve_proposal` lines 168-206 as the literal template). Fill the four test files listed in `<files>` with bodies that match the `<behavior>` cases. Use `pytest-asyncio` + the existing in-memory SQLCipher engine fixture pattern from `tests/unit/test_proposal_writer.py`. Use `freezegun.freeze_time` to pin time inside `test_proposal_writer_timeout.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_p3_alembic_round_trip.py tests/unit/test_p3_schema_additions.py tests/unit/test_proposal_writer_timeout.py tests/unit/test_proposal_state_machine_expired.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PROPOSAL_TIMEOUT_DEFAULT_MIN" src/gekko/agent/proposal_writer.py` returns at least one line and the value is the integer 30
    - `grep -n '("PENDING", "EXPIRED")' src/gekko/approval/proposals.py` returns exactly one match inside the STATE_TRANSITIONS frozenset
    - `grep -n "expires_at" src/gekko/agent/proposal_writer.py` returns at least one line (the kwarg on the INSERT site)
    - `grep -n "def expire_proposal" src/gekko/approval/proposals.py` returns one line
    - `uv run pytest tests/unit/test_p3_alembic_round_trip.py tests/unit/test_p3_schema_additions.py tests/unit/test_proposal_writer_timeout.py tests/unit/test_proposal_state_machine_expired.py -x` is green
    - Full P1 + P2 suite remains green: `uv run pytest tests/unit tests/integration -x --ignore=tests/unit/test_slack_action_dedup.py --ignore=tests/unit/test_expire_stale_proposals.py` (Wave 0 skipped stubs excluded — they are still skipped via the marker so the actual exclusion isn't needed; this argv just defends against the case where someone replaces the skip marker without filling the body)
    - Phase 1 walking-skeleton chain integration test still green: `uv run pytest tests/integration/test_slack_approval_to_executor.py -x`
  </acceptance_criteria>
  <done>ProposalWriter stamps expires_at per D-51; STATE_TRANSITIONS extends with PENDING -> EXPIRED; expire_proposal helper added; 4 unit tests green; P1+P2 suites green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: post_run_result() captures + persists slack_message_ts + slack_message_channel on the Proposal row (closes BLOCKER #1)</name>
  <files>
    src/gekko/reporter/slack.py,
    tests/unit/test_post_run_result_persists_ts.py
  </files>
  <read_first>
    src/gekko/reporter/slack.py:465-519 (post_run_result function — the chat_postMessage response is currently discarded at line 511; Task 4 captures it),
    src/gekko/db/models.py (Proposal model — slack_message_ts and slack_message_channel columns added in Task 2 of this plan),
    src/gekko/agent/proposal_writer.py (ProposalWriter writes the Proposal row before post_run_result is called; the row already exists when post_run_result fires — Task 4 UPDATEs it),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-RESEARCH.md §Pitfall 7 (the exact gap this task closes),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md D-53 (the chat.update of the expired card depends on these columns being populated)
  </read_first>
  <behavior>
    - `post_run_result()` currently calls `await slack_app.client.chat_postMessage(channel=user_id, blocks=blocks, text=fallback)` at line 511 and discards the response. Task 4 captures the response object and reads `response["ts"]` + `response["channel"]` (Slack returns the channel id even when posted to a user DM — it's the DM channel id, which is what `chat.update` requires).
    - Task 4 then opens a fresh SQLCipher session via `_get_session_factory(user_id)` and UPDATEs the Proposal row's `slack_message_ts` + `slack_message_channel` columns. The Proposal row's primary key is `proposal_id` which is available in `result["proposal"]["proposal_id"]` (TradeProposal schema field). Use `update(Proposal).where(Proposal.proposal_id == proposal_id).values(slack_message_ts=ts, slack_message_channel=ch)` in an SQLAlchemy 2.x async session.
    - The persistence MUST be best-effort: if the UPDATE fails (proposal row missing, race with deletion), log `log.warning("post_run_result.persist_ts_failed", proposal_id=..., error=...)` but do NOT re-raise — the Slack DM has already landed and the user has the card; missing ts/channel only degrades the EXPIRED chat.update to DM-only per D-53's fallback.
    - The `propose_no_action` branch (line 514) is unchanged — no Proposal row to update.
    - `test_post_run_result_persists_ts.py` exercises: (a) happy path — mock chat_postMessage to return `{"ts": "1234567890.000100", "channel": "D012ABCDEF"}`; call post_run_result with a propose_trade payload; assert the Proposal row's slack_message_ts and slack_message_channel are updated; (b) missing-proposal-row branch — pre-delete the Proposal row before calling post_run_result; assert the warning is logged but no exception propagates; (c) propose_no_action branch — no Proposal row exists; assert no UPDATE is attempted.
  </behavior>
  <action>
    Open `src/gekko/reporter/slack.py`. At line 511, change `await slack_app.client.chat_postMessage(channel=user_id, blocks=blocks, text=fallback)` to capture the response: `response = await slack_app.client.chat_postMessage(channel=user_id, blocks=blocks, text=fallback)`. Then add: `ts = response.get("ts")`; `ch = response.get("channel")`; `proposal_id = tp.proposal_id`. Add a new helper function `_persist_slack_message_coords(user_id, proposal_id, ts, channel)` near `post_run_result`: async, opens `_get_session_factory(user_id)`, runs the UPDATE, logs `log.warning(...)` and swallows IntegrityError / NoResultFound. Call the helper from post_run_result AFTER the chat_postMessage. Add structlog `log.info("post_run_result.ts_persisted", proposal_id=..., ts=..., channel=...)` for ops visibility. Fill `tests/unit/test_post_run_result_persists_ts.py` per `<behavior>` using respx for mocking chat.postMessage HTTP response and pytest-asyncio for the async test fixtures. The conftest fixture `proposal_factory` already exists (Phase 1) — reuse it to create the Proposal row before the test invokes post_run_result.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_post_run_result_persists_ts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "response\s*=\s*await\s+slack_app\.client\.chat_postMessage" src/gekko/reporter/slack.py` returns 1 match (the response is captured)
    - `grep -n "slack_message_ts" src/gekko/reporter/slack.py` returns at least 2 matches (the UPDATE statement values + the helper signature)
    - `grep -n "slack_message_channel" src/gekko/reporter/slack.py` returns at least 2 matches
    - `grep -n "def _persist_slack_message_coords" src/gekko/reporter/slack.py` returns 1 line
    - `tests/unit/test_post_run_result_persists_ts.py` passes (3 cases: happy path, missing-row, propose_no_action)
    - Phase 1 walking-skeleton chain still green: `uv run pytest tests/integration/test_slack_approval_to_executor.py -x`
  </acceptance_criteria>
  <done>post_run_result captures + persists slack_message_ts + slack_message_channel; Plan 03-04's chat.update path now has valid coords on every post-migration proposal</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration ↔ live DB | DDL touches the live SQLCipher DB; downgrade path must be tested or the operator loses the ability to roll back |
| ProposalWriter (LLM input via TradeProposal model_dump) ↔ DB row | `expires_at` is a SERVER-controlled clock value; the LLM CANNOT supply or influence it. ProposalWriter computes it via `datetime.now(UTC)` AFTER the TradeProposal is built. |
| Strategy.proposal_timeout_minutes (user-authored via chat or form) ↔ ProposalWriter | A malicious or buggy strategy file could set timeout_minutes to a huge value, effectively disabling expiry. The `Field(gt=0)` validator rejects zero/negative; an extreme value (e.g., 99999) is operator self-harm, not a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Alembic 0004 downgrade leaves orphaned rows in slack_action_dedup that reference dropped proposals.expires_at | mitigate | `downgrade()` drops `slack_action_dedup` table BEFORE dropping proposals.expires_at column (FK doesn't exist on slack_action_dedup → proposals, but drop ordering is documented as "table first" in the migration body) |
| T-03-01-02 | Tampering | Strategy.proposal_timeout_minutes from LLM-authored strategy file | mitigate | `Field(None, gt=0)` on Pydantic schema; ProposalWriter falls back to `PROPOSAL_TIMEOUT_DEFAULT_MIN=30` when None or missing |
| T-03-01-03 | Information disclosure | SlackActionDedup.__repr__ leaks slack_trigger_id to logs | mitigate | `__repr__` follows StrategyMetadata pattern (lines 256-261); excludes `slack_trigger_id` from repr output; structlog `_REDACT_KEYS` (Plan 01-02) already redacts `trigger_id` substring as a safety net |
| T-03-01-04 | Information disclosure | New User columns (timezone, quiet_hours_*) leaked via `User.__repr__` | accept | These are user-configurable lifestyle metadata, not credentials. Existing User.__repr__ pattern (lines 164-168) already excludes the credential-bearing columns; quiet_hours_* + timezone are safe to surface |
| T-03-01-05 | Repudiation | `expires_at` is a server-clock value the LLM cannot influence | mitigate | ProposalWriter computes `datetime.now(UTC)` AFTER `TradeProposal.model_validate(merged)` — there is no code path where LLM-authored bytes reach the expires_at field |
| T-03-01-06 | Denial of Service | An extreme `proposal_timeout_minutes` (e.g., 525600 — one year) effectively never expires | accept | Single-operator deployment per REG-03; operator self-harm. P5 (Trust Ladder) may layer a hard ceiling. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | not applicable | This plan introduces NO new package dependencies (per 03-RESEARCH.md §"No new third-party packages required") — the supply-chain threat is moot |
</threat_model>

<verification>
- `uv run alembic upgrade head` then `uv run alembic downgrade -1` then `uv run alembic upgrade head` succeeds (round-trip)
- `uv run pytest tests/unit/test_p3_alembic_round_trip.py tests/unit/test_p3_schema_additions.py tests/unit/test_proposal_writer_timeout.py tests/unit/test_proposal_state_machine_expired.py -x` green
- Phase 1 walking-skeleton chain still green: `uv run pytest tests/integration/test_slack_approval_to_executor.py -x`
- Phase 2 OrderGuard chain still green: `uv run pytest tests/integration/test_orderguard_paper_chain.py -x` (or equivalent — the OrderGuard chain integration test from Plan 02-02)
- ~30 Wave 0 stubs collect via `pytest --collect-only tests/`
</verification>

<success_criteria>
- Alembic 0004 round-trips up → down → up against a fresh SQLCipher DB
- `slack_action_dedup` table queryable; two UNIQUE indexes enforced (verifiable by attempting duplicate insert in a follow-up smoke test)
- `_PROPOSAL_STATUSES` accepts "EXPIRED" — `transition_status(... PENDING -> EXPIRED)` succeeds against the new edge
- `_EVENT_TYPES` accepts "expiration", "dedup_click", "edit_size", "daily_pnl" — `append_event(event_type=...)` succeeds for each
- `Strategy(proposal_timeout_minutes=15)` parses; `Strategy(proposal_timeout_minutes=0)` raises ValidationError; `Strategy(quiet_hours_start="22:00")` parses; `Strategy(quiet_hours_start="bad")` raises ValidationError
- ProposalWriter stamps `expires_at` with the correct value per D-51
- Phase 1 + Phase 2 audit-chain integrity preserved through the 0004 round-trip (audit `walk_chain` returns `[]` after the migration)
</success_criteria>

<output>
Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-01-SUMMARY.md` when done.
</output>
