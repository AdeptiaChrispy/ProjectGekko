---
phase: 03-production-hitl-ux-slack-block-kit-dashboard-fallback
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/gekko/approval/slack_handler.py
  - src/gekko/slack/interactivity.py
  - src/gekko/dashboard/app.py
  - src/gekko/dashboard/routes.py
  - src/gekko/dashboard/templates/login.html.j2
  - src/gekko/dashboard/templates/approvals_index.html.j2
  - src/gekko/dashboard/templates/_proposal_card.html.j2
  - src/gekko/dashboard/templates/edit_size_modal.html.j2
  - src/gekko/dashboard/templates/settings.html.j2
  - src/gekko/dashboard/static/tailwind.css
  - tests/unit/test_dashboard_login.py
  - tests/unit/test_dashboard_approvals.py
  - tests/unit/test_dashboard_edit_size.py
  - tests/unit/test_proposal_card_shared_partial.py
  - tests/unit/test_dashboard_middleware_order.py
  - tests/unit/test_edit_size_not_direct_broker.py
  - tests/integration/test_dashboard_approve_flow.py
  - tests/integration/test_dashboard_edit_size_happy.py
autonomous: true
requirements:
  - HITL-02
  - HITL-03
  - DASH-04

must_haves:
  truths:
    - "User unauthenticated to the dashboard is redirected to /login from any authenticated route per D-57"
    - "User submitting correct passphrase to /login receives a SessionMiddleware-signed cookie (HttpOnly + SameSite=Strict) and 303-redirects to next_url; wrong passphrase re-renders with .login-error"
    - "User on /approvals sees a HTMX-rendered list of PENDING + AWAITING_2ND_CHANNEL + EXPIRED proposals using the shared _proposal_card.html.j2 partial per D-55"
    - "User clicks Approve / Reject on /approvals; the server INSERTs a dedup row with source='dashboard' per D-56 + executes the same server logic as the Slack handlers; HTMX swaps the card with updated state"
    - "User clicks Edit-size on /approvals or Slack; both surfaces share the same handle_edit_size_submit drift check (D-54 + D-27 Knight Capital defense preserved)"
    - "Slack escalate button is a URL button pointing at /approvals/{proposal_id} per D-60"
    - "App restart invalidates all sessions per D-58 ephemeral cookie secret"
    - "SessionMiddleware is registered BEFORE the banner-mode middleware per Starlette reverse-order execution rule (AST gate)"
  artifacts:
    - path: "src/gekko/dashboard/app.py"
      provides: "SessionMiddleware registered with ephemeral os.urandom(32) secret + max_age=8h + HttpOnly + SameSite=Strict per D-57/D-58"
      contains: "SessionMiddleware"
    - path: "src/gekko/dashboard/routes.py"
      provides: "GET /login + POST /login + GET /approvals + POST /approvals/{id}/approve + POST /approvals/{id}/reject + GET /approvals/{id}/edit-size + POST /approvals/{id}/edit-submit + GET /settings + POST /settings + require_session dependency"
      contains: "require_session"
    - path: "src/gekko/dashboard/templates/_proposal_card.html.j2"
      provides: "Shared partial rendering PENDING / EXPIRED / AWAITING_2ND_CHANNEL proposal cards with HTMX hx-post + hx-target=closest article per UI-SPEC §Surface 2"
      contains: "hx-post"
    - path: "src/gekko/slack/interactivity.py"
      provides: "view-submission listener @app.view('edit_size_modal') delegating to handle_edit_size_submit + remove escalate action (D-60 URL button replaces)"
      contains: "edit_size_modal"
    - path: "src/gekko/approval/slack_handler.py"
      provides: "handle_edit_size opens views.open modal per D-54; handle_edit_size_submit runs drift check + dedup + state transition shared with dashboard"
      contains: "handle_edit_size_submit"
  key_links:
    - from: "src/gekko/dashboard/routes.py"
      to: "src/gekko/approval/dedup.py"
      via: "every dashboard handler INSERTs a dedup row with source='dashboard' before the state-machine transition"
      pattern: "source=\"dashboard\""
    - from: "src/gekko/approval/slack_handler.py"
      to: "src/gekko/dashboard/routes.py"
      via: "handle_edit_size_submit and the dashboard POST /approvals/{id}/edit-submit share a private helper _edit_size_logic(...)"
      pattern: "_edit_size_logic"
    - from: "src/gekko/reporter/slack.py"
      to: "src/gekko/dashboard/routes.py"
      via: "build_proposal_card's escalate button becomes a URL button -> {DASHBOARD_BASE_URL}/approvals/{proposal_id} per D-60"
      pattern: "DASHBOARD_BASE_URL"
---

<objective>
Land HITL-04 + DASH-04 + the Slack-side edit-size modal + the D-60 URL-button conversion + the dashboard auth surface in a single vertical slice: the operator can complete approve / reject / edit-size from EITHER Slack OR the dashboard, with cross-surface dedup via the D-56 source column, with the OrderGuard 2% drift check re-applied on every edit-size submit (preserving the Knight Capital defense), and with a localhost-only passphrase-cookie auth layer per D-57 + D-58.

Purpose: DASH-04 is the failure-mode-fallback for HITL-02 + HITL-03 — when Slack is down, the operator must still be able to approve / reject / edit / wait-out-expiry from the browser. D-54's Slack edit-size modal is the largest single net-new code surface in P3; pairing it with the dashboard equivalent in one plan honors the MVP-mode vertical-slice rule (one feature = one plan).

Output:
- `src/gekko/dashboard/app.py` EXTENDED: SessionMiddleware registration per D-57 / D-58.
- `src/gekko/dashboard/routes.py` EXTENDED: 9 new routes (login GET/POST, approvals GET, approvals/{id}/{approve|reject} POST, approvals/{id}/edit-size GET + edit-submit POST, settings GET/POST), `require_session` FastAPI dependency.
- 5 new Jinja2 templates: login, approvals_index, _proposal_card (shared partial), edit_size_modal, settings.
- Tailwind CSS additions per UI-SPEC.
- `src/gekko/approval/slack_handler.py` EXTENDED: `handle_edit_size` opens the Slack modal via `views_open`; `handle_edit_size_submit` is the view_submission handler delegating to a shared `_edit_size_logic` helper used by both Slack and dashboard.
- `src/gekko/slack/interactivity.py` EXTENDED: view-submission listener registration + escalate action removed.
- `src/gekko/reporter/slack.py`: escalate button converted to URL button per D-60.
- Unit + integration tests covering all auth + approval + edit-size + drift flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-RESEARCH.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-PATTERNS.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md

@src/gekko/dashboard/app.py
@src/gekko/dashboard/routes.py
@src/gekko/dashboard/templates/promote_to_live_modal.html.j2
@src/gekko/dashboard/templates/strategies_list.html.j2
@src/gekko/dashboard/templates/first_live_confirm.html.j2
@src/gekko/dashboard/templates/user_agreement.html.j2
@src/gekko/dashboard/static/tailwind.css
@src/gekko/approval/slack_handler.py
@src/gekko/approval/dedup.py
@src/gekko/slack/interactivity.py
@src/gekko/reporter/slack.py
@src/gekko/vault/passphrase.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionMiddleware + /login routes + require_session dependency + middleware-order AST gate</name>
  <files>
    src/gekko/dashboard/app.py,
    src/gekko/dashboard/routes.py,
    src/gekko/dashboard/templates/login.html.j2,
    src/gekko/dashboard/static/tailwind.css,
    tests/unit/test_dashboard_login.py,
    tests/unit/test_dashboard_middleware_order.py
  </files>
  <read_first>
    src/gekko/dashboard/app.py:205-247 (create_app + middleware registration + the existing `app.middleware('http')` decorator at lines 233-245 for banner_mode + kill_active state injection — Starlette executes middleware in REVERSE registration order; SessionMiddleware MUST be added FIRST via add_middleware so it runs LAST and sees the cookie),
    src/gekko/dashboard/routes.py:359-399 (kill_endpoint — universal Form POST + state-update template),
    src/gekko/dashboard/routes.py:539-541 (HTMX partial-return pattern),
    src/gekko/dashboard/templates/user_agreement.html.j2 (full-page extend-base shape — the closest analog for login.html.j2 per PATTERNS §1c),
    src/gekko/dashboard/templates/first_live_confirm.html.j2 (form + hidden-field + error pattern),
    src/gekko/vault/passphrase.py (vault.passphrase.verify_passphrase + set_passphrase — the auth substrate that /login POST validates against),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md §Surface 3 (login page contract — exact copy, classes, ARIA),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md D-57 + D-58 (auth contract — localhost-only + ephemeral per-restart secret)
  </read_first>
  <behavior>
    - In `create_app()`, after the FastAPI app is constructed and BEFORE the existing `app.middleware("http")` decorator block (lines 233-245), call `app.add_middleware(SessionMiddleware, secret_key=os.urandom(32).hex(), max_age=8 * 3600, https_only=False, same_site="strict", session_cookie="gekko_session")`. The secret is regenerated per app start (D-58 ephemeral); imports added at the top: `from starlette.middleware.sessions import SessionMiddleware`.
    - The middleware order matters per Starlette reverse-execution rule: `add_middleware` adds to the OUTER layer (executed last when running, first when responding). `@app.middleware("http")` adds inner middleware. We need SessionMiddleware to be the OUTERMOST so `request.session` is available to inner middleware and routes. Calling `add_middleware(SessionMiddleware, ...)` before the inner middleware registration is correct.
    - `GET /login` renders `login.html.j2` with `next_url=request.query_params.get("next", "/approvals")`.
    - `POST /login` accepts `passphrase: str = Form(...)` + `next: str = Form("/approvals")`. Validates via `vault.passphrase.verify_passphrase(passphrase)`; on success, `vault.passphrase.set_passphrase(passphrase)` + `request.session["gekko_user_id"] = settings.gekko_user_id` + `request.session["authenticated"] = True`; 303 redirect to `next` (sanitized to start with `/` and not contain `://` to prevent open-redirect). On failure: re-render `login.html.j2` with `error=True`.
    - `require_session(request: Request) -> str` async dependency: if `request.session.get("authenticated") is True` and `request.session.get("gekko_user_id") == settings.gekko_user_id`, returns the user_id. Otherwise raises `HTTPException(status_code=302, headers={"Location": f"/login?next={request.url.path}"})` per PATTERNS §2k.
    - Logging: NEVER log the passphrase value or the cookie value (Plan 01-02 redaction — extend `_REDACT_KEYS` with `passphrase` + `session` + `gekko_session` if not already).
    - `login.html.j2` follows UI-SPEC §Surface 3 verbatim: hero + subtitle + error block (conditional) + form (passphrase input + hidden next + submit) + footer. `autocomplete="off"` on form AND input. `aria-describedby="passphrase-help"` ties footer to input.
    - Tailwind CSS additions per UI-SPEC §Color: append the `.login-page`, `.login-hero`, `.login-subtitle`, `.login-form`, `.login-input`, `.login-error`, `.login-footer` rules below the existing P2 block in `static/tailwind.css`. Do NOT modify existing P1 + P2 rules.
    - `tests/unit/test_dashboard_login.py` covers: (a) GET /login renders form 200; (b) POST /login wrong passphrase re-renders with .login-error + no Set-Cookie; (c) POST /login correct passphrase mints Set-Cookie + 303 redirect to next_url; (d) GET /approvals without cookie → 302 redirect to /login?next=/approvals; (e) GET /approvals with valid cookie → 200; (f) restart app (new SessionMiddleware secret) → old cookie no longer valid → 302 to /login per D-58 ephemeral secret semantics.
    - `tests/unit/test_dashboard_middleware_order.py` is the AST gate per RESEARCH §"Critical Discrepancy" / VALIDATION row: parse `src/gekko/dashboard/app.py` and assert that the `app.add_middleware(SessionMiddleware, ...)` call appears BEFORE any `@app.middleware("http")` decorator in source order (Starlette adds add_middleware-registered middlewares first → they run OUTERMOST → request.session is available to inner middleware + routes). If `app.middleware("http")` runs first in source order, the inner middleware wouldn't see the session unless SessionMiddleware is wrapping it via the deeper `add_middleware` insertion path — the safe and explicit pattern is "add_middleware first in source order."
  </behavior>
  <action>
    Open `src/gekko/dashboard/app.py`. Add `from starlette.middleware.sessions import SessionMiddleware` + `import os` (if not already) at the top. Inside `create_app()`, IMMEDIATELY after the FastAPI app is constructed (and BEFORE the existing `app.middleware("http")` decorator block at lines 233-245), insert: `app.add_middleware(SessionMiddleware, secret_key=os.urandom(32).hex(), max_age=8 * 3600, https_only=False, same_site="strict", session_cookie="gekko_session")`. Confirm the comment cites D-57 + D-58. Open `src/gekko/dashboard/routes.py`. At the top of the routes module (above the existing `@router.get(...)` decorators), add the `require_session` async dependency per PATTERNS §2k. Add `GET /login` (renders login.html.j2) + `POST /login` (validates passphrase + mints session + 303 redirect) handlers. Validate `next` form parameter: must start with `/` and not contain `://` — otherwise default to `/approvals` (open-redirect defense). Create `src/gekko/dashboard/templates/login.html.j2` per UI-SPEC §Surface 3 verbatim, extending `base.html.j2`. Append the new Tailwind utility classes from UI-SPEC §Color (login-page, login-hero, login-form, login-input, login-error, login-footer) to `src/gekko/dashboard/static/tailwind.css` BELOW the existing P2 block. Fill `tests/unit/test_dashboard_login.py` with the 6 cases from `<behavior>` using `httpx.ASGITransport(app=create_app())` (Plan 01-09 lock — bypasses production lifespan + sets `app.state.*` directly per Plan 01-09 D-13). Fill `tests/unit/test_dashboard_middleware_order.py` with an `ast.parse` walk asserting `add_middleware(SessionMiddleware...)` appears in source order before any `@app.middleware("http")` decorator. Extend `_REDACT_KEYS` in `src/gekko/config.py` (or wherever the credential-redaction processor lives) to include `passphrase`, `session`, `gekko_session` if not already.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dashboard_login.py tests/unit/test_dashboard_middleware_order.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SessionMiddleware" src/gekko/dashboard/app.py` returns at least one line with `add_middleware`
    - `grep -nE "os\\.urandom\\(32\\)" src/gekko/dashboard/app.py` returns one match (D-58 ephemeral secret)
    - `grep -n "same_site=\"strict\"" src/gekko/dashboard/app.py` returns one match
    - `grep -n "max_age=8\\s*\\*\\s*3600" src/gekko/dashboard/app.py` returns one match (8-hour idle expiry per D-57)
    - `grep -n "require_session" src/gekko/dashboard/routes.py` returns at least one line (definition)
    - `grep -n '"/login"' src/gekko/dashboard/routes.py` returns at least 2 lines (GET + POST)
    - `tests/unit/test_dashboard_login.py::test_get_login` passes
    - `tests/unit/test_dashboard_login.py::test_post_login_success` passes
    - `tests/unit/test_dashboard_login.py::test_post_login_wrong_passphrase` passes
    - `tests/unit/test_dashboard_middleware_order.py` passes (AST gate)
  </acceptance_criteria>
  <done>SessionMiddleware + /login + require_session + AST gate green; login template + tailwind utilities land per UI-SPEC §Surface 3</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /approvals + shared _proposal_card.html.j2 + POST approve/reject + escalate URL-button conversion (D-60)</name>
  <files>
    src/gekko/dashboard/routes.py,
    src/gekko/dashboard/templates/approvals_index.html.j2,
    src/gekko/dashboard/templates/_proposal_card.html.j2,
    src/gekko/dashboard/static/tailwind.css,
    src/gekko/reporter/slack.py,
    tests/unit/test_dashboard_approvals.py,
    tests/unit/test_proposal_card_shared_partial.py,
    tests/integration/test_dashboard_approve_flow.py
  </files>
  <read_first>
    src/gekko/dashboard/routes.py:622-744 (live_confirm_post — the closest existing analog for /approvals/{id}/approve: transaction → re-read row → check status → transition → append_event → commit → background dispatch),
    src/gekko/dashboard/templates/strategies_list.html.j2 (HTMX list-iteration + per-row hx-post + chip rendering pattern — the literal analog for approvals_index.html.j2),
    src/gekko/approval/slack_handler.py _approve_workflow (Plan 03-02 — the Slack-side server logic; dashboard handlers extract a shared helper _approve_logic and call it from both sides),
    src/gekko/reporter/slack.py:286-315 (the existing escalate button block — replaced by URL button per D-60),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md §Surface 2 (the proposal-card-actions HTML + hx-post + hx-target=closest article + hx-disable-elt="this" pattern + chip rendering for [LIVE]/[EXPIRED]/[AWAITING 2ND CHANNEL]),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md D-55 + D-56 + D-60 (mirror contract + source column + URL button conversion)
  </read_first>
  <behavior>
    - `GET /approvals` (requires `require_session`): SELECT all proposals with `status IN ('PENDING', 'AWAITING_2ND_CHANNEL', 'EXPIRED')` for the user; render `approvals_index.html.j2` which iterates and includes `_proposal_card.html.j2` per UI-SPEC §Surface 2. Empty state renders the `🤖 No proposals pending — agent is waiting...` div role="status".
    - `_proposal_card.html.j2` is the SHARED partial — single Jinja template renders proposal-card HTML for the dashboard; the equivalent Slack render goes through `build_proposal_card` Python builder (D-55 — Python and Jinja are two transports of the SAME schema; render parity is enforced by `test_proposal_card_shared_partial.py` snapshot comparison).
    - `POST /approvals/{id}/approve` (requires `require_session`): same shape as PATTERNS §2j (kill_endpoint). Open `_get_session_factory(user_id)`; transaction; call `claim_action(... action_id="approve_proposal", actor_slack_user_id=None, actor_gekko_user_id=user_id, source="dashboard")`. On `"duplicate"`: re-read row → render `_proposal_card.html.j2` with current status (dashboard idempotency posture per D-56: "the visual state update IS the feedback"). On `"first_write"`: transition `PENDING → APPROVED` (or `PENDING → AWAITING_2ND_CHANNEL` for first-live per the existing P2 D-32 branching) + `append_event(event_type="approval", ...)` (existing Plan 01-08 / Plan 02-06 audit event types — no new event_type needed); commit; background `asyncio.create_task(execute_proposal(decision_id=...))`. Return `_proposal_card.html.j2` with the updated status → HTMX swap.
    - `POST /approvals/{id}/reject`: identical shape with `action_id="reject_proposal"` + transition `PENDING → REJECTED` + `append_event(event_type="rejection", ...)`.
    - The approve / reject server logic is extracted into private helpers `_approve_logic(session, user_id, decision_id) -> tuple[bool, ProposalRow]` and `_reject_logic(session, user_id, decision_id)` in a NEW `src/gekko/approval/actions.py` module so both Slack handler (Plan 03-02) and dashboard handlers can call the same code. The Slack handler in `slack_handler.py` is REFACTORED to call `_approve_logic` for the post-dedup body, preserving the AWAITING_2ND_CHANNEL divert (P2 D-32 lock). The refactor is structural only — no behavior change.
    - Wait — the refactor surface in this task could be too large. SCOPING DOWN: this task does NOT refactor `_approve_workflow` from Plan 03-02; instead it INLINES the post-dedup logic into the dashboard handlers (with explicit comment annotations citing the shared semantics). The shared-helper refactor is deferred to a future plan. This preserves the 50% context budget for this plan.
    - `src/gekko/reporter/slack.py:286-315` — the escalate button block (currently action_id-based, calling handle_escalate_stub): replace with a URL button per D-60. New shape: `{"type": "button", "text": {"type": "plain_text", "text": "Open in dashboard"}, "url": f"{settings.dashboard_url}/approvals/{proposal_id}", "style": "primary"}`. No `action_id`. The `handle_escalate_stub` in `slack_handler.py:434-452` is DELETED or converted to a no-op deprecation warning (URL buttons don't round-trip to handlers). The corresponding entry in `slack/interactivity.py:53-56` is REMOVED.
    - Tailwind additions per UI-SPEC §Color: append `.proposal-card`, `.proposal-card-header`, `.proposal-card-ticker`, `.proposal-card-rationale`, `.proposal-card-evidence`, `.proposal-card-actions`, `.proposal-card.expired` (overlay), `.chip-expired`, `.chip-awaiting`, `.empty-state`, `.empty-state-glyph` to `static/tailwind.css` BELOW the P2 block + the Task 1 login block.
    - `test_proposal_card_shared_partial.py`: snapshot test asserting that the Jinja-rendered `_proposal_card.html.j2` for a given proposal row produces the equivalent block payload as `build_proposal_card(...)` (i.e., the rationale, evidence, ticker, side, qty, account_mode chip selection match across both transports). Use the `build_proposal_card` output as the source of truth; assert the Jinja-rendered HTML contains all the same identifiers.
    - `tests/unit/test_dashboard_approvals.py`: (a) GET /approvals without cookie → 302; (b) GET /approvals with cookie → 200 + the list of PENDING + EXPIRED + AWAITING_2ND_CHANNEL cards; (c) empty state when zero proposals.
    - `tests/integration/test_dashboard_approve_flow.py`: full HTMX cycle: login, GET /approvals, POST /approvals/{id}/approve, assert background `execute_proposal` task spawned (via monkeypatched `asyncio.create_task`), assert HTMX response is the updated `_proposal_card.html.j2` partial with [APPROVED] visual.
  </behavior>
  <action>
    Open `src/gekko/dashboard/routes.py`. Add `GET /approvals` + `POST /approvals/{proposal_id}/approve` + `POST /approvals/{proposal_id}/reject` per `<behavior>`. Each handler uses `Depends(require_session)`. Open `_get_session_factory(user_id)` and call `claim_action` from `gekko.approval.dedup` (Plan 03-02 NEW module) BEFORE any state-machine work. The dedup row uses `actor_slack_user_id=None`, `actor_gekko_user_id=user_id`, `source="dashboard"`. The duplicate branch re-reads the proposal status and re-renders `_proposal_card.html.j2` with the current state (no error toast, no flash message — the swap IS the feedback per D-56). Create `src/gekko/dashboard/templates/approvals_index.html.j2` extending `base.html.j2` per UI-SPEC §Surface 2 (live banner + kill banner + standard nav + page hero `Pending Approvals` + iteration over proposals including `_proposal_card.html.j2` per row + empty state). Create `src/gekko/dashboard/templates/_proposal_card.html.j2` per UI-SPEC §Surface 2 exact HTML structure: `<article class="proposal-card {{ 'expired' if status == 'EXPIRED' else '' }}" aria-label="..." data-proposal-id="...">` + header section with chips + rationale + evidence `<details>` + conditional actions block (PENDING shows buttons; EXPIRED shows the status line; AWAITING_2ND_CHANNEL shows the live-confirm link). Open `src/gekko/reporter/slack.py:286-315` and REPLACE the escalate button with the URL button per D-60. DELETE `handle_escalate_stub` from `slack_handler.py:434-452` (or convert to a no-op + structlog warning + remove from __all__). REMOVE the escalate action registration from `slack/interactivity.py:53-56`. Append Tailwind utilities per UI-SPEC §Color to `static/tailwind.css`. Fill `tests/unit/test_dashboard_approvals.py` + `tests/unit/test_proposal_card_shared_partial.py` + `tests/integration/test_dashboard_approve_flow.py` per `<behavior>` using `httpx.ASGITransport` + the existing P1 cassette fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dashboard_approvals.py tests/unit/test_proposal_card_shared_partial.py tests/integration/test_dashboard_approve_flow.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "(approve_proposal_endpoint|reject_proposal_endpoint|GET /approvals|@router.get\\(\"/approvals\"\\)|@router.post\\(\"/approvals/" src/gekko/dashboard/routes.py | head -5` returns multiple route registrations
    - `grep -nE 'source="dashboard"' src/gekko/dashboard/routes.py` returns at least 2 matches (approve + reject) per D-56
    - `grep -n "_proposal_card.html.j2" src/gekko/dashboard/templates/approvals_index.html.j2` returns at least 1 match (the shared partial include)
    - `grep -n 'hx-post="/approvals/' src/gekko/dashboard/templates/_proposal_card.html.j2` returns at least 2 matches (approve + reject buttons)
    - `grep -n 'hx-target="closest article"' src/gekko/dashboard/templates/_proposal_card.html.j2` returns at least 1 match per UI-SPEC §Surface 2
    - `grep -n "handle_escalate_stub" src/gekko/approval/slack_handler.py` returns 0 matches OR returns 1 match in a no-op deprecation comment
    - `grep -n "Open in dashboard" src/gekko/reporter/slack.py` returns 1 match (D-60 URL-button copy from UI-SPEC §"Copywriting Contract")
    - `grep -nE 'url=f?".*\\{.*\\}/approvals/' src/gekko/reporter/slack.py` returns at least 1 match (the URL button URL construction)
    - `tests/unit/test_dashboard_approvals.py` passes (2 cases)
    - `tests/unit/test_proposal_card_shared_partial.py` passes (snapshot parity between Jinja + Slack builder)
    - `tests/integration/test_dashboard_approve_flow.py` passes (full HTMX cycle)
    - Phase 1 walking-skeleton + Phase 2 OrderGuard chain still green
  </acceptance_criteria>
  <done>/approvals index + shared partial + approve/reject endpoints + D-60 URL button + escalate stub removal; full HTMX cycle integration test green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: handle_edit_size + view_submission modal + dashboard /approvals/{id}/edit-size + drift check shared + AST gate</name>
  <files>
    src/gekko/approval/slack_handler.py,
    src/gekko/slack/interactivity.py,
    src/gekko/dashboard/routes.py,
    src/gekko/dashboard/templates/edit_size_modal.html.j2,
    src/gekko/dashboard/templates/settings.html.j2,
    tests/unit/test_dashboard_edit_size.py,
    tests/unit/test_edit_size_not_direct_broker.py,
    tests/integration/test_dashboard_edit_size_happy.py
  </files>
  <read_first>
    src/gekko/approval/slack_handler.py:414-431 (handle_edit_size_stub — currently DMs "coming in Phase 3"; gets REPLACED with the modal-open + submission flow per D-54),
    src/gekko/slack/interactivity.py:29-56 (existing @slack_app.action listener pattern — same shape for @slack_app.view("edit_size_modal")),
    src/gekko/dashboard/templates/promote_to_live_modal.html.j2 (the closest existing modal partial — same .modal-backdrop > .modal > .modal-body > form structure per PATTERNS §1c row for edit_size_modal),
    src/gekko/execution/checks/_qty_price.py (the OrderGuard 2% drift check — the source of truth for the drift formula; D-54 says re-run THIS check inside handle_edit_size_submit),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-RESEARCH.md §3 Pattern 3 (the literal views_open payload shape; private_metadata round-trip; response_action='errors' validation pattern),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md §Surface 1 (Slack modal layout) + §Surface 2 (dashboard HTMX modal — same shape, different transport),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md D-54 (the full edit-size contract — modal opens, server validates drift, modal re-renders on drift error, single-interaction approve on pass)
  </read_first>
  <behavior>
    - `handle_edit_size(ack, body, client)` in `slack_handler.py` REPLACES the existing stub at lines 414-431. After `await ack()` (Pitfall 3 — first call must be ack), load the Proposal row + TradeProposal payload, extract `ref_price` (from `tp.limit_price or tp.stop_price or _fetch_ref_price(tp.ticker)`), `target_notional_usd`, `original_qty`, `ticker`, `side`. Call `await client.views_open(trigger_id=body["trigger_id"], view={...})` per RESEARCH §3 Pattern 3: the modal carries `callback_id="edit_size_modal"`, `private_metadata=json.dumps({"decision_id": ..., "ref_price": str(ref_price), "target_notional_usd": str(target_notional), "original_qty": str(original_qty), "ticker": ticker, "side": side, "response_url": body.get("response_url")})`, blocks (input + section + context per UI-SPEC §Surface 1).
    - `@slack_app.view("edit_size_modal")` listener in `slack/interactivity.py`: extracts `meta = json.loads(view["private_metadata"])`; `raw_qty = view["state"]["values"]["qty_block"]["qty"]["value"]` (note: block_id `qty_block`, action_id `qty` per UI-SPEC §Surface 1). Parses `Decimal(raw_qty)`; on InvalidOperation, `await ack({"response_action": "errors", "errors": {"qty_block": "Please enter a numeric quantity."}})`. Compute `new_notional = new_qty * Decimal(meta["ref_price"])`; `drift_pct = abs(new_notional - Decimal(meta["target_notional_usd"])) / Decimal(meta["target_notional_usd"])`. If `drift_pct > Decimal("0.02")` (the D-27 / D-54 invariant): `await ack({"response_action": "errors", "errors": {"qty_block": f"Drift {drift_pct:.2%} exceeds the 2% safety bound..."}})` per RESEARCH §3 Pattern 3 + UI-SPEC §Surface 1 error copy. NO state change, NO audit event. On pass: `await ack()` (empty body closes modal); spawn `asyncio.create_task(_edit_size_submit_workflow(...))`.
    - `_edit_size_submit_workflow(decision_id, new_qty, slack_user_id, meta)` in `slack_handler.py`: open transaction; call `claim_action(... action_id="edit_size", actor_slack_user_id=slack_user_id, actor_gekko_user_id=gekko_user_id, source="slack")`; on `"duplicate"` return; on `"first_write"`: re-load Proposal row; compute old_qty, old_notional, drift_pct (defensively re-validate); `append_event(event_type="edit_size", payload=normalize_decimals({"old_qty", "new_qty", "old_notional", "new_notional", "drift_pct", "actor": slack_user_id}))`; update `proposal.qty` (via TradeProposal.model_copy + payload_json re-serialize per PATTERNS §3); `transition_status(PENDING -> APPROVED)`; commit; spawn `asyncio.create_task(execute_proposal(...))`. The Slack card is updated via `chat.update` to the [APPROVED] visual using the existing `build_proposal_card` path.
    - Dashboard side: `GET /approvals/{proposal_id}/edit-size` (requires `require_session`) returns the `edit_size_modal.html.j2` HTMX partial with the same pre-loaded fields (ref_price, target_notional_usd, original_qty, ticker, side). `POST /approvals/{proposal_id}/edit-submit` accepts `qty: Decimal = Form(...)`; runs THE SAME drift validation as the Slack handler; on drift fail: re-render `edit_size_modal.html.j2` with `error=True` + computed drift_pct (HTMX swaps the modal in place per UI-SPEC §Surface 2); on pass: dedup INSERT (`source="dashboard"`) + edit_size audit event + qty update + transition + background execute_proposal + return `_proposal_card.html.j2` with [APPROVED] visual.
    - `_drift_check(qty, ref_price, target_notional_usd) -> Decimal` helper extracted into `src/gekko/approval/actions.py` (NEW) so both Slack and dashboard call the same function — guards against the two surfaces drifting in their drift math. Returns `drift_pct`; caller compares to `Decimal("0.02")`.
    - `edit_size_modal.html.j2` follows UI-SPEC §Surface 2 exactly: `.modal-backdrop > .modal > .modal-body > form` with `hx-post="/approvals/{{ proposal_id }}/edit-submit"`, qty number input, ref_price + target_notional readout, drift-error block when `error=True`, Cancel link via `hx-get="/modal/close"`.
    - `settings.html.j2` lands HERE (UI-SPEC §Surface 5) since `/settings` is a small additional dashboard route that shares the same template patterns. `GET /settings` (require_session) loads `User.quiet_hours_*` + `User.timezone` + the list of `zoneinfo.available_timezones()`. `POST /settings` (with `timezone: str = Form(...)`, `quiet_hours_start: str = Form("")`, `quiet_hours_end: str = Form("")`) validates: timezone in `zoneinfo.available_timezones()`; quiet_hours_start + quiet_hours_end both blank OR both set; on validation failure, re-render with `.login-error`-style block; on success, UPDATE User row and re-render with success state.
    - AST gate `test_edit_size_not_direct_broker.py`: parse `src/gekko/approval/slack_handler.py` AND `src/gekko/dashboard/routes.py`; assert no call site to `place_order` outside `src/gekko/execution/executor.py`. The invariant: edit-size collapses to PENDING → APPROVED + executor dispatch; never directly hits the broker (which would bypass OrderGuard). Mirrors the Plan 02-06 `test_alpaca_live_construction_locked.py` AST-walk pattern.
    - `tests/unit/test_dashboard_edit_size.py::test_drift_rejected`: POST /approvals/{id}/edit-submit with qty that produces 2.5% drift → 200 response containing the modal partial with the red error block + the existing qty input pre-populated; NO state change in the DB; NO audit event appended.
    - `tests/integration/test_dashboard_edit_size_happy.py`: login → GET /approvals → click Edit-size → POST /edit-submit with valid qty → assert dedup row inserted (source=dashboard), edit_size audit event appended, proposal.qty updated, transition to APPROVED, background execute_proposal task spawned.
  </behavior>
  <action>
    Open `src/gekko/approval/slack_handler.py`. REPLACE `handle_edit_size_stub` (lines 414-431) with the full `handle_edit_size` per RESEARCH §3 Pattern 3 + UI-SPEC §Surface 1. The function opens the modal via `client.views_open(trigger_id=..., view=...)`. Add a new module-level `_edit_size_submit_workflow(decision_id, new_qty, slack_user_id, meta)` async function that does the post-modal-submission dedup + transition + dispatch. Open `src/gekko/slack/interactivity.py`. Add `@slack_app.view("edit_size_modal")` listener delegating to a new `_edit_size_submit` view-submission handler in `slack_handler.py` that runs the drift check + `ack(response_action="errors")` on failure or `ack()` + spawn workflow on pass per `<behavior>`. Create `src/gekko/approval/actions.py` with `_drift_check(qty: Decimal, ref_price: Decimal, target_notional_usd: Decimal) -> Decimal` helper (returns the drift_pct). Open `src/gekko/dashboard/routes.py`. Add `GET /approvals/{proposal_id}/edit-size` + `POST /approvals/{proposal_id}/edit-submit` per `<behavior>`. Add `GET /settings` + `POST /settings` for the quiet-hours config form per UI-SPEC §Surface 5. Create `src/gekko/dashboard/templates/edit_size_modal.html.j2` per UI-SPEC §Surface 2 (HTMX modal swap pattern). Create `src/gekko/dashboard/templates/settings.html.j2` per UI-SPEC §Surface 5. Fill `tests/unit/test_dashboard_edit_size.py` (drift_rejected case) + `tests/unit/test_edit_size_not_direct_broker.py` (AST gate) + `tests/integration/test_dashboard_edit_size_happy.py` (full HTMX cycle) per `<behavior>`. The AST gate parses both `slack_handler.py` and `routes.py`, walks every `Call(func=Name("place_order"))` or `Call(func=Attribute(attr="place_order"))`, asserts zero matches outside `src/gekko/execution/executor.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dashboard_edit_size.py tests/unit/test_edit_size_not_direct_broker.py tests/integration/test_dashboard_edit_size_happy.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def handle_edit_size" src/gekko/approval/slack_handler.py` returns at least 1 match (handle_edit_size, not the deleted _stub)
    - `grep -n "handle_edit_size_stub" src/gekko/approval/slack_handler.py` returns 0 matches OR 1 match in a deprecation comment
    - `grep -n "views_open" src/gekko/approval/slack_handler.py` returns at least 1 match (the modal-open call)
    - `grep -n "@slack_app.view(\"edit_size_modal\")" src/gekko/slack/interactivity.py` returns 1 match
    - `grep -n '"/approvals/{proposal_id}/edit-size"\\|"/approvals/{proposal_id}/edit-submit"' src/gekko/dashboard/routes.py` returns at least 2 matches
    - `grep -nE "(0\\.02|Decimal\\(\"0\\.02\"\\))" src/gekko/approval/actions.py` returns at least 1 match (the drift threshold is locked at 2%)
    - `grep -nE '"/settings"' src/gekko/dashboard/routes.py` returns at least 2 matches (GET + POST)
    - `tests/unit/test_dashboard_edit_size.py::test_drift_rejected` passes
    - `tests/unit/test_edit_size_not_direct_broker.py` passes (AST gate)
    - `tests/integration/test_dashboard_edit_size_happy.py` passes
    - Phase 1 walking-skeleton + Phase 2 OrderGuard chain still green
  </acceptance_criteria>
  <done>Slack modal + view_submission + dashboard /approvals/{id}/edit-size end-to-end; drift check shared via _drift_check helper; AST gate enforces no direct broker calls from edit-size; settings form lands per UI-SPEC §Surface 5</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ FastAPI dashboard | SessionMiddleware-signed cookie carries `gekko_user_id`; the secret rotates per app restart (D-58 ephemeral); HttpOnly + SameSite=Strict + Secure=False (HTTP-on-localhost OK per D-57) |
| Slack `view_submission` ↔ handle_edit_size_submit | slack-bolt signature-verify gate (P1 lock); the modal's `private_metadata` round-trips through Slack but is signed-by-our-process → re-loaded fields (ref_price, target_notional_usd) are TRUSTED only because we wrote them; never re-derive without server-side re-validation |
| Edit-size qty (operator input) ↔ OrderGuard 2% drift check | The drift check is the SAFETY BOUNDARY — operator-supplied qty is untrusted; only qty within 2% of `target_notional_usd` is acceptable. D-27 Knight Capital defense preserved. |
| URL button on Slack proposal card ↔ /approvals/{id} | The URL is server-constructed at card-render time using `settings.dashboard_url`; an attacker controlling `DASHBOARD_BASE_URL` env var could redirect Slack click traffic, but env vars are operator-controlled (single-operator deployment per REG-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Spoofing | Attacker forges a session cookie | mitigate | itsdangerous-signed cookie via SessionMiddleware; D-58 ephemeral secret means a leaked cookie from one restart is dead after the next restart |
| T-03-05-02 | Tampering | Open-redirect via `next` form parameter on /login POST | mitigate | Validate `next` starts with `/` and does not contain `://` before issuing 303 redirect; default to /approvals on validation failure |
| T-03-05-03 | Repudiation | Operator denies clicking Approve on dashboard | mitigate | dedup row records `actor_gekko_user_id` from the session cookie; `source="dashboard"` in audit; `approval` event lands in audit chain |
| T-03-05-04 | Information disclosure | Passphrase logged in structlog | mitigate | `_REDACT_KEYS` extended with `passphrase`, `session`, `gekko_session` per Task 1 |
| T-03-05-05 | Information disclosure | Session cookie value logged | mitigate | Same as above — structlog redaction processor scrubs `gekko_session` substring |
| T-03-05-06 | Denial of Service | Brute-force passphrase attempts | accept | localhost-only binding per D-57 + REG-03; v1 deployment threat model assumes physical security; rate-limit deferred to P6 |
| T-03-05-07 | Elevation of privilege | Operator-supplied qty in edit-size bypasses OrderGuard | mitigate | `_drift_check` is the SAME 2% threshold as OrderGuard's `_qty_price.py` (D-27); AST gate `test_edit_size_not_direct_broker.py` enforces edit-size goes through state machine + executor (which calls OrderGuard) never direct broker |
| T-03-05-08 | Tampering | Slack `view_submission` payload tampering with private_metadata | mitigate | private_metadata is server-constructed at views.open time; submission re-parses then re-validates drift server-side; the modal's ref_price + target_notional_usd are RE-FETCHED from the Proposal row before final commit (defense in depth — payload_json is the source of truth, not private_metadata) |
| T-03-05-09 | Spoofing | Cross-site request forgery on POST /approvals/{id}/approve | mitigate | SameSite=Strict cookie + HTMX `hx-post` from same-origin (no third-party JS allowed per CSP `script-src 'self'`); CSRF tokens deferred to P6 multi-user (single-operator deployment makes the threat low) |
| T-03-05-SC | Tampering | npm/pip/cargo installs | not applicable | No new dependencies — starlette, itsdangerous, slack-bolt all P1-audited |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_dashboard_login.py tests/unit/test_dashboard_middleware_order.py tests/unit/test_dashboard_approvals.py tests/unit/test_proposal_card_shared_partial.py tests/unit/test_dashboard_edit_size.py tests/unit/test_edit_size_not_direct_broker.py -x` green
- `uv run pytest tests/integration/test_dashboard_approve_flow.py tests/integration/test_dashboard_edit_size_happy.py -x` green
- Phase 1 walking-skeleton + Phase 2 OrderGuard + Phase 2 live-mode flow still green: `uv run pytest tests/integration -x` (except for Wave-0 stubs from plans 03-06..03-08 that remain skipped via the marker)
- CSP `script-src 'self'` is preserved: every new template uses HTMX attributes only (no inline `<script>`, no inline `onclick`) — visual verification by inspecting the 5 new template files
- AST gate red-lights any future regression: `test_edit_size_not_direct_broker.py` parses both slack_handler.py + routes.py and asserts zero direct `place_order` calls outside executor.py
</verification>

<success_criteria>
- An unauthenticated browser visit to `http://localhost:8000/approvals` redirects to `/login?next=/approvals`
- Correct passphrase submission mints a session cookie (HttpOnly + SameSite=Strict + 8h max-age) and 303-redirects to `/approvals`
- `/approvals` renders the HTMX-rendered list of PENDING / EXPIRED / AWAITING_2ND_CHANNEL proposals using the shared `_proposal_card.html.j2` partial
- Clicking Approve on /approvals INSERTs a dedup row with `source="dashboard"`, transitions PENDING → APPROVED, spawns background execute_proposal, and HTMX-swaps the card with the [APPROVED] visual
- Clicking Edit-size in Slack opens the modal; submitting a qty within 2% of target_notional_usd closes the modal + transitions to APPROVED + dispatches the executor; submitting a qty with >2% drift re-renders the modal with the `❌ Drift X% exceeds the 2% safety bound` error block without any state change
- Slack's escalate button now opens `/approvals/{proposal_id}` in the operator's browser per D-60
- App restart invalidates all sessions per D-58 (forced re-login at the next page load)
- The dashboard `/settings` form lets the operator configure quiet hours per user-level controls (per-strategy override is DEFERRED to P6 per UI-SPEC §Surface 5 scope)
</success_criteria>

<output>
Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-05-SUMMARY.md` when done.
</output>
