---
phase: 03-production-hitl-ux-slack-block-kit-dashboard-fallback
plan: 06
type: execute
wave: 5
depends_on:
  - 03-01
  - 03-03
  - 03-04
files_modified:
  - src/gekko/reporter/daily_pnl.py
  - src/gekko/scheduler/jobs.py
  - src/gekko/dashboard/app.py
  - src/gekko/execution/executor.py
  - tests/unit/test_daily_pnl_aggregation.py
  - tests/unit/test_daily_pnl_respects_quiet.py
  - tests/unit/test_severity_tier_dm.py
  - tests/unit/test_executor_error_dms_coverage.py
autonomous: true
requirements:
  - REPT-01

must_haves:
  truths:
    - "User receives a Slack DM at 16:30 America/New_York every trading day containing today's gross P&L, per-strategy breakdown, open positions count, fills count, errors count per UI-SPEC §Surface 6"
    - "Daily P&L DM is suppressed on market-closed days (weekends, NYSE holidays) per D-59"
    - "Daily P&L DM respects user quiet hours per D-48 (daily_pnl is in the ROUTINE category set; the 16:30 ET cron fires regardless, but if 16:30 ET falls within the user's quiet window, the DM defers to window-open like other routine informational categories). The 'fixed at 4:30pm post-close ET regardless' phrasing in CONTEXT.md §E refers to the cron TRIGGER firing time, NOT to the DM bypass classification."
    - "Severity-tier emoji prefix applied to executor-error DMs: ⚠️ informational (MarketClosed), ❌ error (BrokerOrderError + OrderGuardRejected), 🚫 kill-state changes"
    - "Phase-1 carry-forward audit: every silent FAILED transition in executor.py + kill_switch.py has an operator-visible DM with the right severity tier"
  artifacts:
    - path: "src/gekko/reporter/daily_pnl.py"
      provides: "send_daily_pnl_digest(*, user_id) async cron entry point + audit-log aggregation query + Block Kit digest builder per UI-SPEC §Surface 6 + D-59 market-closed-day gate"
      contains: "async def send_daily_pnl_digest"
    - path: "src/gekko/scheduler/jobs.py"
      provides: "register_daily_pnl_cron(scheduler, *, user_id) registrar adding CronTrigger(hour=16, minute=30, timezone=ZoneInfo('America/New_York')) job"
      contains: "register_daily_pnl_cron"
    - path: "src/gekko/dashboard/app.py"
      provides: "lifespan registers daily_pnl cron job alongside expire-stale sweep + P1 schedule jobs"
      contains: "register_daily_pnl_cron"
    - path: "src/gekko/execution/executor.py"
      provides: "Severity-tier emoji prefix on MarketClosed (⚠️), BrokerOrderError (❌), and any existing silent-FAILED transition that lacks a DM"
      contains: "⚠️|❌|🚫"
  key_links:
    - from: "src/gekko/scheduler/jobs.py"
      to: "src/gekko/reporter/daily_pnl.py"
      via: "add_job uses 'gekko.reporter.daily_pnl:send_daily_pnl_digest' module:fn string per Plan 01-09 pickle-safe lock"
      pattern: "gekko.reporter.daily_pnl:send_daily_pnl_digest"
    - from: "src/gekko/reporter/daily_pnl.py"
      to: "src/gekko/execution/executor.py"
      via: "send_daily_pnl_digest calls _send_slack_dm_respecting_quiet_hours(user_id, blocks, category='daily_pnl')"
      pattern: "category=\"daily_pnl\""
    - from: "src/gekko/reporter/daily_pnl.py"
      to: "pandas_market_calendars"
      via: "D-59 market-closed-day gate: nyse.schedule(start_date=today, end_date=today).empty -> early return"
      pattern: "pandas_market_calendars"
---

<objective>
Land REPT-01 end-to-end: a daily P&L digest DM fired at 16:30 America/New_York every trading day (per D-48 + D-59), built from today's audit-log fills + cap_rejections + errors aggregated per strategy, plus severity-tier emoji prefixes (⚠️ / ❌ / 🚫) on the existing executor-error DM paths from P1 + P2. The carry-forward audit confirms every silent-FAILED transition in `executor.py` + `kill_switch.py` has an operator-visible DM.

Purpose: REPT-01 is the rest of the "operational visibility" surface — the operator wants a daily summary of what the agent did, with positive / negative P&L glyphs and a clean error-count signal. The severity-tier emoji prefix unifies the DM language across MarketClosed (informational, not a panic) vs BrokerOrderError (error, attention needed) vs kill-state changes (highest priority). The cron job is the only NEW DM channel P3 ships — every other DM category exists from P1 + P2.

Output:
- `src/gekko/reporter/daily_pnl.py` NEW module with `send_daily_pnl_digest(*, user_id)` async cron entry point + audit-log SELECT + per-strategy aggregation + D-59 NYSE schedule gate + Block Kit digest builder per UI-SPEC §Surface 6.
- `src/gekko/scheduler/jobs.py` EXTENDED: `register_daily_pnl_cron(scheduler, *, user_id)` registrar per PATTERNS §2f.
- `src/gekko/dashboard/app.py` EXTENDED: lifespan registers the daily_pnl cron alongside the Phase 1 jobs and the Plan 03-04 expire-stale sweep.
- `src/gekko/execution/executor.py` EXTENDED: severity-tier emoji prefixes on existing MarketClosed (line 454) + BrokerOrderError (line 654) DMs + audit-coverage verification.
- 4 unit tests covering aggregation, market-closed-day gate, severity tiers, and executor-error DM coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-RESEARCH.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-PATTERNS.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md

@src/gekko/reporter/slack.py
@src/gekko/scheduler/jobs.py
@src/gekko/dashboard/app.py
@src/gekko/execution/executor.py
@src/gekko/execution/kill_switch.py
@src/gekko/audit/log.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: send_daily_pnl_digest module + audit-log aggregation + D-59 NYSE schedule gate + Block Kit builder</name>
  <files>
    src/gekko/reporter/daily_pnl.py,
    tests/unit/test_daily_pnl_aggregation.py,
    tests/unit/test_daily_pnl_respects_quiet.py
  </files>
  <read_first>
    src/gekko/reporter/slack.py:198-323 (build_proposal_card — the Block Kit list-of-dicts builder; the digest builder follows the same shape: header → section → section → context → actions),
    src/gekko/execution/executor.py:334-432 (on_fill_event — the audit-log SELECT + payload-parse pattern via json.loads(row.payload_json); the digest aggregation reads events with the same shape),
    src/gekko/scheduler/jobs.py existing pandas_market_calendars usage (the calendar instance is lru_cache'd per Plan 01-08 D-15; reuse the same `is_market_open` pattern — but D-59 needs `schedule(start_date=today, end_date=today)` not `is_market_open`, since 16:30 ET is after-hours by construction),
    src/gekko/execution/executor.py:188-216 (_send_slack_dm seam + _send_slack_dm_respecting_quiet_hours wrapper from Plan 03-03 — daily_pnl is a BYPASS category that ALWAYS fires per D-48 informational-category clarification in CONTEXT.md §E),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md §Surface 6 (the EXACT Block Kit shape — header / gross_pnl section / per-strategy section / counts context / actions footer; the `${value:+,.2f}` USD format string; the 📈 / 📉 sign glyphs),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md D-48 (daily P&L bypass categories) + D-59 (NYSE schedule gate)
  </read_first>
  <behavior>
    - `send_daily_pnl_digest(*, user_id: str) -> bool` is the cron entry point. Returns True if a DM fired, False if skipped (market-closed day or other reason).
    - D-59 NYSE schedule gate (first check at function entry): `import pandas_market_calendars as mcal`; `nyse = mcal.get_calendar("NYSE")`; `today_et = datetime.now(ZoneInfo("America/New_York")).date()`; `schedule = nyse.schedule(start_date=today_et, end_date=today_et)`. If `schedule.empty` (weekend, holiday) → log `structlog.info("daily_pnl.market_closed_skip", date=str(today_et))` and return False. No DM sent.
    - Audit-log aggregation: SELECT all `Event` rows where `user_id=user_id` AND `inserted_at` falls within `[today_et 00:00 ET, today_et 23:59 ET]` (converted to UTC for the WHERE clause); parse `payload_json` via `json.loads`. Aggregate:
      - `fills_count`: count of events with `event_type == "fill"`.
      - `gross_pnl_usd`: sum of fill `pnl_usd` field if present (Plan 01-08 fill audit-event payload may or may not carry pnl — if missing, the digest renders `gross_pnl_usd = Decimal("0.00")` and a footnote line "P&L computed lazily — wire pnl_usd in Plan TBD"; but for v1 the SUM of `(side == "BUY" ? -1 : +1) * qty * fill_price` over today's fills gives the realized P&L for trades that round-tripped within the day; positions held overnight are excluded per swing-horizon scope).
      - `per_strategy`: dict[strategy_name, {pnl_usd, fills_count}].
      - `errors_count`: count of events with `event_type == "error"`.
      - `cap_rejections_count`: count of events with `event_type == "cap_rejection"`.
      - `open_positions_count`: SELECT distinct ticker from FILLED proposals with `account_mode IN ('PAPER','LIVE')` for this user — a rough open-positions count; precise open-position tracking is P6 scope.
    - Block Kit digest construction per UI-SPEC §Surface 6 exact shape: header `📊 Daily P&L — {today_iso}`; section with `gross_pnl_usd` line using `${value:+,.2f}` format + 📈/📉 sign glyph; section with per-strategy bullet list (or `*Per-strategy P&L:* _no fills today_` when empty); context with open-positions / fills / errors counts; actions footer with `Open dashboard` URL button → `{settings.dashboard_url}/approvals`.
    - DM dispatch: `await _send_slack_dm_respecting_quiet_hours(user_id, ..., category="daily_pnl")`. `daily_pnl` is a ROUTINE category per D-48 strict reading (the Suppressed list explicitly enumerates "Daily P&L summary"). It is NOT added to the bypass set. Behavior: the cron TRIGGER fires at 16:30 ET on every trading day (D-59 NYSE gate); the DM goes through the same quiet-hours-aware wrapper as routine fill confirmations and gets deferred to window-open when 16:30 ET falls within the user's quiet window. For the typical operator (US quiet hours 10pm-7am), 16:30 ET is comfortably outside the window and the DM lands immediately. CONTEXT.md §E's "fixed at 4:30pm post-close ET regardless" describes the cron trigger, NOT the DM bypass classification — clarified during plan-checker iteration 1 (2026-06-17).
    - The DM uses `client.chat_postMessage(channel=settings.slack_user_id, blocks=...)` via a new `_send_slack_dm_blocks(user_id, blocks)` helper (since `_send_slack_dm` is text-only); the new helper is also added to `executor.py` next to `_send_slack_dm` so the existing identity-split seam is preserved (PATTERNS §2e).
    - Audit event: at the end of the function (after successful DM), append `daily_pnl` event to the audit log with payload `{date: today_iso, gross_pnl: <str>, fills_count, errors_count}` per UI-SPEC §Surface 6 state transitions + D-45 audit-shape contract.
    - Tests in `test_daily_pnl_aggregation.py`: (a) seed today's events (3 fills across 2 strategies + 1 error); assert per-strategy aggregation matches; assert gross_pnl_usd format string; (b) zero fills today → `_no fills today_` branch; (c) negative gross P&L → 📉 glyph; positive → 📈; (d) `pandas_market_calendars.schedule(...).empty == True` (mock by patching `nyse.schedule`) → return False, no DM sent (D-59 gate).
    - `test_daily_pnl_respects_quiet.py`: monkeypatch `_resolve_quiet_hours` to return True; call `send_daily_pnl_digest`; assert the DM is DEFERRED (routine category respects quiet hours per D-48). Second case: `_resolve_quiet_hours` returns False; DM fires immediately. Third case: D-59 NYSE-closed-day gate already returned False before quiet-hours-check even runs (the function returns early, no DM, no defer).
  </behavior>
  <action>
    Create `src/gekko/reporter/daily_pnl.py` with `send_daily_pnl_digest(*, user_id: str) -> bool` per `<behavior>`. Module imports: `pandas_market_calendars`, `zoneinfo.ZoneInfo`, `datetime`, `Decimal`, `json`. Module-local `_get_session_factory(user_id)` shim per PATTERNS §2d. Internal helpers: `_aggregate_today_events(session, user_id, today_et) -> DigestData` (dataclass with fills_count, gross_pnl_usd, per_strategy dict, errors_count, cap_rejections_count, open_positions_count); `_build_digest_blocks(digest_data, today_iso) -> list[dict]` (the Block Kit list-of-dicts builder per UI-SPEC §Surface 6); `_send_slack_dm_blocks(user_id, blocks, *, category)` (the Block Kit equivalent of `_send_slack_dm`, identity-split-safe via `settings.slack_user_id`, routes through the quiet-hours-aware wrapper). After successful DM, append the `daily_pnl` audit event. Open `src/gekko/execution/executor.py`: add `_send_slack_dm_blocks(user_id, blocks, *, category)` next to `_send_slack_dm` (it mirrors `_send_slack_dm` lines 188-216 but accepts `blocks` instead of `text` AND consults `_resolve_quiet_hours` per category — bypass categories fire immediately, routine categories defer when in window). Do NOT modify the bypass set from Plan 03-03 Task 2 — `daily_pnl` stays in the ROUTINE category per D-48. Fill `tests/unit/test_daily_pnl_aggregation.py` + `tests/unit/test_daily_pnl_respects_quiet.py` per `<behavior>` using freezegun for time pinning (pin to a known trading day like 2026-06-17 16:30 ET) and respx for mocking the chat.postMessage HTTP call.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_daily_pnl_aggregation.py tests/unit/test_daily_pnl_respects_quiet.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def send_daily_pnl_digest" src/gekko/reporter/daily_pnl.py` returns exactly 1 line
    - `grep -nE "(claude_agent_sdk|anthropic)" src/gekko/reporter/daily_pnl.py` returns NO matches
    - `grep -nE "(pandas_market_calendars|mcal)" src/gekko/reporter/daily_pnl.py` returns at least 1 match (D-59 NYSE schedule gate)
    - `grep -nE "(daily_pnl).*(category|class)" src/gekko/reporter/daily_pnl.py` returns at least 1 match (the DM call is annotated with `category="daily_pnl"`)
    - `grep -n "daily_pnl" src/gekko/execution/executor.py` returns NO match in the bypass set of `_send_slack_dm_respecting_quiet_hours` (it is a routine category per D-48)
    - `grep -nE "(📈|📉|📊)" src/gekko/reporter/daily_pnl.py` returns at least 3 matches (the three Surface 6 emojis)
    - `tests/unit/test_daily_pnl_aggregation.py` passes (4 cases)
    - `tests/unit/test_daily_pnl_respects_quiet.py` passes
    - Phase 1 + Phase 2 chain still green
  </acceptance_criteria>
  <done>Daily P&L digest builder + market-closed-day gate + audit aggregation + bypass-quiet semantics green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: register_daily_pnl_cron + lifespan registration</name>
  <files>
    src/gekko/scheduler/jobs.py,
    src/gekko/dashboard/app.py
  </files>
  <read_first>
    src/gekko/scheduler/jobs.py register_expire_stale_sweep (from Plan 03-04 Task 1 — the same shape, different trigger type),
    src/gekko/scheduler/jobs.py:107-154 (schedule_strategy_daily — the existing CronTrigger pattern),
    src/gekko/dashboard/app.py lifespan (Plan 03-04 Task 3 just added register_expire_stale_sweep here — this task adds register_daily_pnl_cron alongside),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-PATTERNS.md §2f (the APScheduler registrar template — both registrars use coalesce=True, max_instances=1)
  </read_first>
  <behavior>
    - `register_daily_pnl_cron(scheduler: AsyncIOScheduler, *, user_id: str) -> str` adds a `CronTrigger(hour=16, minute=30, timezone=ZoneInfo("America/New_York"))` job with `id=f"daily-pnl-{user_id}"`, `replace_existing=True`, `coalesce=True`, `max_instances=1`. The `module:fn` string ref is `"gekko.reporter.daily_pnl:send_daily_pnl_digest"`. Returns the job_id.
    - In `dashboard/app.py` lifespan, AFTER the `register_expire_stale_sweep` call from Plan 03-04 Task 3, add: `register_daily_pnl_cron(app.state.scheduler, user_id=settings.gekko_user_id)`. Both registrars use the SAME P1 scheduler instance per PATTERNS §2f warning.
    - No NEW tests in this task — the cron registration + 16:30 ET firing is a manual-only verification per VALIDATION §"Manual-Only Verifications" row 5 ("Observe one real-day DM at 16:30 America/New_York"). The cron-job-existence side is verified by an extension of `test_sweep_persistence.py` (from Plan 03-04) OR by reusing the existing dashboard-startup integration test to assert `scheduler.get_job("daily-pnl-{user_id}")` returns non-None after lifespan startup.
  </behavior>
  <action>
    Open `src/gekko/scheduler/jobs.py`. Add `register_daily_pnl_cron(scheduler, *, user_id)` after `register_expire_stale_sweep`. Use `CronTrigger(hour=16, minute=30, timezone=ZoneInfo("America/New_York"))` per UI-SPEC §Surface 6 + D-48 fixed schedule. Open `src/gekko/dashboard/app.py` lifespan. After the existing `register_expire_stale_sweep` call from Plan 03-04, add `register_daily_pnl_cron(app.state.scheduler, user_id=settings.gekko_user_id)`. Add a structlog `log.info("scheduler.daily_pnl_registered", job_id=...)` line for ops visibility. No new test file is added; an assertion is added to the existing dashboard lifespan integration test (or `test_sweep_persistence.py` from Plan 03-04 if it exercises the same fixture) confirming the daily-pnl job lives in the scheduler after startup.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_sweep_persistence.py -x &amp;&amp; uv run python -c "from gekko.scheduler.jobs import register_daily_pnl_cron; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def register_daily_pnl_cron" src/gekko/scheduler/jobs.py` returns 1 line
    - `grep -n "CronTrigger" src/gekko/scheduler/jobs.py | grep -n "hour=16" | head -1` returns at least 1 match (16:30 ET schedule)
    - `grep -n "register_daily_pnl_cron" src/gekko/dashboard/app.py` returns at least 1 line in lifespan
    - The register call lands AFTER `register_expire_stale_sweep` in source order: `grep -nA 10 "register_expire_stale_sweep" src/gekko/dashboard/app.py | grep -q "register_daily_pnl_cron"`
    - `uv run pytest tests/integration/test_sweep_persistence.py -x` still green (extended to assert daily_pnl job presence)
    - Phase 1 walking-skeleton chain + Phase 2 + Plan 03-04 sweep chain green
  </acceptance_criteria>
  <done>register_daily_pnl_cron lands in scheduler/jobs.py; lifespan wires it alongside expire-stale sweep; cron job persists across restart</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Severity-tier emoji prefix on existing executor + kill-switch DMs + carry-forward audit</name>
  <files>
    src/gekko/execution/executor.py,
    tests/unit/test_severity_tier_dm.py,
    tests/unit/test_executor_error_dms_coverage.py
  </files>
  <read_first>
    src/gekko/execution/executor.py around line 454 (the MarketClosed branch DM — currently `f"Market closed; deferred order for {tp.ticker}. P7 will add retry-on-open."` per PATTERNS §2l),
    src/gekko/execution/executor.py around line 654 (the BrokerOrderError branch DM — currently `f"Broker order failed: {exc}"` or equivalent — verify exact line at execute time),
    src/gekko/execution/executor.py cap_rejection branch (Plan 02-02 — emit a DM on OrderGuard reject_code? If yes, gets ❌; if no, this is the carry-forward gap),
    src/gekko/execution/kill_switch.py:145-300 (kill DMs — already DM operator on kill; gets 🚫 prefix),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-PATTERNS.md §2l (severity-tier glyph map),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md §"Copywriting Contract" → "Symbol vocabulary" (locked glyphs)
  </read_first>
  <behavior>
    - MarketClosed DM (around line 454) — prepend `⚠️ ` (informational): `f"⚠️ Market closed; deferred order for {tp.ticker}. P7 will add retry-on-open."` per PATTERNS §2l.
    - BrokerOrderError DM (around line 654) — prepend `❌ ` (error): `f"❌ Broker order failed for {tp.ticker}: {exc}"` (preserve existing message content; prefix only).
    - OrderGuardRejected / cap_rejection DM (from Plan 02-02) — if a DM fires today on cap_rejection, prepend `❌`; if no DM currently fires (carry-forward gap), THIS task adds the DM with `❌ OrderGuard rejected {tp.ticker} {tp.side}: {reject_code}` per `_send_slack_dm(user_id, ...)` direct call (category="executor_error" — bypass per D-48). The carry-forward note in CONTEXT.md confirms cap_rejection paths in executor.py are the v1 silent-failure mode this plan closes.
    - kill_switch.py DMs (any operator-visible DM in the kill flow) — prepend `🚫`: `f"🚫 Kill switch activated for user {user_id}. Cancelled {n_cancelled} of {n_total} open orders."` (preserve existing message; prefix only).
    - The AST gate `test_quiet_hours_dm_gate.py` (Plan 03-03 Task 2) already enforces every `_send_slack_dm` call site is annotated with its bypass-category designation — this task verifies the executor-error DMs are annotated correctly AND the new prefix character is present.
    - Carry-forward audit: walk `src/gekko/execution/executor.py` and `src/gekko/execution/kill_switch.py`; identify every code path that transitions a Proposal to `FAILED` status (via `transition_status(..., to_status="FAILED")`); assert each path has a corresponding `_send_slack_dm` call with the operator notification. If any silent-FAILED transition exists, ADD the DM with the appropriate severity-tier prefix per the glyph map.
    - `test_severity_tier_dm.py`: triggers MarketClosed branch via the in-memory cassette; asserts the DM contains `⚠️`. Triggers BrokerOrderError via mock raise; asserts the DM contains `❌`. Triggers kill_switch via the in-memory kill_endpoint; asserts the DM contains `🚫`.
    - `test_executor_error_dms_coverage.py`: AST-walk both `executor.py` and `kill_switch.py`; for every `Call(func=Name("transition_status"))` where `to_status="FAILED"`, assert the same try/except block contains a `_send_slack_dm` call. (This is the carry-forward coverage gate per CONTEXT.md "Carry-forward audit" + STATE.md "P3 backlog item executor-error → Slack surfacing on MarketClosed/BrokerOrderError".)
  </behavior>
  <action>
    Open `src/gekko/execution/executor.py`. Locate the MarketClosed DM (around line 454); prepend `⚠️ ` to the existing message string. Locate the BrokerOrderError DM (around line 654); prepend `❌ `. If the cap_rejection branch (from Plan 02-02) does NOT currently emit a DM to the operator, ADD `await _send_slack_dm(user_id, f"❌ OrderGuard rejected {tp.ticker} {tp.side}: {reject_code}")` immediately after the audit event is appended. The `# bypass-category: executor_error` annotation from Plan 03-03 Task 2 stays in place above each direct `_send_slack_dm` call. Open `src/gekko/execution/kill_switch.py`. Locate any operator-visible DM (Plan 02-05 kill flow); prepend `🚫 ` to the message. Run an AST-walk audit of both files: for every `transition_status(..., to_status="FAILED")` call, verify the same try/except block contains a `_send_slack_dm` call within ~10 lines. If any path lacks the DM, ADD it with the right severity prefix and category annotation. Fill `tests/unit/test_severity_tier_dm.py` with 3 cases (MarketClosed ⚠️, BrokerOrderError ❌, kill 🚫) using respx for mocking Slack HTTP. Fill `tests/unit/test_executor_error_dms_coverage.py` as an AST-walk over both files asserting every silent-FAILED path has a sibling DM.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_severity_tier_dm.py tests/unit/test_executor_error_dms_coverage.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "(⚠️|⚠ ) Market closed" src/gekko/execution/executor.py` returns at least 1 match
    - `grep -nE "❌ Broker order" src/gekko/execution/executor.py` returns at least 1 match
    - `grep -nE "❌ OrderGuard" src/gekko/execution/executor.py` returns at least 1 match
    - `grep -n "🚫" src/gekko/execution/kill_switch.py` returns at least 1 match
    - `tests/unit/test_severity_tier_dm.py` passes (3 cases)
    - `tests/unit/test_executor_error_dms_coverage.py` passes (AST gate: every FAILED transition has a sibling DM)
    - The Plan 03-03 `test_quiet_hours_dm_gate.py` AST gate still passes (every `_send_slack_dm` call site classified)
    - Phase 1 walking-skeleton + Phase 2 OrderGuard chain + Plan 03-04 sweep chain still green
  </acceptance_criteria>
  <done>Severity-tier emoji prefixes land on every executor + kill DM path; carry-forward audit AST gate confirms zero silent-FAILED transitions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APScheduler cron ↔ daily_pnl handler | Same trust boundary as Plan 03-04 sweep; SQLCipher jobstore + passphrase-on-start. |
| pandas_market_calendars NYSE data ↔ D-59 gate | The library bundles the NYSE calendar locally; no network call at gate-check time; offline-safe per Plan 01-08 lock. |
| Audit-log SELECT for aggregation ↔ DM render | Read-only side of the audit chain; no chain-mutating side effects in the digest handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | Manipulated NYSE schedule causes the gate to misfire | accept | pandas_market_calendars is a P1 + P2 audited dependency; the library bundles the calendar locally; no remote attack surface |
| T-03-06-02 | Information disclosure | Daily P&L DM leaks today's positions / P&L to a leaked Slack account | accept | The operator IS the recipient by construction; Slack account compromise is out of scope (single-operator deployment per REG-03) |
| T-03-06-03 | Denial of Service | Audit-log aggregation walks N events; N could be unbounded over time | accept | Daily window scope means N is bounded by today's events (~tens to hundreds in v1 swing-horizon scope); per-strategy aggregation is in-memory dict; performance is non-issue |
| T-03-06-04 | Repudiation | Operator denies receiving the daily summary | mitigate | `daily_pnl` audit event lands per UI-SPEC §Surface 6 state transitions with `{date, gross_pnl, fills_count, errors_count}` payload; `walk_chain` verifies integrity |
| T-03-06-05 | Spoofing | Attacker tampers with Slack mention to redirect digest | mitigate | Identity-split seam (`settings.slack_user_id` from environment) is the authoritative channel; `_send_slack_dm_blocks` reuses this seam |
| T-03-06-06 | Elevation of privilege | Future contributor wires daily_pnl through `_send_slack_dm_respecting_quiet_hours` with the wrong category | mitigate | AST gate `test_quiet_hours_dm_gate.py` (Plan 03-03 Task 2) classifies every `_send_slack_dm` call site; daily_pnl in the bypass set per UI-SPEC §Surface 6 |
| T-03-06-07 | Tampering | Severity-tier emoji prefixes accidentally stripped in a future refactor | mitigate | `test_severity_tier_dm.py` is the regression gate — any future change to the executor DM text without the prefix red-lights the test |
| T-03-06-SC | Tampering | npm/pip/cargo installs | not applicable | No new dependencies — pandas_market_calendars + APScheduler + slack-bolt all P1 + P2 audited |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_daily_pnl_aggregation.py tests/unit/test_daily_pnl_respects_quiet.py tests/unit/test_severity_tier_dm.py tests/unit/test_executor_error_dms_coverage.py -x` green
- `uv run pytest tests/integration/test_sweep_persistence.py -x` green (extended to assert daily_pnl job presence)
- AST gates from Plans 03-03 + 03-04 still green: `uv run pytest tests/unit/test_quiet_hours_dm_gate.py tests/unit/test_expiry_no_sdk_import.py tests/unit/test_transition_status_callers.py -x`
- Phase 1 + Phase 2 + Plan 03-04 chains green
- `grep -nE "(claude_agent_sdk|anthropic)" src/gekko/reporter/daily_pnl.py` returns NO matches
</verification>

<success_criteria>
- A real trading day's 16:30 America/New_York triggers a Slack DM containing today's gross P&L (📈/📉 sign-prefixed), per-strategy breakdown, open positions count, fills count, errors count per UI-SPEC §Surface 6 (manual demo per VALIDATION §"Manual-Only Verifications" row 5)
- A weekend or NYSE holiday at 16:30 ET fires the cron but the handler returns early via D-59 — no DM, structlog logs `daily_pnl.market_closed_skip`
- A quiet-hours-configured operator (e.g., 09:00-17:30 ET sleep schedule) still receives the digest at 16:30 ET (bypass category per D-48 informational clarification + UI-SPEC §Surface 6)
- MarketClosed DMs now lead with ⚠️; BrokerOrderError DMs lead with ❌; OrderGuard cap_rejection DMs lead with ❌; kill-state-change DMs lead with 🚫
- The carry-forward Phase-1 backlog item ("executor-error → Slack notification") is CLOSED: every FAILED transition in `executor.py` + `kill_switch.py` has an operator-visible DM with the correct severity tier
- AST gates enforce: (a) `daily_pnl` module is a deterministic Python firewall (zero `claude_agent_sdk` imports); (b) every `_send_slack_dm` call site is annotated; (c) every FAILED transition has a sibling DM
</success_criteria>

<output>
Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-06-SUMMARY.md` when done.
</output>
