---
phase: 03-production-hitl-ux-slack-block-kit-dashboard-fallback
plan: 07
type: execute
wave: 6
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
  - 03-06
files_modified:
  - tests/integration/test_p3_walking_skeleton.py
  - README.md
  - .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md
autonomous: true
requirements:
  - HITL-02
  - HITL-03
  - HITL-05
  - DASH-04
  - REPT-01

must_haves:
  truths:
    - "End-to-end cassette test passes: PENDING proposal -> Slack dup-click deduped -> operator edits size in Slack modal -> drift check passes -> state transition -> executor fires -> fill DM -> daily P&L cron captures it"
    - "Dashboard-fallback cassette test passes: Slack down -> /login -> /approvals -> approve from dashboard -> same chain ends in fill"
    - "Expiry cassette test passes: PENDING proposal with expires_at in the past -> sweep fires -> chat.update -> separate DM"
    - "Audit chain walk_chain() returns [] across all three cassette scenarios — chain integrity preserved end-to-end"
    - "README has a Phase 3 demo section with the manual-operator recipe per the Phase 1+2 pattern"
    - "deferred-items.md captures the 5 manual-only verifications (real Slack retry, 60s sweep wall-clock, DST corner, browser session, real 16:30 ET daily DM) for the operator's end-of-phase demo"
  artifacts:
    - path: "tests/integration/test_p3_walking_skeleton.py"
      provides: "Cassette-style end-to-end integration test exercising the full P3 happy path + dashboard fallback + expiry; the load-bearing assertion is audit-chain integrity + exactly-once execution"
      contains: "test_p3_happy_path|test_dashboard_fallback|test_expiry_chain"
    - path: "README.md"
      provides: "Phase 3 demo recipe section appended below the existing Phase 1 + Phase 2 demo sections"
      contains: "Phase 3"
    - path: ".planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md"
      provides: "5 manual-only verification rows tracked for operator demo per VALIDATION §Manual-Only Verifications"
      contains: "Manual-Only Verifications"
  key_links:
    - from: "tests/integration/test_p3_walking_skeleton.py"
      to: "src/gekko/approval/dedup.py"
      via: "the dup-click branch of the cassette exercises claim_action"
      pattern: "claim_action"
    - from: "tests/integration/test_p3_walking_skeleton.py"
      to: "src/gekko/approval/expiry.py"
      via: "the expiry chain in the cassette invokes expire_stale_proposals directly (no APScheduler in the cassette)"
      pattern: "expire_stale_proposals"
    - from: "tests/integration/test_p3_walking_skeleton.py"
      to: "src/gekko/reporter/daily_pnl.py"
      via: "the cassette runs send_daily_pnl_digest at the end to verify the daily summary captures the day's fills"
      pattern: "send_daily_pnl_digest"
---

<objective>
Land the end-of-phase walking-skeleton cassette test exercising the full P3 happy path + dashboard fallback + expiry chain, plus the README demo recipe + deferred-items.md for the manual-operator verifications. This plan is the closure on Phase 3 — every prior plan's primitive is exercised in a single end-to-end scenario, and the audit chain `walk_chain()` returns `[]` proving chain integrity across the full HITL UX surface.

Purpose: The Phase 1 + Phase 2 closing pattern (Plan 01-09 + Plan 02-07) established that the final plan ships an integration cassette + README recipe + deferred-items.md for the operator demo. P3 follows the same shape. The cassette is the executable verification of the phase's 5 requirements; the README + deferred-items are the operator's bridge from automated tests to the wall-clock manual demo.

Output:
- `tests/integration/test_p3_walking_skeleton.py` NEW: three integration test functions covering happy-path, dashboard-fallback, and expiry chains, all asserting audit-chain integrity.
- `README.md` EXTENDED: Phase 3 demo recipe section below the existing Phase 1 + Phase 2 sections.
- `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md` NEW: tracks the 5 manual-only verifications per VALIDATION §"Manual-Only Verifications" for the operator's end-of-phase demo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-VALIDATION.md

@README.md
@tests/integration/test_slack_approval_to_executor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: P3 walking-skeleton cassette — happy path (Slack approve -> fill -> daily P&L)</name>
  <files>
    tests/integration/test_p3_walking_skeleton.py
  </files>
  <read_first>
    tests/integration/test_slack_approval_to_executor.py (Phase 1 walking-skeleton — the literal template; asyncio.create_task monkeypatch drain pattern; in-memory SQLCipher engine fixture; mocked place_order + chat.postMessage),
    tests/integration/test_dedup_race.py (Plan 03-02 — the dedup-race cassette; the dup-click branch is woven into the P3 happy path here),
    tests/integration/test_dashboard_edit_size_happy.py (Plan 03-05 — the dashboard edit-size cassette; the edit-size branch is woven into the P3 happy path here),
    src/gekko/approval/dedup.py (claim_action — the dup-click primitive under test),
    src/gekko/reporter/daily_pnl.py (send_daily_pnl_digest — the end-of-day cron under test),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-VALIDATION.md (the per-task verification map names the test_p3_walking_skeleton.py file)
  </read_first>
  <behavior>
    - Test `test_p3_happy_path` exercises: (1) seed a Strategy + User (with `quiet_hours_start=None` to avoid suppressing the chain); (2) build a PENDING TradeProposal via the real ProposalWriter (uses Plan 03-01 `expires_at` stamping); (3) simulate a Slack approve action by calling `handle_approve(ack, body, client)` directly with a constructed body; (4) simulate a Slack dup-click by calling `handle_approve` AGAIN with the same body — assert the dedup table catches the second and ephemeral fires (via mocked `_post_ephemeral`); (5) the FIRST approve proceeds: assert the proposal transitions to APPROVED, the executor fires (mocked `place_order` returns a broker_order_id), the on_fill_event lands (simulated via direct call to `on_fill_event`), and the fill DM uses the quiet-hours-respecting wrapper (Plan 03-03); (6) at end-of-day, call `send_daily_pnl_digest(user_id=...)` directly — assert it returns True, the Block Kit digest contains 1 fill in the per-strategy aggregate, and a `daily_pnl` audit event lands.
    - Assert: `walk_chain` returns `[]` after the full scenario — every event hash chains correctly.
    - Assert: `place_order` is called EXACTLY ONCE (no double-execution under the dup-click).
    - Mock surfaces: `_post_ephemeral` (Slack ephemeral POST), `place_order` (broker), `chat_postMessage` + `chat_update` (Slack outbound), `pandas_market_calendars` (NYSE schedule returns non-empty for today so the digest fires).
    - Edit-size sub-scenario woven into the happy path: BEFORE the approve fires, simulate the operator clicking Edit-size: call `handle_edit_size(ack, body, client)` to open the modal (mocked `views_open`); simulate the operator submitting a new qty within 2% drift; verify `handle_edit_size_submit` runs the drift check, dedups via `claim_action(action_id="edit_size")`, transitions PENDING -> APPROVED, dispatches the executor. The approve flow above becomes redundant in this branch — pick ONE: either approve-with-edit OR approve-without-edit; the cassette structure uses two separate test functions (`test_p3_happy_path_approve` + `test_p3_happy_path_with_edit_size`) for clarity.
  </behavior>
  <action>
    Create `tests/integration/test_p3_walking_skeleton.py`. Use `pytest.mark.asyncio` + the in-memory SQLCipher engine fixture pattern from `tests/integration/test_slack_approval_to_executor.py` (Plan 01-08 / Plan 01-09 cassette). Monkeypatch: `gekko.execution.executor.place_order` (return mock broker_order_id), `gekko.approval.slack_handler._post_ephemeral` (capture ephemeral payloads), `gekko.execution.executor._send_slack_dm` (capture DMs), `slack_app.client.chat_update` (capture card updates), `pandas_market_calendars.get_calendar("NYSE").schedule` (return non-empty DataFrame for today). Use `asyncio.create_task` drain pattern from Plan 01-08. Build the test_p3_happy_path_approve test per `<behavior>` (steps 1-6 with the approve-without-edit flow); build the test_p3_happy_path_with_edit_size test exercising the edit-size branch instead of plain approve. Assert all stated invariants (chain integrity, exactly-once execution, dedup row count, daily_pnl audit event presence).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_p3_walking_skeleton.py::test_p3_happy_path_approve tests/integration/test_p3_walking_skeleton.py::test_p3_happy_path_with_edit_size -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_p3_walking_skeleton.py::test_p3_happy_path_approve` passes
    - `tests/integration/test_p3_walking_skeleton.py::test_p3_happy_path_with_edit_size` passes
    - `place_order` is called exactly once across each test
    - `walk_chain` returns `[]` at the end of each test
    - The daily_pnl Block Kit digest contains 1 fill in the per-strategy aggregate (verified via the captured `chat_postMessage` payload — blocks list contains a section with the strategy name and the fill count)
    - Phase 1 walking-skeleton + Phase 2 OrderGuard chain still green
  </acceptance_criteria>
  <done>End-to-end Slack approve + edit-size cassette green; chain integrity preserved; daily P&L captures the day's fills</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P3 walking-skeleton cassette — dashboard fallback + expiry chains</name>
  <files>
    tests/integration/test_p3_walking_skeleton.py
  </files>
  <read_first>
    tests/integration/test_dashboard_approve_flow.py (Plan 03-05 — the dashboard HTMX cycle cassette; the literal template for the fallback chain),
    tests/integration/test_sweep_persistence.py (Plan 03-04 — APScheduler restart cassette; the expiry chain reuses the sweep semantics),
    src/gekko/approval/expiry.py (expire_stale_proposals — the sweep primitive under test),
    src/gekko/dashboard/routes.py /login + /approvals + /approvals/{id}/approve (Plan 03-05 — the routes under test)
  </read_first>
  <behavior>
    - Test `test_dashboard_fallback` exercises: (1) seed a PENDING proposal; (2) "Slack is down" — do NOT call Slack handlers; (3) use `httpx.ASGITransport(app=create_app())` to POST `/login` with the correct passphrase + a known session cookie; (4) GET `/approvals` — assert the proposal appears in the rendered HTML; (5) POST `/approvals/{id}/approve` — assert dedup row inserted with `source="dashboard"`, proposal transitions to APPROVED, background `execute_proposal` task spawned (via monkeypatched `asyncio.create_task`), HTMX response is the updated `_proposal_card.html.j2` partial; (6) drain the background task; (7) the executor fires `place_order` (mocked), on_fill_event lands; (8) assert audit chain integrity.
    - Test `test_expiry_chain` exercises: (1) seed a PENDING proposal with `expires_at = utcnow() - 5min` (past due); (2) call `expire_stale_proposals(user_id=...)` directly (bypass APScheduler — the sweep logic is what's under test, not the cron firing); (3) assert the proposal transitions to EXPIRED; (4) assert an `expiration` audit event lands with the D-50 payload shape; (5) assert `chat.update` is called (mocked) with the expired-card blocks; (6) assert a separate DM is captured with the D-53 copy; (7) attempt a Slack approve click on the now-expired proposal — assert `transition_status(PENDING -> APPROVED)` raises ValueError because the state is now EXPIRED; assert the handler catches and fires the D-43 ephemeral with the "Proposal already expired at HH:MM" variant per UI-SPEC §Surface 7.
    - Both tests assert: audit chain `walk_chain` returns `[]`; no double-execution; the dedup table has the expected rows.
  </behavior>
  <action>
    Append `test_dashboard_fallback` + `test_expiry_chain` to `tests/integration/test_p3_walking_skeleton.py`. Use the same fixture surface as Task 1. For `test_dashboard_fallback`, use `httpx.AsyncClient(transport=httpx.ASGITransport(app=create_app()))` per Plan 01-09 pattern (bypass real lifespan; set `app.state.scheduler` to a sentinel; pre-set the session cookie via direct `client.cookies` manipulation — OR POST /login first to mint a real cookie, then carry it to /approvals). For `test_expiry_chain`, directly invoke `expire_stale_proposals` (sync test of the sweep primitive — no APScheduler needed; cron-firing is covered by `test_sweep_persistence.py` from Plan 03-04). Mock `slack_app.client.chat_update` to capture the expired-card update; mock `_send_slack_dm` to capture the expiry notice DM. Assert all stated invariants.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_p3_walking_skeleton.py::test_dashboard_fallback tests/integration/test_p3_walking_skeleton.py::test_expiry_chain -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_p3_walking_skeleton.py::test_dashboard_fallback` passes
    - `tests/integration/test_p3_walking_skeleton.py::test_expiry_chain` passes
    - `place_order` is called exactly once in `test_dashboard_fallback`; zero times in `test_expiry_chain` (expired proposals never execute)
    - `walk_chain` returns `[]` at the end of each test
    - The dedup table has 1 row in `test_dashboard_fallback` (`source="dashboard"`); 0 rows in `test_expiry_chain` until the late Slack click attempt produces a "duplicate" ephemeral
    - `chat_update` is captured exactly once in `test_expiry_chain` with the expired-card block structure (assert presence of `[EXPIRED]` chip + status-line context block)
    - All 4 P3 walking-skeleton tests (Task 1's two + Task 2's two) pass in a single `uv run pytest tests/integration/test_p3_walking_skeleton.py -x` invocation
  </acceptance_criteria>
  <done>Dashboard fallback + expiry cassette green; full P3 chain covered end-to-end across 4 tests in one file; no double-execution; chain integrity preserved</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: README demo recipe + deferred-items.md for manual operator demo</name>
  <files>
    README.md,
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md
  </files>
  <read_first>
    README.md existing Phase 1 + Phase 2 demo sections (the operator-facing walking-skeleton recipes — the literal style template for the Phase 3 section),
    .planning/phases/02-orderguard-real-money-alpaca-live-safety-floor/deferred-items.md (the Phase 2 deferred-items file — the literal template for the Phase 3 deferred-items.md),
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-VALIDATION.md §"Manual-Only Verifications" (5 rows to capture in deferred-items.md)
  </read_first>
  <behavior>
    - `README.md` gains a `### Phase 3 — Production HITL UX demo` section below the existing Phase 1 + Phase 2 sections. Recipe steps:
      1. Confirm Phase 1 + Phase 2 setup is complete (paper trading + Slack workspace + Alpaca creds wired).
      2. Run `uv run alembic upgrade head` to apply 0004 if not already.
      3. Run `uv run gekko serve` — confirm the boot log shows `scheduler.sweep_registered` and `scheduler.daily_pnl_registered`.
      4. Trigger a strategy manually: `uv run gekko run <strategy_name>` — wait for the proposal Slack DM.
      5. Verify idempotency (HITL-02): click Approve twice rapidly in Slack — observe ONE execution + ONE ephemeral "already approved" toast.
      6. Verify quiet hours (HITL-05): set `User.quiet_hours_start=22:00, quiet_hours_end=07:00, timezone='America/New_York'` via `/settings`; observe a scheduled cycle at 23:00 ET — confirm no proposal DM lands; observe `gekko run` at 23:30 ET — confirm proposal DM does land (manual override).
      7. Verify timeout=REJECT (HITL-03): create a proposal with `Strategy.proposal_timeout_minutes=1` (or wait the default 30); after the timer expires, observe the Slack card swap to greyed-out [EXPIRED] state + a separate DM.
      8. Verify edit-size (HITL-04): click Edit-size on a PENDING card; submit a qty within 2% drift — observe APPROVED. Submit a qty with >2% drift — observe the modal re-render with the red drift error.
      9. Verify dashboard fallback (DASH-04): visit `http://localhost:8000/login`; enter passphrase; approve a proposal from `/approvals`; observe the same execution chain as a Slack approve.
      10. Verify daily P&L (REPT-01): wait until 16:30 America/New_York on a trading day; observe the Block Kit digest DM.
    - `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md` captures the 5 manual-only verifications from VALIDATION §"Manual-Only Verifications" using the Phase 2 deferred-items.md table shape: `| Category | Item | Status | Note |`.
    - The 5 rows in deferred-items.md correspond exactly to the 5 rows in VALIDATION.md §"Manual-Only Verifications" (real Slack dup-click, 60s sweep wall-clock, DST corner, browser session, real 16:30 ET daily DM).
  </behavior>
  <action>
    Open `README.md`. Append a `### Phase 3 — Production HITL UX demo` section below the existing Phase 2 demo section. Use the same Markdown style and numbered-list shape as the Phase 1 + Phase 2 sections. Cover all 10 verification steps in `<behavior>`. Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md` using the Phase 2 deferred-items.md as the literal template. Populate with the 5 rows from VALIDATION §"Manual-Only Verifications", each with status `pending (operator demo)` and a note explaining why the verification is manual-only (e.g., "Wall-clock + real Slack retry; cannot mock Slack's actual retry timing in a cassette" for row 1).
  </action>
  <verify>
    <automated>grep -q "### Phase 3 — Production HITL UX demo" README.md &amp;&amp; test -f .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md &amp;&amp; grep -q "Manual-Only Verifications" .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 3" README.md` returns at least 1 match (the new section header)
    - `README.md` contains the verbatim string "Verify idempotency (HITL-02)" (or equivalent step naming for HITL-02)
    - `README.md` contains the string "Verify quiet hours (HITL-05)"
    - `README.md` contains the string "Verify dashboard fallback (DASH-04)"
    - `README.md` contains the string "Verify daily P&L (REPT-01)"
    - `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md` exists
    - The deferred-items.md table contains 5 rows matching the 5 rows of VALIDATION §"Manual-Only Verifications"
  </acceptance_criteria>
  <done>README demo recipe lands per Phase 1 + 2 style; deferred-items.md captures 5 manual operator demos for end-of-phase verification</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test fixture ↔ real Slack / real Alpaca | The cassette test runs against MOCKED Slack + MOCKED Alpaca. No live broker, no live Slack workspace. |
| README demo recipe ↔ operator action | The README is documentation; the operator's command-line trust boundary applies (single-operator deployment). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | Test fixture leaks real credentials | accept | Cassette fixtures use in-memory SQLCipher engines with test-only passphrases; no real Alpaca creds present in test files |
| T-03-07-02 | Information disclosure | README leaks operator's specific Slack workspace ID | mitigate | README uses placeholder strings ("<your strategy_name>"); no concrete identifiers |
| T-03-07-03 | Repudiation | Operator denies running the demo recipe | accept | deferred-items.md tracks pending status; operator updates the status field when each row is verified manually |
| T-03-07-SC | Tampering | npm/pip/cargo installs | not applicable | No new dependencies — pytest + pytest-asyncio + freezegun + respx all P1 + P2 audited |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_p3_walking_skeleton.py -x` green (4 tests: happy_path_approve, happy_path_with_edit_size, dashboard_fallback, expiry_chain)
- Phase 1 + Phase 2 + Plans 03-01 through 03-06 chains all green
- `grep -q "Phase 3" README.md` succeeds
- `test -f .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/deferred-items.md` succeeds
- Full P3 suite: `uv run pytest tests/unit tests/integration -x` green (all Wave-0 stubs from 03-01 are now filled by 03-02..03-07)
</verification>

<success_criteria>
- A single `uv run pytest tests/integration/test_p3_walking_skeleton.py -x` invocation exercises:
  1. Slack approve (with optional Edit-size branch) → state → executor → fill → daily P&L
  2. Dashboard fallback when Slack is down → /login → /approvals → approve → executor → fill
  3. Expiry chain: PENDING → expire_stale_proposals → EXPIRED + chat.update + DM
- All four tests pass; `walk_chain` returns `[]` at the end of each; `place_order` is called zero or one times per test (never twice — exactly-once execution)
- The dedup table records every action with the correct source ("slack" vs "dashboard"); the audit chain captures every event
- `README.md` has a runnable Phase 3 demo recipe the operator can execute end-to-end against real Slack + real Alpaca paper
- `deferred-items.md` enumerates the 5 manual-only verifications that the operator MUST complete before declaring Phase 3 fully closed (per the Phase 1 + Phase 2 closeout pattern)
- Full P3 phase requirements (HITL-02, HITL-03, HITL-05, DASH-04, REPT-01) are observable in either the cassette OR the manual demo recipe
</success_criteria>

<output>
Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-07-SUMMARY.md` when done.
</output>
