---
phase: 03-production-hitl-ux-slack-block-kit-dashboard-fallback
plan: "10"
type: execute
wave: 8
depends_on:
  - "03-08"
  - "03-09"
files_modified:
  - src/gekko/approval/slack_handler.py
  - tests/unit/test_slack_retry_gate.py
autonomous: true
gap_closure: true
requirements:
  - HITL-02

must_haves:
  truths:
    - "_extract_retry_num is removed (deleted) from slack_handler.py; if any unexpected call site outside handle_approve/handle_reject is found, tombstone with a dead-code docstring — deletion is the default"
    - "Exactly-once execution is preserved via claim_action alone; code comments and documentation reflect this accurately"
    - "handle_approve and handle_reject no longer call _extract_retry_num as a dedup layer"
    - "Tests assert that the retry gate path does not interfere with normal approve/reject workflows and that claim_action is the sole dedup primitive"
  artifacts:
    - path: "src/gekko/approval/slack_handler.py"
      provides: "_extract_retry_num deleted; retry gate block removed from handle_approve and handle_reject"
    - path: "tests/unit/test_slack_retry_gate.py"
      provides: "tests confirming claim_action is the sole dedup layer; no retry gate side-effects"
  key_links:
    - from: "handle_approve"
      to: "claim_action"
      via: "_approve_workflow → claim_action UNIQUE INSERT"
      pattern: "claim_action"
---

<objective>
Close the HITL-02 partial gap (WR-08): _extract_retry_num in slack_handler.py reads body["headers"]["x-slack-retry-num"] which does not exist in Socket Mode (the production transport). The retry gate always returns 0 and is dead code in production. Exactly-once execution currently relies solely on claim_action's UNIQUE constraint — which is correct and sufficient — but the dead code misleads maintainers into believing there is an HTTP-header retry filter when there is none.

This plan removes the retry gate block from handle_approve and handle_reject (and deletes _extract_retry_num), then adds a focused test that confirms claim_action is the sole dedup primitive and that the approve/reject workflows are not affected by the removal.

Wave 8: depends on 03-08 and 03-09 completing first (no shared files, but logical gate-closure ordering — auth and reporting fixed before cleanup).

Purpose: Code that documents a guarantee it does not provide is worse than no code. Removing the gate makes the dedup contract clear: claim_action UNIQUE INSERT is the only idempotency layer, and it is both necessary and sufficient.

Output: Cleaned slack_handler.py; targeted test for the Socket Mode dedup contract.
</objective>

<execution_context>
@C:\Users\chris.platika\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris.platika\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-REVIEW.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead retry gate from handle_approve/handle_reject; delete _extract_retry_num</name>

  <read_first>
  - src/gekko/approval/slack_handler.py lines 140-235 (_extract_retry_num definition + full handle_approve including the retry gate block at lines 189-225)
  - src/gekko/approval/slack_handler.py lines 330-410 (handle_reject — contains an analogous retry_num gate block starting with `retry_num = _extract_retry_num(body)` at line 524)
  - src/gekko/approval/dedup.py (claim_action signature + UNIQUE INSERT + duplicate return path — confirms it is the complete dedup layer)
  </read_first>

  <files>src/gekko/approval/slack_handler.py</files>

  <action>
  In src/gekko/approval/slack_handler.py, make the following targeted changes:

  1. Remove the retry gate block from handle_approve. The block starts at `retry_num = _extract_retry_num(body)` (line ~194) and ends at the `finally: if _engine is not None: await _engine.dispose()` clause (line ~226). Delete this entire `if retry_num > 0:` block and the `retry_num = _extract_retry_num(body)` line that precedes it. After deletion, the flow goes directly from `await ack()` and extracting `decision_id` + `slack_user_id` to `asyncio.create_task(_approve_workflow(...))`. The claim_action call inside _approve_workflow is the dedup layer — it remains untouched.

  2. Remove the retry gate block from handle_reject. The block has the same shape: `retry_num = _extract_retry_num(body)` (line ~524) followed by an `if retry_num > 0:` block ending at `await _engine.dispose()`. Delete this entire block and the preceding `retry_num = _extract_retry_num(body)` line. After deletion, the flow goes directly from `await ack()` and extracting `decision_id` + `slack_user_id` to `asyncio.create_task(_reject_workflow(...))`.

  3. Delete _extract_retry_num entirely. The grep output confirms call sites are at lines 194 and 524 (both inside the blocks being deleted). After both blocks are removed, _extract_retry_num has no remaining call sites. Delete: the function definition (lines ~146-158), its docstring, and the section header comment block above it ("# X-Slack-Retry-Num gate helpers", lines ~141-143). If — after reading the file — any unexpected call site outside handle_approve and handle_reject is found, do not delete the function body; instead tombstone it with a docstring that states: "Dead code in Socket Mode (the production transport). X-Slack-Retry-Num is an HTTP header absent in Socket Mode WebSocket envelopes. Exactly-once execution is provided by claim_action's UNIQUE constraint (dedup.py). Do not add retry-gate logic here; it will silently not work in production." But the default is deletion.

  4. After the retry gate block deletion and function deletion, confirm the section header comment block ("# X-Slack-Retry-Num gate helpers") is also removed (it becomes an orphaned header if the function is deleted).

  Do NOT alter: claim_action calls inside _approve_workflow or _reject_workflow, handle_edit_size, handle_dup_click, or any other handler. Do not touch dedup.py. Do not change ack() calls or background task dispatch patterns.
  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_slack_retry_gate.py tests/unit/test_approve_dedup.py -x --timeout=30 -q 2>/dev/null || .venv/Scripts/python.exe -m pytest tests/unit/test_slack_retry_gate.py -x --timeout=30 -q</automated>
  </verify>

  <acceptance_criteria>
  - grep -n "_extract_retry_num" src/gekko/approval/slack_handler.py returns no output (function deleted and no call sites remain)
  - grep -n "retry_num" src/gekko/approval/slack_handler.py returns no output in handle_approve or handle_reject function bodies
  - claim_action calls in _approve_workflow and _reject_workflow are untouched
  </acceptance_criteria>

  <done>_extract_retry_num deleted from slack_handler.py; retry gate block removed from both handle_approve and handle_reject; no residual _extract_retry_num call sites remain; claim_action remains sole dedup primitive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test — Socket Mode dedup contract via claim_action only</name>

  <read_first>
  - src/gekko/approval/slack_handler.py (post-Task-1 state — handle_approve flow)
  - src/gekko/approval/dedup.py (claim_action signature and return values "first_write" / "duplicate")
  - tests/unit/test_dedup.py (existing claim_action test — use same in-memory engine setup)
  - tests/unit/test_approve_dedup.py (if it exists — check for existing retry gate tests to update)
  </read_first>

  <behavior>
  - Test 1: Calling handle_approve twice with the same (proposal_id, action_id, slack_user_id) — first call: claim_action returns "first_write", workflow proceeds; second call: claim_action returns "duplicate", ephemeral response fires, execution not repeated. This should work WITHOUT any X-Slack-Retry-Num header present in the body.
  - Test 2: Calling handle_approve with a body that has NO "headers" key at all (Socket Mode shape) — should not raise any exception; dedup still functions via claim_action.
  - Test 3: Calling handle_reject twice with the same key — same dedup behavior via claim_action.
  </behavior>

  <files>tests/unit/test_slack_retry_gate.py</files>

  <action>
  Create tests/unit/test_slack_retry_gate.py.

  Use an in-memory SQLite engine (same pattern as tests/unit/test_dedup.py — create all tables, seed a PENDING proposal). Monkeypatch _get_session_factory to return the test engine's session factory.

  For each test: construct a minimal Slack body dict that matches Socket Mode shape — specifically, the body dict does NOT have a "headers" key (confirming Socket Mode where HTTP headers are absent). Shape: {"actions": [{"value": proposal_id}], "user": {"id": "U_TESTER"}, "trigger_id": "fake_trigger"}.

  Test 1 and 3 (double-click idempotency without retry header): mock _approve_workflow / _reject_workflow to record calls. Call handle_approve (or via the underlying _approve_workflow path) twice. Assert the executor was invoked exactly once. Use the established pattern from tests/unit/test_dedup.py for seeding and asserting dedup table state.

  Test 2 (no headers key does not raise): wrap handle_approve in try/except and assert no AttributeError or KeyError is raised when body has no "headers" key.

  If test_approve_dedup.py already tests retry-gate behavior using body["headers"], update those tests to remove assertions about retry-gate short-circuit behavior (since the gate is removed) and keep only the claim_action dedup assertions.

  Use pytest.mark.asyncio for async tests. Import asyncio. Use unittest.mock.AsyncMock for ack and client.

  Add a `<verify>` grep to confirm no residual call sites: `grep -n "_extract_retry_num" src/gekko/approval/slack_handler.py` must return no output.
  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_slack_retry_gate.py -v --timeout=30 -q</automated>
  </verify>

  <acceptance_criteria>
  - All tests pass
  - grep -n "_extract_retry_num" src/gekko/approval/slack_handler.py returns no output
  - grep -n "retry_num" src/gekko/approval/slack_handler.py returns no output inside handle_approve or handle_reject bodies
  </acceptance_criteria>

  <done>Tests confirm claim_action is the sole dedup layer; Socket Mode body shape (no headers key) does not raise; double-click idempotency verified through claim_action; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack WebSocket envelope → slack_handler | Socket Mode delivers action payloads over WebSocket; no HTTP headers present |
| claim_action UNIQUE INSERT → SQLite | The sole idempotency primitive; enforced by DB constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Tampering | Slack at-least-once delivery causing double-execution | mitigate | claim_action UNIQUE INSERT is the sole and sufficient dedup layer; DB constraint enforces exactly-once regardless of transport |
| T-03-10-02 | Repudiation | Dead retry-gate code misleads maintainers into trusting a non-functional guard | mitigate | Delete _extract_retry_num and the retry gate blocks from both handle_approve and handle_reject; tests verify claim_action is sole guard |
| T-03-10-03 | Spoofing | Malicious Slack payload with crafted retry headers | accept | Socket Mode does not use HTTP headers; even in HTTP mode the gate was advisory only — claim_action provides the enforceable guarantee |
| T-03-10-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No package installs required; no slopcheck needed |
</threat_model>

<verification>
After both tasks complete:

1. pytest tests/unit/test_slack_retry_gate.py -v --timeout=30 — all pass

2. grep -n "_extract_retry_num" src/gekko/approval/slack_handler.py — no output (function deleted, no call sites)

3. grep -n "retry_num" src/gekko/approval/slack_handler.py — should not appear in handle_approve or handle_reject function bodies

4. grep -n "claim_action" src/gekko/approval/slack_handler.py — still present in _approve_workflow and _reject_workflow (unchanged)

5. pytest tests/ -x --timeout=30 -q --ignore=tests/test_cli.py --ignore=tests/test_config.py --ignore=tests/test_research_tools.py — no regressions
</verification>

<success_criteria>
- _extract_retry_num deleted from slack_handler.py (tombstone only as last resort if unexpected call site found outside the two handlers)
- Retry gate block removed from both handle_approve AND handle_reject unconditionally
- claim_action remains the sole and documented dedup primitive
- New tests confirm Socket Mode body (no headers key) does not raise and dedup works via claim_action
- No regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-10-SUMMARY.md` when done.
</output>
