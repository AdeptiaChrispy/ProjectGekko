---
phase: 03-production-hitl-ux-slack-block-kit-dashboard-fallback
plan: 14
type: execute
wave: 9
depends_on: ["03-13"]
files_modified:
  - src/gekko/dashboard/templates/edit_size_modal.html.j2
  - src/gekko/dashboard/routes.py
  - src/gekko/reporter/slack.py
  - src/gekko/slack/interactivity.py
  - src/gekko/approval/slack_handler.py
  - .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md
  - tests/unit/test_dashboard_edit_size.py
  - tests/unit/test_slack_block_kit.py
autonomous: true
gap_closure: true
requirements: ["HITL-04"]

must_haves:
  truths:
    - "Dashboard edit-size renders a native HTML range slider with min=1 step=1 max=cap-derived; live readout shows N shares, dollar notional, and percent of equity as the handle moves"
    - "Slack proposal card's Edit Size button is a URL button deep-linking to /approvals/{id}/edit-size; no in-Slack modal opens"
    - "handle_edit_size and handle_edit_size_view_submission are retired from active production paths; their Bolt view registration is neutralized"
    - "_check_edit_size_caps remains the sole server-side gate on edit_size_submit POST; an over-cap qty is still rejected"
    - "GET /approvals/{id}/edit-size passes max_shares, account_equity, ref_price, proposed_qty to the template context"
  artifacts:
    - path: "src/gekko/dashboard/templates/edit_size_modal.html.j2"
      provides: "Slider-based edit-size modal partial"
      contains: "input type=\"range\""
    - path: "src/gekko/reporter/slack.py"
      provides: "Slack card Edit Size as URL button"
      contains: "url.*edit-size"
    - path: "src/gekko/slack/interactivity.py"
      provides: "Neutralized edit_size_modal view handler"
    - path: ".planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md"
      provides: "Surface 1 updated to slider + deep-link contract"
  key_links:
    - from: "src/gekko/dashboard/routes.py::edit_size_get"
      to: "edit_size_modal.html.j2"
      via: "TemplateResponse context keys: max_shares, account_equity_display, ref_price, qty"
    - from: "src/gekko/reporter/slack.py::build_proposal_card"
      to: "/approvals/{id}/edit-size"
      via: "url field on Edit Size button element"
    - from: "src/gekko/dashboard/routes.py::edit_size_submit"
      to: "src/gekko/approval/actions.py::_check_edit_size_caps"
      via: "server-side gate; slider max is display-only"
---

<objective>
Implement the D-62 edit-size slider redesign: replace the dashboard number-input modal with a
native HTML range slider that shows max shares, live dollar/percent readout, and an at-cap
variant; retire the Slack in-modal edit path in favor of a URL deep-link to the dashboard
slider; update 03-UI-SPEC.md Surface 1 to match.

Purpose: Close the UAT Test 2 gap — the modal was technically correct (03-11 cap math) but
illegible for a non-technical operator. A slider makes the allowed band, the current position
within it, and the dollar exposure self-evident without the operator reasoning about percents.

Output: Updated edit_size_modal.html.j2 (slider), updated edit_size_get route (passes max_shares
and equity context), updated slack.py (URL button replacing action button), neutralized Bolt
handlers in interactivity.py and slack_handler.py, updated UI-SPEC Surface 1, regression-guard
tests confirming the submit path still rejects over-cap qty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard slider — update edit_size_get route + replace number-input template</name>

  <files>
    src/gekko/dashboard/routes.py
    src/gekko/dashboard/templates/edit_size_modal.html.j2
    tests/unit/test_dashboard_edit_size.py
  </files>

  <read_first>
    src/gekko/dashboard/routes.py (lines 488–565: edit_size_get function in full — understand current context keys)
    src/gekko/dashboard/routes.py (lines 568–783: edit_size_submit in full — equity-fetch pattern + _check_edit_size_caps call to preserve unchanged)
    src/gekko/approval/actions.py (full file — _check_edit_size_caps signature, max_shares_approx formula)
    src/gekko/dashboard/templates/edit_size_modal.html.j2 (current template — replace number input with range slider)
    src/gekko/dashboard/templates/promote_to_live_modal.html.j2 (modal shape pattern to preserve)
    tests/unit/test_dashboard_edit_size.py (existing tests to keep green — test_edit_above_hard_cap_rejected, test_happy_path_closes_modal)
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md (D-62 slider spec — range, readout, at-cap variant, equity-fetch-failure variant)
  </read_first>

  <behavior>
    - Test: GET /approvals/{id}/edit-size response context contains keys max_shares (int), account_equity_display (str formatted "$X,XXX.XX"), ref_price, qty (proposed)
    - Test: GET context max_shares equals floor(max_position_pct * account_equity / ref_price) when equity > 0
    - Test: GET context when equity fetch fails (equity=0), max_shares equals qty (proposed qty used as fallback) and equity_fetch_failed flag is True
    - Test: template renders input[type="range"] with min="1", step="1", max="{{ max_shares }}", value="{{ qty }}"
    - Test: template contains element with id="size-readout" for JS to update
    - Test: existing test_edit_above_hard_cap_rejected still passes (POST still rejects over-cap qty via _check_edit_size_caps)
    - Test: existing test_happy_path_closes_modal still passes
  </behavior>

  <action>
    **Part A — Update edit_size_get route in src/gekko/dashboard/routes.py:**

    The GET handler currently passes: proposal_id, ticker, qty, side, ref_price,
    target_notional_usd, original_notional, drift_error=None.

    Add these new context keys (computed in the GET handler, per D-62):
    - max_shares (int): floor(max_position_pct * account_equity / ref_price).
      Derivation: (a) load the Strategy row and parse its payload_json into a
      Strategy Pydantic object to read hard_caps.max_position_pct;
      (b) fetch account equity using the same asyncio.wait_for(_broker.get_account(),
      timeout=2.5) pattern already in edit_size_submit (lines 699-725) — 2.5s timeout,
      fail-open to equity=Decimal("0") on any exception;
      (c) if equity > 0 and ref_price > 0: max_shares = int(max_position_pct * equity /
      ref_price); otherwise max_shares = int(qty) (proposed qty — slider still usable
      but only downward from current);
    - account_equity_display (str): f"${equity:,.2f}" when equity > 0, else "" (empty string);
    - equity_fetch_failed (bool): True when equity == 0 (the fail-open path);
    - max_position_pct (str): str(strategy_obj.hard_caps.max_position_pct) for the
      JS readout to compute percent-of-equity client-side, or "0" if strategy could not load.

    Clamp: if max_shares < int(qty), set max_shares = int(qty) so the at-cap variant
    is handled (slider handle sits at max, operator can only go down).

    Keep min_shares = 1 (hardcoded, per D-62 "Range: 1 share → max shares").

    The equity-fetch and strategy-load logic in edit_size_get should follow the same
    pattern as edit_size_submit (lines 676-725) — open a session factory, load the
    Strategy row by strategy_id from the Proposal row, then build the broker and call
    get_account(). Factor out if desired to a private _fetch_equity_and_strategy helper,
    but do NOT change the submit path — its behavior must remain byte-for-byte identical.

    **Part B — Replace edit_size_modal.html.j2 with slider template:**

    Replace the existing `<input type="number">` in edit_size_modal.html.j2 with:

    1. A native HTML range slider:
       `<input type="range" id="edit-qty" name="qty" min="1" step="1"
              max="{{ max_shares }}" value="{{ qty }}" required
              data-ref-price="{{ ref_price }}"
              data-equity="{{ account_equity_display }}"
              data-max-pct="{{ max_position_pct }}"
              class="edit-size-slider"
              aria-describedby="size-readout"
              oninput="updateSizeReadout(this)">`

    2. A live readout element below the slider:
       `<div id="size-readout" class="edit-size-readout" aria-live="polite"></div>`

    3. A small vanilla-JS inline script block (NOT in base.html.j2, scoped to this
       partial only) that defines updateSizeReadout(el):
       - reads qty = parseInt(el.value, 10)
       - reads refPrice = parseFloat(el.dataset.refPrice || "0")
       - reads equity = el.dataset.equity (already formatted string, e.g. "$10,000.00")
       - reads maxPct = parseFloat(el.dataset.maxPct || "0")
       - computes notional = qty * refPrice
       - computes pctDisplay = (maxPct > 0 && equity) ?
             `${(notional / <raw_equity_number> * 100).toFixed(1)}% of your ${equity}` : ""
         NOTE: because equity is pre-formatted with commas, the JS must strip non-digit
         chars to get the raw number for the percent computation. Use:
           const rawEquity = parseFloat(equity.replace(/[^0-9.]/g, "")) || 0;
       - sets document.getElementById("size-readout").textContent to:
           if pctDisplay: `${qty} shares ≈ $${notional.toLocaleString('en-US',
             {minimumFractionDigits:2, maximumFractionDigits:2})} — ${pctDisplay}`
           else: `${qty} shares` (equity-fetch-failure fallback, per D-62)
       - also sets the hidden-name field (see item 5) value to qty for form submission.
       - call updateSizeReadout on the slider element via an oninput attribute on the
         input[type=range] AND also call it once on DOMContentLoaded via an inline script
         at the bottom of the partial to initialize the readout on first render.

    CSP note: CLAUDE.md and this project's CSP posture is `script-src 'self'` with
    NO inline scripts. However, the inline `oninput` attribute IS allowed under
    `script-src 'self'` when there is no separate inline script block — HTMX uses
    inline attributes throughout this codebase (hx-post, hx-get). The `oninput`
    handler must be a named function reference (not an arrow function literal) and
    the function definition must live in a `<script>` tag inside this partial only
    (not base.html.j2). This partial is only ever injected into #modal-mount, so
    no script will execute in the base page context. If CSP blocks it at runtime,
    use a `<script nonce="">` approach — but based on the VENDOR.md SRI pattern
    this project uses `script-src 'self'` which allows scripts loaded from same origin;
    inline script blocks are blocked only when `unsafe-inline` is NOT in the policy.
    Because the base.html.j2 `<meta http-equiv="Content-Security-Policy">` uses
    `script-src 'self'` (without `unsafe-inline`), you MUST instead deliver the JS
    as a static file. Create `src/gekko/dashboard/static/edit-size-slider.js` with
    the updateSizeReadout function and include it via:
    `<script src="/static/edit-size-slider.js"></script>` inside the partial.
    The oninput="updateSizeReadout(this)" attribute on the range input is then
    a reference to a function defined in an external file — CSP-safe.

    4. At-cap variant (per D-62): when equity_fetch_failed is False and max_shares ==
       int(qty), render a note below the slider:
       `<p class="form-help">This is your maximum for this strategy.</p>`

    5. Equity-fetch-failure variant (per D-62): when equity_fetch_failed is True,
       render a caution note:
       `<p class="form-help"><span style="color:#92400e">⚠ Cap couldn't be confirmed
       (broker unreachable). Server will verify at submit.</span></p>`

    6. Keep the existing form structure: `<form hx-post="/approvals/{{ proposal_id }}/edit-submit"
       hx-target="#modal-mount" hx-swap="innerHTML">`. The form MUST submit the `qty` field
       that the server reads in edit_size_submit. Since type=range submits its value, the
       `name="qty"` on the range input is sufficient — no hidden field needed.

    7. Keep the header showing `{{ side }} {{ qty }} {{ ticker }} (~${{ original_notional }})`.

    8. Keep the Cancel link and "Approve at this size" submit button.

    9. Keep the `{% if drift_error %}` error block — this handles the server re-render
       path when _check_edit_size_caps returns False on submit.

    **Part C — Add static JS file:**
    Create `src/gekko/dashboard/static/edit-size-slider.js` with the
    `updateSizeReadout(el)` function described in item 3 above, plus a
    `document.addEventListener('DOMContentLoaded', ...)` that calls
    `updateSizeReadout` on any `.edit-size-slider` element present at load time
    (so the readout is populated on initial render, not just on drag).

    **Part D — tests/unit/test_dashboard_edit_size.py:**
    Add two new test functions. Keep all existing tests unchanged.
    - test_edit_size_get_context_keys: mock _get_session_factory to return a session
      with the proposal row (reuse _make_session_and_row helper), mock the broker
      get_account to return {"equity": "10000"}, assert the GET /approvals/{id}/edit-size
      response has status 200 and that the rendered HTML contains 'type="range"',
      'name="qty"', 'min="1"', 'step="1"'.
    - test_edit_size_get_equity_fail_open: mock broker get_account to raise an exception;
      assert GET still returns 200 and the rendered HTML contains 'type="range"' and
      the equity-failure caution note text "Cap couldn't be confirmed".
  </action>

  <verify>
    <automated>cd "C:\Users\chris.platika\Desktop\Project Gekko" && .venv/Scripts/python.exe -m pytest tests/unit/test_dashboard_edit_size.py -x -q 2>&amp;1 | tail -20</automated>
  </verify>

  <done>
    GET /approvals/{id}/edit-size returns a template with input[type="range"] min=1 step=1
    max=cap-derived. test_edit_size_get_context_keys and test_edit_size_get_equity_fail_open
    pass. All pre-existing edit-size tests (test_edit_above_hard_cap_rejected,
    test_happy_path_closes_modal, test_live_proposal_strategy_load_failure_rejected) stay green.
    Static JS file exists at src/gekko/dashboard/static/edit-size-slider.js.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slack URL deep-link + retire handle_edit_size modal handlers</name>

  <files>
    src/gekko/reporter/slack.py
    src/gekko/slack/interactivity.py
    src/gekko/approval/slack_handler.py
    tests/unit/test_slack_block_kit.py
  </files>

  <read_first>
    src/gekko/reporter/slack.py (lines 430–465: the four action buttons block — Edit Size is at line 444-447, must become a URL button)
    src/gekko/slack/interactivity.py (full file — @slack_app.action("edit_size") at line 48 and @slack_app.view("edit_size_modal") at line 54 must be neutralized)
    src/gekko/approval/slack_handler.py (lines 605–715: handle_edit_size; lines 718–980: handle_edit_size_view_submission; lines 980–1010: __all__ exports — confirm no other external caller)
    src/gekko/approval/slack_handler.py (lines 1–30: module docstring — update to reflect retirement)
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md (D-62 Slack section: "Edit size button becomes URL button deep-linking to {DASHBOARD_BASE_URL}/approvals/{proposal_id}/edit-size; handle_edit_size + handle_edit_size_view_submission RETIRED")
    tests/unit/test_slack_block_kit.py (existing tests to keep green — find tests that assert edit_size action_id presence and update their expectations)
  </read_first>

  <behavior>
    - Test: build_proposal_card for a PENDING proposal returns blocks where the Edit Size
      button element has a "url" key pointing to "…/approvals/{id}/edit-size" and NO
      "action_id" key (URL buttons do not round-trip)
    - Test: build_proposal_card Edit Size button does NOT have action_id="edit_size"
    - Test: @slack_app.view("edit_size_modal") handler in interactivity.py, when called
      directly, logs a warning and calls ack() exactly once without invoking
      handle_edit_size_view_submission (the submission handler is retired)
    - Test: handle_edit_size_view_submission when imported is a no-op stub that acks and
      logs a deprecation warning
  </behavior>

  <action>
    **Part A — src/gekko/reporter/slack.py: convert Edit Size to URL button (per D-62):**

    In build_proposal_card, locate the "Edit Size" button block (currently lines ~442-447,
    action_id="edit_size"). Replace with:

    ```
    {
        "type": "button",
        "text": {"type": "plain_text", "text": "Edit size"},
        "url": f"{_get_dashboard_url()}/approvals/{decision_id_value}/edit-size",
    }
    ```

    No action_id, no value (URL buttons do not round-trip to Bolt action handlers).
    The text changes from "Edit Size" to "Edit size" per the D-62 / UI-SPEC copywriting
    contract (all-lowercase except first letter, verb+noun pattern).
    Use _get_dashboard_url() (already defined in the file) to build the base URL.
    decision_id_value is already in scope — same variable used for Approve/Reject buttons.

    **Part B — src/gekko/slack/interactivity.py: neutralize view registration:**

    The `@slack_app.view("edit_size_modal")` block (lines 54-59) must not be deleted
    entirely — a dead callback_id registration that Slack has cached can cause a
    "dispatch_failed" error if Slack sends a late callback. Convert the handler to a
    no-op ack stub with a deprecation log:

    ```python
    @slack_app.view("edit_size_modal")
    async def _edit_size_submit(
        ack: Any, body: dict[str, Any], client: Any, view: dict[str, Any]
    ) -> None:
        """RETIRED (D-62, Plan 03-14). URL button replaced modal; ack and ignore."""
        from gekko.logging_config import get_logger as _gl
        _gl(__name__).warning(
            "slack.edit_size_modal.submission.retired",
            note="handle_edit_size_view_submission retired by Plan 03-14 (D-62); use dashboard slider",
        )
        await ack()
    ```

    Remove the import of handle_edit_size_view_submission from the from-import at
    the top of the file (line 22). It is no longer called from interactivity.py.

    Also neutralize the `@slack_app.action("edit_size")` handler (lines 48-51):
    convert it to a URL-button ack stub. URL buttons do NOT fire a Bolt action
    callback, but keep the handler as a safety net for stale clients that may still
    have the old action button cached. Convert to:

    ```python
    @slack_app.action("edit_size")
    async def _edit_size(ack: Any, body: dict[str, Any], client: Any) -> None:
        """RETIRED (D-62, Plan 03-14). Edit size is now a URL button; ack stale callbacks."""
        await ack()
    ```

    Remove the import of handle_edit_size from the from-import at the top of the file.

    **Part C — src/gekko/approval/slack_handler.py: retire handle_edit_size and
    handle_edit_size_view_submission:**

    Do NOT delete these functions — tests and the __all__ export reference them.
    Instead neutralize each function body:

    handle_edit_size: Replace the modal-opening body with:
    ```python
    await ack()
    from gekko.logging_config import get_logger as _gl
    _gl(__name__).warning(
        "slack.edit_size.button.retired",
        note=(
            "D-62: Edit-size is a URL button — this Bolt action handler should "
            "not be called in production. Stale client callback received."
        ),
        decision_id=body["actions"][0]["value"] if body.get("actions") else None,
    )
    ```

    handle_edit_size_view_submission: Replace the validation/submission body with:
    ```python
    from gekko.logging_config import get_logger as _gl
    _gl(__name__).warning(
        "slack.edit_size_modal.submission.retired",
        note="D-62: handle_edit_size_view_submission retired by Plan 03-14; acking no-op.",
    )
    await ack()
    ```

    Keep handle_edit_size_stub alias and both names in __all__ — backward compat for
    any test that imports them by name. Keep _edit_size_submit_workflow function in place
    (it is a private helper; removing it would risk breaking any test that patches it).

    Update the module docstring (lines 8-9) to note: "handle_edit_size and
    handle_edit_size_view_submission are retired stubs (D-62, Plan 03-14)."

    **Part D — tests/unit/test_slack_block_kit.py:**
    Find any existing test that asserts `action_id == "edit_size"` on the Edit Size
    button and update the assertion to instead assert:
    - the button element has a "url" key
    - the url contains "/edit-size"
    - the button element does NOT have an "action_id" key

    Do not remove other assertions in those tests.
  </action>

  <verify>
    <automated>cd "C:\Users\chris.platika\Desktop\Project Gekko" && .venv/Scripts/python.exe -m pytest tests/unit/test_slack_block_kit.py tests/unit/test_dashboard_edit_size.py tests/unit/test_edit_size_caps.py tests/unit/test_edit_size_not_direct_broker.py -x -q 2>&amp;1 | tail -20</automated>
  </verify>

  <done>
    build_proposal_card Edit Size button has url=".../approvals/{id}/edit-size" and no
    action_id. interactivity.py view handler is a no-op ack stub. handle_edit_size and
    handle_edit_size_view_submission are no-op ack stubs in slack_handler.py. All listed
    tests pass including pre-existing regression guards.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update 03-UI-SPEC.md Surface 1 to slider contract + run full regression</name>

  <files>
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md
  </files>

  <read_first>
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md (Surface 1 section, lines 283–384 — current Slack modal contract to replace with slider contract)
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md (D-62 full text — authoritative slider spec, at-cap variant, equity-fetch-failure variant, Slack=deep-link language)
    .planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-HUMAN-UAT.md (UAT Test 2 gap — missing items to confirm the spec now addresses them)
  </read_first>

  <action>
    Rewrite the "Surface 1" section of 03-UI-SPEC.md to reflect the D-62 slider contract.
    The existing Surface 1 describes the Slack `views_open` modal with a number_input.
    Replace the entire Surface 1 section with a new section titled:
    "Surface 1: Edit-Size — Dashboard Slider (primary) + Slack URL Deep-Link (D-62)"

    The new Surface 1 must cover:

    **Dashboard (primary surface):**
    - Route: GET /approvals/{id}/edit-size — returns edit_size_modal.html.j2 partial,
      loaded into #modal-mount via HTMX hx-get.
    - Slider element: `<input type="range" name="qty" min="1" step="1"
      max={max_shares} value={proposed_qty} data-ref-price data-equity data-max-pct
      oninput="updateSizeReadout(this)">` — whole-share snaps (step=1), no fractional.
    - max_shares formula: floor(strategy.hard_caps.max_position_pct * account_equity /
      ref_price). Clamped so max_shares >= proposed_qty (at-cap: handle sits at max).
    - Live readout element (#size-readout, aria-live="polite"):
      "{N} shares ≈ ${notional} — {pct}% of your ${equity}" (JS computes client-side
      from data-attributes — display only, NOT the authority for submission).
    - At-cap variant: when max_shares == proposed_qty, note rendered:
      "This is your maximum for this strategy."
    - Equity-fetch-failure variant: when equity=0 (broker unreachable, fail-open):
      shares-only readout; caution note "Cap couldn't be confirmed (broker unreachable).
      Server will verify at submit." Server still runs _check_edit_size_caps with
      equity=0 (PAPER: fail-open pass; LIVE: fail-closed reject per CR-01).
    - Submit: POST /approvals/{id}/edit-submit — server runs _check_edit_size_caps
      as the sole authority; slider max is a UI affordance, not the cap.
    - Primary CTA: "Approve at this size".

    **Slack (secondary surface, per D-62):**
    - The "Edit size" button on the proposal card is a Block Kit URL button:
      `{"type": "button", "text": {"type": "plain_text", "text": "Edit size"},
       "url": f"{DASHBOARD_BASE_URL}/approvals/{proposal_id}/edit-size"}`.
    - No action_id (URL buttons do not fire Bolt callbacks).
    - No in-Slack modal (handle_edit_size + handle_edit_size_view_submission retired).
    - Slack opens the dashboard slider page in the operator's browser.
    - Approve and Reject remain inline in Slack (unchanged).

    **Reconcile lingering Slack-modal language:**
    - Remove or strike-through all references to `views_open`, `callback_id`,
      `private_metadata` JSON, `view_submission` listener, and `number_input` block
      type from Surface 1. These describe the retired path.
    - Keep references to _check_edit_size_caps as the server-side gate (unchanged).
    - Keep the idempotency posture: dedup table still applies on dashboard submit
      (source='dashboard'); the Slack URL click itself is not recorded by the dedup
      table (no round-trip callback).
    - Update the "ARIA / accessibility" subsection to describe the HTML range slider's
      aria-describedby="#size-readout" and aria-live="polite" on the readout element.

    Also update the "Slack parallel" note at the bottom of Surface 1 to read:
    "Slack parallel: Edit size button is a URL deep-link to the dashboard slider page.
    The dashboard is the primary and only edit surface. Slack keeps Approve/Reject
    inline. (D-62, Plan 03-14)"

    Finally update the Copywriting Contract table entry for "Edit size submit" to:
    | Edit size submit (dashboard slider) | `Approve at this size` | Same CTA; moved from Slack modal to dashboard slider by D-62 |

    Do NOT modify any other Surface sections (2 through 7). Do NOT modify the design
    system, spacing, typography, or color sections. This is an in-place rewrite of
    Surface 1 only.
  </action>

  <verify>
    <automated>cd "C:\Users\chris.platika\Desktop\Project Gekko" && .venv/Scripts/python.exe -m pytest tests/unit/test_dashboard_edit_size.py tests/unit/test_edit_size_caps.py tests/unit/test_slack_block_kit.py tests/unit/test_edit_size_not_direct_broker.py tests/integration/test_dashboard_edit_size_happy.py -x -q 2>&amp;1 | tail -25</automated>
  </verify>

  <done>
    03-UI-SPEC.md Surface 1 describes slider (range input, max_shares formula, live readout,
    at-cap variant, equity-failure variant) and Slack=URL-button-deep-link. All listed tests
    pass. The phrase "views_open" and "number_input" and "callback_id" no longer appear in
    Surface 1. The phrase "_check_edit_size_caps" remains as the server-side gate description.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slider value → POST /approvals/{id}/edit-submit | The slider max is a UI affordance rendered client-side. The browser can submit any qty value (forged, negative, oversized) — the slider max does not restrict what the form body contains. |
| GET /approvals/{id}/edit-size | Returns equity value as a display string (e.g., "$10,000.00") and max_shares as an integer. No broker credentials, no API keys. Operator-controlled session. |
| Slack URL button click | Opens the dashboard in a browser. No Bolt callback fires. The URL contains only the proposal_id (already public to the operator's Slack workspace). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14-01 | Tampering | POST /approvals/{id}/edit-submit — qty field | mitigate | Two-layer server-side gate: (1) `_check_edit_size_caps(qty, ref_price, strategy, equity)` in edit_size_submit rejects qty whose notional exceeds max_position_pct * account_equity regardless of what the slider displayed; (2) `OrderGuard.place_order` (check_hard_caps + check_qty_price_sanity) re-runs independently at execute_proposal time. Slider max is strictly a UX affordance — no bypass possible. |
| T-03-14-02 | Tampering | Forged negative or zero qty on POST | mitigate | `_check_edit_size_caps` returns (False, "Quantity must be at least 1 share.") for qty <= 0; edit_size_submit already validates `new_qty <= 0` before calling the cap check (lines 596-598 of routes.py). |
| T-03-14-03 | Information Disclosure | GET /approvals/{id}/edit-size — equity value in template context | accept | The equity value is a dollar number, not a credential. It is the same value the operator sees in their own broker dashboard. No API keys, no secrets are rendered. Structlog's _redact processor already strips key_blob/secret_blob from logs. |
| T-03-14-04 | Elevation of Privilege | Unauthenticated access to GET/POST /approvals/{id}/edit-size | mitigate | Both routes are on the auth-gated router (APIRouter with dependencies=[Depends(require_session)] per Plan 03-08 / CR-01). Unauthenticated requests receive 302 → /login before reaching the handler. |
| T-03-14-05 | Tampering | Slack URL button — proposal_id in URL | accept | proposal_id is a non-secret opaque UUID already visible to the operator in the Slack card payload. An attacker who controls the Slack workspace could construct a URL, but the dashboard require_session gate ensures only the authenticated operator can act on it. Single-operator deployment per D-57. |
| T-03-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed by this plan. All changes are to existing source files and templates. No slopcheck required. |
</threat_model>

<verification>
Full regression suite confirming no regressions introduced:

```
cd "C:\Users\chris.platika\Desktop\Project Gekko" && .venv/Scripts/python.exe -m pytest tests/unit/test_dashboard_edit_size.py tests/unit/test_edit_size_caps.py tests/unit/test_edit_size_not_direct_broker.py tests/unit/test_slack_block_kit.py tests/integration/test_dashboard_edit_size_happy.py tests/integration/test_p3_walking_skeleton.py -x -q 2>&1 | tail -30
```

Acceptance assertions (grep, not subjective):

1. Slider element present:
   `grep -c 'type="range"' src/gekko/dashboard/templates/edit_size_modal.html.j2`
   must return >= 1.

2. URL deep-link in slack.py Edit Size button (no action_id):
   `grep -v '^#' src/gekko/reporter/slack.py | grep -c '"edit_size"'`
   must return 0 (no remaining action_id="edit_size" string in non-comment code).

3. URL pointing to /edit-size in slack.py:
   `grep -c 'edit-size' src/gekko/reporter/slack.py`
   must return >= 1.

4. Retired handlers are stubs (still ack, no modal open):
   `grep -c 'views_open' src/gekko/approval/slack_handler.py`
   must return 0.

5. Static JS file exists:
   Verify `src/gekko/dashboard/static/edit-size-slider.js` is present.

6. UI-SPEC Surface 1 updated:
   `grep -c 'URL button' ".planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md"`
   must return >= 1 (within the Surface 1 section, not elsewhere).
</verification>

<success_criteria>
The plan is complete when ALL of the following are true:

1. src/gekko/dashboard/templates/edit_size_modal.html.j2 renders an `<input type="range">`
   with min=1, step=1, max="{{ max_shares }}", value="{{ qty }}" and a live-readout element
   (#size-readout) updated by external JS (edit-size-slider.js, CSP-safe).

2. src/gekko/dashboard/routes.py edit_size_get passes max_shares (int), account_equity_display
   (str), equity_fetch_failed (bool), and max_position_pct (str) to the template context.
   edit_size_submit is UNCHANGED — _check_edit_size_caps remains the server-side authority.

3. src/gekko/reporter/slack.py build_proposal_card has a URL button for Edit size pointing
   at {DASHBOARD_BASE_URL}/approvals/{proposal_id}/edit-size with no action_id key.

4. src/gekko/slack/interactivity.py @slack_app.view("edit_size_modal") is a no-op ack stub;
   @slack_app.action("edit_size") is a no-op ack stub. Both imports removed from top-of-file.

5. src/gekko/approval/slack_handler.py handle_edit_size and handle_edit_size_view_submission
   are no-op ack stubs with deprecation log warnings. No views_open call remains.

6. 03-UI-SPEC.md Surface 1 describes the slider contract (range input, max_shares formula,
   live readout, at-cap and equity-failure variants) and Slack=URL-deep-link. No
   "views_open" or "number_input" language in Surface 1.

7. All pre-existing edit-size regression tests pass:
   test_edit_above_hard_cap_rejected, test_happy_path_closes_modal,
   test_live_proposal_strategy_load_failure_rejected, test_edit_size_not_direct_broker.

8. New tests pass: test_edit_size_get_context_keys, test_edit_size_get_equity_fail_open.
</success_criteria>

<output>
Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-14-SUMMARY.md` when done.
</output>
