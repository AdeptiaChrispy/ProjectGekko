---
phase: 03-production-hitl-ux-slack-block-kit-dashboard-fallback
plan: 15
type: execute
wave: 9
depends_on: ["03-14"]
files_modified:
  - src/gekko/dashboard/routes.py
  - src/gekko/dashboard/templates/edit_size_modal.html.j2
  - src/gekko/dashboard/templates/_proposal_card.html.j2
  - tests/integration/test_dashboard_hitl_actions.py
autonomous: true
requirements: [HITL-02, HITL-04, DASH-04]

must_haves:
  truths:
    - "Duplicate edit-size click (POST /approvals/{id}/edit-submit a second time on the same proposal) returns HTTP 200 with the current proposal card — never a 500 InvalidRequestError"
    - "GET /approvals/{id}/edit-size without HX-Request header returns a full HTML page (includes html/head/body with CSS and HTMX script tags); with HX-Request: true it returns the bare fragment"
    - "POST approve / reject / edit-submit against a terminal proposal (FILLED, EXPIRED, REJECTED) returns HTTP 200 with the current card state — never a ValueError 500"
    - "Terminal-state proposal cards (FILLED, EXECUTING, APPROVED, REJECTED, EXPIRED, AWAITING_2ND_CHANNEL, APPROVED_LIVE) do not render the Approve / Reject / Edit-size action buttons"
    - "Real-SQLite integration test (test_dashboard_hitl_actions.py) passes: all coverage cases green, no claim_action/transition_status/append_event mocks"
    - "Existing edit-size, OrderGuard, and walking-skeleton integration suites remain green"
  artifacts:
    - path: "src/gekko/dashboard/routes.py"
      provides: "approve_proposal_endpoint, reject_proposal_endpoint, edit_size_get, edit_size_submit — all three bugs fixed"
      contains: "_get_session_factory.*user_id"
    - path: "src/gekko/dashboard/templates/_proposal_card.html.j2"
      provides: "Action buttons rendered only when status == PENDING"
    - path: "src/gekko/dashboard/templates/edit_size_modal.html.j2"
      provides: "Non-HX path wraps base layout; HX path returns bare fragment"
    - path: "tests/integration/test_dashboard_hitl_actions.py"
      provides: "Real-SQLite HITL action integration suite"
  key_links:
    - from: "edit_size_submit duplicate branch"
      to: "_get_session_factory fresh session outside session2.begin()"
      via: "mirrors approve_proposal_endpoint duplicate branch pattern"
      pattern: "_get_session_factory.*user_id"
    - from: "edit_size_get"
      to: "base.html.j2 (non-HX path) / bare fragment (HX path)"
      via: "request.headers.get('HX-Request') check"
      pattern: "HX-Request"
    - from: "approve_proposal_endpoint / reject_proposal_endpoint / edit_size_submit"
      to: "status guard before transition_status"
      via: "check row.status before attempting transition"
      pattern: "TERMINAL_STATUSES"
---

<objective>
Gap-closure plan 03-15: fix the 3-bug cluster surfaced during live UAT Test 2 (third_round_2026_06_22) and add the real-SQLite integration coverage that caused these bugs to be invisible.

Purpose: All three bugs share a root theme — dashboard action endpoints lack real-session integration tests; existing unit tests mock claim_action / transition_status / append_event, hiding seam failures at exactly those integration points.

Output:
- routes.py: Bug B fix (fresh session for duplicate re-read in edit_size_submit), Bug A fix (non-HX deeplink wraps base layout), Bug C fix (terminal-state guard in approve, reject, and edit-submit before transition_status call).
- _proposal_card.html.j2: suppress action buttons for all non-PENDING statuses.
- tests/integration/test_dashboard_hitl_actions.py: real-SQLite integration suite covering the full coverage matrix.
</objective>

<execution_context>
@C:\Users\chris.platika\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris.platika\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-CONTEXT.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-UI-SPEC.md
@.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-HUMAN-UAT.md
@src/gekko/dashboard/routes.py
@src/gekko/approval/dedup.py
@src/gekko/approval/proposals.py
@src/gekko/dashboard/templates/edit_size_modal.html.j2
@src/gekko/dashboard/templates/_proposal_card.html.j2
@src/gekko/dashboard/templates/base.html.j2
@tests/conftest.py
@tests/integration/test_dedup_race.py
@tests/integration/test_dashboard_approve_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Bug B (duplicate fresh-session) and Bug C (terminal-state guard) in routes.py</name>
  <files>src/gekko/dashboard/routes.py</files>

  <read_first>
    Read the following specific sections before writing any code:
    1. approve_proposal_endpoint (~lines 320–413): the CORRECT pattern for the duplicate re-read branch — after the `async with sf() as session, session.begin()` block closes, it opens a FRESH `sf2, engine2 = _get_session_factory(user_id)` in its own `try/finally` with `engine2.dispose()`. This is the Bug B analog to mirror.
    2. reject_proposal_endpoint (~lines 416–484): identical fresh-session duplicate pattern.
    3. edit_size_submit (~lines 899–991): the BROKEN duplicate branch at the `else:` clause (~lines 968–977) re-reads `updated_row` on `session2` INSIDE `async with sf3() as session2, session2.begin()`. After `claim_action` calls `session.rollback()` on the duplicate path, any `.execute()` on that rolled-back session raises SQLAlchemy `InvalidRequestError`. This is Bug B.
    4. dedup.py lines 151–155: `await session.rollback()` is called on the MANDATORY IntegrityError path. The outer `begin()` block is now unusable. This confirms that any subsequent `.execute()` on session2 will fail.
    5. proposals.py `transition_status` (~lines 115–170): raises `ValueError("Invalid proposal status transition: ...")` when the current row status is not in STATE_TRANSITIONS for (from_status, to_status). Bug C: a FILLED/EXPIRED/REJECTED proposal's transition attempt raises, producing a 500.
  </read_first>

  <behavior>
    - Bug B: After claim_action returns "duplicate" inside the `session2.begin()` block, the session is rolled back. The `else:` re-read that follows on `session2` crashes. Fix: move the duplicate re-read to OUTSIDE the `async with sf3() as session2, session2.begin()` block, using a FRESH `sf4, engine4 = _get_session_factory(user_id)` in its own `try/finally`. The `engine3.dispose()` in the existing `finally` remains correct for the first-write path; the new `engine4.dispose()` covers the duplicate path's fresh session. The variable must be named distinctly (e.g. `updated_row` via `sf4`).
    - Bug C: Define a module-level constant `_TERMINAL_STATUSES = frozenset({"FILLED", "EXPIRED", "REJECTED", "FAILED", "APPROVED", "APPROVED_LIVE", "EXECUTING", "AWAITING_2ND_CHANNEL"})`. Before calling `approve_proposal` / `reject_proposal` / `transition_status(from_status="PENDING", to_status="APPROVED")` in each of the three action endpoints (approve_proposal_endpoint, reject_proposal_endpoint, edit_size_submit), insert a check: if `row.status not in ("PENDING",)` (or equivalently `row.status in _TERMINAL_STATUSES`), skip the transition and set a flag so the endpoint re-renders the current card with HTTP 200 ("already handled"). Do NOT solely rely on catching ValueError — the status check is the primary prevention; add a secondary `except ValueError` around `transition_status` calls as a defensive belt, but the pre-check guard must be present.
    - Apply the Bug C guard to all three endpoints: approve_proposal_endpoint, reject_proposal_endpoint, and edit_size_submit.
    - Do NOT alter _check_edit_size_caps, OrderGuard, the dedup UNIQUE constraint, or the dual-channel first-live gate.
    - Money math stays Decimal; normalize_decimals before append_event (unchanged — existing code already does this).
    - All DB access goes through `_get_session_factory(user_id)` with engine dispose in finally (established pattern).
  </behavior>

  <action>
    Step 1 — Add _TERMINAL_STATUSES constant near the top of routes.py (after imports, before first route handler). Value: frozenset of all statuses that are not PENDING: FILLED, EXPIRED, REJECTED, FAILED, APPROVED, APPROVED_LIVE, EXECUTING, AWAITING_2ND_CHANNEL.

    Step 2 — Bug C: approve_proposal_endpoint. Inside the `async with sf() as session, session.begin():` block, after loading the row and confirming `outcome == "first_write"`: before calling `await approve_proposal(session, proposal_id, actor=user_id)`, check `if row.status in _TERMINAL_STATUSES`. If so, do not call approve_proposal or transition_status; set a local `already_terminal = True`. After the begin block, if `already_terminal`, re-read via the existing fresh `sf2, engine2` pattern and render the card with 200 (same re-render path as the duplicate branch). Add a defensive `except ValueError` around the `approve_proposal` call that also sets `already_terminal = True` and rolls back the session before re-read.

    Step 3 — Bug C: reject_proposal_endpoint. Same pattern as Step 2 applied to `await reject_proposal(session, proposal_id, actor=user_id)`.

    Step 4 — Bug B + Bug C: edit_size_submit. Inside the `async with sf3() as session2, session2.begin():` block, after loading `row2` on `outcome == "first_write"`:
      - Add terminal-state check: if `row2.status in _TERMINAL_STATUSES`, do not call transition_status; set `already_terminal = True`.
      - Add defensive `except ValueError` around `transition_status(session2, proposal_id, from_status="PENDING", to_status="APPROVED")` that also sets `already_terminal = True`.
      - For the `else:` (duplicate) branch: remove the `updated_row = (await session2.execute(...)).scalar_one_or_none()` re-read that is currently INSIDE the begin block. Leave the `else:` block empty (just `pass`) so the begin block exits cleanly without any execute on the rolled-back session.
      After the `finally: if engine3 is not None: await engine3.dispose()` block:
      - Add a new block: `if outcome == "duplicate" or already_terminal:` that opens `sf4, engine4 = _get_session_factory(user_id)` in a `try/finally`, reads the proposal row with `engine4.dispose()` in finally, and assigns `updated_row`.
      This ensures the duplicate path and terminal path both get a clean re-read outside any rolled-back transaction.

    Step 5 — Verify no operation on a session after claim_action's rollback in any of the three endpoints.
  </action>

  <verify>
    <automated>
      python -m pytest tests/integration/test_dashboard_approve_flow.py tests/integration/test_dashboard_edit_size_happy.py tests/integration/test_dedup_race.py -x -q
    </automated>
  </verify>

  <done>
    - _TERMINAL_STATUSES frozenset defined in routes.py
    - edit_size_submit duplicate branch re-reads via a FRESH _get_session_factory call OUTSIDE the sf3 begin block; no .execute() on session2 after rollback
    - approve_proposal_endpoint, reject_proposal_endpoint, and edit_size_submit each contain a status guard that returns 200 with the current card when the proposal is already in a terminal state
    - Existing integration tests (approve_flow, edit_size_happy, dedup_race) still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Bug A (non-HX deeplink wraps base layout) and suppress terminal-state action buttons</name>
  <files>
    src/gekko/dashboard/routes.py
    src/gekko/dashboard/templates/edit_size_modal.html.j2
    src/gekko/dashboard/templates/_proposal_card.html.j2
  </files>

  <read_first>
    1. edit_size_get handler (~lines 492–658): the final `return templates.TemplateResponse(request, "edit_size_modal.html.j2", {...})` unconditionally returns the bare partial. There is no check for whether the request is a direct browser navigation vs. an HTMX swap.
    2. base.html.j2: the full layout — `<!DOCTYPE html>`, head with CSP meta tag, `<link rel="stylesheet" href="/static/tailwind.css">`, `<script src="/static/htmx.min.js" defer></script>`, banner, header, `{% block content %}{% endblock %}`, modal-mount, footer. The non-HX path must embed the modal HTML inside this layout. CSP is `script-src 'self'` — no external script src permitted.
    3. edit_size_modal.html.j2: the bare `<div class="modal-backdrop"...>` fragment. The `<script src="/static/edit-size-slider.js"></script>` tag at the bottom loads the same-origin slider JS — this is fine under `script-src 'self'`.
    4. _proposal_card.html.j2 (~lines 66–97): the conditional actions block currently shows approve/reject/edit-size buttons only when `status == "PENDING"` (line 67), and shows a static EXPIRED status line when `status == "EXPIRED"` (line 89), and shows the dual-channel link when `status == "AWAITING_2ND_CHANNEL"` (line 93). There is no rendering for other terminal statuses (FILLED, REJECTED, FAILED, APPROVED, EXECUTING, APPROVED_LIVE) — these statuses render no actions block at all, which is correct. However, the Bug C diagnosis states cards "likely still render action buttons on terminal proposals" — confirm the actual template and document whether a fix is needed or the current template is already correct.
  </read_first>

  <action>
    Bug A fix in edit_size_get:
    1. Before the final return statement in edit_size_get, check: `is_htmx = request.headers.get("HX-Request") == "true"`.
    2. If `is_htmx` is True: return the existing bare-fragment response (unchanged behavior for HTMX swap into #modal-mount).
    3. If `is_htmx` is False (direct browser navigation from Slack URL button): render a full HTML page. The implementation must wrap the modal content in the base layout. Preferred approach: render `edit_size_modal.html.j2` to a string (using `templates.get_template("edit_size_modal.html.j2").render(ctx)`) and inject it as the content of a minimal inline page that includes the same `<link>` and `<script>` tags as base.html.j2 (do NOT extend base.html.j2 with block overrides unless the template system already supports it). Alternatively, redirect to `/approvals` — simpler, avoids a new template, and the user lands on the styled page where HTMX then loads the modal via the normal path. If redirecting, append `?open_edit={proposal_id}` as a query param so the approvals page can auto-open the modal via a small hx-trigger or inline script at page load. Whichever approach is simpler given the template structure, choose it. Document the choice in the task action log. The key invariant: `GET /approvals/{id}/edit-size` without HX-Request returns a response with a `<html>` tag (either inline full page or redirect to approvals page); with `HX-Request: true` it returns the bare fragment without `<html>`.
    4. CSP constraint: if constructing an inline page, the `<script src="/static/edit-size-slider.js">` already present in the fragment is same-origin and CSP-compliant. Do NOT add any `<script src="https://...">` without an SRI integrity attribute — the SRI lint test (`tests/unit/test_dashboard_templates_sri.py`) will fail.

    _proposal_card.html.j2 terminal-state button suppression:
    After reading the template, the action buttons are already inside `{% if status == "PENDING" %}`. If the template already suppresses buttons for non-PENDING statuses, no change is needed — document this observation in the commit message. If any terminal state incorrectly renders interactive action buttons, add the appropriate suppression. The primary terminal-state concern from Bug C is: proposals in FILLED, REJECTED, FAILED, APPROVED, EXECUTING, APPROVED_LIVE states should render a read-only status line (similar to the EXPIRED path) rather than action buttons. Add handling for these statuses in the `{% if status == "PENDING" %}... {% elif status == "EXPIRED" %}...{% endif %}` block so all terminal statuses display a static status chip rather than an empty/invisible actions area.

    Do NOT add any inline event handlers (oninput, onclick, etc.) — CSP script-src 'self' blocks them. JS binding goes in .js files only.
  </action>

  <verify>
    <automated>
      python -m pytest tests/unit/test_dashboard_templates_sri.py -x -q
    </automated>
  </verify>

  <acceptance_criteria>
    - `GET /approvals/{id}/edit-size` without `HX-Request` header: response text contains `&lt;html` (full page) OR returns 3xx redirect to /approvals (either satisfies the "styled page" requirement).
    - `GET /approvals/{id}/edit-size` with `HX-Request: true` header: response does NOT contain `&lt;!DOCTYPE html` — it is the bare fragment.
    - SRI lint test (`test_dashboard_templates_sri.py`) still passes — no new external script src without integrity attr introduced.
    - _proposal_card.html.j2: Jinja2 `{% if status == "PENDING" %}` block contains all interactive action buttons; no action buttons rendered outside this block.
  </acceptance_criteria>

  <done>
    - edit_size_get checks HX-Request header; non-HX path returns full-page HTML or redirects to /approvals; HTMX-swap path returns bare fragment unchanged
    - _proposal_card.html.j2 action buttons confirmed/enforced as PENDING-only
    - SRI lint test still passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Real-SQLite integration test suite for dashboard HITL actions</name>
  <files>tests/integration/test_dashboard_hitl_actions.py</files>

  <read_first>
    1. tests/conftest.py: `temp_sqlcipher_db` fixture (yields AsyncEngine with Base.metadata schema created, no Alembic), `_TEST_PASSPHRASE = "test-passphrase"`, `clean_settings_env` fixture. The `_get_session_factory` monkeypatch seam: tests pass `lambda _uid: (sf, None)` where `sf = make_session_factory(temp_sqlcipher_db)`.
    2. tests/integration/test_dedup_race.py: full example of the real-SQLite integration pattern — `_seed_race_start` creates User + Strategy + PENDING Proposal rows via `make_session_factory(temp_sqlcipher_db)`, then `monkeypatch.setattr(module, "_get_session_factory", lambda _u: (sf, None))` for both `dedup` and `executor` modules, no mocking of `claim_action` or `transition_status`.
    3. src/gekko/approval/dedup.py: `_get_session_factory` symbol is `gekko.approval.dedup._get_session_factory` — must be patched on this module.
    4. src/gekko/dashboard/routes.py: `_get_session_factory` symbol is `gekko.dashboard.routes._get_session_factory` — must be patched on this module.
    5. The FastAPI app is created via `from gekko.dashboard.app import create_app`. Login via `POST /login` with `data={"passphrase": _TEST_PASSPHRASE}` is required before hitting auth-gated endpoints (per Plan 03-08 two-router pattern).
    6. `_build_proposal_ctx` in routes.py expects the proposal row to have `payload_json` with valid JSON containing ticker, side, qty, rationale, evidence fields. See `_make_trade_proposal` helper in test_dedup_race.py for the canonical minimal TradeProposal shape (3 EvidenceSnippet items, 1 AlternativeConsidered, 32-char client_order_id).
  </read_first>

  <behavior>
    The test file MUST NOT mock claim_action, transition_status, or append_event. All three must run against the real SQLite engine.

    Coverage matrix — 8 test cases minimum:

    Test 1 — approve first_write happy path:
      Seed PENDING proposal. POST /approvals/{id}/approve. Assert: HTTP 200, response contains proposal card HTML, proposal row in DB has status APPROVED, SlackActionDedup row exists with action_id="approve_proposal" and source="dashboard".

    Test 2 — approve duplicate (second call on same proposal):
      Seed PENDING proposal. POST /approvals/{id}/approve twice. Both calls return HTTP 200 (never 500). Second call response contains proposal card reflecting current state.

    Test 3 — approve on terminal proposal (Bug C gate):
      Seed proposal with status="FILLED". POST /approvals/{id}/approve. Assert: HTTP 200, response is a proposal card (not a 500 or error page), no ValueError raised.

    Test 4 — edit-submit first_write happy path:
      Seed PENDING proposal with payload_json containing a valid TradeProposal. POST /approvals/{id}/edit-submit with qty matching the proposed qty (or within cap). Assert: HTTP 200, proposal row status=APPROVED, SlackActionDedup row with action_id="edit_size".

    Test 5 — edit-submit duplicate (Bug B regression gate):
      Seed PENDING proposal. POST /approvals/{id}/edit-submit once (first_write). POST /approvals/{id}/edit-submit again (duplicate). Both return HTTP 200. Second call must NOT return 500. This is the load-bearing Bug B regression test.

    Test 6 — edit-submit on terminal proposal (Bug C gate for edit path):
      Seed proposal with status="FILLED". POST /approvals/{id}/edit-submit. Assert: HTTP 200, proposal card returned, no ValueError/500.

    Test 7 — reject first_write happy path:
      Seed PENDING proposal. POST /approvals/{id}/reject. Assert: HTTP 200, proposal row status=REJECTED.

    Test 8 — edit-size GET: HX-Request fragment vs direct-nav full page (Bug A regression gate):
      Seed PENDING proposal. GET /approvals/{id}/edit-size with header HX-Request: true → assert 200 + response text does NOT contain "<!DOCTYPE" (bare fragment). GET /approvals/{id}/edit-size WITHOUT HX-Request header → assert 200 + response text contains "<html" OR assert 3xx redirect (depending on which implementation was chosen in Task 2).

    Fixture pattern:
      - Use `temp_sqlcipher_db` from conftest.
      - `sf = make_session_factory(temp_sqlcipher_db)`.
      - Set passphrase: `import gekko.vault.passphrase as _vault; _vault.set_passphrase("test-passphrase")`.
      - Monkeypatch `gekko.dashboard.routes._get_session_factory` and `gekko.approval.dedup._get_session_factory` both to `lambda _uid: (sf, None)`.
      - Mock `gekko.execution.executor.execute_proposal` (AsyncMock) and `asyncio.create_task` to prevent real broker calls and allow test completion without background tasks.
      - Mock `gekko.brokers.alpaca.AlpacaBroker.get_account` for edit_size_get/edit_size_submit equity fetch (AsyncMock returning {"equity": "10000"}).
      - Set required env: monkeypatch GEKKO_USER_ID, ANTHROPIC_API_KEY, ALPACA_PAPER_API_KEY, ALPACA_PAPER_SECRET_KEY, SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_USER_ID (match pattern from test_dedup_race.py).
      - Clear audit locks: `from gekko.audit import log as _audit_log; _audit_log._append_locks.clear()`.
      - Create app via `create_app()` INSIDE the monkeypatch context so it reads the patched session factory.
      - `httpx.AsyncClient(transport=httpx.ASGITransport(app=app), base_url="http://test", follow_redirects=False)`.
      - Login: `POST /login` with `data={"passphrase": "test-passphrase"}` before hitting gated endpoints.
      - In `finally`: call `_vault.clear()` and `get_settings.cache_clear()`.

    Seed helper `_seed_pending_proposal(sf, user_id, proposal_id, status="PENDING")`:
      Use the same FK-ordered INSERT pattern as test_dedup_race.py: add User, flush, add Strategy, flush, add Proposal. Use `TradeProposal` from `gekko.schemas.proposal` to build a valid `payload_json` with 3 EvidenceSnippet items, 1 AlternativeConsidered, 32-char `client_order_id = "d" * 32`, `order_type="market"`, `account_mode="PAPER"`, `target_notional_usd="10000"`, `limit_price=None`, `stop_price=None`, `qty="100"`.
  </behavior>

  <action>
    Create `tests/integration/test_dashboard_hitl_actions.py` with:

    File-level marker: `pytestmark = pytest.mark.integration`

    Imports: pytest, pytest_asyncio, httpx, asyncio, json, from decimal import Decimal, from sqlalchemy import select, from unittest.mock import AsyncMock MagicMock patch, and all gekko domain imports needed (make_session_factory, models, schemas, vault, config, dashboard.app).

    Module-level constant: `_TEST_PASSPHRASE = "test-passphrase"` (matches conftest).

    `_make_trade_proposal(user_id, decision_id)` helper: builds and returns a `TradeProposal` instance (mirrors the helper in test_dedup_race.py). Use qty=Decimal("100"), target_notional_usd=Decimal("10000"), limit_price=None, stop_price=None, order_type="market".

    `_seed_pending_proposal(sf, user_id, proposal_id, status="PENDING")` async helper: seeds User + Strategy + Proposal rows with the FK-ordered flush pattern. Sets `payload_json = tp.model_dump_json()` where `tp = _make_trade_proposal(user_id, proposal_id)`. Returns the `(strategy_id, tp)` tuple.

    One module-level or session-scoped `_setup_app_and_client(sf, monkeypatch, vault_pass)` context manager or helper that: patches routes._get_session_factory, dedup._get_session_factory, executor.execute_proposal, asyncio.create_task, and AlpacaBroker.get_account, calls `create_app()`, yields the httpx.AsyncClient with login cookie already obtained.

    Eight `@pytest.mark.asyncio` test functions, one per coverage case above. Each uses `temp_sqlcipher_db` and `monkeypatch` fixtures. Each calls `_vault.clear()` and `get_settings.cache_clear()` in a `finally` block.

    For Test 5 (Bug B regression): assert that the second `edit-submit` POST response status_code is 200 and the text contains "proposal-card" (not a 500 or SQLAlchemy error traceback). This is the load-bearing regression gate.

    For Test 3 and Test 6 (Bug C gates): seed the proposal with `status="FILLED"` directly in the INSERT. Assert response status_code == 200 and response text does not contain "500" or "ValueError".

    For Test 8 (Bug A gate): do not mock edit_size_get's DB read — seed a real PENDING proposal and let the route load it. Send one request with headers={"HX-Request": "true"} and one without.
  </action>

  <verify>
    <automated>
      python -m pytest tests/integration/test_dashboard_hitl_actions.py -x -q
    </automated>
  </verify>

  <acceptance_criteria>
    - All 8 tests in test_dashboard_hitl_actions.py pass (exit 0).
    - Test 5 (Bug B duplicate edit-submit) explicitly asserts HTTP 200 for the second call — this is the non-negotiable regression gate.
    - Test 3 and Test 6 (Bug C terminal state) explicitly assert HTTP 200 — non-negotiable gates.
    - Test 8 (Bug A) asserts fragment vs. full-page behavior — non-negotiable gate.
    - No claim_action, transition_status, or append_event patches in this test file.
    - Existing suites: `python -m pytest tests/integration/test_dashboard_approve_flow.py tests/integration/test_dashboard_edit_size_happy.py tests/integration/test_dedup_race.py tests/integration/test_p3_walking_skeleton.py -x -q` all pass.
  </acceptance_criteria>

  <done>
    - tests/integration/test_dashboard_hitl_actions.py exists with 8 passing tests
    - No mocks of claim_action, transition_status, or append_event
    - All existing integration suites remain green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dashboard POST /approvals/{id}/approve | Auth-gated (require_session on router); user_id is session-derived, not from request body. Input: proposal_id from URL path. |
| browser → dashboard POST /approvals/{id}/edit-submit | Auth-gated; qty from form body (operator input). |
| browser → dashboard GET /approvals/{id}/edit-size | Auth-gated; HX-Request header from browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15-01 | Tampering | transition_status in approve/reject/edit-submit | mitigate | Status guard checks row.status before transition; ValueError caught defensively. Terminal proposals return 200 with current card — no state mutation on already-closed proposals. |
| T-03-15-02 | Repudiation | duplicate re-read fresh session in edit_size_submit | mitigate | claim_action UNIQUE INSERT is the dedup primitive (D-41). The fresh-session re-read only reads the current state for rendering; no write occurs on the duplicate path. audit dedup_click event is still written by claim_action's fresh-session path in dedup.py. |
| T-03-15-03 | Elevation of Privilege | non-HX edit_size_get returning full page | accept | The full-page response contains no additional capabilities — it renders the same edit-size modal content that HTMX would swap. The user must be authenticated (require_session) for either path. No new attack surface opened. |
| T-03-15-04 | Information Disclosure | error tracebacks leaking in 500 responses | mitigate | Bug B and Bug C fixes eliminate the 500 paths. Defensive ValueError catch in transition_status callers ensures terminal-state clicks render the card, not a traceback. FastAPI's default exception handler sanitizes 500 responses in production. |
| T-03-15-05 | Denial of Service | repeated terminal-state clicks causing DB write storms | accept | No write occurs on the terminal-state path (status guard prevents transition_status; claim_action duplicate path only writes a dedup_click audit event, bounded by the UNIQUE constraint). One dedup_click per repeated action — not a write storm. |
| T-03-15-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. All fixes use existing gekko dependencies. |
</threat_model>

<verification>
Run after all three tasks complete:

```
python -m pytest tests/integration/test_dashboard_hitl_actions.py -v
python -m pytest tests/integration/test_dedup_race.py tests/integration/test_dashboard_approve_flow.py tests/integration/test_dashboard_edit_size_happy.py tests/integration/test_p3_walking_skeleton.py -v
python -m pytest tests/unit/test_dashboard_templates_sri.py -v
```

All three commands must exit 0.

Manual spot-check (after `uv run gekko serve`):
1. Bug B: click "Edit size" on a proposal, submit. Click "Edit size" on the same proposal again and submit. Second response should be a styled card, not a 500.
2. Bug A: copy the Slack "Edit size" URL button URL (format: http://localhost:8000/approvals/{id}/edit-size), open it directly in a browser tab (not via HTMX). Page should be styled (shows CSS/nav).
3. Bug C: approve an already-FILLED proposal card (if one is visible). Should return 200 with current card, not 500.
</verification>

<success_criteria>
- All 8 cases in test_dashboard_hitl_actions.py pass with real SQLite (no claim_action / transition_status / append_event mocks)
- Bug B: second edit-submit POST on the same proposal returns HTTP 200
- Bug A: GET /approvals/{id}/edit-size without HX-Request returns a full HTML page (contains html tag) or redirects to /approvals
- Bug C: POST approve/reject/edit-submit against FILLED/EXPIRED/REJECTED returns HTTP 200
- _proposal_card.html.j2 action buttons are inside `{% if status == "PENDING" %}` block only
- Existing suites (approve_flow, edit_size_happy, dedup_race, walking_skeleton, SRI lint) remain green
</success_criteria>

<output>
Create `.planning/phases/03-production-hitl-ux-slack-block-kit-dashboard-fallback/03-15-SUMMARY.md` when done.
</output>
