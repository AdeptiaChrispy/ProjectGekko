---
phase: 04-agent-architecture-cost-bounds
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - migrations/versions/0005_p4_cost_ceiling.py
  - src/gekko/db/models.py
  - src/gekko/agent/pricing.py
autonomous: true
requirements:
  - COST-01
  - COST-03
  - COST-05

must_haves:
  truths:
    - "Alembic 0005 migration applies cleanly against a real SQLCipher DB (upgrade + downgrade round-trip)"
    - "users table gains daily_cost_ceiling_usd (TEXT, server_default '5.00'), cost_alert_80_sent_date (TEXT nullable), cost_alert_100_sent_date (TEXT nullable)"
    - "events ck_event_type CheckConstraint now accepts llm_cost and suspicious_content"
    - "User ORM model has the three new Phase-4 Mapped columns"
    - "_EVENT_TYPES tuple in models.py includes llm_cost and suspicious_content"
    - "pricing.py exports SONNET_INPUT_PER_MTOK, SONNET_OUTPUT_PER_MTOK, HAIKU_INPUT_PER_MTOK, HAIKU_OUTPUT_PER_MTOK, DEFAULT_DAILY_CEILING_USD, tokens_to_usd()"
    - "test_pricing.py turns GREEN"
  artifacts:
    - path: "migrations/versions/0005_p4_cost_ceiling.py"
      provides: "Alembic revision adding 3 users columns + 2 event_types to CheckConstraint"
    - path: "src/gekko/db/models.py"
      provides: "User ORM with 3 new Mapped columns + _EVENT_TYPES extended"
    - path: "src/gekko/agent/pricing.py"
      provides: "Pricing constants (Decimal) + tokens_to_usd() fallback formula"
  key_links:
    - from: "migrations/versions/0005_p4_cost_ceiling.py"
      to: "src/gekko/db/models.py"
      via: "frozen vocabulary copy"
      pattern: "_FROZEN_EVENT_TYPES_POST.*llm_cost.*suspicious_content"
    - from: "src/gekko/agent/pricing.py"
      to: "src/gekko/agent/cost_ceiling.py"
      via: "import DEFAULT_DAILY_CEILING_USD"
      pattern: "DEFAULT_DAILY_CEILING_USD"
---

<objective>
Wave 2 schema substrate — the two changes that everything else in Phase 4 depends on: (1) Alembic migration 0005 that extends the users table with cost ceiling columns and the events CheckConstraint with the two new event_types, plus ORM model alignment; (2) pricing constants module that is the canonical location for all model pricing data (per D-10 discretion decision: store pricing in a single maintainable module).

Purpose: Every subsequent plan writes `llm_cost` and `suspicious_content` events — without migration 0005 those writes fail at the DB CheckConstraint. The pricing module is imported by cost_ceiling.py (Wave 3) for the fallback formula and by test_pricing.py (already stubbed in Wave 1).

Output: Migration file, updated models.py, new pricing.py. test_pricing.py turns GREEN.
</objective>

<execution_context>
@C:\Users\chris.platika\Desktop\Project Gekko\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris.platika\Desktop\Project Gekko\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-CONTEXT.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-PATTERNS.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-RESEARCH.md
@C:\Users\chris.platika\Desktop\Project Gekko\src\gekko\db\models.py
@C:\Users\chris.platika\Desktop\Project Gekko\migrations\versions\0004_p3_hitl_ux.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic migration 0005 + ORM User model extension</name>
  <files>
    migrations/versions/0005_p4_cost_ceiling.py,
    src/gekko/db/models.py
  </files>
  <read_first>
    - migrations/versions/0004_p3_hitl_ux.py — exact pattern to mirror: file header, revision wiring, frozen vocabulary constants, _in_check helper, batch_alter_table pattern, upgrade() + downgrade() structure
    - src/gekko/db/models.py — lines 94-112 (_EVENT_TYPES), lines 180-192 (Phase-3 User columns to mirror), lines 115-117 (_in_check function)
    - 04-PATTERNS.md §migrations/versions/0005_p4_cost_ceiling.py — complete pattern including frozen vocabulary and column definitions
    - 04-RESEARCH.md §Pattern 3 — exact SQL column types and server_default values
  </read_first>
  <behavior>
    Migration 0005 upgrade():
    - batch_alter_table("users"): add daily_cost_ceiling_usd (String, nullable=True, server_default="'5.00'"); add cost_alert_80_sent_date (String, nullable=True); add cost_alert_100_sent_date (String, nullable=True)
    - batch_alter_table("events"): drop_constraint("ck_event_type", type_="check"); create_check_constraint("ck_event_type", _in_check("event_type", _FROZEN_EVENT_TYPES_POST)) where _FROZEN_EVENT_TYPES_POST adds "llm_cost" and "suspicious_content" to the end of _FROZEN_EVENT_TYPES_PRE
    Migration 0005 downgrade() reverses in opposite order (events CHECK first, then users columns)

    models.py User class changes:
    - Append three new Mapped columns after the Phase-3 quiet-hours block (after line 192)
    - daily_cost_ceiling_usd: Mapped[str | None] = mapped_column(String, nullable=True)
    - cost_alert_80_sent_date: Mapped[str | None] = mapped_column(String, nullable=True)
    - cost_alert_100_sent_date: Mapped[str | None] = mapped_column(String, nullable=True)
    - Extend _EVENT_TYPES tuple with "llm_cost" and "suspicious_content" at the end (preserving existing order)
  </behavior>
  <action>
    Create migrations/versions/0005_p4_cost_ceiling.py using 0004_p3_hitl_ux.py as the direct template. Key callouts:
    - revision = "0005_p4_cost_ceiling"; down_revision = "0004_p3_hitl_ux"
    - _in_check helper: copy verbatim from models.py lines 115-117 (do NOT import from models — migrations are frozen artifacts)
    - _FROZEN_EVENT_TYPES_PRE: copy ALL entries from _EVENT_TYPES in models.py at time of writing (the current 17-entry tuple); _FROZEN_EVENT_TYPES_POST adds "llm_cost" and "suspicious_content"
    - server_default value for daily_cost_ceiling_usd: use `"'5.00'"` (single-quoted string inside the double-quoted Python string — this is what SQLite stores as the default)
    - Use op.batch_alter_table for ALL SQLite ALTER TABLE operations per Phase-2/3 convention

    Edit models.py to extend _EVENT_TYPES and add the three User columns. Place the column additions immediately after the Phase-3 quiet-hours comment block (after the `timezone` column). Add a comment header: `# Phase-4 / D-02 + D-12 daily cost ceiling + alert-sent-date columns.`

    Do NOT change the User.__repr__ — the three new columns are not credential-sensitive and do not need exclusion.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m alembic -x passphrase=test_passphrase upgrade head 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - 0005_p4_cost_ceiling.py created; `alembic upgrade head` runs without error
    - alembic downgrade 0004_p3_hitl_ux then upgrade head round-trip succeeds
    - users table has daily_cost_ceiling_usd, cost_alert_80_sent_date, cost_alert_100_sent_date columns
    - events ck_event_type CHECK accepts "llm_cost" and "suspicious_content"
    - _EVENT_TYPES in models.py contains "llm_cost" and "suspicious_content"
    - .venv/Scripts/python.exe -m pytest tests/unit/ -x -q remains GREEN (existing suite)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pricing.py — constants module (D-10 discretion, per D-02 default)</name>
  <files>
    src/gekko/agent/pricing.py
  </files>
  <read_first>
    - 04-PATTERNS.md §src/gekko/agent/pricing.py — complete pattern including imports, constant declarations with source comments, tokens_to_usd() signature, __all__
    - 04-RESEARCH.md §RQ-2 — verified pricing: Sonnet 4.6 $3.00/$15.00 per MTok, Haiku 4.5 $1.00/$5.00 per MTok
    - src/gekko/agent/budget.py — analog: small module, module-level constants, __all__ pattern
    - tests/unit/test_pricing.py — the stub tests that this task turns GREEN
  </read_first>
  <behavior>
    - SONNET_INPUT_PER_MTOK = Decimal("3.00")
    - SONNET_OUTPUT_PER_MTOK = Decimal("15.00")
    - HAIKU_INPUT_PER_MTOK = Decimal("1.00")
    - HAIKU_OUTPUT_PER_MTOK = Decimal("5.00")
    - DEFAULT_DAILY_CEILING_USD = Decimal("5.00") — D-02 configurable default; override in Settings
    - tokens_to_usd(input_tokens: int, output_tokens: int, *, model: str = "sonnet") -> Decimal — formula fallback only; used when ResultMessage.total_cost_usd is None
    - MTOK = Decimal("1_000_000") (or 1000000) for the division; formula: (Decimal(input_tokens) / MTOK) * input_rate + (Decimal(output_tokens) / MTOK) * output_rate
    - tokens_to_usd raises ValueError for unknown model string (not silently returns 0)
  </behavior>
  <action>
    Create src/gekko/agent/pricing.py. Module-level Decimal constants with inline source comments (pricing page URL + verification date). tokens_to_usd() uses a dict dispatch on the model argument: map "sonnet" → (SONNET_INPUT_PER_MTOK, SONNET_OUTPUT_PER_MTOK), "haiku" → (HAIKU_INPUT_PER_MTOK, HAIKU_OUTPUT_PER_MTOK). ValueError if model not in the dict. __all__ exports all constants and the function. No external dependencies beyond stdlib decimal.

    IMPORTANT: DEFAULT_DAILY_CEILING_USD is defined here as the authoritative constant for the default value. The migration server_default='5.00' and the Settings form placeholder='5.00' are derived copies, but this module is the single named source for code that needs to reference the default programmatically.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_pricing.py -x -q</automated>
  </verify>
  <done>test_pricing.py passes GREEN — all pricing constants and tokens_to_usd() assertions pass. No import errors. Sonnet $3/$15, Haiku $1/$5 per MTok verified by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | Schema change adds user-configurable cost ceiling column with server_default |
| pricing constants → cost ledger | Decimal constants are the fallback formula source; prefer SDK total_cost_usd over formula |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | daily_cost_ceiling_usd column | mitigate | Column is user-scoped (read/write gated by user_id in every query); Settings route uses require_session per Plan 03-08 pattern; D-21 multi-tenant invariant preserved |
| T-04-03 | Information Disclosure | pricing.py constants | accept | Pricing constants are public information (from Anthropic docs); no credentials or PII |
| T-04-04 | Tampering | migration frozen vocabulary | mitigate | _FROZEN_EVENT_TYPES_PRE is a literal copy of models.py at write time — validated by the existing test suite running against a real migrated DB |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — all dependencies already installed and in-use |
</threat_model>

<verification>
After both tasks complete:
- `.venv/Scripts/python.exe -m pytest tests/unit/test_pricing.py -x -q` passes GREEN
- `.venv/Scripts/python.exe -m alembic upgrade head` succeeds
- `.venv/Scripts/python.exe -m pytest tests/unit/ -x -q` — existing suite stays GREEN
- Manual check: `users` table schema has the 3 new columns; `events` ck_event_type accepts "llm_cost"
</verification>

<success_criteria>
- Migration 0005 applies and reverses cleanly (upgrade + downgrade round-trip)
- users.daily_cost_ceiling_usd TEXT DEFAULT '5.00', users.cost_alert_80_sent_date, users.cost_alert_100_sent_date exist
- events ck_event_type accepts "llm_cost" and "suspicious_content"
- pricing.py exports all 5 constants and tokens_to_usd(); test_pricing.py GREEN
- Existing unit test suite remains GREEN
</success_criteria>

<output>
Create `.planning/phases/04-agent-architecture-cost-bounds/04-02-SUMMARY.md` when done.
</output>
