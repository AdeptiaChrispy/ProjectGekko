---
phase: 04-agent-architecture-cost-bounds
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/gekko/agent/cost_ceiling.py
  - src/gekko/agent/runtime.py
  - src/gekko/execution/executor.py
autonomous: true
requirements:
  - COST-01
  - COST-04

must_haves:
  truths:
    - "CeilingCheck dataclass + check_cost_ceiling() async function exist in gekko.agent.cost_ceiling"
    - "check_cost_ceiling() is deterministic (no LLM calls) and fires before any query() in trigger_strategy_run()"
    - "At 80% ceiling: check_cost_ceiling() returns action=degrade; at 100%: returns action=halt"
    - "halt branch in trigger_strategy_run() returns outcome=skipped_cost_halt without calling query()"
    - "just_crossed_80/just_crossed_100 flags prevent repeat Slack DMs (D-06/D-08)"
    - "Cost-alert Slack DMs bypass quiet hours (cost_alert added to _BYPASS_CATEGORIES per open question 3)"
    - "SC-2 gap closed: _INJECTION_PATTERNS regex in runtime.py; suspicious_content event appended when match found in brief.evidence[].quote_text"
    - "injected_content_flags: list[str] = [] added to ResearchBrief (forward-compat slot per RESEARCH §RQ-6)"
    - "test_cost_ceiling.py and test_suspicious_content.py turn GREEN"
  artifacts:
    - path: "src/gekko/agent/cost_ceiling.py"
      provides: "CeilingCheck dataclass + check_cost_ceiling() deterministic gate"
      exports: ["CeilingCheck", "check_cost_ceiling"]
    - path: "src/gekko/agent/runtime.py"
      provides: "Cost ceiling gate insertion after quiet-hours gate; _INJECTION_PATTERNS regex; suspicious_content event write"
    - path: "src/gekko/execution/executor.py"
      provides: "_BYPASS_CATEGORIES extended with cost_alert"
  key_links:
    - from: "src/gekko/agent/runtime.py"
      to: "src/gekko/agent/cost_ceiling.py"
      via: "await check_cost_ceiling()"
      pattern: "check_cost_ceiling"
    - from: "src/gekko/agent/cost_ceiling.py"
      to: "src/gekko/db/models.py"
      via: "Event.event_type == 'llm_cost'"
      pattern: "llm_cost"
    - from: "src/gekko/agent/runtime.py"
      to: "src/gekko/audit/log.py"
      via: "append_event(..., event_type='suspicious_content')"
      pattern: "suspicious_content"
---

<objective>
Wave 3 delivers the two safety-critical behavioral changes that define Phase 4's core security posture: (1) the deterministic cost-ceiling gate (`cost_ceiling.py`) that the LLM cannot reason past, wired into `trigger_strategy_run()` immediately after the quiet-hours gate; (2) the SC-2 suspicious-content audit event gap closure — the regex scanner + `append_event` call that logs injection patterns detected in `ResearchBrief.evidence` quote text.

This plan also adds `"cost_alert"` to `_BYPASS_CATEGORIES` in executor.py so the 80%/100% Slack DMs fire immediately regardless of quiet hours (per open question 3 from RESEARCH: cost-halt is operator-safety information).

Purpose: After this plan, the ceiling guard enforces halt/degrade before any LLM call, and every injection attempt in research evidence is logged to the audit chain. Both test files turn GREEN.

Output: cost_ceiling.py (new), runtime.py (modified — gate + SC-2 scan), executor.py (modified — bypass category).
</objective>

<execution_context>
@C:\Users\chris.platika\Desktop\Project Gekko\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris.platika\Desktop\Project Gekko\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-CONTEXT.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-PATTERNS.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-RESEARCH.md
@C:\Users\chris.platika\Desktop\Project Gekko\src\gekko\agent\runtime.py
@C:\Users\chris.platika\Desktop\Project Gekko\src\gekko\approval\quiet_hours.py
@C:\Users\chris.platika\Desktop\Project Gekko\src\gekko\execution\executor.py
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-01-SUMMARY.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cost_ceiling.py — deterministic ceiling guard module</name>
  <files>
    src/gekko/agent/cost_ceiling.py
  </files>
  <read_first>
    - src/gekko/approval/quiet_hours.py — the EXACT analog: _get_session_factory() seam (lines 49-64), ZoneInfo timezone-boundary computation (lines 146-178), deterministic pre-LLM gate shape
    - 04-PATTERNS.md §src/gekko/agent/cost_ceiling.py — CeilingCheck dataclass, check_cost_ceiling() signature, cost-event row fetch pattern, "one DM" tracking via cost_alert_*_sent_date columns
    - 04-RESEARCH.md §RQ-4 — CeilingCheck dataclass definition; §RQ-3 — Python-side Decimal sum pattern (fetch rows, sum in Python — do NOT use SQL SUM on JSON field)
    - src/gekko/db/models.py — User columns (daily_cost_ceiling_usd, cost_alert_80_sent_date, cost_alert_100_sent_date added in Plan 04-02); Event model for the llm_cost query
    - src/gekko/agent/pricing.py — DEFAULT_DAILY_CEILING_USD for the null-ceiling fallback
    - tests/unit/test_cost_ceiling.py — the stub tests; understand expected call signatures
  </read_first>
  <behavior>
    CeilingCheck dataclass fields: action: Literal["allow", "degrade", "halt"], current_spend: Decimal, ceiling: Decimal, pct: Decimal (0-100), just_crossed_80: bool, just_crossed_100: bool

    check_cost_ceiling(session_factory, *, user_id: str) -> CeilingCheck:
    - Load User row (user_id) to get daily_cost_ceiling_usd (TEXT → Decimal; None → DEFAULT_DAILY_CEILING_USD)
    - Compute today_start_utc_str using user.timezone (ZoneInfo, defaulting to "America/New_York") — mirror quiet_hours.py ZoneInfo pattern exactly
    - Fetch all Event rows where user_id == user_id AND event_type == "llm_cost" AND ts >= today_start_utc_str
    - Sum cost_usd from each row's payload_json using Python Decimal: sum(Decimal(json.loads(r.payload_json)["cost_usd"]) for r in rows)
    - pct = (current_spend / ceiling) * 100 (Decimal arithmetic)
    - action: halt if pct >= 100, degrade if pct >= 80, else allow
    - just_crossed_80: True if action in (degrade, halt) AND user.cost_alert_80_sent_date != today's local date string (YYYY-MM-DD in user's timezone)
    - just_crossed_100: True if action == halt AND user.cost_alert_100_sent_date != today's local date string
    - When just_crossed_* is True, update the column on the user row (flush inside the same session) so subsequent calls return False
    - Uses _get_session_factory(user_id) seam from quiet_hours.py — monkeypatched in tests
  </behavior>
  <action>
    Create src/gekko/agent/cost_ceiling.py. Mirror quiet_hours.py's structure: module docstring, _get_session_factory() import-lazy seam, the main public async function, private helpers.

    Key implementation notes (from RESEARCH + PATTERNS):
    - JSON extract for cost_usd: fetch rows as `(Event.payload_json,)` tuples, then `json.loads(r[0])["cost_usd"]` — do NOT use SQL json_extract() which returns TEXT in SQLite
    - Decimal fallback for ceiling: if user.daily_cost_ceiling_usd is None or empty, use DEFAULT_DAILY_CEILING_USD from pricing.py
    - "One DM" gate: compare `user.cost_alert_80_sent_date` with `now_local.date().isoformat()` (NOT UTC date); update the column in the same DB session when crossing for the first time today
    - ZoneInfo import: `from zoneinfo import ZoneInfo, ZoneInfoNotFoundError` — handle ZoneInfoNotFoundError identically to quiet_hours.py (fall back to "America/New_York", log a warning)
    - The function signature accepts `session_factory` as a keyword-only argument to allow test injection without using the _get_session_factory() seam (tests pass a pre-built factory directly)
    - __all__ exports CeilingCheck and check_cost_ceiling
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_cost_ceiling.py -x -q</automated>
  </verify>
  <done>test_cost_ceiling.py passes GREEN — all 8 test functions (80% degrade, 100% halt, halt-skipped outcome, tz-midnight reset, single-DM-80, single-DM-100, triage-gate-skips stub, allow-below-80). check_cost_ceiling() exists and is importable from gekko.agent.cost_ceiling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runtime.py — ceiling gate insertion + SC-2 suspicious-content event; executor.py — cost_alert bypass</name>
  <files>
    src/gekko/agent/runtime.py,
    src/gekko/execution/executor.py
  </files>
  <read_first>
    - src/gekko/agent/runtime.py — lines 471-505 (quiet-hours gate + session_factory construction; insertion point for ceiling gate); lines 520-534 (_run_researcher and _run_decision calls; SC-2 scan goes between them); lines 395-423 (append_event caller pattern to mirror)
    - 04-PATTERNS.md §src/gekko/agent/runtime.py — ceiling guard insertion pseudocode; suspicious-content scan site; Slack bypass pattern
    - 04-RESEARCH.md §RQ-6 — injection pattern regex + the 5 patterns: "SYSTEM\s*:", "OVERRIDE\s*:", "ignore\s+previous\s+instructions", "disregard\s+your\s+instructions", "forget\s+your\s+instructions" (re.IGNORECASE)
    - src/gekko/execution/executor.py — lines 249-254 (_BYPASS_CATEGORIES frozenset definition)
    - src/gekko/schemas.py — ResearchBrief definition (or wherever it lives) to add injected_content_flags field
    - tests/unit/test_suspicious_content.py — the stub tests that this task turns GREEN
    - tests/unit/test_cost_ceiling.py — test_halt_returns_skipped and test_triage_gate_skips to confirm runtime integration (these must also be GREEN)
  </read_first>
  <behavior>
    runtime.py ceiling gate (inserted AFTER quiet-hours gate, BEFORE session_factory construction at line ~495):
    - Import check_cost_ceiling lazily (inside the function, same pattern as quiet_hours import)
    - All trigger sources (not just "schedule") go through the ceiling check — a manual run also deducts from the daily pool (per PATTERNS.md)
    - halt branch: log "agent.cycle.skipped_cost_halt" with spend_usd + ceiling_usd; if just_crossed_100 → fire cost alert Slack DM (category="cost_alert"); return outcome="skipped_cost_halt"
    - degrade branch: set degradation_mode=True local variable (consumed in Wave 4 plan for Haiku triage gate); log "agent.cycle.degraded_cost_ceiling"; if just_crossed_80 → fire cost alert Slack DM; continue execution
    - allow branch: continue normally

    runtime.py SC-2 suspicious-content scan (inserted AFTER brief is returned from _run_researcher, BEFORE _run_decision call):
    - _INJECTION_PATTERNS: module-level re.Pattern compiled once with the 5 injection signatures + re.IGNORECASE
    - Loop over brief.evidence; for each evidence item where evidence.quote_text is not None: if _INJECTION_PATTERNS.search(evidence.quote_text): append suspicious_content event via new async with session_factory() block
    - payload keys: run_id, source_type (evidence.source_type), source_url (str(evidence.source_url)), pattern_matched=True
    - Use normalize_decimals({...}) on the payload before append_event (no Decimal values here, but normalize_decimals is still the canonical call for all payloads)

    ResearchBrief (wherever it's defined — check src/gekko/schemas.py or src/gekko/agent/):
    - Add injected_content_flags: list[str] = [] field (forward-compat slot per RESEARCH §RQ-6)
    - Since ResearchBrief uses ConfigDict(extra='allow'), this is purely additive

    executor.py change:
    - _BYPASS_CATEGORIES frozenset: add "cost_alert" to the existing set {kill_active, executor_error, first_live_fill}
  </behavior>
  <action>
    Edit runtime.py in three targeted sections:
    1. Ceiling gate: insert between the quiet-hours return block (~line 493) and the session_factory/engine construction (~line 495). The gate returns early (halt) or sets a local degradation_mode flag (degrade). Import check_cost_ceiling inside the function to avoid circular imports.
    2. SC-2 scan: insert between `brief = await _run_researcher(...)` (~line 522) and `tool_outcome, tool_payload = await _run_decision(...)` (~line 531). Add _INJECTION_PATTERNS as a module-level re.compile() constant near the top of the file (after imports).
    3. Slack DM for cost alerts: call the existing quiet-hours-aware DM sender function (the same one used by executor.py for kill alerts) with category="cost_alert". The function signature may be `_send_slack_dm` or `_send_slack_dm_respecting_quiet_hours` — read executor.py/runtime.py to find the actual name, then use it consistently.

    Edit executor.py: Find _BYPASS_CATEGORIES (lines 249-254 per PATTERNS.md) and add "cost_alert" to the frozenset.

    For ResearchBrief: find the definition (grep for "class ResearchBrief" in src/gekko/), then add `injected_content_flags: list[str] = []` as a new field. The field is not required for SC-2 test — it's a forward-compat slot.

    CRITICAL: Do NOT add model="haiku" anywhere in _run_decision or any function feeding it. The D-05 AST gate added in Plan 04-01 will catch this. The degradation_mode flag from the ceiling gate is passed to Wave 4's triage implementation.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_suspicious_content.py tests/unit/test_cost_ceiling.py::test_halt_returns_skipped -x -q</automated>
  </verify>
  <done>
    - test_suspicious_content.py GREEN: injection patterns trigger suspicious_content events; clean content does not
    - test_cost_ceiling.py::test_halt_returns_skipped GREEN: trigger_strategy_run with mocked halt ceiling returns outcome=skipped_cost_halt
    - test_decision_prompt_isolation.py still GREEN (AST gate catches no violations)
    - .venv/Scripts/python.exe -m pytest tests/unit/ -x -q — full unit suite GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research evidence → SC-2 scanner | EvidenceSnippet.quote_text is untrusted external content scanned for injection patterns |
| check_cost_ceiling() → LLM | Gate is deterministic Python; fires before any query() call; LLM output cannot reach or influence the gate |
| cost_alert Slack DM → quiet hours | Bypasses quiet hours via cost_alert category — operator safety information |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Cost-ceiling talk-past | mitigate | Gate is deterministic pre-query() Python code; inserted before session_factory construction so no model output can reach it; gate reads from DB only — no LLM input reaches the comparison logic |
| T-04-06 | Tampering | Decision-agent Haiku model (D-05) | mitigate | AST gate test_decision_never_haiku_model (Plan 04-01) locks the invariant; degradation_mode flag passed to Wave 4 Haiku triage goes into _run_researcher path only, never _run_decision |
| T-04-07 | Tampering | Prompt injection via research content | mitigate | _INJECTION_PATTERNS regex detects and logs suspicious patterns; neutralization already via D-40 Decision prompt boundary (verified SC-1/SC-2 in RESEARCH); detection fires after brief parse but before Decision phase |
| T-04-08 | Information Disclosure | suspicious_content audit event | accept | Event contains source_type + source_url + pattern_matched=True — no user credentials or PII; audit chain is SQLCipher-encrypted at rest |
| T-04-09 | Spoofing | cost_alert Slack DM bypass | accept | Bypass category system exists for kill_active/executor_error already; cost_alert is operator-safety information warranting same treatment |
| T-04-SC | Tampering | slopcheck | accept | No new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
- `.venv/Scripts/python.exe -m pytest tests/unit/test_suspicious_content.py -x -q` GREEN
- `.venv/Scripts/python.exe -m pytest tests/unit/test_cost_ceiling.py -x -q` — ceiling tests using runtime integration GREEN
- `.venv/Scripts/python.exe -m pytest tests/unit/test_decision_prompt_isolation.py -x -q` GREEN (D-05 gate still clean)
- `.venv/Scripts/python.exe -m pytest tests/unit/ -x -q` — full suite GREEN
</verification>

<success_criteria>
- cost_ceiling.py: CeilingCheck dataclass + check_cost_ceiling() async function; deterministic, no LLM calls
- runtime.py: ceiling gate fires after quiet-hours gate and before session_factory; SC-2 regex + event write fires between Researcher and Decision phases; _INJECTION_PATTERNS compiled at module level
- executor.py: "cost_alert" in _BYPASS_CATEGORIES
- ResearchBrief: injected_content_flags field present
- Both test files GREEN; full existing suite GREEN
</success_criteria>

<output>
Create `.planning/phases/04-agent-architecture-cost-bounds/04-03-SUMMARY.md` when done.
</output>
