---
phase: 04-agent-architecture-cost-bounds
plan: 04
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - src/gekko/agent/runtime.py
  - src/gekko/scheduler/jobs.py
autonomous: true
requirements:
  - COST-01
  - COST-04
  - COST-05

must_haves:
  truths:
    - "Every query() call (researcher, decision, triage) writes an llm_cost event to the audit table with Decimal USD, input_tokens, output_tokens, call_type"
    - "ResultMessage.total_cost_usd (float) is converted to Decimal(str(value)) before storage — never stored as float"
    - "In degradation mode: Haiku pre-triage query() fires with model=haiku, max_turns=1, allowed_tools=[] before the researcher query()"
    - "Triage responds NO → outcome=triage_skipped (no researcher or decision query() fired)"
    - "In degradation mode: _RESEARCHER_MAX_TURNS reduced to 6 (from 12) and research context trimmed"
    - "Cadence x2: reschedule_strategy_degraded() calls scheduler.reschedule_job() in APScheduler 3.x API; restore_strategy_normal_cadence() restores original"
    - "D-05 invariant: model=haiku NEVER appears in _run_decision or build_decision_prompt — AST gate remains GREEN"
    - "test_cost_ceiling.py::test_triage_gate_skips GREEN; test_cost_ledger.py GREEN"
  artifacts:
    - path: "src/gekko/agent/runtime.py"
      provides: "ResultMessage capture in _run_researcher + _run_decision; llm_cost event writes; Haiku triage gate in degradation path; context-trim in degradation path"
    - path: "src/gekko/scheduler/jobs.py"
      provides: "reschedule_strategy_degraded() + restore_strategy_normal_cadence() functions"
  key_links:
    - from: "src/gekko/agent/runtime.py"
      to: "src/gekko/audit/log.py"
      via: "append_event(..., event_type='llm_cost')"
      pattern: "llm_cost"
    - from: "src/gekko/agent/runtime.py"
      to: "claude_agent_sdk.types.ResultMessage"
      via: "isinstance(msg, SDKResultMessage)"
      pattern: "ResultMessage"
    - from: "src/gekko/scheduler/jobs.py"
      to: "apscheduler.schedulers.asyncio.AsyncIOScheduler"
      via: "scheduler.reschedule_job()"
      pattern: "reschedule_job"
---

<objective>
Wave 4 implements the cost-tracking pipeline and degradation mechanics: (1) ResultMessage capture in both query() loops so real SDK costs land in the audit table (COST-05); (2) the Haiku pre-triage gate that runs in degradation mode before the researcher (D-04 tactic 2) — Haiku is used ONLY here, never in _run_decision (D-05 invariant); (3) context-trim for degradation mode (D-04 tactic 3 — reduced max_turns + trimmed brief); (4) cadence x2 functions in jobs.py for APScheduler 3.11.2 (D-04 tactic 1).

This is the "thin slice that makes cost tracking end-to-end real": after this plan a full cycle writes llm_cost events to the audit chain, the ceiling guard has real spend data to check, and degradation mode does all three tactics.

Purpose: COST-01/COST-04/COST-05 all require this plan's changes to be testable end-to-end. test_cost_ledger.py + triage-gate stubs turn GREEN.

Output: runtime.py (extended — ResultMessage capture, triage gate, context-trim), jobs.py (extended — reschedule functions).
</objective>

<execution_context>
@C:\Users\chris.platika\Desktop\Project Gekko\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris.platika\Desktop\Project Gekko\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-CONTEXT.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-PATTERNS.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-RESEARCH.md
@C:\Users\chris.platika\Desktop\Project Gekko\src\gekko\agent\runtime.py
@C:\Users\chris.platika\Desktop\Project Gekko\src\gekko\scheduler\jobs.py
@C:\Users\chris.platika\Desktop\Project Gekko\docs\sdk-shape.md
@C:\Users\chris.platika\Desktop\Project Gekko\.planning\phases\04-agent-architecture-cost-bounds\04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cost ledger writes — ResultMessage capture in _run_researcher + _run_decision</name>
  <files>
    src/gekko/agent/runtime.py
  </files>
  <read_first>
    - src/gekko/agent/runtime.py — the _run_researcher() async function (lines ~270-320) and _run_decision() async function (~330-370); the existing `async for msg in query(...)` loop bodies; lines 395-423 (append_event caller pattern)
    - 04-PATTERNS.md §src/gekko/agent/runtime.py — "ResultMessage capture pattern" (add before loop, capture inside), "Cost ledger write — copy append_event caller pattern"
    - 04-RESEARCH.md §RQ-1 — SDKResultMessage.total_cost_usd: float | None; AssistantMessage.usage dict shape {input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens}
    - docs/sdk-shape.md delta #6 — authoritative SDK reference for usage dict
    - tests/unit/test_cost_ledger.py — stub tests: what call shapes they assert
    - src/gekko/audit/canonical.py — normalize_decimals; src/gekko/audit/log.py — append_event signature
  </read_first>
  <behavior>
    In BOTH _run_researcher() and _run_decision():
    - Declare result_msg: SDKResultMessage | None = None, input_tokens: int = 0, output_tokens: int = 0 before the async for loop
    - Inside the loop: if isinstance(msg, SDKResultMessage): result_msg = msg; elif isinstance(msg, AssistantMessage): if msg.usage: input_tokens += msg.usage.get("input_tokens", 0); output_tokens += msg.usage.get("output_tokens", 0); then existing logic continues
    - After the loop: cost_usd = Decimal(str(result_msg.total_cost_usd or 0.0)) if result_msg else Decimal("0")
    - Write llm_cost event: async with session_factory() as session, session.begin(): await append_event(session, user_id=user_id, strategy_id=strategy_db_id, event_type="llm_cost", payload=normalize_decimals({...}))
    - Payload shape: run_id, strategy_name, model (string alias used), call_type ("researcher" or "decision"), input_tokens, output_tokens, cost_usd

    _run_researcher() and _run_decision() must receive session_factory + user_id + strategy_db_id as parameters (they may already receive these or need them threaded through from trigger_strategy_run — check the current signatures and add as needed). Do NOT open a new DB engine inside these functions — use the session_factory already established by trigger_strategy_run.
  </behavior>
  <action>
    Edit runtime.py to add ResultMessage capture to both query() loops. The functions _run_researcher and _run_decision currently return (text, tool_call) or similar — they need access to session_factory + user_id + strategy_db_id to write the ledger event. If these are not in the current signatures, add them as keyword-only parameters and update the callers in trigger_strategy_run().

    Import SDKResultMessage at the top of the file: `from claude_agent_sdk.types import ResultMessage as SDKResultMessage` (alongside the existing AssistantMessage, TextBlock, ToolUseBlock imports).

    The ledger write uses `async with session_factory() as session, session.begin()` — same as the existing _persist_proposal_rejected_event pattern. Do NOT reuse an open session from a broader context — each ledger write gets its own transaction (lightweight, correct).

    IMPORTANT: The `cost_usd` value is always converted via `Decimal(str(float_or_zero))` before being placed in the payload dict — never pass a float directly. normalize_decimals() is called on the entire payload dict before append_event.

    The `model` field in the payload should be the string alias used in the ClaudeAgentOptions for that call (e.g., "sonnet" for researcher + decision, "haiku" for triage). If the model alias is a variable, use that variable; do not hardcode "sonnet" if the options already carry the model.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_cost_ledger.py -x -q</automated>
  </verify>
  <done>test_cost_ledger.py GREEN — llm_cost event written per query() with Decimal USD, call_type present, normalize_decimals called. ResultMessage.total_cost_usd=None correctly defaults to Decimal("0").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Haiku triage gate + context-trim + cadence x2 (D-04 all three tactics)</name>
  <files>
    src/gekko/agent/runtime.py,
    src/gekko/scheduler/jobs.py
  </files>
  <read_first>
    - src/gekko/agent/runtime.py — the degradation_mode flag set in Plan 04-03's ceiling-gate block; the trigger_strategy_run() call flow after the ceiling gate; how budget = BudgetTracker() is constructed (soft_max_calls parameter)
    - 04-PATTERNS.md §src/gekko/scheduler/jobs.py — reschedule_strategy_degraded() + restore_strategy_normal_cadence() function signatures and APScheduler 3.x reschedule_job() call
    - 04-RESEARCH.md §RQ-5 — Haiku pre-triage gate: model="haiku", max_turns=1, allowed_tools=[], prompt template, "NO" in result → triage_skipped; context-trim: _RESEARCHER_MAX_TURNS=6 (vs 12 normal), pass degraded context budget to BudgetTracker
    - src/gekko/scheduler/jobs.py — existing schedule_strategy_daily() (lines 108-155) for the reschedule analog; _parse_schedule_time() helper
    - tests/unit/test_cost_ceiling.py::test_triage_gate_skips — the stub this task turns GREEN
  </read_first>
  <behavior>
    Triage gate (in trigger_strategy_run, AFTER the ceiling gate sets degradation_mode=True, BEFORE BudgetTracker construction):
    - If degradation_mode: build triage_opts = ClaudeAgentOptions(model="haiku", max_turns=1, allowed_tools=[], system_prompt="You are a triage agent. Answer YES or NO only.")
    - triage_prompt: summary of the strategy + last cycle date; "Is there likely new market information that warrants a full research run today? YES or NO."
    - Iterate query(prompt=triage_prompt, options=triage_opts); collect ResultMessage for cost ledger write (call_type="triage")
    - If "NO" in triage_text.upper(): log "agent.cycle.skipped_triage"; write triage llm_cost event; return outcome="triage_skipped"
    - If "YES" or anything else: continue to researcher with reduced context

    Context-trim (degradation mode, if triage returned YES):
    - BudgetTracker(soft_max_calls=6) instead of default 12
    - Pass degraded=True (or similar) to build_researcher_prompt() to request max_evidence_items=3 instead of the normal default

    Cadence x2 in jobs.py:
    - reschedule_strategy_degraded(scheduler, *, user_id, strategy_name, original_schedule_time) -> str: parse original HH:MM, compute degraded_hh = (hh + 12) % 24, call scheduler.reschedule_job(job_id, trigger=CronTrigger(hour=degraded_hh, minute=mm, timezone=tz)); return job_id
    - restore_strategy_normal_cadence(scheduler, *, user_id, strategy_name, schedule_time) -> str: call schedule_strategy_daily(scheduler, ...) which handles replace_existing=True
    - Both functions log via structlog (existing log = get_logger(__name__) pattern)

    Where to call reschedule_strategy_degraded: from trigger_strategy_run when degradation_mode=True and it's a "schedule" trigger. Restore is called in the scheduler's midnight-reset job (or whenever the ceiling resets — deferred to the settings route POST handler in Wave 5, where raising the ceiling triggers restore).
  </behavior>
  <action>
    Edit runtime.py to add the triage gate block between the ceiling-gate degradation_mode assignment and the BudgetTracker() construction. The triage gate is ONLY active when degradation_mode=True — do NOT run it on normal (allow) cycles.

    The triage llm_cost event write follows the same pattern as Task 1 above (call_type="triage", model="haiku").

    CRITICAL D-05 check: model="haiku" appears ONLY in the triage gate block, which is separate from _run_decision() and build_decision_prompt(). The AST gate in test_decision_prompt_isolation.py checks _run_decision and build_decision_prompt by function name — the triage gate is in trigger_strategy_run, which is NOT in the _DECISION_FN_NAME_PATTERNS list. Verify this before finishing: run the AST gate test.

    Edit jobs.py to add the two reschedule helper functions. Use APScheduler 3.x AsyncIOScheduler.reschedule_job() (NOT remove + re-add) per RESEARCH §Pitfall 6. Import CronTrigger from apscheduler.triggers.cron (already imported for schedule_strategy_daily). Add structlog logging per the existing log = get_logger(__name__) pattern in jobs.py.

    For build_researcher_prompt(): check if it already accepts a max_evidence_items parameter or similar. If not, add it as an optional parameter with a default that preserves existing behavior (None → use the normal default), so degraded mode can pass a reduced value.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_cost_ceiling.py tests/unit/test_cost_ledger.py tests/unit/test_decision_prompt_isolation.py -x -q</automated>
  </verify>
  <done>
    - test_cost_ceiling.py::test_triage_gate_skips GREEN: mocked Haiku query() returning "NO" causes outcome=triage_skipped
    - test_cost_ledger.py GREEN: all ledger tests pass including triage call_type
    - test_decision_prompt_isolation.py GREEN: D-05 AST gate finds no model="haiku" in decision-path functions
    - reschedule_strategy_degraded() and restore_strategy_normal_cadence() exist in jobs.py
    - .venv/Scripts/python.exe -m pytest tests/unit/ -x -q — full unit suite GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Haiku triage → Decision agent | Triage output is a YES/NO string consumed by Python logic only; it never feeds into the Decision prompt |
| ResultMessage.total_cost_usd → Decimal | SDK-provided float converted via str() before Decimal to avoid binary float imprecision in the money ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | Haiku triage model impersonating Decision | mitigate | Triage is in trigger_strategy_run(), not in _run_decision() or build_decision_prompt(); AST gate test_decision_never_haiku_model confirms model="haiku" does not appear in decision-path functions |
| T-04-11 | Tampering | llm_cost event float-to-Decimal conversion | mitigate | Always Decimal(str(float_or_zero)) — never Decimal(float_value) directly; test_cost_ledger.py::test_cost_usd_is_decimal_not_float asserts this at the test layer |
| T-04-12 | Denial of Service | Triage query() consuming cost before the actual halt | accept | Triage query fires ONLY in degrade mode (80-99%), not at halt (100%); Haiku cost is ~1/3 of Sonnet; the triage DM already fired at 80% crossing |
| T-04-13 | Tampering | reschedule_job persisting across restarts | mitigate | APScheduler 3.x SQLAlchemyJobStore persists the trigger to SQLite; test with restart per RESEARCH Assumption A4; restore_strategy_normal_cadence uses replace_existing=True |
| T-04-SC | Tampering | slopcheck | accept | No new packages installed |
</threat_model>

<verification>
After both tasks complete:
- `.venv/Scripts/python.exe -m pytest tests/unit/test_cost_ceiling.py tests/unit/test_cost_ledger.py tests/unit/test_decision_prompt_isolation.py -x -q` — all GREEN
- `.venv/Scripts/python.exe -m pytest tests/unit/ -x -q` — full unit suite GREEN
- Manually confirm: `_INJECTION_PATTERNS` and `SDKResultMessage` imports are in runtime.py; `reschedule_strategy_degraded` is in jobs.py
</verification>

<success_criteria>
- _run_researcher() and _run_decision() each write one llm_cost event per call, with Decimal USD
- Haiku triage gate fires in degradation_mode; "NO" returns triage_skipped without downstream query() calls
- Context-trim: BudgetTracker(soft_max_calls=6) + max_evidence_items=3 in degradation mode
- reschedule_strategy_degraded() + restore_strategy_normal_cadence() added to jobs.py with APScheduler 3.x API
- D-05 AST gate stays GREEN (model="haiku" is only in trigger_strategy_run, not _run_decision)
- All 11 VALIDATION.md unit tests GREEN
</success_criteria>

<output>
Create `.planning/phases/04-agent-architecture-cost-bounds/04-04-SUMMARY.md` when done.
</output>
