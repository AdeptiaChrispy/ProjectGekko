---
phase: 04-agent-architecture-cost-bounds
plan: 06
type: execute
wave: 1
depends_on: ["04-05"]
files_modified:
  - src/gekko/dashboard/routes.py
  - tests/unit/test_spend_route.py
autonomous: true
requirements: [COST-02, COST-05]
gap_closure: true

must_haves:
  truths:
    - "/spend renders a non-zero today_total when llm_cost events exist with the canonical wrapper shape"
    - "Per-strategy names (not 'Unknown') and non-zero spend appear in the by_strategy breakdown"
    - "7-day history buckets carry non-zero spend for days with llm_cost events"
    - "The hardened test feeds canonical-wrapped rows and FAILS against the pre-fix read path, PASSES after the unwrap fix"
    - "Existing green suites (test_cost_ceiling, test_decision_prompt_isolation, SRI lint) remain green"
  artifacts:
    - path: "src/gekko/dashboard/routes.py"
      provides: "spend_get with canonical-payload unwrap in both today_rows and history_rows loops"
      contains: "inner = payload.get"
    - path: "tests/unit/test_spend_route.py"
      provides: "Hardened test using canonical-wrapped rows; explicit assertion that cost and strategy_name surface"
      contains: "canonical"
  key_links:
    - from: "routes.py spend_get today_rows loop"
      to: "payload['payload']['cost_usd']"
      via: "inner = payload.get('payload', payload)"
      pattern: "inner\\.get\\(\"cost_usd\""
    - from: "routes.py spend_get history_rows loop"
      to: "payload['payload']['cost_usd']"
      via: "inner = payload.get('payload', payload)"
      pattern: "inner\\.get\\(\"cost_usd\""
---

<objective>
Close SC-5 / COST-02 gap: spend_get in routes.py reads cost_usd and strategy_name at the top
level of the canonical event payload_json, but append_event stores them one level deeper inside
the nested "payload" key. This causes the /spend dashboard to display $0.00 for every cost and
"Unknown" for every strategy name, even though the ledger data is correct. The fix mirrors the
pattern already used by cost_ceiling.py line 202 exactly.

Purpose: Phase 4's observable SC-5 truth ("dashboard shows spend per strategy and per user with
the daily ceiling visible") is not satisfied until the dashboard can actually read the cost data
it correctly stores. The ceiling enforcement (cost_ceiling.py) already works; only the display
path is broken.

Output:
  - routes.py: unwrap applied in both the today_rows loop (lines ~1299-1310) and the
    history_rows loop (lines ~1333-1348); Decimal math and auth gating unchanged.
  - test_spend_route.py: _make_llm_cost_row replaced with canonical-wrapper shape;
    new test_spend_get_canonical_payload_unwrap asserts non-zero spend + correct
    strategy_name surface and is annotated to fail pre-fix / pass post-fix.
</objective>

<execution_context>
@C:\Users\chris.platika\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris.platika\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-architecture-cost-bounds/04-CONTEXT.md
@.planning/phases/04-agent-architecture-cost-bounds/04-PATTERNS.md
@.planning/phases/04-agent-architecture-cost-bounds/04-VERIFICATION.md
@src/gekko/agent/cost_ceiling.py
@src/gekko/audit/log.py
@src/gekko/audit/canonical.py
@src/gekko/dashboard/routes.py
@tests/unit/test_spend_route.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix spend_get canonical-payload unwrap in routes.py</name>
  <files>src/gekko/dashboard/routes.py</files>
  <behavior>
    - Given: append_event stores payload_json as the full canonical-subset string
      {"event_type":"llm_cost","payload":{"cost_usd":"0.05","strategy_name":"strat-a",...},"ts":"...","user_id":"..."}
    - When: spend_get parses a row, it must do `inner = payload.get("payload", payload)` then
      read `inner.get("cost_usd", "0")` and `inner.get("strategy_name", "Unknown")`
    - Then: today_total accumulates the correct Decimal sum; by_strategy carries real names; history buckets carry real costs
    - Edge: rows with no nested "payload" key (legacy flat shape) tolerate the `.get("payload", payload)`
      fallback so old rows are read as-is (no KeyError, no silent $0)
    - Invariant: Decimal math is preserved (no float); auth gating via require_session is unchanged
  </behavior>
  <action>
    In src/gekko/dashboard/routes.py, locate the spend_get function (route GET /spend, around
    line 1228). There are two payload-reading loops that both have the identical nesting bug:

    TODAY LOOP (around lines 1299-1310): after `payload = json.loads(row.payload_json)`, add a
    second line `inner = payload.get("payload", payload)` immediately before the cost/strategy
    reads. Then change `payload.get("cost_usd", "0")` to `inner.get("cost_usd", "0")` and
    `payload.get("strategy_name", "Unknown")` to `inner.get("strategy_name", "Unknown")`. The
    variable name `payload` may remain for the outer dict; introduce `inner` as a new local.

    HISTORY LOOP (around lines 1333-1348): identical fix — after `payload = json.loads(row.payload_json)`,
    add `inner = payload.get("payload", payload)`, then change `payload.get("cost_usd", "0")` to
    `inner.get("cost_usd", "0")`.

    The exact pattern to mirror is cost_ceiling.py lines 198-204:
      payload = json.loads(payload_json)
      inner = payload.get("payload", payload)
      cost_str = inner.get("cost_usd", "0")

    Do NOT change any other logic in spend_get: the DB query, the user row load, the Decimal
    accumulation, the by_strategy sort, the 7-day history fill, the template render call, and
    the require_session auth dependency all remain exactly as they are. The change is two
    two-line insertions, one in each loop.

    Do NOT introduce float arithmetic. Keep `Decimal(str(...))` wrapping around the cost_str.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_spend_route.py -x -q</automated>
  </verify>
  <done>
    All 6 existing test_spend_route.py tests pass. The two loops in spend_get each contain
    `inner = payload.get("payload", payload)` and read cost_usd / strategy_name from `inner`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden test_spend_route.py — canonical-wrapper rows + regression gate</name>
  <files>tests/unit/test_spend_route.py</files>
  <behavior>
    - Given: _make_llm_cost_row currently builds FLAT JSON (no canonical wrapper) — this is the
      root cause the bug was not caught by CI
    - When: the helper is updated to wrap the cost fields inside "payload" key (matching what
      append_event actually stores), all existing spend route tests must still pass
    - New test: test_spend_get_canonical_payload_unwrap feeds two canonical-wrapped rows
      (cost_usd="0.05" / strategy_name="strat-a", cost_usd="0.03" / strategy_name="strat-b"),
      then asserts:
        (a) resp.text contains "0.08" or "0.0800" (today total)
        (b) resp.text contains "strat-a"
        (c) resp.text contains "strat-b"
      This test FAILS against the pre-fix routes.py (all three assertions fail because the route
      reads the wrong level) and PASSES after the Task 1 fix
    - Existing 6 tests: must remain green after _make_llm_cost_row is updated to canonical shape
  </behavior>
  <action>
    In tests/unit/test_spend_route.py, update _make_llm_cost_row (lines 30-45) to emit the
    canonical wrapper shape that append_event actually stores. Change row.payload_json from:
      json.dumps({"cost_usd": cost_usd, "strategy_name": strategy_name, ...})
    to:
      json.dumps({
          "event_type": "llm_cost",
          "payload": {
              "cost_usd": cost_usd,
              "strategy_name": strategy_name,
              "model": "sonnet",
              "call_type": "researcher",
              "input_tokens": 100,
              "output_tokens": 50,
          },
          "ts": ts or datetime.now(UTC).isoformat(),
          "user_id": "testuser",
      })
    The outer "event_type", "ts", and "user_id" keys must be present at the top level to match
    canonical_json output from audit/log.py lines 139-146. The cost fields live exclusively
    inside "payload".

    Then add a new test function test_spend_get_canonical_payload_unwrap at the end of the file.
    It should follow the same httpx.AsyncClient + patch("gekko.dashboard.routes._get_session_factory")
    pattern as test_spend_get_shows_today_total (lines 115-176). Feed two canonical-wrapped rows
    via _make_llm_cost_row("0.05", "strat-a", today_ts) and _make_llm_cost_row("0.03", "strat-b",
    today_ts). Return them from both the today_rows and history_rows mock execute calls. Add a
    docstring: "Regression gate: canonical-wrapped rows must surface non-zero costs and real
    strategy names — would FAIL against top-level read, PASSES after inner = payload.get('payload',
    payload) unwrap." Assert:
      assert "0.08" in resp.text or "0.0800" in resp.text
      assert "strat-a" in resp.text
      assert "strat-b" in resp.text

    Do NOT remove or change any existing test function. The 6 existing tests must still pass with
    the updated _make_llm_cost_row.

    The test must be decorated @pytest.mark.asyncio and follow the existing _vault.set_passphrase /
    _vault.clear() try/finally guard pattern used by all other tests in this file.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_spend_route.py -x -q -v</automated>
  </verify>
  <done>
    7 tests pass (6 existing + 1 new). _make_llm_cost_row emits canonical-wrapped JSON. The new
    test_spend_get_canonical_payload_unwrap asserts non-zero total, "strat-a", and "strat-b"
    are present in the response body.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm existing green suites remain green</name>
  <files></files>
  <action>
    Run the full unit test suite excluding integration tests. The three suites that must remain
    green with zero regressions are:
      - tests/unit/test_cost_ceiling.py (8 tests, COST-01/COST-04 ceiling enforcement)
      - tests/unit/test_decision_prompt_isolation.py (D-05 AST gate + isolation gate)
      - Any SRI/CSP lint tests if they exist in tests/unit/

    Do not modify any file outside routes.py and test_spend_route.py. If any pre-existing test
    is red, note it but do not count it as a regression introduced by this plan (per project
    memory: 3 pre-existing .env-based unit failures exist; deselect with -k if needed to
    distinguish).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/ -x -q --ignore=tests/unit/test_spend_route.py -o "addopts="</automated>
  </verify>
  <done>
    No regressions introduced. test_cost_ceiling and test_decision_prompt_isolation remain green.
    Any pre-existing failures are the known .env-based ones, not new failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/spend | Authenticated user session crosses into route handler; require_session enforces it |
| payload_json→spend display | Stored canonical JSON parsed in Python; display-only, no re-execution of stored data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Information Disclosure | spend_get cross-user row leak | mitigate | Route sits on auth-gated `router = APIRouter(dependencies=[Depends(require_session)])`; every DB query filters by `user_id` from `require_session`; the unwrap fix does not touch auth or user_id scoping — confirmed by reading lines 1228-1260 of routes.py |
| T-04-06-02 | Tampering | Decimal precision drift in unwrap | mitigate | `inner.get("cost_usd", "0")` returns a string stored by `normalize_decimals`; `Decimal(str(...))` wrapping is preserved from the original code; no float arithmetic introduced |
| T-04-06-03 | Information Disclosure | strategy_name leaking between users | accept | strategy_name comes from the llm_cost event payload which is already user-scoped (Event.user_id == user_id in the DB query); the unwrap does not widen the query |
| T-04-06-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; fix is pure Python source edit |
</threat_model>

<verification>
After both tasks complete:

1. `test_spend_route.py` runs 7/7 green with canonical-wrapped rows
2. `test_cost_ceiling.py` runs 8/8 green (ceiling enforcement unchanged)
3. `test_decision_prompt_isolation.py` runs green (no agent-path edits)
4. Grep confirms the unwrap pattern exists in both loops:
   `grep -n "inner = payload.get" src/gekko/dashboard/routes.py`
   (must return 2 matches — one per loop)
5. Grep confirms the flat-JSON helper is gone:
   `grep -n '"cost_usd": cost_usd' tests/unit/test_spend_route.py`
   (must return 0 matches — cost_usd now lives inside "payload" key)
</verification>

<success_criteria>
- SC-5 gap closed: /spend will render non-zero today_total, real strategy names, and non-zero
  7-day history when llm_cost events are in the DB
- Two unwrap lines added (one per loop in spend_get); no other logic changed
- test_spend_route.py hardened: 7 tests pass; _make_llm_cost_row emits canonical wrapper;
  new regression gate would fail against pre-fix code
- Zero regressions in the broader unit suite
</success_criteria>

<output>
Create `.planning/phases/04-agent-architecture-cost-bounds/04-06-SUMMARY.md` when done
</output>
