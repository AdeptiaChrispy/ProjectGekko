---
phase: 05-trust-ladder-per-strategy-promotion-portfolio-caps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_trust_streak.py
  - tests/unit/test_trust_routes.py
  - tests/unit/test_portfolio_caps.py
  - tests/unit/test_capital_ceiling.py
  - tests/unit/test_anomaly.py
  - tests/unit/test_auto_execute.py
  - tests/unit/test_trust_safety_invariants.py
  - tests/unit/test_migration_0007.py
  - tests/unit/test_scheduler.py
  - tests/conftest.py
  - src/gekko/db/models.py
  - migrations/versions/0007_p5_trust_ladder.py
  - src/gekko/approval/proposals.py
  - src/gekko/execution/executor.py
autonomous: true
requirements: [TRUST-01, TRUST-02, TRUST-03, TRUST-04, TRUST-05, TRUST-06]
must_haves:
  truths:
    - "Migration 0007 upgrades from head 0006_p4_cost_ceiling_repair and round-trips (upgrade→downgrade→upgrade) without data loss"
    - "strategy_metadata carries trust_level (default 'propose-only'), trust_promoted_at, capital_ceiling_usd (default '1000.00'), anomaly_threshold_pct (default '0.10')"
    - "users carries the four portfolio-cap columns with conservative server_defaults (0.50 / 0.30 / 0.15 / 200.00)"
    - "events CHECK vocabulary accepts the five new event types: trust_promoted, trust_demoted, anomaly_demotion, capital_scaled, auto_execution"
    - "approval audit-event payloads carry strategy_name + account_mode (per D-T01/D-T02 streak attribution)"
    - "cap_rejection audit-event payloads carry strategy_name (per D-T02 streak-reset attribution)"
    - "Wave-0 test stub files exist for every Phase-5 requirement and import the (not-yet-built) targets so they fail RED until later plans land"
  artifacts:
    - migrations/versions/0007_p5_trust_ladder.py
    - tests/unit/test_trust_streak.py
    - tests/unit/test_portfolio_caps.py
    - tests/unit/test_capital_ceiling.py
    - tests/unit/test_anomaly.py
    - tests/unit/test_auto_execute.py
    - tests/unit/test_trust_safety_invariants.py
    - tests/unit/test_migration_0007.py
    - tests/unit/test_scheduler.py
  key_links:
    - "0007 down_revision MUST be 0006_p4_cost_ceiling_repair (the current Alembic head, NOT 0005)"
    - "_FROZEN_EVENT_TYPES_PRE in 0007 MUST equal the current _EVENT_TYPES in models.py before this plan's edit; _POST = _PRE + the five new types"
    - "approve_proposal extra_payload enrichment is the data source the Plan-02 streak scanner reads"
  prohibitions:
    - "MUST NOT use event_type='error' + a context discriminator for any trust/anomaly/capital event (BL-01 anti-pattern)"
    - "MUST NOT change down_revision to 0005 (would skip the 0006 repair and corrupt the chain)"
    - "MUST NOT store money/percent as float columns — money-as-TEXT, percent-as-fraction-TEXT convention"
---

<objective>
Lay the Phase-5 foundation: the Alembic 0007 migration (new StrategyMetadata + User columns + events CHECK extension), the ORM mirror in models.py, the audit-payload enrichment that the clean-streak scanner depends on, and the full set of Wave-0 test stubs for every TRUST-* requirement.

Purpose: Every other Phase-5 slice (trust helpers, portfolio caps, anomaly, auto-execute) reads these columns and audit-event fields. Without enriched approval/cap_rejection payloads the streak gate cannot attribute approvals to a strategy+mode (RESEARCH Pitfall 1). This plan is the schema + data-contract floor.
Output: migration 0007, extended models.py, enriched approval/cap_rejection payloads, 9 Wave-0 test files + conftest fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-CONTEXT.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-RESEARCH.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md
@migrations/versions/0005_p4_cost_ceiling.py
@migrations/versions/0006_p4_cost_ceiling_repair.py
@src/gekko/db/models.py
@src/gekko/approval/proposals.py
</context>

<artifacts_this_phase_produces>
This plan introduces (the rest of the phase consumes them):
- **New migration:** `migrations/versions/0007_p5_trust_ladder.py` (revision `0007_p5_trust_ladder`, down_revision `0006_p4_cost_ceiling_repair`).
- **New StrategyMetadata columns:** `trust_level` (TEXT, server_default `'propose-only'`), `trust_promoted_at` (TEXT nullable), `capital_ceiling_usd` (TEXT nullable, server_default `'1000.00'`), `anomaly_threshold_pct` (TEXT nullable, server_default `'0.10'`).
- **New User columns:** `max_total_exposure_pct` (`'0.50'`), `max_sector_concentration_pct` (`'0.30'`), `max_correlated_ticker_pct` (`'0.15'`), `max_total_daily_loss_usd` (`'200.00'`) — all TEXT, nullable, with server_defaults.
- **New `_EVENT_TYPES` values:** `trust_promoted`, `trust_demoted`, `anomaly_demotion`, `capital_scaled`, `auto_execution`.
- **Enriched audit payloads:** `approval` events gain `strategy_name` + `account_mode`; `cap_rejection` events gain `strategy_name`.
- **New test files:** the 9 listed in `files_modified`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test stubs + conftest fixtures for all TRUST-* requirements</name>
  <files>tests/unit/test_trust_streak.py, tests/unit/test_trust_routes.py, tests/unit/test_portfolio_caps.py, tests/unit/test_capital_ceiling.py, tests/unit/test_anomaly.py, tests/unit/test_auto_execute.py, tests/unit/test_trust_safety_invariants.py, tests/unit/test_migration_0007.py, tests/unit/test_scheduler.py, tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (existing fixtures: per-user session-factory shim, in-memory SQLite, broker MagicMocks)
    - tests/unit/test_orderguard.py (reuse `_make_strategy` / `_make_order_request` helpers + the AST-gate skeleton at the zero-decorators test)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-RESEARCH.md (## Validation Architecture → Phase Requirements → Test Map; Wave 0 Gaps list)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-VALIDATION.md (Per-Task Verification Map invariants)
  </read_first>
  <behavior>
    - test_trust_streak.py: compute_clean_streak partitions approvals by strategy_name+account_mode; a cap_rejection mid-window zeroes the streak; trust_demoted / anomaly_demotion / material-edit boundary resets the window; cross-strategy approvals do not bleed; paper vs live counted separately (D-T01/D-T02/D-T03/D-T05).
    - test_trust_routes.py: promote route re-checks eligibility server-side and returns the blocked-explanation block when ineligible (SC-5/D-T18b); demote is one-click; capital increase requires typed-name confirm.
    - test_portfolio_caps.py: each of the four aggregate caps raises OrderGuardRejected with its reject_code when the aggregate (not per-strategy) limit is exceeded; blank column = disabled = no raise.
    - test_capital_ceiling.py: total deployed capital (open positions for the strategy's tickers + this order) over capital_ceiling_usd raises OrderGuardRejected("capital_ceiling"); lowering is unconstrained.
    - test_anomaly.py: drawdown >= threshold demotes + cancels + DMs; a strategy already propose-only is a no-op; anomaly trips BEFORE max_daily_loss_usd (threshold ordering, D-T11).
    - test_auto_execute.py: auto branch routes auto-within-caps proposals through execute_proposal (OrderGuard re-checks); LIVE strategy with first_live_trade_confirmed_at IS NULL routes to AWAITING_2ND_CHANNEL, not direct execute.
    - test_trust_safety_invariants.py: AST gate — no module outside strategy/trust.py assigns trust_level = "auto-within-caps"; the auto-branch is guarded by a trust check.
    - test_migration_0007.py: 0007 upgrade/downgrade round-trips; backfill defaults present.
    - test_scheduler.py: the anomaly IntervalTrigger + market-open snapshot jobs register on the existing scheduler (NYSE-gated, coalesce, max_instances=1); the snapshot job writes the stable start-of-day denominator evaluate_drawdown reads (Plan 04 Task 3 contract).
  </behavior>
  <action>
    Create the 9 test files listed in files_modified as RED Wave-0 stubs. Each imports its eventual target (e.g. `from gekko.strategy.streak import compute_clean_streak, StreakResult`) so the file fails to collect/pass until the implementing plan lands — this is the Nyquist scaffold. Where the target does not yet exist, gate the import with `pytest.importorskip` ONLY for the migration test (which can run now); the rest assert real behavior and are EXPECTED RED until Waves 2-5. Add shared fixtures to tests/conftest.py: a `seed_approval_events(session, user_id, strategy_name, account_mode, n)` helper that writes enriched `approval` events, and a `seed_cap_rejection(session, user_id, strategy_name, reject_code)` helper — both used by the streak tests. Reuse `_make_strategy` / `_make_order_request` from test_orderguard.py for the cap tests. Mark every Phase-5 test command per VALIDATION: `.venv/Scripts/python.exe -m pytest tests/unit/test_trust_*.py -x`. Do NOT inline implementations in the stubs — assert against the documented contracts (StreakResult fields per RESEARCH Pattern 4; reject_codes portfolio_total_exposure / portfolio_sector_concentration / portfolio_correlated_ticker / portfolio_daily_loss / capital_ceiling).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_migration_0007.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 9 test files exist and are importable/collectable (collect-only succeeds; behavioral tests may be RED).
    - conftest.py exposes seed_approval_events + seed_cap_rejection helpers.
    - Each test file references its requirement ID in a module docstring (TRUST-01..06).
  </acceptance_criteria>
  <done>Wave-0 stubs collect; migration test is runnable; behavioral stubs are RED pending later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORM columns + _EVENT_TYPES extension + Alembic 0007 migration</name>
  <files>src/gekko/db/models.py, migrations/versions/0007_p5_trust_ladder.py, tests/unit/test_migration_0007.py</files>
  <read_first>
    - src/gekko/db/models.py (StrategyMetadata class, User class, _EVENT_TYPES tuple + the comment block above it, the ck_event_type _in_check usage)
    - migrations/versions/0005_p4_cost_ceiling.py (the batch_alter_table add_column + drop_constraint/create_check_constraint pattern, frozen-local _FROZEN_EVENT_TYPES_PRE/_POST)
    - migrations/versions/0006_p4_cost_ceiling_repair.py (confirm it is the current head and what columns/defaults it already added)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (migrations/versions/0007 section — exact revision header + add_column block)
  </read_first>
  <action>
    In models.py: add the four new StrategyMetadata columns and the four new User columns mirroring the existing `Mapped[str | None] = mapped_column(String, ..., server_default=text("'...'"))` shape used by `daily_cost_ceiling_usd` (money/percent stored as TEXT). Append the five new event types to `_EVENT_TYPES` exactly as the Phase-4 block appended `llm_cost` / `suspicious_content`, with an explanatory comment block above matching the existing BL-01/Phase-3/Phase-4 comment convention. Create migrations/versions/0007_p5_trust_ladder.py per D-T16/D-T11 defaults: revision `"0007_p5_trust_ladder"`, down_revision `"0006_p4_cost_ceiling_repair"` (the current head — confirm against `migrations/versions/` listing). Copy `_FROZEN_EVENT_TYPES_PRE` as a literal of the CURRENT _EVENT_TYPES (pre-this-edit value) and `_FROZEN_EVENT_TYPES_POST = _PRE + ("trust_promoted","trust_demoted","anomaly_demotion","capital_scaled","auto_execution")`. upgrade() uses three `op.batch_alter_table` blocks (strategy_metadata add 4 cols, users add 4 cols, events drop+recreate ck_event_type with _POST). downgrade() reverses in opposite order (events CHECK first, then drop columns). Money columns are TEXT; percentages are fraction strings ("0.50" = 50%). Add a frozen-vocab equality test in test_migration_0007.py asserting _FROZEN_EVENT_TYPES_PRE matches the prior head's POST (Plan 01-03 convention).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_migration_0007.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'down_revision' migrations/versions/0007_p5_trust_ladder.py` shows `0006_p4_cost_ceiling_repair` (not 0005).
    - models.py _EVENT_TYPES contains all five new values; `grep -c "event_type=\"error\"" src/gekko/strategy/ src/gekko/anomaly/ 2>/dev/null` is irrelevant here (this plan adds no trust events) but the migration POST tuple lists the five new types.
    - test_migration_0007.py passes: upgrade applies the 8 columns + extended CHECK, downgrade reverses, round-trip preserves existing rows.
    - New columns are TEXT with the documented server_defaults.
  </acceptance_criteria>
  <done>0007 round-trips on a real SQLCipher DB; ORM + migration vocabularies agree; defaults backfill on existing rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enrich approval + cap_rejection audit payloads with strategy_name + account_mode</name>
  <files>src/gekko/approval/proposals.py, src/gekko/execution/executor.py</files>
  <read_first>
    - src/gekko/approval/proposals.py (approve_proposal — the `approval` event write; current payload {proposal_id, actor, slack_action_id}; the extra_payload mechanism)
    - src/gekko/execution/executor.py (the two cap_rejection raise sites; tp.strategy_name + tp.account_mode in scope; the CR-02 fill-payload enrichment as the precedent move)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-RESEARCH.md (Pattern 4 option b + Pitfall 1)
  </read_first>
  <action>
    In approve_proposal (proposals.py): extend the `approval` event payload so it always carries `strategy_name` and `account_mode`. Source them from the LOCKED Proposal row (Proposal.strategy_id → Strategy.strategy_name, Proposal.account_mode) so the values are authoritative and TOCTOU-safe (mirror BLOCKER #5 — read from the locked row, never re-derive from live strategy state). Pass them through the existing extra_payload merge so both the HITL approve handler and the future auto-branch (Plan 05) emit identically-shaped approval events. In executor.py: at both cap_rejection raise/record sites, add `strategy_name` (from `tp.strategy_name`, already in scope) to the cap_rejection payload — same enrichment move CR-02 made for fill events; `reject_code` is already present. Update the Wave-0 streak fixtures only if their assumed payload keys differ. Do NOT change canonical-subset top-level keys (event_type/payload/ts/user_id) — add fields inside the inner payload dict only (the audit chain tolerates new inner-payload keys per Plan 01-04).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_orderguard.py tests/unit/test_approval_proposals.py -x -k "approval or cap_rejection or enrich" </automated>
  </verify>
  <acceptance_criteria>
    - approve_proposal writes `strategy_name` + `account_mode` into the approval event inner payload, sourced from the locked Proposal row.
    - Both cap_rejection write sites include `strategy_name` in the inner payload.
    - Existing approval/cap_rejection tests still pass (additive change — no removed keys).
    - No change to the canonical-subset top-level schema (audit chain integrity preserved).
  </acceptance_criteria>
  <done>approval and cap_rejection events carry the attribution the streak scanner (Plan 02) needs; existing audit-chain tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → SQLCipher DB | Schema change touching the per-user trust/cap state at rest |
| audit-event writer → events log | Enriched payloads become the authority for the streak gate |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | migration 0007 down_revision | high | mitigate | down_revision pinned to 0006_p4_cost_ceiling_repair + frozen-vocab equality test; round-trip migration test gates the change |
| T-05-02 | Repudiation | approval/cap_rejection payloads | high | mitigate | strategy_name+account_mode sourced from the LOCKED proposal row (TOCTOU-safe per BLOCKER #5); audit hash-chain (D-16) makes silent edits detectable |
| T-05-03 | Tampering | trust/cap columns at rest | medium | accept | per-user SQLCipher whole-DB encryption (D-19) protects state at rest; no new crypto needed |
| T-05-04 | Information Disclosure | new TEXT money columns | low | accept | money-as-TEXT convention; no PII; __repr__ excludes sensitive blobs (Plan 01-03 lock) |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/unit/test_migration_0007.py -x` green.
- Full unit suite collects with the 9 new files (`--collect-only`); behavioral stubs RED is expected.
- `grep down_revision` confirms 0006 chain head.
</verification>

<success_criteria>
Migration 0007 round-trips; ORM + migration agree on columns and event vocabulary; approval/cap_rejection payloads carry attribution; Wave-0 stubs in place for every TRUST-* requirement.
</success_criteria>

<output>
Create `.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-01-SUMMARY.md` when done.
</output>
