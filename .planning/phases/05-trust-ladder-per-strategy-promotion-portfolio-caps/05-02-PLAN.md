---
phase: 05-trust-ladder-per-strategy-promotion-portfolio-caps
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/gekko/strategy/trust.py
  - src/gekko/strategy/streak.py
  - src/gekko/strategy/promotion.py
  - src/gekko/dashboard/routes.py
  - src/gekko/dashboard/templates/strategies_list.html.j2
  - src/gekko/dashboard/templates/_promote_auto_confirm_modal.html.j2
  - src/gekko/dashboard/templates/_promote_auto_blocked_modal.html.j2
  - src/gekko/dashboard/static/tailwind.css
  - src/gekko/cli.py
  - tests/unit/test_trust_streak.py
  - tests/unit/test_trust_routes.py
  - tests/unit/test_trust_safety_invariants.py
autonomous: true
requirements: [TRUST-01, TRUST-05, TRUST-06]
must_haves:
  truths:
    - "Operator can view per-strategy trust level (PROPOSE-ONLY gray badge / AUTO ✓ green badge) on the strategies list (SC-1)"
    - "compute_clean_streak returns a StreakResult partitioned by strategy_name + account_mode, scanning the append-only events log backward to the window boundary (D-T01/D-T02/D-T03)"
    - "Promote requires 10 clean approvals with zero cap-breaches in the window; the server re-checks eligibility on POST and never silently fails (SC-5/D-T18b)"
    - "Ineligible promote click opens a blocked-explanation modal stating which criterion failed + streak progress (SC-5)"
    - "Demote is one-click (no confirm), flips the badge immediately, and takes effect on the next decision cycle (SC-1/D-T04)"
    - "Promote/demote/streak have CLI parity (no Slack promote command — D-T04)"
    - "Material edits (watchlist or hard_caps) demote auto strategies and write a trust_demoted event reason=material_edit, resetting the streak (D-T05)"
  artifacts:
    - src/gekko/strategy/trust.py
    - src/gekko/strategy/streak.py
    - tests/unit/test_trust_safety_invariants.py
  key_links:
    - "trust.py is the ONLY module permitted to set trust_level = 'auto-within-caps' (AST gate enforces this)"
    - "promote route calls compute_clean_streak BEFORE promote_strategy_to_auto and returns the blocked block when not eligible"
    - "streak scanner reads strategy_name + account_mode from the Plan-01-enriched approval payloads"
  prohibitions:
    - "MUST NOT add a Slack promote/demote command (D-T04 — autonomy is never a phone tap)"
    - "MUST NOT promote without a server-side eligibility re-check (UI is affordance only — D-T18b)"
    - "No module outside strategy/trust.py may assign trust_level = 'auto-within-caps' (locked by AST gate)"
    - "MUST NOT compute the streak from a counter column — the append-only events log is the source of truth (D-14)"
---

<objective>
Ship the autonomy-axis trust state and the promote/demote surface: a `trust.py` helper module (mirroring promotion.py), a deterministic `streak.py` clean-approval scanner, dashboard promote/demote/blocked surfaces, CLI parity, the material-edit reset hook, and the safety-invariant AST gate. This is the vertical slice that lets an operator see and change a strategy's autonomy level — with SC-5 blocked-explanation as a first-class outcome.

Purpose: TRUST-01 (view + promote-with-confirm + one-click demote), TRUST-05 (blocked-with-explanation), TRUST-06 (safety-invariant gate). Earned per-configuration (D-T05); server is the authority (D-T18b).
Output: trust.py, streak.py, promote/demote routes + modals + CLI, AST gate test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-CONTEXT.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-RESEARCH.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-UI-SPEC.md
@src/gekko/strategy/promotion.py
@src/gekko/execution/checks/_hard_caps.py
</context>

<artifacts_this_phase_produces>
- **New module `src/gekko/strategy/trust.py`:** `promote_strategy_to_auto(*, user_id, strategy_name)`, `demote_strategy_from_auto(*, user_id, strategy_name, reason, drawdown_pct=None)`, `load_trust_level(*, user_id, strategy_name, account_mode) -> str`. Writes `trust_promoted` / `trust_demoted` events (strategy_id=None, strategy_name in payload). The SOLE writer of `trust_level = "auto-within-caps"`.
- **New module `src/gekko/strategy/streak.py`:** `compute_clean_streak(*, user_id, strategy_name, account_mode) -> StreakResult`; `@dataclass StreakResult(clean_count, threshold=10, eligible, block_reason, last_breach_date, last_reset_date)`.
- **New routes:** `GET/POST /strategies/{name}/promote-auto` (+ `/confirm-modal`, `/blocked-modal`), `POST /strategies/{name}/demote-auto`.
- **New CLI commands:** `gekko strategy promote-auto <name>`, `demote-auto <name>`, `trust-status <name>`.
- **New templates:** trust badges in strategies_list, _promote_auto_confirm_modal, _promote_auto_blocked_modal; Tailwind classes appended below the Phase-3 block.
- **New test:** tests/unit/test_trust_safety_invariants.py (AST gate).
</artifacts_this_phase_produces>

<assumptions>
These three load-bearing Open Questions are resolved as researcher-recommended defaults and surfaced here for operator review:
- **OQ#1 (TRUST-05/06 intent):** TRUST-05 = blocked-with-explanation (SC-5); TRUST-06 = the safety-invariant AST gate + audit-trail completeness. Confirmed mapping; the AST gate + five new event types cover them.
- **OQ#5 is handled in Plan 04** (anomaly cancellation state). Not in scope here.
- The streak window boundary is the most recent `trust_demoted` OR `anomaly_demotion` event for this strategy+mode; material-edit resets are modeled as a `trust_demoted` event with `reason="material_edit"` so the scanner needs no snapshot-diffing (RESEARCH Pattern 4).
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: streak.py clean-approval scanner + StreakResult</name>
  <files>src/gekko/strategy/streak.py, tests/unit/test_trust_streak.py</files>
  <read_first>
    - src/gekko/execution/checks/_hard_caps.py (_check_daily_loss — the select(Event).where(...).limit(1000) scan + Decimal aggregation loop; the payload = outer.get("payload", outer) unwrap)
    - src/gekko/reporter/daily_pnl.py (_aggregate_today_events — the per-event-type branch + JSON parse)
    - src/gekko/strategy/promotion.py (_get_session_factory shim lines, load_strategy_metadata)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (streak.py section: event-scan + per-event-type branch patterns)
    - tests/unit/test_trust_streak.py (the RED stub from Plan 01 — its assertions define the contract)
  </read_first>
  <behavior>
    - 10 enriched approval events for (strategy, mode) with no cap_rejection → eligible=True, clean_count=10, block_reason=None.
    - 9 approvals → eligible=False, clean_count=9, block_reason="insufficient_streak".
    - A cap_rejection for this strategy inside the window → eligible=False, block_reason="cap_breach", last_breach_date set.
    - A trust_demoted reason=material_edit event is the window boundary → block_reason="material_edit_reset", last_reset_date set, count restarts after it.
    - Approvals for a DIFFERENT strategy or a different account_mode do not count toward this strategy+mode (no bleed).
  </behavior>
  <action>
    Implement compute_clean_streak as a deterministic backward scan: `select(Event).where(Event.user_id == user_id).order_by(Event.id.desc()).limit(1000)`; iterate; unwrap `payload = outer.get("payload", outer)`; the window boundary is the FIRST (most-recent) `trust_demoted` or `anomaly_demotion` whose payload `strategy_name` matches AND (for trust_demoted) capture `reason` to set block_reason/last_reset_date; stop the scan there. Within the window: count `approval` events where payload `strategy_name == strategy_name` AND `account_mode == account_mode`; if a `cap_rejection` with matching strategy_name appears within the window, set block_reason="cap_breach" + last_breach_date and zero eligibility. threshold=10 (D-T01). eligible = clean_count >= threshold AND block_reason is None. Use the `_get_session_factory` shim + `try/finally: engine.dispose()`. Return the StreakResult dataclass with exactly the fields UI-SPEC Surface 5 consumes (clean_count, threshold, eligible, block_reason, last_breach_date, last_reset_date). Decimal not needed here (counts are ints) but follow the house JSON-unwrap idiom exactly.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_trust_streak.py -x</automated>
  </verify>
  <acceptance_criteria>
    - StreakResult has the six documented fields; threshold defaults to 10.
    - Partitions by strategy_name AND account_mode (paper vs live separate — D-T03).
    - cap_rejection mid-window and trust_demoted/anomaly_demotion boundary both handled; cross-strategy bleed prevented.
    - test_trust_streak.py is GREEN.
  </acceptance_criteria>
  <done>The eligibility authority computes correctly from the enriched audit log; UI-SPEC Surface 5 fields are populated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: trust.py promote/demote/load helpers + material-edit reset hook + AST gate</name>
  <files>src/gekko/strategy/trust.py, src/gekko/dashboard/routes.py, tests/unit/test_trust_safety_invariants.py</files>
  <read_first>
    - src/gekko/strategy/promotion.py (promote_strategy_to_live / demote_strategy_from_live / stamp_first_live_trade set-once guard / load_strategy_metadata / __all__ — the exact template)
    - src/gekko/dashboard/routes.py (the strategy-save route where a material edit is detected — watchlist/hard_caps change creating a new snapshot version per D-05)
    - tests/unit/test_orderguard.py (test_orderguard_place_order_ast_zero_decorators — the ast.parse/ast.walk gate skeleton)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (trust.py section + test_trust_safety_invariants section)
  </read_first>
  <action>
    Create src/gekko/strategy/trust.py mirroring promotion.py: copy the `_get_session_factory` shim verbatim; NO `claude_agent_sdk` import. Implement `promote_strategy_to_auto` (UPSERT StrategyMetadata.trust_level="auto-within-caps" + trust_promoted_at=now_iso; append `trust_promoted` event with payload {strategy_name, account_mode, trust_promoted_at, clean_count}); `demote_strategy_from_auto(reason, drawdown_pct=None)` (set trust_level="propose-only"; append `trust_demoted` event with payload {strategy_name, reason, drawdown_pct?}); `load_trust_level` (read via load_strategy_metadata, return the column or "propose-only" default). All use `async with sf() as session, session.begin():` + `finally: engine.dispose()`, `strategy_id=None`, `normalize_decimals(payload)`. Add new names to `__all__`. In the strategy-save route (routes.py): when a material edit is detected (watchlist or hard_caps changed → new snapshot version per D-05), if the strategy is currently auto-within-caps, call `demote_strategy_from_auto(reason="material_edit")` so the streak window resets (D-T05) — thesis-only/cosmetic edits do NOT trigger this. Create tests/unit/test_trust_safety_invariants.py with the AST-walk gate: (1) scan all of `src/gekko/` and assert NO assignment node sets `trust_level` to the literal `"auto-within-caps"` outside `strategy/trust.py`; (2) assert the runtime.py auto-branch is preceded by a trust-level guard (Plan 05 lands the branch — keep this assertion tolerant if the branch is absent, but FAIL if a direct execute_proposal call appears in runtime without a `trust ==`/`auto-within-caps` guard in the same function).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_trust_safety_invariants.py -x</automated>
  </verify>
  <acceptance_criteria>
    - trust.py has no claude_agent_sdk import (existing grep gate passes).
    - `grep -rn 'auto-within-caps' src/gekko/ | grep -v 'strategy/trust.py'` shows only READS/comparisons, never an assignment of trust_level to that literal (AST gate enforces).
    - promote/demote/load write the correct event types with strategy_name in payload, strategy_id=None.
    - Material edit on an auto strategy emits trust_demoted reason=material_edit.
    - test_trust_safety_invariants.py GREEN.
  </acceptance_criteria>
  <done>trust.py is the sole writer of the auto trust level; material edits reset trust; AST gate locks the invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dashboard promote/demote/blocked routes + modals + badges + CLI parity</name>
  <files>src/gekko/dashboard/routes.py, src/gekko/dashboard/templates/strategies_list.html.j2, src/gekko/dashboard/templates/_promote_auto_confirm_modal.html.j2, src/gekko/dashboard/templates/_promote_auto_blocked_modal.html.j2, src/gekko/dashboard/static/tailwind.css, src/gekko/cli.py</files>
  <read_first>
    - src/gekko/dashboard/routes.py (promote_to_live route — typed-confirm + server-side guard pattern; require_session router dependency; settings_post for form idioms)
    - src/gekko/dashboard/templates/strategies_list.html.j2 (where to add the trust badge + capital chip + promote/demote/scale actions per UI-SPEC Surface 1 markup)
    - src/gekko/cli.py (the strategy promote-live / demote-live CLI commands — the exact parity shape)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-UI-SPEC.md (Surfaces 1, 2, 5 — verbatim markup + deterministic copy + the required tailwind.css additions)
    - tests/unit/test_trust_routes.py (the RED stub — route contract)
  </read_first>
  <action>
    Add routes under the existing `Depends(require_session)` router, all filtering by user_id: `GET /strategies/{name}/promote-auto/confirm-modal` (renders _promote_auto_confirm_modal only when compute_clean_streak(...).eligible, else renders the blocked modal); `GET /strategies/{name}/promote-auto/blocked-modal` (renders _promote_auto_blocked_modal from StreakResult — clean_count, block_reason, dates → the deterministic Surface-5 copy); `POST /strategies/{name}/promote-auto` (typed strategy_name_confirm must match; THEN re-check compute_clean_streak server-side — if not eligible return the blocked block, NEVER promote (D-T18b); else call promote_strategy_to_auto and return the updated .strategy-row with hx-swap-oob so the badge flips green); `POST /strategies/{name}/demote-auto` (one-click, no confirm; call demote_strategy_from_auto(reason="operator"); return updated row + a role="status" line noting "takes effect next decision cycle"). Render the two badges (badge-trust-auto / badge-trust-propose) + capital chip in strategies_list per Surface 1 markup; the disabled-styled promote button routes to the blocked modal (rendered, not hidden — SC-5). Append the Phase-5 Tailwind classes (badge-trust-*, chip-capital, chip-auto-executed, trust-progress-*, promotion-blocked, anomaly-notice, strategy-row*) verbatim from the UI-SPEC BELOW the Phase-3 block — do not modify existing rules. Add CLI parity in cli.py: `strategy promote-auto <name>` (typed-name confirm + eligibility check + promote_strategy_to_auto), `strategy demote-auto <name>`, `strategy trust-status <name>` (prints StreakResult). NO Slack promote command (D-T04). Use Unicode glyphs only (CSP script-src 'self'); no inline onclick/script.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_trust_routes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Forged POST /promote-auto on an ineligible strategy returns the blocked-explanation block, not a promotion (D-T18b) — asserted in test_trust_routes.py.
    - Eligible promote with matching typed name promotes + OOB-swaps the green badge; name mismatch re-renders with the deterministic mismatch error.
    - Demote is a single POST with no confirm modal; returns flipped badge + next-cycle copy.
    - CLI promote-auto/demote-auto/trust-status exist; `grep -rn 'promote' src/gekko/slack' 2>/dev/null` shows no Slack promote command.
    - tailwind.css additions appended below the Phase-3 block; existing rules unchanged.
  </acceptance_criteria>
  <done>Operator can view trust, promote (eligible→confirm; ineligible→explanation), and one-click demote on dashboard + CLI; server is the authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/CLI → promote route | Operator intent to grant autonomy crosses here (untrusted form input) |
| route → trust.py | The only sanctioned path to set auto trust level |
| events log → streak scanner | Eligibility authority derived from the append-only audit log |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | forged promote POST | high | mitigate | server re-checks compute_clean_streak on POST (D-T18b); typed-name confirm; require_session; returns blocked block, never promotes |
| T-05-06 | Tampering | trust_level mutated outside trust.py | high | mitigate | AST gate forbids any assignment of trust_level="auto-within-caps" outside strategy/trust.py |
| T-05-07 | Tampering | streak inflation via cross-strategy approval bleed | high | mitigate | streak partitions by strategy_name + account_mode from enriched audit payloads (Plan 01) |
| T-05-08 | Spoofing | cross-user strategy access | medium | mitigate | every route filters by user_id from require_session (D-21) |
| T-05-09 | Repudiation | promote/demote not auditable | medium | mitigate | trust_promoted / trust_demoted events with strategy_name + reason in payload |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/unit/test_trust_streak.py tests/unit/test_trust_routes.py tests/unit/test_trust_safety_invariants.py -x` green.
- `grep -rn 'auto-within-caps' src/gekko/` — only trust.py assigns it.
- No Slack promote command anywhere.
</verification>

<success_criteria>
SC-1 (view + promote-confirm + one-click demote next-cycle), SC-5 (blocked-with-explanation), TRUST-06 AST gate all satisfied; trust earned per-configuration with material-edit reset.
</success_criteria>

<output>
Create `.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-02-SUMMARY.md` when done.
</output>
