---
phase: 05-trust-ladder-per-strategy-promotion-portfolio-caps
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/gekko/execution/checks/_portfolio_caps.py
  - src/gekko/execution/checks/_capital_ceiling.py
  - src/gekko/execution/checks/__init__.py
  - src/gekko/execution/orderguard.py
  - src/gekko/strategy/trust.py
  - src/gekko/dashboard/routes.py
  - src/gekko/dashboard/templates/settings.html.j2
  - src/gekko/dashboard/templates/capital_scale.html.j2
  - src/gekko/dashboard/templates/_capital_increase_modal.html.j2
  - src/gekko/cli.py
  - tests/unit/test_portfolio_caps.py
  - tests/unit/test_capital_ceiling.py
autonomous: true
requirements: [TRUST-02, TRUST-03]
must_haves:
  truths:
    - "OrderGuard.place_order runs check_portfolio_caps + check_capital_ceiling after check_hard_caps on EVERY order — HITL and auto alike (D-T08)"
    - "Two strategies each within their own per-strategy caps but breaching an aggregate portfolio cap get the order hard-rejected with a portfolio_* reject_code (SC-2)"
    - "check_capital_ceiling rejects when total deployed capital (open positions for the strategy + this order) exceeds capital_ceiling_usd (D-T15)"
    - "Portfolio caps are user-level config in Settings; the four caps default to 50/30/15/$200 and are runtime-editable; blank = disabled (D-T06/D-T09)"
    - "Capital scaling is a separate page: increase requires typed-name confirm + capital_scaled audit event; decrease applies immediately; neither touches trust_level or the streak (D-T14/D-T17)"
    - "First-promotion default capital ceiling is $1,000 (D-T16)"
  artifacts:
    - src/gekko/execution/checks/_portfolio_caps.py
    - src/gekko/execution/checks/_capital_ceiling.py
  key_links:
    - "the new checks insert AFTER check_hard_caps and BEFORE check_qty_price_sanity in orderguard.py place_order"
    - "set_capital_ceiling lives in trust.py and writes the capital_scaled event; the route enforces confirm-on-increase"
    - "portfolio caps load from the User row columns added in Plan 01"
  prohibitions:
    - "MUST NOT add a second order path that bypasses OrderGuard (D-T08 — portfolio is portfolio)"
    - "place_order MUST stay zero-decorator (Knight-Capital AST gate)"
    - "No claude_agent_sdk import in any checks/*.py module"
    - "MUST NOT issue N×M broker calls per strategy — aggregate over a single get_positions() (Alpaca nets one position per ticker — RESEARCH Pitfall 4)"
    - "MUST NOT use float for cap/ceiling math — Decimal + normalize_decimals only"
---

<objective>
Ship the deterministic cap-enforcement slice: two new OrderGuard checks (portfolio-level aggregate caps + per-strategy capital ceiling) stacking inside the existing place_order pipeline, the Settings portfolio-caps fieldset, and the separate capital-scaling rung (page + confirm-on-increase). Every order — auto or HITL — inherits these caps because the auto path (Plan 05) routes through the same execute_proposal→OrderGuard last line.

Purpose: TRUST-02 (portfolio caps reject what per-strategy caps allow) + TRUST-03 (capital scaling as a separate confirmed rung). Deterministic, Decimal-exact, LLM-cannot-reason-past.
Output: _portfolio_caps.py, _capital_ceiling.py, orderguard wiring, Settings fieldset, capital page + modal + routes + CLI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-CONTEXT.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-RESEARCH.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-UI-SPEC.md
@src/gekko/execution/checks/_hard_caps.py
@src/gekko/execution/orderguard.py
@src/gekko/execution/checks/__init__.py
</context>

<artifacts_this_phase_produces>
- **New check `src/gekko/execution/checks/_portfolio_caps.py`:** `check_portfolio_caps(*, req, strategy, broker, user_id)` — four aggregate caps raising `OrderGuardRejected` with reject_codes `portfolio_total_exposure`, `portfolio_sector_concentration`, `portfolio_correlated_ticker`, `portfolio_daily_loss`.
- **New check `src/gekko/execution/checks/_capital_ceiling.py`:** `check_capital_ceiling(*, req, strategy, broker, user_id)` — reject_code `capital_ceiling`.
- **New trust.py helper:** `set_capital_ceiling(*, user_id, strategy_name, new_ceiling_usd)` writing the `capital_scaled` event (old→new in payload); does NOT touch trust_level or the streak (D-T17).
- **New routes:** `GET /strategies/{name}/capital`, `GET /strategies/{name}/capital/review`, `POST /strategies/{name}/capital`; extended `POST /settings` with the four portfolio-cap fields.
- **New CLI:** `gekko strategy scale-capital <name> <amount>`.
- **New templates:** capital_scale page, _capital_increase_modal, Settings Portfolio-Caps fieldset.
</artifacts_this_phase_produces>

<assumptions>
- **OQ#4 (correlated-ticker semantics under Alpaca position netting) RESOLVED as the measurable interpretation:** a single Alpaca account nets ONE position per ticker, so `max_correlated_ticker_pct` caps the account's single per-ticker position (market_value / equity) against the cap — aggregation reads ONE `get_positions()` call, never N×M per-strategy broker calls (RESEARCH A2/Pitfall 4). Surfaced for operator review; the "across strategies" framing is attribution, not separate holdings.
- Reject ordering: portfolio caps run after hard caps (a per-strategy breach still reports first); capital ceiling runs after portfolio (most-specific-last). Documented choice (RESEARCH Pattern 1).
- Default cap numbers 50/30/15/$200 from UI-SPEC Surface 7 (server_defaults set in Plan 01's migration); operator-editable.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: check_portfolio_caps + check_capital_ceiling OrderGuard checks</name>
  <files>src/gekko/execution/checks/_portfolio_caps.py, src/gekko/execution/checks/_capital_ceiling.py, src/gekko/execution/checks/__init__.py, tests/unit/test_portfolio_caps.py, tests/unit/test_capital_ceiling.py</files>
  <read_first>
    - src/gekko/execution/checks/_hard_caps.py (_check_position_pct reject pattern; _check_sector_exposure aggregation + _resolve_sector + >25-position canary; _check_daily_loss event scan; _ref_price_for helper; _get_session_factory shim; the equity<=0 early-return guards)
    - src/gekko/execution/checks/__init__.py (barrel re-export convention)
    - tests/unit/test_portfolio_caps.py + tests/unit/test_capital_ceiling.py (RED stubs — the contract: reject_codes + blank=disabled + aggregate-breach scenarios)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (_portfolio_caps.py + _capital_ceiling.py sections; Alpaca netting clarification)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-RESEARCH.md (Code Examples: _check_total_exposure)
  </read_first>
  <behavior>
    - max_total_exposure: (Σ market_value + proposed notional) / equity over cap → raise portfolio_total_exposure.
    - max_sector_concentration: aggregate sector exposure across all positions + proposed → raise portfolio_sector_concentration (reuse _resolve_sector; cache lookups within the call).
    - max_correlated_ticker: the account's single position in req.symbol (market_value + proposed) / equity over cap → raise portfolio_correlated_ticker.
    - max_total_daily_loss_usd: portfolio-wide realized+unrealized loss today over the USD cap → raise portfolio_daily_loss.
    - capital_ceiling: Σ(market_value of this strategy's tickers) + proposed notional > capital_ceiling_usd → raise capital_ceiling.
    - Any blank/NULL cap column → return early (disabled).
  </behavior>
  <action>
    Create _portfolio_caps.py mirroring _hard_caps.py exactly: copy the `_get_session_factory` shim, `_ref_price_for`, and the get_account()/get_positions() aggregation shape. Implement a `_load_portfolio_caps(user_id)` reading the four User columns (added Plan 01); each cap is a Decimal fraction (pct) or Decimal USD; blank=None=disabled→early return. Implement the four sub-checks (total exposure, sector concentration, correlated-ticker, daily loss) each raising `OrderGuardRejected(reject_code, msg, extra={...})` with the documented reject_codes; Decimal-exact throughout; aggregate over a SINGLE get_positions() call (Alpaca nets one position per ticker — do not loop broker calls per strategy); cache sector lookups within one invocation; keep the >25-position perf canary. Compose them in `check_portfolio_caps(*, req, strategy, broker, user_id)`. Create _capital_ceiling.py mirroring _check_position_pct: `check_capital_ceiling(*, req, strategy, broker, user_id)` reading StrategyMetadata.capital_ceiling_usd (server_default "1000.00"), summing this strategy's deployed capital + proposed notional, raising `OrderGuardRejected("capital_ceiling", ...)` on breach. Re-export both from checks/__init__.py (two imports + two __all__ entries). NO claude_agent_sdk import; never call broker.place_order here.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_portfolio_caps.py tests/unit/test_capital_ceiling.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Five reject_codes implemented; blank cap = disabled = no raise.
    - Aggregation uses one get_positions() call (no per-strategy broker-call fan-out).
    - All cap math is Decimal; no float arithmetic (`grep -n 'float(' src/gekko/execution/checks/_portfolio_caps.py src/gekko/execution/checks/_capital_ceiling.py` returns nothing relevant to cap math).
    - No claude_agent_sdk import in either module.
    - Both test files GREEN.
  </acceptance_criteria>
  <done>The two deterministic checks reject aggregate + capital breaches with unique reject_codes, Decimal-exact, single-get_positions aggregation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire checks into OrderGuard place_order (preserve zero-decorator invariant)</name>
  <files>src/gekko/execution/orderguard.py, tests/unit/test_orderguard.py</files>
  <read_first>
    - src/gekko/execution/orderguard.py (place_order pipeline — the check_hard_caps call site and the check_qty_price_sanity call after it; the docstring hard-invariants; cancellation passthroughs)
    - tests/unit/test_orderguard.py (test_orderguard_place_order_ast_zero_decorators + the no-SDK-import gate)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (orderguard.py MODIFY section — exact insertion point)
  </read_first>
  <action>
    Insert `await check_portfolio_caps(req=req, strategy=self._strategy, broker=self._wrapped, user_id=self._user_id)` and then `await check_capital_ceiling(...)` immediately AFTER the existing `check_hard_caps` call and BEFORE the `check_qty_price_sanity` block, matching the existing await-call style. Import both from gekko.execution.checks. Do NOT add any decorator to place_order (Knight-Capital invariant). Do NOT add a second order path — these checks run inside the one pipeline that every order (HITL + auto) traverses (D-T08). Add an integration-style behavioral test asserting two strategies each within their per-strategy caps but breaching the aggregate portfolio cap → OrderGuardRejected("portfolio_*") at place_order time (proves the stack ordering and that the LLM cannot reason past it).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_orderguard.py tests/unit/test_portfolio_caps.py -x</automated>
  </verify>
  <acceptance_criteria>
    - place_order calls the two new checks after check_hard_caps, before qty_price.
    - test_orderguard_place_order_ast_zero_decorators still passes (place_order stays undecorated).
    - No-SDK-import gate over orderguard.py + checks/*.py still passes.
    - Aggregate-breach behavioral test GREEN.
  </acceptance_criteria>
  <done>Portfolio + capital checks run on every order through the single OrderGuard pipeline; zero-decorator invariant preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Settings portfolio-caps fieldset + capital-scaling page/route/CLI</name>
  <files>src/gekko/strategy/trust.py, src/gekko/dashboard/routes.py, src/gekko/dashboard/templates/settings.html.j2, src/gekko/dashboard/templates/capital_scale.html.j2, src/gekko/dashboard/templates/_capital_increase_modal.html.j2, src/gekko/cli.py, tests/unit/test_settings_route.py</files>
  <read_first>
    - src/gekko/dashboard/routes.py (settings_get/settings_post — the existing Decimal validation + role=status success chip; the require_session router; the promote_to_live typed-confirm route as the capital-increase template)
    - src/gekko/dashboard/templates/settings.html.j2 (where to append the Portfolio Caps fieldset below Quiet Hours + Cost Ceiling)
    - src/gekko/strategy/trust.py (add set_capital_ceiling here, mirroring promote/demote shape)
    - src/gekko/cli.py (strategy command group for scale-capital parity)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-UI-SPEC.md (Surface 3 capital page + increase modal; Surface 7 portfolio-caps fieldset — verbatim markup, deterministic copy, default numbers, validation messages)
  </read_first>
  <action>
    Add `set_capital_ceiling(*, user_id, strategy_name, new_ceiling_usd)` to trust.py: UPSERT StrategyMetadata.capital_ceiling_usd (Decimal-as-TEXT) + append `capital_scaled` event with payload {strategy_name, old_ceiling, new_ceiling}; MUST NOT touch trust_level or write any trust event (D-T17). Extend `POST /settings` (routes.py) with the four portfolio-cap fields: validate each is a non-negative number within range (pct 0–100 stored as fraction; USD non-negative; blank=disabled) mirroring the existing ceiling Decimal validation; write to the User row; re-render with the existing success chip; on out-of-range return the existing .login-error block with the field-specific message ("Max total exposure must be between 0 and 100."). Render the Portfolio Caps fieldset in settings.html.j2 per Surface 7 (four cap-field-rows, defaults shown from the User row, deterministic help copy). Add capital routes: `GET /strategies/{name}/capital` (capital_scale page — current ceiling + numeric input + decoupling note), `GET /strategies/{name}/capital/review` (compares new vs current; renders _capital_increase_modal with typed-name confirm if higher, else applies directly), `POST /strategies/{name}/capital` (if increase: require strategy_name_confirm match THEN set_capital_ceiling; if decrease/unchanged: set_capital_ceiling directly — always writes capital_scaled; returns success fragment + OOB capital-chip update). Add CLI `gekko strategy scale-capital <name> <amount>` (typed-name confirm on increase). All routes under require_session, filter by user_id. Unicode glyphs only; no inline script.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_capital_ceiling.py tests/unit/test_settings_route.py -x</automated>
  </verify>
  <acceptance_criteria>
    - set_capital_ceiling writes capital_scaled (old→new) and leaves trust_level + streak untouched (asserted).
    - Settings persists the four portfolio caps; out-of-range pct → field-specific error; blank → disabled.
    - Capital increase requires typed-name confirm; decrease applies immediately; both audit.
    - CLI scale-capital exists with confirm-on-increase.
  </acceptance_criteria>
  <done>Operator configures portfolio caps in Settings and scales per-strategy capital as a separate confirmed rung that never touches trust.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any approved order → OrderGuard.place_order | The single deterministic enforcement point for all caps |
| Settings/capital form → User/StrategyMetadata | Operator-set limits cross here (untrusted numeric input) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | manual approval exceeding portfolio cap | high | mitigate | portfolio caps run inside OrderGuard.place_order on EVERY order (D-T08); no second path; behavioral test proves a per-strategy-OK order still rejects on aggregate breach |
| T-05-11 | Elevation of Privilege | capital-ceiling bypass via parallel order path | high | mitigate | capital ceiling enforced in place_order; auto path (Plan 05) routes through execute_proposal→OrderGuard; AST zero-decorator gate intact |
| T-05-12 | Input Validation | out-of-range cap form values | medium | mitigate | server validates 0–100% / non-negative USD before storage (mirrors settings_post ceiling validation) |
| T-05-13 | Elevation of Privilege | forged capital-increase POST | medium | mitigate | typed-name confirm on increase + require_session + user_id filter; capital_scaled audit event |
| T-05-14 | Denial of Service | N×M broker calls inflate latency | low | mitigate | aggregate over one get_positions(); cache sector lookups; >25-position canary |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/unit/test_portfolio_caps.py tests/unit/test_capital_ceiling.py tests/unit/test_orderguard.py tests/unit/test_settings_route.py -x` green.
- place_order zero-decorator + no-SDK-import gates green.
</verification>

<success_criteria>
SC-2 portfolio caps reject cross-strategy aggregate breaches on every order; SC-3 capital scaling is a separate confirmed rung enforced in OrderGuard, default $1,000, never touching trust.
</success_criteria>

<output>
Create `.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-03-SUMMARY.md` when done.
</output>
