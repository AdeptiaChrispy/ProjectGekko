---
phase: 05-trust-ladder-per-strategy-promotion-portfolio-caps
plan: 05
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - src/gekko/agent/runtime.py
  - src/gekko/approval/proposals.py
  - src/gekko/execution/executor.py
  - src/gekko/reporter/daily_pnl.py
  - src/gekko/dashboard/templates/_proposal_card.html.j2
  - tests/unit/test_auto_execute.py
  - tests/unit/test_trust_safety_invariants.py
autonomous: true
requirements: [TRUST-02, TRUST-06]
must_haves:
  truths:
    - "A strategy in auto-within-caps auto-approves + executes its TradeProposal without HITL, routing through approve_proposal → execute_proposal (SC-2)"
    - "Every auto-executed decision still writes a full-rationale auto_execution audit event (SC-2)"
    - "Auto-execution sends a real-time informational Slack DM (no approve/reject buttons) that RESPECTS quiet hours (D-T18)"
    - "Auto-executed fills surface in the daily P&L digest with an aggregate count + per-strategy annotation (SC-2/D-T18)"
    - "The auto path reaches the broker ONLY via execute_proposal → OrderGuard, which re-checks all caps as the last line (D-T08)"
    - "A LIVE auto strategy with first_live_trade_confirmed_at IS NULL routes to AWAITING_2ND_CHANNEL (dual-channel HITL), NOT direct execute — live + auto stacks both gates (D-T03/CRITICAL)"
    - "Trust level is evaluated ONCE at the auto-branch (proposal-build time); demotion takes effect next cycle (SC-1 / TOCTOU lesson)"
  artifacts:
    - tests/unit/test_auto_execute.py
  key_links:
    - "the auto-branch sits in trigger_strategy_run AFTER write_proposal and BELOW the SDK boundary (deterministic Python, not an LLM call)"
    - "load_trust_level(account_mode=proposal.account_mode) reads the per-mode trust from trust.py (Plan 02)"
    - "auto-exec informational DM uses a ROUTINE category (respects quiet hours); anomaly DM (Plan 04) uses bypass — never inverted"
  prohibitions:
    - "MUST NOT call broker.place_order directly — always route through execute_proposal so OrderGuard re-checks (D-T08)"
    - "MUST NOT skip the live first-trade dual-channel gate for LIVE auto strategies (D-T03 — route to AWAITING_2ND_CHANNEL when first_live_trade_confirmed_at IS NULL)"
    - "MUST NOT re-derive trust level at execute time — evaluate once at the auto-branch (BLOCKER #5 TOCTOU lesson)"
    - "MUST NOT let the auto-exec informational DM bypass quiet hours (D-T18 — it is not safety-critical)"
---

<objective>
Ship the auto-execution slice: the deterministic auto-branch in trigger_strategy_run that, for an auto-within-caps strategy, auto-approves and executes the proposal through the existing execute_proposal→OrderGuard last line (so caps are re-checked), writes the auto_execution audit event, fires a quiet-hours-respecting informational DM, and surfaces auto-executed trades in the daily digest and on the proposal card. Critically, LIVE auto strategies still pass the Phase-2 first-live dual-channel gate.

Purpose: TRUST-02 / SC-2 (auto-execute without HITL but recorded + surfaced) + TRUST-06 (the live+auto gate stacking, locked by the AST/behavioral safety gate). This is the slice that actually hands execution authority to the agent — and the one that most must not bypass a guard.
Output: auto-branch in runtime.py, auto_execution event, informational DM, digest + card surfacing, live+auto gate, expanded safety-invariant tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-CONTEXT.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-RESEARCH.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md
@.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-UI-SPEC.md
@src/gekko/agent/runtime.py
@src/gekko/approval/proposals.py
@src/gekko/execution/executor.py
@src/gekko/reporter/daily_pnl.py
</context>

<artifacts_this_phase_produces>
- **runtime.py auto-branch:** after write_proposal in trigger_strategy_run — `load_trust_level` → if TradeProposal and auto-within-caps: live+auto gate check, then approve_proposal(actor="auto-execute") + auto_execution event + execute_proposal; run summary gains `outcome="auto_executed"`.
- **proposals.py:** approve_proposal accepts the auto actor + execution_path="auto" in extra_payload (additive; the enriched approval from Plan 01 already carries strategy_name+account_mode).
- **executor.py:** auto-exec informational DM (no buttons) via _send_slack_dm_respecting_quiet_hours with a ROUTINE category, fired on the auto fill.
- **daily_pnl.py:** auto_execution + anomaly_demotion aggregation branches + digest lines (Surface 4c / 6c).
- **_proposal_card.html.j2:** the AUTO-EXECUTED blue chip in place of the Approve/Reject row.
- **test_auto_execute.py** + expanded test_trust_safety_invariants.py (live+auto + OrderGuard-re-check behavioral gates).
</artifacts_this_phase_produces>

<assumptions>
- **OQ#2 (who runs the auto-branch) RESOLVED: inside trigger_strategy_run.** Scheduled cycles are the primary auto trigger; a caller-side branch would miss them. propose-only strategies still return the PENDING proposal for the calling surface to post the HITL card (unchanged). The run summary carries `outcome` so surfaces render the right thing.
- Trust is evaluated ONCE at the auto-branch (proposal-build time); next-cycle re-read implements SC-1 demotion timing (BLOCKER #5 TOCTOU lesson).
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-branch in trigger_strategy_run + live+auto dual-channel gate + auto_execution event</name>
  <files>src/gekko/agent/runtime.py, src/gekko/approval/proposals.py, tests/unit/test_auto_execute.py, tests/unit/test_trust_safety_invariants.py</files>
  <read_first>
    - src/gekko/agent/runtime.py (the write_proposal block in trigger_strategy_run; the session_factory usage; the return/run-summary shape; the gate-before-LLM idiom showing where the SDK boundary ends)
    - src/gekko/approval/proposals.py (approve_proposal signature + extra_payload; STATE_TRANSITIONS including PENDING→AWAITING_2ND_CHANNEL; the existing first-live dual-channel diversion the Slack approve handler performs)
    - src/gekko/execution/executor.py (execute_proposal entry — confirm it always builds OrderGuard via _build_broker; the first_live_trade_confirmed_at / AWAITING_2ND_CHANNEL handling)
    - src/gekko/strategy/trust.py (load_trust_level — Plan 02) + load_strategy_metadata (first_live_trade_confirmed_at)
    - tests/unit/test_auto_execute.py (RED stub — contract: routing, OrderGuard re-check, live+auto gate)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (runtime.py MODIFY section + RESEARCH Pattern 3 + critical subtlety)
  </read_first>
  <behavior>
    - propose-only strategy → unchanged: returns PENDING proposal for the caller to post the HITL card.
    - auto-within-caps + PAPER (or LIVE with first_live_trade_confirmed_at set) → approve_proposal(actor="auto-execute", extra_payload={execution_path:"auto"}) + auto_execution event, then execute_proposal(proposal_id, user_id); run summary outcome="auto_executed".
    - auto-within-caps + LIVE + first_live_trade_confirmed_at IS NULL → route to AWAITING_2ND_CHANNEL (dual-channel HITL), NOT direct execute (D-T03).
    - NoActionProposal → never auto-executes (only TradeProposal).
    - A cap breach on the auto path → execute_proposal→OrderGuard raises → cap_rejection event + FAILED status (proves OrderGuard re-checks).
  </behavior>
  <action>
    Insert the auto-branch in trigger_strategy_run AFTER the write_proposal block (the trust check is deterministic Python, BELOW the SDK boundary — not an LLM call). Call `load_trust_level(user_id=..., strategy_name=..., account_mode=proposal.account_mode)` (account_mode exists only on TradeProposal). If `isinstance(proposal, TradeProposal) and trust == "auto-within-caps"`: FIRST apply the live+auto gate — if `proposal.account_mode == "LIVE"` and the strategy's `first_live_trade_confirmed_at IS NULL`, route to the dual-channel HITL path (transition to AWAITING_2ND_CHANNEL + DM dashboard URL exactly as the Slack approve handler does; NO direct execute) and set outcome accordingly; OTHERWISE auto-approve: `async with session_factory() as s, s.begin(): approve_proposal(s, proposal.proposal_id, actor="auto-execute", extra_payload={"execution_path":"auto"})` + `append_event(... event_type="auto_execution", payload=normalize_decimals({proposal_id, strategy_name, account_mode, side, qty, ticker, rationale_summary}))`, then `await execute_proposal(proposal.proposal_id, user_id)` (OrderGuard re-checks as the last line — never call broker.place_order directly). Add `outcome` (`"auto_executed"` / `"awaiting_2nd_channel"` / `"proposed"`) to the run summary. Evaluate trust ONCE here; do not re-read at execute time (TOCTOU). Update test_trust_safety_invariants.py: the runtime auto-branch must be guarded by `trust == "auto-within-caps"` (AST) and the live+auto behavioral gate must hold.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_auto_execute.py tests/unit/test_trust_safety_invariants.py -x</automated>
  </verify>
  <acceptance_criteria>
    - auto-within-caps TradeProposal routes through approve_proposal → execute_proposal; NoActionProposal and propose-only do not.
    - LIVE auto + first_live_trade_confirmed_at IS NULL → AWAITING_2ND_CHANNEL, asserted; no direct execute, no first auto fill without a preceding first-live confirm.
    - auto_execution event written with full rationale payload.
    - A cap-breaching auto proposal yields cap_rejection + FAILED (OrderGuard re-checked) — asserted.
    - `grep -n 'broker.place_order' src/gekko/agent/runtime.py` returns nothing (no direct broker path).
    - test_auto_execute.py + test_trust_safety_invariants.py GREEN.
  </acceptance_criteria>
  <done>Auto strategies execute without HITL through the single OrderGuard-protected path; live+auto stacks the dual-channel gate; trust evaluated once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-execution informational DM (respects quiet hours) + AUTO-EXECUTED card chip</name>
  <files>src/gekko/execution/executor.py, src/gekko/dashboard/templates/_proposal_card.html.j2, tests/unit/test_executor.py</files>
  <read_first>
    - src/gekko/execution/executor.py (on_fill_event DM send site; _send_slack_dm_respecting_quiet_hours; _BYPASS_CATEGORIES — confirm the auto-exec category is NOT in it; the fill-category selection precedent; how execution_path is available at fill time, e.g. from the proposal/approval payload)
    - src/gekko/dashboard/templates/_proposal_card.html.j2 (the header where the chip goes; the Approve/Reject actions row to suppress for auto cards)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-UI-SPEC.md (Surface 4a DM Block Kit + deterministic copy; Surface 4b card chip markup)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (executor.py MODIFY section — bypass-vs-respect routing)
  </read_first>
  <action>
    In executor.py on_fill_event: when the fill belongs to an auto-executed proposal (execution_path == "auto"), build the Surface-4a informational Block Kit (section with "🤖 Auto-executed — {strategy} {side} {qty} {ticker} @ ~${price}", a rationale section ~140 chars, and the "No action needed — FYI" context; NO actions/approve/reject block) and send it via `_send_slack_dm_respecting_quiet_hours` with a ROUTINE category (NOT in _BYPASS_CATEGORIES — it must be suppressed during quiet hours, D-T18). Do NOT invert with the anomaly DM (Plan 04, which bypasses). Swallow DM-send exceptions (never abort the fill). In _proposal_card.html.j2: when execution_path == "auto", render the blue `.chip-auto-executed` ("AUTO-EXECUTED", aria-label) in the card header and suppress the Approve/Reject actions row (the timeline is already closed); rationale + evidence <details> stay. Use the existing chip CSS appended in Plan 02.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_auto_execute.py tests/unit/test_executor.py -x -k "auto or dm or quiet"</automated>
  </verify>
  <acceptance_criteria>
    - Auto-exec informational DM has no actions block and uses a category NOT in _BYPASS_CATEGORIES (respects quiet hours) — asserted.
    - DM send failure does not abort the fill.
    - Auto card shows AUTO-EXECUTED chip and no Approve/Reject buttons.
    - Anomaly DM (bypass) and auto-exec DM (respect) are not inverted — both directions asserted across this plan + Plan 04.
  </acceptance_criteria>
  <done>Auto-executed trades stay legible via a quiet-hours-respecting FYI DM + a closed-timeline card chip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Daily digest auto_execution + anomaly_demotion surfacing</name>
  <files>src/gekko/reporter/daily_pnl.py, tests/unit/test_daily_pnl.py</files>
  <read_first>
    - src/gekko/reporter/daily_pnl.py (_aggregate_today_events per-event-type branch + per-strategy dict accumulation; _build_digest_blocks where lines render)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-UI-SPEC.md (Surface 4c auto-exec digest lines + Surface 6c anomaly digest line — deterministic copy)
    - .planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-PATTERNS.md (daily_pnl.py MODIFY section)
  </read_first>
  <action>
    In _aggregate_today_events: add an `elif row.event_type == "auto_execution":` branch (count + per-strategy attribution) and an `elif row.event_type == "anomaly_demotion":` branch (collect name + drawdown_pct). In _build_digest_blocks: render the aggregate auto-exec line ("🤖 Auto-executed today: {N} trades across {M} strategies"), append ` 🤖` to per-strategy breakdown lines whose fills came from an auto strategy, and render the anomaly summary line ("🛑 Anomaly demotions today: {name} (−{X}% single-day)") when present. This is the SC-2 review surface for auto-executed decisions (D-T18). Keep all money math Decimal and reuse the existing payload-parse + per-strategy accumulation idiom.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_daily_pnl.py -x -k "auto or anomaly or digest"</automated>
  </verify>
  <acceptance_criteria>
    - Digest counts auto_execution events (aggregate + per-strategy) and annotates auto fills with 🤖.
    - Digest renders an anomaly-demotion summary line when an anomaly_demotion event occurred today.
    - Existing digest tests still pass (additive branches).
  </acceptance_criteria>
  <done>The daily P&L digest is the SC-2 auto-execution review surface and folds in anomaly demotions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM Decision output → auto-branch | The deterministic trust gate sits BELOW the SDK boundary; the LLM never decides trust level |
| auto-branch → execute_proposal → OrderGuard | The single enforcement path the auto trade must traverse |
| auto fill → informational DM | Non-safety FYI (must respect quiet hours) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Elevation of Privilege | auto-execute bypassing promotion criteria | high | mitigate | auto-branch guarded by load_trust_level == "auto-within-caps"; trust.py is the sole writer (AST gate); promotion gate (Plan 02) is the only way to reach that state |
| T-05-21 | Tampering | auto path reaching broker without OrderGuard | high | mitigate | auto-branch routes through execute_proposal (always builds OrderGuard); grep gate: no broker.place_order in runtime.py; behavioral test: cap-breaching auto proposal → cap_rejection + FAILED |
| T-05-22 | Elevation of Privilege | LIVE+auto skipping first-live dual-channel gate | high | mitigate | first_live_trade_confirmed_at check routes first live auto trade to AWAITING_2ND_CHANNEL; behavioral test locks it |
| T-05-23 | Repudiation | auto-execution not auditable | high | mitigate | auto_execution event with full rationale payload; digest + card surface it (SC-2) |
| T-05-24 | Race (TOCTOU) | trust re-derived at execute time | medium | mitigate | trust evaluated ONCE at the auto-branch (proposal-build time); demotion effective next cycle (SC-1), mirroring BLOCKER #5 account_mode locking |
| T-05-25 | Information Disclosure | auto-exec FYI DM ignoring quiet hours | low | mitigate | auto-exec DM uses a routine (non-bypass) category — suppressed in quiet hours (D-T18) |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/unit/test_auto_execute.py tests/unit/test_trust_safety_invariants.py tests/unit/test_executor.py tests/unit/test_daily_pnl.py -x` green.
- `grep -n 'broker.place_order' src/gekko/agent/runtime.py` → empty.
- Full unit suite green before phase verify (MEMORY: exit 124 at suite exit ≠ failure; deselect the 3 pre-existing .env failures).
</verification>

<success_criteria>
SC-2: auto-within-caps executes without HITL but every decision is recorded (auto_execution event) + surfaced (FYI DM + digest + card chip), and portfolio caps re-checked on the auto path via OrderGuard; live+auto stacks the first-live dual-channel gate (TRUST-06).
</success_criteria>

<output>
Create `.planning/phases/05-trust-ladder-per-strategy-promotion-portfolio-caps/05-05-SUMMARY.md` when done.
</output>
