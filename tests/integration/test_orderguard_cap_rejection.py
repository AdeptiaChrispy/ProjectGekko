"""Wave-0 stub — OrderGuard cap_rejection end-to-end.

# WAVE-0 STUB: owned by plan 02-02 — DO NOT delete the skip until that plan's tasks land

Covers EXEC-04 — full end-to-end: Decision agent emits a proposal that
trips max_position_pct; OrderGuard rejects with reject_code='hard_cap_position_pct';
a cap_rejection event is written; the Slack DM surfaces the human-readable reason.
"""

from __future__ import annotations

import pytest

pytest.skip("Wave-0 stub", allow_module_level=True)


def test_cap_rejection_position_pct_end_to_end_placeholder() -> None:
    """Will assert full chain through cap_rejection event."""
    pass
