"""Wave-0 stub — D-27 target_notional_usd schema field on TradeProposal.

# WAVE-0 STUB: owned by plan 02-01 Task 3 — DO NOT delete the skip until that plan's tasks land

Plan 02-01 Task 3 replaces this stub with the 8 target_notional_usd behaviors
(required field, gt=0 validator, propose_trade schema exposure, NOT in
_runtime_only).
"""

from __future__ import annotations

import pytest

pytest.skip("Wave-0 stub", allow_module_level=True)


def test_target_notional_usd_required_field_placeholder() -> None:
    """Will assert missing target_notional_usd raises pydantic.ValidationError."""
    pass


def test_target_notional_usd_in_propose_trade_schema_placeholder() -> None:
    """Will assert target_notional_usd appears in _PROPOSE_TRADE_SCHEMA properties."""
    pass
