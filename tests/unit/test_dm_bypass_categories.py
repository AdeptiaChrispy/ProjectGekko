"""Wave 0 stub — populated in Plan 03-03.

Tests that kill/error/first-live DMs always fire regardless of quiet hours.
"""

from __future__ import annotations

import pytest


@pytest.mark.asyncio
async def test_kill_state_dm_bypasses_quiet_hours() -> None:
    pytest.skip("Wave 0 stub — populated in Plan 03-03")


@pytest.mark.asyncio
async def test_error_dm_bypasses_quiet_hours() -> None:
    pytest.skip("Wave 0 stub — populated in Plan 03-03")


@pytest.mark.asyncio
async def test_first_live_dm_bypasses_quiet_hours() -> None:
    pytest.skip("Wave 0 stub — populated in Plan 03-03")
