"""Wave 0 stub — populated in Plan 03-02.

AST gate: every caller of transition_status catches CHECK violation (no silent swallow).
"""

from __future__ import annotations

import pytest


def test_transition_status_callers_catch_value_error() -> None:
    pytest.skip("Wave 0 stub — populated in Plan 03-02")
